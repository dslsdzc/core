# Core Spec IR Schema — 规约层 IR (.corespecir)

## 概述

`.corespecir` 是 Core 的规约层中间表示，与 `.ccr`（实现层 IR）通过符号引用精确关联。格式待定，当前为占位阶段。

## 设计目标

- 独立于实现层 IR 存储和演化
- 规约表达式限于纯逻辑运算，无副作用
- 通过完全限定符号名引用实现层实体
- 支持前置条件、后置条件、循环不变量、类型不变量、边界条件

## 状态

当前编译器引导阶段尚未实现规约层 IR 生成。
