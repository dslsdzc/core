# R2 P5（收尾）实施计划：DFNode.TK 单槽化 + 大步删旧路径（路线 3）+ 文档/台账

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 兑现统一设计 spec §9 的 P5 行（**收尾**）+ §3.4「收尾：单槽 = 类型项引用 + `atom_of(term)` 取原子（后端改动面 ≈ 20 处比较点，收尾批一并改）」：① **单槽化**——把 DFNode 的**类型面**从「迁移期双槽（`OFF_DF_TK` 混用码 + `OFF_DF_TK_TERM` 项引用）」收拢为**项引用唯一真源 + 派生码**，并把混用码里的**非类型辅码**（旗标/宽度）显式分离（`OFF_DF_AUX`）；② **大步删旧路径（路线 3）**——删 `type_equal_legacy` 与全部回落面、删桥接层 legacy 副本、删站点 6 与不可达死分支、**影子判定对拍层下线**，并把「判定面回归网」切换为**冻结基线同源对拍 + 行为探针 + 突变控制**；③ **文档/台账**——spec 行标 / findings 终态 / TODO 划销链 / **P6 交接包（附录 E）**。**边界纪律（本批为收尾批）**：任何「新能力」（盘面项索引、corearch `atom_of` 读回承接面、对象布局表示位、`MAX_*` 解除、可选程序收回 `.cir` 缓存、命名消费语法、iterable 接线）**一律转 P6**，本批只做**删除 / 收口 / 零行为变化的单源化**。

**Architecture:** 六条主线：① **前置侦查与基线冻结**（Task 0：全 op `tk` 语义分类表 + 站点评级表 + -1 面清点 + 冻结基线二进制；**源码零改动**）；② **预存缺陷**（Task 1：`.cir` 暖态 SIGSEGV 根因修复——它是单槽化 `.cir` 面的地基，且属「响亮失败」级预存面）；③ **单槽化**（Task 2：槽语义对调 + 辅码分离 + `atom_of` 契约 + **派生码不变量**「新派生值 ≡ 旧 `tk`」）；④ **unknown 清零**（Task 3：命名面判定化——**身份链 + 可判定面加强**，不是结构性展开）；⑤ **删除批**（Task 4 删 legacy + 回落 + 副本 + 死分支；Task 5 影子层下线 + 回归网切换）；⑥ **独立收口与收官**（Task 6：TODO #2026-09-11-14 `EXPR_LET` 判定点；Task 7：收官）。

**Tech Stack:** Core 自举栈。`src/compiler/checker.cr`（`type_equal_legacy` :504 / `type_equal_engine` / `type_equal` 包装 / `type_compat_strict` :442 / `EXPR_LET` 分支 :3079）· `src/compiler/ty_shadow.cr`（桥接 `sh_term_of_ti` :275 / `sh_tk_term_of_code` :377 / 影子挂点与站点表 :472-538 / 展开层 :678 / `sh_*_ak_legacy` :101/:109）· `src/compiler/type_engine.cr`（判定核 `lit_implies` :165 / 变型表 :264 / `tt_list_variance_at` :291）· `src/compiler/dyn_arr.cr`（DFNode 布局 :103-121）· `src/compiler/dataflow.cr`（`df_create_node` / `df_rebuild_ir` :455）· `src/compiler/ir_gen.cr`（`emit` / :129 / :338）· `src/compiler/cir_cache.cr`（`:48` `CIR_CACHE_VER=17` / 装载重派生 :448-453）· `src/arch/x86_64/instr.cr` :460 · `regalloc.cr` :457 · `lower_to_core.cr` :250/:574 · `dump.cr` :125 · `ccr_io.cr`（NOD 盘记录 :143 / :511 / :768）。

**Spec:** `docs/superpowers/specs/2026-09-10-type-interface-unification-design.md` **§3.4**（迁移期双槽 → 收尾单槽）、**§4 步 4**（收尾 = 删旧分支/旧映射/双槽，单源化）、**§6.2**（DFNode.TK 升格落地记录 + 「收尾（单槽化）归 P5」）、**§9 P5 行**；风险表 **R5**（双槽期漂移：**term 为准，atom 由 `atom_of(term)` 派生**——本批的单源化口径即此句）。**P5 起点文档 = P4 计划附录 D**（`docs/superpowers/plans/2026-09-12-r2-p4-carrier.md:345-405`；D-1 口径表 / D-2 任务面 / D-3 十四项 / D-4 检查单）。

**前置状态（已实测，非本计划目标）**：P4 全阶段收官于 `9bcb7083`（工作副本 `8d1885df`，`jj status` 干净、无空描述改动）；`selftest-types` **355/355**（`src/ci/run.sh:71`）；ELF canary `95084e7bc68d6550d21d3d96fa3afd89c67a5d89edce5656a3d2e74fc923d475` 全批 IDENTICAL；自举链 `corec2==corec3` cmp IDENTICAL + N06=0 + 冒烟 42；全枚举 **55/55**（48 selfhost + 7 bootstrap）；`CIR_CACHE_VER=17`（`cir_cache.cr:48`，P4 T4 实测后**保持不 bump**）；`ESZ_DFNODE=72`（9 槽，`dyn_arr.cr:119-121`）；DFNode 项槽（`OFF_DF_TK_TERM`）**无生产消费者**（P4 T4 §7-②）。

---

## 实施状态总表（R2 P5，收官时点 2026-09-14；提交链 12 提交）

| 任务 | 内容 | 提交 | 状态 |
|---|---|---|---|
| T0 | 前置侦查 + 冻结基线（源码零改动） | `2b9b5463` | ✅ |
| T1 | `.cir` 暖态 SIGSEGV 根因修复（单一复用读缓冲；载入失败 ⇒ miss） | `11913f8a` | ✅ |
| T2 | DFNode.TK 单槽化（α）：类型面单源 + 辅码分离 + `atom_of` 契约 | `7f6c38cb` | ✅ |
| T3 | unknown 清零——命名面判定化（身份链 + `te_named_pair`；不接结构性展开） | `461d96f3` + 收口注 `5a333e0c` | ✅ |
| T3b | 不变槽元素三态 `te_elem_cmp`（T3 停条件② 残留面收口） | `faca94e0` | ✅ |
| T4 | 删 `type_equal_legacy` + 全部回落面 + 桥接 legacy 副本 + 死分支/站点 6（引擎唯一权威 + 残留 -1 政策 P-A/ICE04） | `e6db38b8` | ✅ |
| T5 | 影子层整体下线 + 回归网切换（冻结基线同源对拍 + 行为探针 + 突变控制 + 三态纪律） | `b6bf33cb` + 收口注 `d65ff9ad` + `66316751` | ✅ |
| T6 | `EXPR_LET`/全局声明位点兼容判定（TODO #2026-09-11-14 收口；TA02 硬错） | `04c20487` | ✅ |
| T7 | 收官：全量回归 + 判据复验 + 统一台账 + 文档 + P6 交接包（附录 E） | 本提交 | ✅ |

**未随批收口（显式转 P6，逐条 = 附录 E）**：β 盘面项索引 + corearch `atom_of` 读回 · 对象布局表示位 · `MAX_*` 解除（合批）· 可选程序收回 `.cir` 缓存 · `.csr`/TagNode 消费面 · 命名消费语法/iterable/形状可写视图 · T3b 揭开的 ¬ 面过判 · T6 的 `-> never` 调用面 · #20/F3 调用位点 · 冻结基线复现配方 · ICE04/P-A 签核 · 非硬名单诊断不阻 `build` · `string == int` 混比 · 单条目 >1GiB 分片 · 设计先例引用面。

---

## 收官总述：类型接口化工作流的终态（一页；维护者先读）

**单一权威（现在谁说最后一句）**

| 面 | 权威 | 位置 |
|---|---|---|
| 类型判定 | **语义子类型引擎**（`ty_equiv`/`ty_sub` 三态）——历史 `type_equal_legacy` 结构判等**已删**（T4），回落计数**已删**，「引擎 -1」不再静默：⇒ **ICE04 硬错**（P-A，入 `main.cr` 硬名单 ⇒ `build` 拒绝落盘） | `checker.cr::type_equal` → `type_engine.cr` |
| DFNode 类型面 | **类型项引用唯一真源 + 派生码**：`OFF_DF_TK`(40) = 类型项引用（-1 = 无）、`OFF_DF_AUX`(64) = 辅码；`iri_tk`/盘面一律 `sh_dfn_code_of_slots` **派生**；分类表 = 唯一分类真源 | `dyn_arr.cr` · `ty_shadow.cr` |
| 声明位点兼容 | `check_let_annot_compat`（局部 `EXPR_LET` + 全局 `check_global_let` 共用）⇒ **TA02 硬错** | `checker.cr` |
| 盘面 | `.ccr` v8 八段；NOD `tk` = **派生码**（进程内项引用**禁止**跨进程 —— D20）；`CIR_CACHE_VER=17` | `ccr_io.cr` · `cir_cache.cr` |
| 回归网（T5 起） | ① 冻结基线同源对拍（pre-P5 二进制 × 当前源 vs 当前二进制 × 当前源，72 档 `check` rc + 日志逐档 diff）② 行为探针（套件 + 30 档探针语料）③ 突变控制 ④ 三态纪律（未知/不可译 ⇒ 拒绝，绝不静默当 0/1） | `/tmp/p3t0_run.sh` + `tests/selfhost/*.py` + `main.cr` 硬名单 |

**退役的仪器（逐件 + 死亡证据 = 统一台账）**：`type_equal_legacy`（含 7 自调）+ 两处回落 + `g_replace_*` 计数（T4；证据 = 全语料清零判据 0）· 影子对拍通道 = 12 函数 + `sh_finish` + 17 个 `g_shadow_*` + 2 个 CLI 旗标 + checker 9 处挂点 + hits 存-复原 hack（T5；证据 = `sh_compare` 零调用点 ⇒ 全语料 `decisions=0`）· 站点 6（`EXPR_BINARY+OP_ASSIGN`）与 `ir_gen` 同分支（T4；证据 = parser 三面不可达 + 语料 0 命中）· 桥接 legacy 副本 `sh_*_ak_legacy`（T4；证据 = 零生产调用）· 桥接缓存**保留**（生产面，改名 `g_term_map`）。

**判据（收官终值，全部本实例实跑）**：五 CI job rc=0 · 全枚举 **58/58**（51 selfhost + 7 bootstrap）· `selftest-types` **404/404** · `check src/compiler` rc=0 · ELF canary `95084e7b…d475`（28822B）IDENTICAL · `corec2==corec3` `cmp` IDENTICAL（`5559beef…`）+ N06=0 + 冒烟 42 + `corec3 --help` rc=1 · 冻结基线同源对拍 **72 档零差异** · 探针全集 **30 档零差异** · 突变控制逐条转红 + 精确回滚 · `.ccr` 两套口径（`ptr_arith` 90823B/90966B · `generics_test` 135341B/135484B，冷态）· `--dump-objects`/`.cir` DOT 逐字节同 · 构建确定性 ×2（`corec f737e26b…`）。

**留 P6**：附录 E 逐条（现状/出处/影响面/建议/前置）。

---

## 裁决

**待维护者签核（四项；开工前须落纸。未签核时本计划 Task 步骤按「推荐」写死；改裁定 ⇒ 对应任务重定后重审）**

| # | 议题 | 推荐 | 备选与代价 |
|---|---|---|---|
| 裁-1 | **单槽化范围** | **α 本批 + β 推迟 P6**（α = 内存面单源化 + 派生码；β = 盘面项索引 + corearch `atom_of` 读回） | 备选：**β 并入本批**。代价 = `.ccr` NOD 记录扩面 + `CCR_VERSION` 8→9 级联 + corearch 发射路径改造 + 新记录布局台账；收益 = §6.4 图节点级类型链盘面自洽。**判据**：β 是**新能力**而非收尾（无在野消费者，P4 T4 §7-② 已登记），与「本批只做删除/收口」的边界纪律冲突 |
| 裁-2 | **unknown 清零路线**（D-3-4 的 (a)/(b) 二选一） | **(a) 命名面判定化**（Task 3；**身份链 + 可判定面加强**，**不接**结构性展开层） | 备选：(b) legacy 长期化 ⇒ **本批 Task 4/5 取消**、P5 不再是「删旧收尾」、影子层保留；另注：**结构性展开**（P3a 展开层直接接等价面）**不采**——P3a 已实测反证（同形不同名 struct 被判等价 = 语义漂移，且推翻 P2a 对拍归零基线，`ty_shadow.cr:678-689`） |
| 裁-3 | **影子层去向** | **整体下线**（Task 5；对照物随 legacy 一并消失，保留 = 无对照物的自欺） | 备选：保留为**调试通道**（需自造第二判据 ⇒ 等于重造 legacy；不采） |
| 裁-4 | **对象布局表示位 + `MAX_*` 定长槽**（D-3-7 / D-3-8） | **P5 不做，合并为一个 P6 批次**（前置四条见附录 E）；P5 只做「未覆盖面**不得退化为静默错值**」的守门复验 | 备选：本批合并做。代价 = 记录布局（`dyn_arr.cr` 定长槽）+ `ast.cr` 镜像归位 + `.ccr` SYM/段布局 + `OFF_*` 常量族 + 读回闸 + `sh_*` 展开层 **6 面同改** + `CCR_VERSION`/`CIR_CACHE_VER` 级联 bump（T6 解除评估 §3 已实测）；在野需求 = 0（语料最大结构体 9 字段 / 无超限枚举） |

**本计划裁定（D19–D28，实施时按此执行）**

| # | 决策 | 理由 |
|---|---|---|
| D19 | **单槽化形态（α）= 槽语义对调 + 辅码显式分离**：`OFF_DF_TK`（40）语义改为**类型项引用**（-1 = 无项；类型面唯一真源）；`OFF_DF_TK_TERM`（64）**改名 `OFF_DF_AUX`** 承接原混用码的**非类型部分**（旗标/宽度；0 = 无）；`ESZ_DFNODE` **保持 72B**（两槽对调语义，不增不减） | ① 现状 `tk` 是**混用槽**（`IR_BOUNDS_CHECK` 的 `1` = 动态上限旗标、`IR_DEREF`/`IR_STORE_PTR` 的 `8` = 访问宽度）⇒「单槽」在物理上不可能承载非类型信息（spec §3.4 的「单槽」= **类型面**单源，**非**「字段减一」）；② 不改字节数 ⇒ `.ccr`/`.cir` 布局与后端零扰动面最大化；③ 槽改名（`TK_TERM` → `AUX`）正是消灭「`tk` 被当类型行读」的陷阱类（P4 T4 §2 实测：宽度码 `8` 数值上恰是 `TI_DEX_S` 行、旗标 `1` 恰是 `TI_DEX` 行） |
| D20 | **项引用稳定形态裁定（D-3-2，单槽化前置）**：① **本批不引入任何跨进程项引用**——凡跨边界者（`.ccr` NOD 盘记录、`.cir` 快照）**一律派生码**（即：码 = 类型行/辅码，由类型表与分类表唯一决定）；② **未来形态规范（写入格式 spec 注释）**：需要跨边界时唯一许可形态 = **TYPE 段文件内项索引**（D13 确定性装填 ⇒ 文件内索引是稳定引用，corearch 已能重建该项表）；③ **禁令**：进程内项表索引**不得**跨进程/跨复位边界（`bad_term=329` 红证，`p4-task4-report.md` §4） | ① 稳定引用问题的根因是「进程内表索引被当稳定 ID」，而本仓已有根治先例 = 装载侧重派生（`cir_cache.cr:448-453`）；② 任何「纯函数值与其入参并存」= 第二真源（D15 修正的附因）；③ 把形态**规范**与**实现**分开落纸：规范立即生效（约束未来批次），实现（β）转裁定裁-1 |
| D21 | **`atom_of` 契约**：`sh_atom_of_term(term)`：`TT_ATOM ∧ b ≥ 0 ⇒ b`（类型行）；其余（union/product/μ/空链/负）⇒ **-1 = 非单一原子**（**不得**近似、不得回 0） | 桥接层单原子项的 `b` 槽**就是**类型行（`tt_atom(nak, ti, -1)`，`ty_shadow.cr:281` 区域）；非原子项（`T?` 的 union、元组/结构 product）在**现有后端分派点**上语义等价于「不是 TI_STR / 不是 TI_DEX」——实施时逐点核（Task 2 Step 4） |
| D22 | **两条硬不变量**（单槽化）：① **派生码不变量**：`iri_tk` 的新派生值 ≡ 旧值（**全语料逐节点对拍 + (op,tk) 直方图 diff = 空**）；② **互斥不变量**：辅码 ≠ 0 ⇒ 项 = -1；项 ≥ 0 ⇒ 辅码 = 0（分类表逐 op 唯一归位） | ① 把「零行为变化」变成**可判定**（而非「看起来没变」）；② 互斥不变量是防「一个 op 同时占两槽」的半成品消费面 |
| D23 | **类型行不可译 ⇒ save 拒绝**（照 D13 先例）：`emit` 期类型面 op 的 `tk` 若 `sh_term_of_ti` 回 -1（行越界/桥接缺口）⇒ **建项失败**，`save_ccr` 拒绝（rc=1 + 诊断），**不得**留空项静默落盘 | 三态纪律（-1 不得当 0/1）；D13 已确立「段内容 = 表纯函数，不可译即拒绝」的形态，节点项面同源 |
| D24 | **删除顺序（硬性）**：**清零（Task 3，判据 = 全语料 + 探针语料的回落计数 = 0）→ 删 legacy（Task 4）→ 影子层下线（Task 5）**；Task 4 与 Task 5 **必须连续提交**（Task 4 删掉对照物后影子判定对拍**停摆**——中间态不跑影子判据，且不得发布） | 影子通道是删除批的**安全网**（找不到差异 ⇒ 才敢删），删在清零之前 = 盲删；对照物与影子层是**同一根绳**（影子 `ok` 的唯一来源 = legacy，`checker.cr:612`） |
| D25 | **站点处置**：**站点 6 删除**（不可达：`tok2op`（`parser.cr:170-184`）不产 `OP_ASSIGN`；`T_EQ` → `EXPR_ASSIGN`（`:227`）；`+=` 族显式构造 `EXPR_ASSIGN` 包裹的 `EXPR_BINARY` 且 op ∈ {ADD,SUB,MUL,DIV}（`:216-224`）⇒ `EXPR_BINARY+OP_ASSIGN` 分支全仓不可达）；站点 1–5/7–10 **判定点全部保留**（只去 `sh_site_begin` 挂点），站点 3（2 次命中）同此；**站点 id 不重编号**（6 退役为 0 槽，注记落表） | D-3-6「零命中 ≠ 可删」：删前先出**评级表**（Task 0 Step 4）；站点下线 **≠** 判定点下线——判定调用一律保留（本任务是「删对拍挂点」，不是「删检查」） |
| D26 | **删件 → 替代证据（安全网替代，逐件落纸）**：① 影子判定对拍（in-process 旧/新对照）→ **冻结基线二进制的同源对拍**（72 档 × `check`，pre-P5 二进制 × 当前源 vs 当前二进制 × 当前源）+ **行为探针集**（Task 3 产物，**行为断言形态**，不依赖 legacy 存活）+ **突变控制**；② legacy 回落（引擎 -1 的兜底）→ **清零判据**（回落计数 = 0）+ **残留 -1 的显式政策**（硬错/登记，**绝不静默当 0/1**，三态纪律）；③ 桥接调试计数（`g_shadow_hits/entries` 在生产路径，`ty_shadow.cr:48`）→ 删除（含 `sh_tk_term_of_code` 的 hits 存-复原 hack） | 「网」不能只删不换；每件删除必须在**同一提交**里指明「过去谁覆盖它 / 现在谁覆盖它」，且新网必须**在删除前已验证可用**（Task 3 的探针必须在影子存活期跑出 0 差异，删除后再跑同一批） |
| D27 | **不收回/不新建**：可选程序 `.cir` 缓存**不收回**（随 D-3-9-(i) ⇒ P6，与 D-3-7 同批）；`--type-shadow`/`--type-shadow-dump`/`--verify-named-dedup` 三个隐藏通道**随影子层下线一并删除**（`main.cr:236-238`/`:244`/`:182`）；`tt_display` 零 `str_intern` 改造**不做**（登记 P6） | 收尾批的「删」与「不新增」边界；隐藏通道是调试/对拍结构进生产路径的同族（TODO #2026-09-11-8 同族清偿口径） |
| D28 | **判据引用口径逐字继承**（P4 D-1/Global Constraints）：`.ccr` **两套命令口径**必须带标（`corec ccr` vs `corec build --static`，差恒 143B）；**冷/热**：非可选程序冷≠热为**预存**（各自确定性可重复，豁免「冷≡热」判据）、可选面零足迹程序冷≡热成立；TYPE 段「纯函数」承诺**项面成立、行面不成立**（热路径行表 12/冷 15，前缀关系）；影子对拍**站点覆盖与差异数同报**；dumps 目录空 = `sh_dump_write` 早退 ⇒ **空洞证据不得引用**；比较任何 `.ccr`/缓存态产物前 `clean-cache` | 判据面已在 P4 收口；P5 不得回退到「与旧版逐字节同」的旧口径（TODO #2026-09-11-9），也不得复引 P4 作废值 |

---

## Global Constraints

- **jj only（禁 git 含只读/复合）**；**提交路径限定**（多 agent 在场：一律 `jj commit -m … <paths>`）；所有命令 `nice -n 19`；一构建一编译**串行**；cwd = 仓库根（`/tmp` cwd 触发 import 解析假失败）。
- **基线纪律**：每任务开工先取**同任务起点**基线（binary sha + 语料计数），**不得沿用 P4 计数**（P4 期间源码多次变更，`decisions`/`.ccr` 记录值跨提交不可比——T4 §7-⑧ 口径）。
- **ELF canary（硬闸，全批）**：`clean-cache` → `build tests/suite/ptr_arith.cr --static -o /tmp/…` → sha256 `95084e7bc68d6550d21d3d96fa3afd89c67a5d89edce5656a3d2e74fc923d475`。**任一任务变即停下上报**。⚠ 该语料对类型层不敏感——它是「发射面零泄漏」证据，**不得**当新语义正确性证据（P3 附录 C.5-6 口径继承）。
- **三态纪律（不得稀释）**：引擎 -1（预算耗尽/未覆盖面）不得当 0/1 用；载入失败/段缺失/行不可译/项不可建 ⇒ **拒绝**（不得静默当空表当 0）；save 侧装填遇不可译行/签名项 ⇒ **save 拒绝**。
- **新硬错先 report-only**：本批新增的任何 rc=1 门（含 Task 6 的 `EXPR_LET` 判定、Task 3 若引入的残留 -1 政策）必须先在**全语料**（`src/compiler/_import.cr` 自指 + `tests/suite` + `tests/selfhost` 内联源 + `src/stdlib` + `examples`）report-only 跑一遍，清单审查后才开门（硬门 = `main.cr` parse 阶段 `g_diag_count > 0` 硬名单闸，本轮实读 ≈:149-170；照 #29/TM03 先例）。
- **清单双注册面（B.6，硬性）**：任何清单/段表改动必须**三面同核**——`build_selfhost_native.py`（`corec_files`/`corearch_files`/`corelsp_files`）↔ `src/targets/x86_64-linux/_import.cr`（project-mode）↔ `src/compiler/_import.cr`；每任务回归面**必含** `tests/selfhost/test_backend_bootstrap.py`（rc=0 + 构建日志 `error[` = 0）。
- **回归网（本批切换；D26）**：Task 5 之前 = 影子对拍每任务复跑（**站点覆盖与差异数同报**；载体面零覆盖声明继承）；Task 5 之后 = **冻结基线同源对拍**（Task 0 冻结的 pre-P5 二进制 × 当前源 vs 当前二进制 × 当前源，72 档 `check` 日志 + rc 逐档 diff）+ 行为探针全集中跑 + 突变控制。**载体正确性证据**（段/读回/TK/表示）**一律** = 结构性断言 + corearch 读回对拍 + 定向探针（影子通道对其零覆盖，继承声明）。
- **`.cir` 缓存面**：`CIR_CACHE_VER` 手工 bump 是格式/语义代变更的既定配方；**DF 记录布局/语义一变即必须 bump**（v16 教训）。本批 D19 下**布局零变化 ⇒ 不 bump**（实施时以「盘记录逐字段对照 + `test_cache_identity.py:VER_EXPECTED` 不动」复证并登记）。
- **不得回退既有收纳**：TS01-04/TK02 硬门（#29）、TM03/TG02 门（#31/#33）、F2 常量档拒绝语义与「N 不回身份」、F5 槽位契约（#25/#28）、效应/纯度判据（#26/#27）、`iface_satisfies` 轴序 A>C>B、逐成员判定、P4 全批（段机制/确定性装填/允许清单制/可选表示）——本计划一律不动；冲突 → 停下上报。
- **本语言无三元运算符**；取模须非负；键比较不得依赖 i64 回绕；noclobber（`>|`）；自测用例**只增不减**；判据「≥N 例」= 下限（可增不可减）。
- **多 agent 纪律**：不并发构建；提交路径限定；本批为只读侦查 + 计划，实施时逐任务串行。

---

## 侦查底座（2026-09-13 实读；file:line = 当前工作树（父提交 `9bcb7083` = P4 收官，侦查时工作副本为空、无未描述改动；工作副本 id 随编辑变动，不作为锚点）；未标注者为本轮实读实核）

**判定面（删除对象）**
- `type_equal_legacy`：定义 `checker.cr:504`（降级注记 :495-503）；**内部递归自调 7 处**（:518 数组 / :528 元组 / :534 引用 / :537 指针 / :540 切片 / :547 可选 / :560 泛型应用）；**回落调用 2 处**（:590 桥接缺口→`g_replace_bridge`；:599 引擎 -1→`g_replace_unknown`）；**影子对照 1 处**（:612 `if type_equal_legacy(t1, t2) { ok = 1; }`）。判定入口链 = `type_equal`（:604-615）→ `type_equal_engine`（:573-601）→ `ty_equiv`。
- `g_replace_unknown`/`g_replace_bridge`：声明 `globals.cr:421`；复位 `checker.cr:246`；摘要打印 `ty_shadow.cr:628/:630`。
- 钉住回落语义的自测例 **5 处**（删 legacy 时必须重钉）：`t3.unknown_fallback_legacy`（`type_selftest.cr:1814`）· `t3.bridge_fallback_legacy`（:1821）· `unf.nominal_equiv_atomic_unknown`（:2487，断言 `ty_equiv(命名项, 命名项) == -1`）· `unf.nominal_type_equal_false`（:2492，断言回落 +1）· `t1.n_face_gate_nested`（:2691，断言**不**回落）。
- 桥接层 legacy 副本：`sh_native_ak_legacy`（`ty_shadow.cr:101`）/`sh_base_ak_legacy`（:109）；零生产调用，仅 `type_selftest.cr:1957/:1966/:1977` 三例引用。
- 影子层：站点表 `ty_shadow.cr:472-483`（10 站点；**站点 6** = `checker.cr:2454-2463` 的 `EXPR_BINARY+OP_ASSIGN`，:479 注「当前 parser 已无生产点 = 遗留路径，无样本」——**本轮独立复核成立**：`tok2op`（`parser.cr:170-184`）零 `OP_ASSIGN`、`T_EQ` → `EXPR_ASSIGN`（:227）、`+=` 族（:216-224）显式包裹且 op ∈ {ADD,SUB,MUL,DIV}；对侧 `ir_gen.cr:927-929` 同分支）；`sh_site_begin`（:504，关态早退 + 10 槽直方图）；环缓冲 `sh_record`（:519 起）/`sh_compare`（:539）/`sh_site_name`（:574）/`sh_kind_name`（:588）/`sh_report`（:607）/`sh_dump_write`（:651）；`sh_finish`/`sh_dump_write` 调用点（`main.cr:212-216`）；CLI：`--type-shadow`（`main.cr:236`）、`--type-shadow-dump`（:237）、`g_shadow_on`（:244）、`--verify-named-dedup`（:182/:238）。
- **桥接缓存（生产面，**不删**）**：`g_shadow_map`（16B/条 {ti, term}）+ `g_shadow_entries/hits`（`ty_shadow.cr:56-62`）；重置钩子 `sh_map_reset`（:130-133）经 `init_types()` 调用（`checker.cr:240`，P2a 评审 Critical 修复）+ `sh_unf_map_reset`（:243）+ `tt_layer_reset`（`type_terms.cr:144`，`ccr_types.cr:57-59` 亦调）。**登记**：计数在生产路径（`ty_shadow.cr:48` 注「影子关时也读写」）⇒ `sh_tk_term_of_code` 须存-复原 `g_shadow_hits`（:383-386 区域）——D26-③ 删除对象。
- 引擎命名面（清零对象）：`lit_implies` 两处 `AK_NAMED ⇒ g_ty_uncovered=1; return -1`（`type_engine.cr:192` 否定面 / :198 正原子面）；`ak_disjoint` 对 NAMED **不断言互斥**（:76-78，P0 终审 Important B 实证）；变型表 `ty_variance_of`（:264-277）表外构造子一律**不变**（含 AK_NAMED）；不变槽 mismatch ⇒ `g_ty_uncovered=1; return -1`（`tt_list_variance_at` :305-307）；`tt_type_elem_same`（:231）+ 其登记注（:219-230：**「确定不等价 ⇒ 0」加强须先有「链元素皆类型项」不变量**）。桥接命名构造点**两处**：`sh_term_of_ti`（`ty_shadow.cr:328`/`:330`）+ `sh_sig_term_of_ti`（:951）——**两处必须同步**（双构造点契约，照域/模式项先例）。
- 展开层（**本批不动**）：边界声明 + 反证 `ty_shadow.cr:678-700`（只在满足判定/域查询；等价面一律不经；「把展开项接进等价判定」= 同形不同名 struct 判等 + 推翻 P2a 归零基线）；实现 `sh_struct_term`（:833）/`sh_variant_term`（:874）/`sh_enum_domain_term`（:887）。**注意**：`sh_unf_map_reset` 等缓存不得 `str_intern` 面新增（留给 P6）。

**DFNode / 载体面（单槽化对象）**
- 布局：`ESZ_DFNODE=72`、`OFF_DF_TK=40`（混用码）、`OFF_DF_TK_TERM=64`（项引用；P4 新增）——`dyn_arr.cr:103-121`（含三条契约注：① `OFF_DF_TK` 语义/字节零变化；② 项槽只在 **emit 期**派生（时点不得后移）；③ 唯一写点 = `df_create_node` + `.cir` 装载重派生）。
- 写点：`df_create_node`（定义 `dataflow.cr:98`；tk 写 :108、项槽写 :109）；**唯一调用点 = `emit`**（定义 `ir_gen.cr:329`，调用 :345——本轮 grep 全仓仅此一处调用）；派生函数 `sh_tk_term_of_code`（`ty_shadow.cr:377`，**允许清单 = {IR_CONST, IR_BINARY}**，另有 4 处排除说明含「IR_ALLOC/IR_CALL/IR_I2F/IR_F2I/IR_LOAD 传类型行但后端不按 ti 分派 ⇒ 暂不入清单，**留待 P5 单槽化时按需再裁**」；hack = 存-复原 `g_shadow_hits`，:384-386）。
- 派生链：`df_rebuild_ir`（`dataflow.cr:448-455`，tk → `iri_set_tk`）· `ir_gen.cr:338`（`iri_set_tk(idx, type_kind)`）。
- 消费点全清点：`instr.cr:460`（`ti := iri_tk`，全后端 5 分派点：`IR_CONST`==TI_STR / `IR_BINARY`==TI_DEX / `IR_DEREF`+`IR_STORE_PTR` 的 access_width / `IR_BOUNDS_CHECK` != 0）· `regalloc.cr:457`（dex 危险面）· `lower_to_core.cr:250/:574`（HIT 降级）· `dump.cr:125`（文本）· `ir_gen.cr:129`（`arr_len_lit_of` 的 `TI_STR` 判定，读 **DF 节点** tk）· `interp.cr:256/:546`（读 DF 节点 tk）· `provenance_verify.cr:57`（读 DF 节点 tk 当**宽度**）· 盘面 `ccr_io.cr:511`（→ `OFF_NS_TK`）/:768（→ NOD 记录）· 快照 `cir_cache.cr:323`（写 `iri_tk`）/:540（读）/:448-453（DF 节点装载 + 项槽重派生）。
- 盘记录：`ESZ_NOD_DISK=36`（`ccr_io.cr:143`）；`.ccr` 八段结构断言套件 `tests/selfhost/test_ccr_types.py`（34 例）；快照版本 `CIR_CACHE_VER=17`（`cir_cache.cr:48`，:28-46 为「为何不 bump」的长注）。
- 冷/暖面：DF 节点装载 loop（`cir_cache.cr:431-453`）+ IRI 装载 loop（:530-545）**两路对称**；快照入场闸 = 版本 `:383` / 编译器身份 `:389-391` / 指纹 `:396`；P4 ㉙ 用例锁「冷/暖 (op,tk,term) 逐行同」。

**TODO / 未收口面**
- `#32`（`EXPR_LET` 无检查）：`checker.cr:3079-3097` 的 `EXPR_LET` 分支——`val_ti := infer_expr(val_c)` 后 `ti := val_ti; if type_node >= 0 { ti = res_type_node(type_node); }`（:3096-3097）**无任何兼容检查**；可复用判定 = `type_compat_strict`（:442）+ `diag_type_incompatible`（:486）。姊妹面 = #20（F3 调用位点无诊断：`unify_types`（定义 :1905）的返回值在 :2017 被丢弃——**TODO #2026-09-11-14 记录的旧锚 `:1053` 已随行漂移**，以本次实读 :2017 为准）。**全局声明面同形**（`checker.cr:1614-1620` 全局 let 注册亦无比对——#32 的修法须显式划界是否覆盖）。
- `#35` **已由 P4 T6 划销**（`test_enum_limit.py` 13 例）；`#43` = P4 T5 落地（其「登记面」= 可选程序关缓存 + 暖态 SIGSEGV，本计划 Task 1/D27 处理）；`#44` = P4 收官（本计划 Task 7 接续）。
- 判据面：`src/ci/run.sh:68-90`（selfhost-tests 挂点，含 `test_ccr_types`/`test_optional`/`test_enum_limit` 等）；72 档同源对拍 runner `/tmp/p3t0_run.sh`（**本轮实查存在**，2026-09-12 版）。

---

## Task 0（前置，纯侦查 + 基线冻结；**源码零改动**）

**Files:** 无源码改动。产物 = 报告 `.superpowers/sdd/p5-task0-report.md`（工作区，不入提交）+ 冻结基线二进制 `/tmp/p5t0/base/{corec,corearch,corelsp}` + 直方图/清点原始输出留档（`/tmp/p5t0/`）。

**Interfaces（产物四表，全部落报告；数字一律实测，未实测标「待实测」）：**
- **表 A：全 op `tk` 语义分类表**（单槽化 Step 1 的输入）——逐 op：`{类型行 / 旗标 / 宽度 / 无}` + 后端消费者（`instr.cr:460` 的 5 分派点 / `regalloc.cr:457` / `lower_to_core.cr:250·574` / `dump.cr:125`）+ 是否入项槽 + 依据（P4 T4 §2 方法扩面：`instr.cr` 分支逐条审计 + `ir_gen.cr` 全 `emit(` 调用点逐 op 静态审计 + 全语料 NOD (op,tk) 直方图）。**必须显式再裁 P4 排除项**：`IR_ALLOC`/`IR_CALL`/`IR_I2F`/`IR_F2I`/`IR_LOAD`/`IR_LOAD_ENUM_TAG`（P4 T4 §2 注明「留待 P5」）。
- **表 B：站点评级表**（D-3-6 / D25）——站点 1..10 逐条 `{语义 / 可达性证明 / 语料命中 / 判定点保留? / 挂点去留}`；站点 6 附不可达三面证明（`parser.cr:170-184` / `:227` / `:216-224`）。
- **表 C：P5 起点基线**——冻结二进制三 sha + 冒烟；72 档 × {`check`, `shadow`} 的 `decisions`/`agree`/全计数/站点覆盖/rc 分布（**以此为新基线**，P4 的 32620 等值不得直接沿用）。
- **表 D：-1 面清点（report-only）**——探针语料逐形态记录的 `ty_equiv`/`ty_sub` 负值计数 + `g_ty_uncovered`/`g_ty_exhausted` 归因（Task 3 的输入清单）。

- [x] **Step 1: 冻结基线**：`nice -n 19 python3 build_selfhost_native.py`（串行、无其他构建）→ 记录三二进制 sha + 冒烟 42；**再构建一次**取「构建确定性」（两次逐字节同）；拷贝到 `/tmp/p5t0/base/`。判据：两次构建 sha 同 + 冒烟 42 + `/tmp/p5t0/base/corec` 可独立跑 `check`。
- [x] **Step 2: 语料基线与 runner**：核 `/tmp/p3t0_run.sh` 可用（不存在则按 T7 口径重建：72 档 = `tests/suite` + 自源 + 后端/内核单元 + `src/stdlib` + `examples`，逐档 `clean-cache`）；复跑 {`check`, `shadow`} → 表 C。
- [x] **Step 3: 表 A**（全 op `tk` 语义分类表；含排除项再裁）——**这是 Task 2 的判据输入，必须逐 op 有依据行，不得留空**。
- [x] **Step 4: 表 B**（站点评级表；站点 6 不可达证明）。
- [x] **Step 5: 表 D**（-1 面清点）：构造/汇总**命名面 + 泛型应用面 + 边界形态**探针（同/异名命名、两次实例化的同实参泛型应用、递归命名 `struct Node { next: *Node }`、`*Named`/`[Named;N]`/`Named?`/元组含命名、别名一层、泛型形参位、union/μ 面、预算耗尽面），逐条记录引擎负值 + 归因（在第二次 `ty_budget_reset` 前读 `ty_uncovered`/`ty_exhausted`——P1 Step 0 口径）。
- [x] **Step 6: D-4 开工检查单逐项核**（附录 D 六项）并落报告：口径表 / D-3-2 先裁 / D-3-4 先裁 / D-3-7+8 合并评估 / D-3-9-(ii) 排期 / #32 排入 / 判据沿用清单。
- [x] **Step 7: 报告 + 提交**（**仅报告不入源码**；提交 = 计划文件本身 + 报告路径登记若维护者要求）——本任务默认**不提交源码**（`jj status` 应干净）。

**判据**：四表齐 + 检查单逐项有结论 + 源码零改动（`jj status` 干净）+ 报告内数字全部可追溯（命令 + 原始输出片段）。

### Task 0 实施记录（2026-09-13；报告 `.superpowers/sdd/p5-task0-report.md`）

**冻结基线**（本批下游任务的比较基准）：`build_selfhost_native.py` ×2 逐字节同；`corec 5d2b15ad…` · `corearch 493dc490…` · `corelsp 18b94bd9…`（= P4 记录值）；ELF canary `95084e7b…d475`（28822B）IDENTICAL；`corec2==corec3` cmp IDENTICAL（双 `4919023b…`）+ N06=0 + 冒烟 42 + `--help` rc=1；全枚举 **55/55**；`check src/compiler` rc=0；`selftest-types` **355/355**；`.ccr` 两套口径冷态四条与附录 D-1 逐条同（90823/134701/90966/134844）。**72 档语料基线**（runner `/tmp/p3t0_run.sh`，sha `12dc571f…`）：`check` rc 分布 **34×0/38×1**；shadow 70 档 `decisions=32620 agree=32620`、全计数 0、站点覆盖 `assign-node=24721 · fn-body-ret=5828 · if-branch=2037 · struct-field-type=28 · array-elem-type=4 · generic-apply-base=2`（其余 4 站 0；站点 6 = 0 命中佐证不可达）。产物 `/tmp/p5t0/{base/,parity/base_{check,shadow},tkhist/,probes/,negprobes/,enum_chain.out,corec2,corec3}`。

**四表结论（摘要）**：**表 A** 51 opcode 逐行分类（类型行 9 · 旗标 1 · 宽度 2 · 无 38 · 填充伪值 1）；后端 `ti` 消费者机器审计 = 恰 5 分支；现行允许清单量化复核（790828 节点）`term≥0` 恰 = {CONST,BINARY} 且清单外带项 = 0。**表 B** 10 站点评级（5 站有命中、5 站零命中）+ 站点 6 不可达三面证明（parser `tok2op`/`T_EQ`/`+=` 族 + 语料 0 命中）。**表 D** 9 探针：异名同形命名 + 同实参泛型应用两次实例化为实测引擎 -1（均 `unknown_engine_uncovered` + `replace_unknown` 回落），其余形态 0 负值；`ty_sub` 负值无计数通道（读码代替，证据级登记）；预算耗尽面用户语料不可达。

**新增开工前未决项（T2 前必须落纸；报告 §6）**：① **F1** `ir_gen.cr:1758` 的 5 参 `emit` 调用 ⇒ `IR_HOTPATCH_ROUTE` 的 tk = r9 残留（实测 0/4 两态；`bootstrap/backend/x86_64_stack_asm.py:222-226` 只为实参写寄存器）——建议 T2 同提交补显式第 6 实参并把派生码 4→0 登记为已裁决偏差；② **F2** `atom_of` 对**复合行**不可逆（实测 `ptr_ref_first` BINARY tk=10 = 指针行 ⇒ `tt_atom(AK_PTR,-1,chain)` 的 b=-1 ⇒ 派生 ≠ 旧值）——T2 须在「扩 atom_of 携行 / 复合行走 aux / 分类表限原子行」三条中先裁（D22-① 在这三类 op 上不可满足）；③ op22/41/0 零发射点（死 opcode）、op35/36 仅 `@x()` 括号形态可达（语句形态静默无 IR）——登记不修。

## Task 1：`.cir` 暖态 SIGSEGV 根因修复（预存缺陷；D-3-9-(ii)，独立可做）

**Files:** Modify `src/compiler/cir_cache.cr`（根因处）· 可能 `src/compiler/main.cr`（cache 使能面）/ `src/stdlib/fmt.cr`（`str_len` 消费面——**只在根因确证涉及时**）· New/Modify `tests/selfhost/test_cache_identity.py`（或新套件 `test_cir_warm_path.py`）。

**背景（登记值，实施时**先复现**，不得引用为已证）**：`corec build src/compiler/main.cr -O 0` **冷 rc=0 / 暖 rc=139**；gdb 栈 = `load_cir_cache`（`cir_cache.cr:373`）→ `str_len`（`fmt.cr:4`）← `load64`；**基线二进制同病**（P4 T5 §7-5 实测，非 P4 引入）。P4 T5 的「可选面关 `.cir` 缓存」只**顺带规避可选程序**，非本缺陷的修法。

- [x] **Step 1: 红态复现（本任务自跑）**：`clean-cache` → 冷运行 rc=0 → **第二次**运行 rc=139（复现记录 = 命令 + rc + 是否稳定可重复）；gdb 栈复现并落报告；用 `/tmp/p5t0/base/corec`（Task 0 冻结）复证「预存、非本批引入」。⚠ 复现时注意 TODO #2026-09-10-1 家族的 clean-cache 口径（跨重建必须 clean，相对 cwd）。
- [x] **Step 2: 归因（写读对称表）**：把快照的写侧（`cir_cache.cr` 写点）与读侧（:430-560 两个装载 loop + 入场闸 版本 `:383` / 身份 `:389-391` / 指纹 `:396`）逐字段列成**对称表**（字段 / 写偏移 / 读偏移 / 长度来源），核 `pos` 推进与长度域；**定位根因**（候选：长度字段读偏/越界、0 长与空串混淆、函数计数与实际记录数不一致、身份闸后的残余旧条目），**不得**先加守卫吞异常（若最终只能加守卫，必须在报告里说明根因不可达的实测证据 + 停下上报）。
- [x] **Step 3: 用例（红→绿）≥6 例**：① 冷/暖两次运行 rc 均 0；② 冷/暖产物（ELF + `.ccr`，带口径）逐字节同；③ 多函数/多串/0 串/大串/含可选程序（关缓存面不受影响）各一；④ 既有 `test_cache_identity.py` 全绿不回归；⑤ 自源暖态（本缺陷的红态语料）转绿。
- [x] **Step 4: 判据**：新套件全绿；**ELF canary IDENTICAL**；五 CI job rc=0；全枚举 55/55；`corec2==corec3` cmp IDENTICAL + N06=0 + 冒烟 42；`.ccr` 记录值（两套口径）冷态复测；`test_backend_bootstrap.py` rc=0 + `error[`=0；自源 `check src/compiler` rc=0。
- [x] **Step 5: 提交**：`fix: R2 P5 Task 1——.cir 暖态 SIGSEGV 根因修复（...根因一句话...）；冷/暖两态产物一致回归`

**停条件**：若归因指向格式缺陷（需 `CIR_CACHE_VER` bump）或更深的缓存设计 ⇒ **停下上报**（bump 是允许面但须单独裁决 + 记失效面）；若暖态 SIGSEGV 与「快照面语义」纠缠到无法在本任务内隔离 ⇒ 上报转独立批（不硬塞进 P5）。

### Task 1 实施记录（2026-09-13；报告 `.superpowers/sdd/p5-task1-report.md`；证据 `/tmp/p5t1b/`）

**根因（实测，非引用）**：装载侧**逐函数** `read_file(path)` = `alloc(fsize+1)`，而 bump 分配器（`rt.s alloc`）**不回收** ⇒ 946 个快照读缓冲**累计 1.1674 GiB > 1 GiB 堆**（`rt.s heap_start: .space 1024*1024*1024`；单条目 max 3,987,879B = 3.80MiB，因快照含**全程序边表** ⇒ 尺寸 O(程序) 而非 O(函数)）⇒ `alloc` 耗尽返回 0（`.Lalloc_oom`）⇒ `load_cir_cache` 首行 `str_len(0)` → `load64(0,-8)` = **暖态 SIGSEGV**（gdb 三跑同栈 `load64(rdi=0,rsi=-8) ← str_len ← load_cir_cache`；冷态不读快照 ⇒ rc=0）。**停条件核**：写读对称表逐字段核（字段/写偏移/读偏移/长度来源 + `pos` 推进）⇒ **无长度域/格式缺陷 ⇒ 不 bump `CIR_CACHE_VER`**（保持 17）。

**修法（根因级，非加钳位）**：`cir_cache.cr::cir_read_snapshot`（新增 30 行）= **单一复用读缓冲** `g_cir_read_buf/g_cir_read_cap`（峰值 = 最大单条目；读侧自此与写侧既有 `g_cir_write_buf/g_cir_write_cap` **同构**——读侧原是唯一逐调用新分配方）；`w64(buf,-8,fsize+1)` 保住 `str_len` 口径 ⇒ **消费面（r64/str_sub 四点）零改动**；载入失败（open 失败 / `fsize<=0` / 短读 / 缓冲不可得=read EFAULT / 长度<56）⇒ `-1` = **cache miss**（三态纪律：载入失败 ⇒ 拒绝，miss = 无害重建，绝不静默当空表）。刻意**不写** `buf == 0` 形态空判（该 string-int 混比在 bootstrap 侧 `x86_64_stack_asm.py:164` 编成 `str_eq(buf,0)` ⇒ `str_len(0)` 自崩溃；**建议另单**，登记于报告 §6-①）。

**判据（全部本实例实跑）**：新套件 **19/19**（`COREC_WARM_SELF=1` ⇒ **21/21**；RED 对照：冻结基线 × 同套件 = **15 PASS/1 FAIL**（D3 rc=-11））· `build_selfhost_native.py` rc=0（`corec` `8edb967f…` 两次构建逐字节同；`corearch`/`corelsp` = T0 记录值）· `check src/compiler` rc=0 · `test_backend_bootstrap` rc=0 + `error[`=0 · `selftest-types` **355/355** · 全枚举 **56/56**（48+1 selfhost + 7 bootstrap；+1 = 本套件）· ELF canary `95084e7b…d475` **IDENTICAL** · **暖≡冷（自源）**：冷 + 暖×2 三跑 ELF 同 sha `13230542f79da591c4ea…`（`cmp` IDENTICAL）、`.ccr` **八段冷==暖1==暖2**、946 条目戳三跑全等（**真命中**）、冻结基线冷产物亦同 sha（零发射面漂移）· 自举链 `corec2==corec3` `cmp` **IDENTICAL**（双 `13230542…`）+ N06=**0** + 冒烟 **42** + `--help` rc=1 · **突变控制**（M1：`load_cir_cache` 回灌修复前读路径 ⇒ 套件 **15 PASS/1 FAIL** 转红；精确回滚 ⇒ 源 sha `1220742f…` 复原 + 三二进制**逐字节回原值**）· 语料既有失败面无新增。

**登记（不静默）**：① `string == int` 混比双路径分歧（bootstrap `str_eq` 分派；本修复绕开未修，建议另单）· ② `g_cir_read_cap` 于 alloc 失败时仍前进 = **良性确定性**（此后恒 EFAULT ⇒ miss；与写侧同形）· ③ 单条目 > 1GiB 的「格式分片」议题（当前 3.99MB，非本任务面）· ④ `.cir` 其余口径逐字未动（VER=17 / 身份闸 / 指纹闸 / 项槽重派生 / save 侧）。**交叉引用**：TODO #2026-09-13-4 登记面 ⓪、#44 交接包 D-3-9-(ii) **本提交闭合**。

## Task 2：单槽化（α）——内存面单源化 + 辅码分离 + `atom_of` 契约

**Files:** Modify `src/compiler/dyn_arr.cr`（槽语义/偏移改名 + 布局注）· `src/compiler/dataflow.cr`（`df_create_node` 签名 + 存储 + `df_rebuild_ir` 派生）· `src/compiler/ir_gen.cr`（`emit` 分流；:129 消费点）· `src/compiler/ty_shadow.cr`（分类表驱动 + `sh_atom_of_term`；`sh_tk_term_of_code` 改造）· `src/compiler/cir_cache.cr`（装载侧重派生；**布局零变化**）· `src/compiler/dump.cr`（`--dump-tk-terms` 扩展）· `src/compiler/type_selftest.cr`（`t4.tk_*` 重钉 + 新例）· `tests/selfhost/test_ccr_types.py`（T4 节扩展）· New `tests/selfhost/test_single_slot.py`（或并入既有套件）。

**Interfaces:**
```
ESZ_DFNODE : int = 72;                  // 字节数不变（D19）
OFF_DF_TK   : int = 40;                 // ★语义变更：类型项引用（g_type_terms 行号；-1 = 无项）= 类型面唯一真源
OFF_DF_AUX  : int = 64;                 // 原 OFF_DF_TK_TERM 槽位改名：非类型辅码（旗标/宽度；0 = 无）

fn sh_tk_face_of_code(opcode: int, tk: int) -> int   // 分类表（Task 0 表 A 的代码化）：0 = 类型行 / 1 = 辅码 / 2 = 无
fn sh_tk_term_of_code(opcode: int, tk: int) -> int   // 类型行 ⇒ sh_term_of_ti(tk)；辅码/无 ⇒ -1（分类表驱动，取代 {CONST,BINARY} 硬清单）
fn sh_atom_of_term(term: int) -> int                 // D21：TT_ATOM ∧ b ≥ 0 ⇒ b；否则 -1

fn df_create_node(opcode, dest, src1, src2, src3, tk_term: int, tk_aux: int) -> int
//   存储：OFF_DF_TK ← tk_term；OFF_DF_AUX ← tk_aux（互斥不变量 D22-②）
```
- [x] **Step 1: 用例（红）≥10 例**：① **类型面**：分类表为「类型行」的每类 op 各一正控（项槽 = `sh_term_of_ti(tk)` 同项）；② **辅码面**：`IR_BOUNDS_CHECK`（旗标 1）/`IR_DEREF`/`IR_STORE_PTR`（宽度 8/4/2/1）三负控——项槽 **-1** ∧ 辅码槽 = 原码；③ **数值陷阱负控**（P4 同款）：旗标 `1` 与宽度 `8` **不得**被当类型行建项（`TI_DEX`/`TI_DEX_S` 行）；④ **无类型面**：`tk` 恒 0 的 op ⇒ 两槽皆空；⑤ **派生码不变量**：全语料 `iri_tk` 逐节点 == 旧值（对拍脚本 + (op,tk) 直方图 diff 空）；⑥ **互斥不变量**：任一节点两槽不同时非空；⑦ **不可译拒绝**（D23）：构造行越界/桥接缺口的类型面 op ⇒ `save_ccr` 拒绝 rc=1 + 诊断 + 无产物；⑧ **冷/暖**：项槽与辅码槽两态同值同索引；⑨ **`.ccr`/`--dump-objects` 逐字节同**（发射面零泄漏）；⑩ **版本不动**：`CIR_CACHE_VER == 17` ∧ `test_cache_identity.py:VER_EXPECTED` 不动。
- [x] **Step 2: 写点分流**：`emit` 按 `sh_tk_face_of_code(opcode, type_kind)` 分流：类型行 ⇒ `tk_term = sh_term_of_ti(tk)`、`tk_aux = 0`；辅码 ⇒ `tk_term = -1`、`tk_aux = tk`；无 ⇒ 皆空。`df_create_node` 签名同步（**唯一调用点 = `emit`**，逐处核）；`dataflow.cr` 保持「仅存储、零前端符号引用」纪律（注记落文件头）。
- [x] **Step 3: 派生面**：`df_rebuild_ir` 由 `(tk_term, tk_aux)` 派生 `iri_tk`：`tk_aux != 0 ⇒ tk_aux`；否则 `atom_of(tk_term)`；否则 0。**逐节点对拍旧值**（Step 1-⑤）。`.cir` 装载侧（`cir_cache.cr:448-453`）保持「重派生项槽」并把辅码槽按盘上码重派生（盘面仍 8 槽；DF 节点装载 loop 内两槽皆**重派生**，不得读盘项槽——P4 时点纪律）。
- [x] **Step 4: 消费点核（逐点登记，后端预期零改动）**：`ir_gen.cr:129`（DF 节点 tk 的 `TI_STR` 判定）改走 `atom_of`（或按分类表保留派生码——**二者择一并落纸**）；`interp.cr:256/:546`、`provenance_verify.cr:57` 同款逐点核（**读类型面 vs 读辅码面**必须分类正确——`provenance_verify.cr:57` 读的是**宽度** ⇒ 应改读 `AUX`）；`instr.cr:460`/`regalloc.cr:457`/`lower_to_core.cr:250·574`/`dump.cr:125` **保持读 `iri_tk`**（派生码不变 ⇒ 零改动，逐点核并登记「读的是派生码，不是槽」）。
- [x] **Step 5: 判据（零行为变化，硬闸）**：**ELF canary IDENTICAL**（变即停下上报）· `.ccr` 冷态逐字节同（同源基线，带口径）· `--dump-objects` 逐字节同 · **全语料 NOD/IRI (op,tk) 直方图 diff 空 + 逐节点对拍空** · `selftest-types` ≥355 → ≥365 · `test_ccr_types.py` ≥34 → ≥40 · 五 CI job rc=0 · 全枚举 55/55 · 自举链（cmp IDENTICAL + N06=0 + 冒烟 42）· `test_backend_bootstrap.py` rc=0 + `error[`=0 · 影子对拍复跑（下线前；**预期零变化**，站点覆盖同报）· **突变控制 ≥3 条**（分类表某格改坏 ⇒ 用例 + 直方图转红；辅码槽写成项 ⇒ 互斥断言红；`atom_of` 近似 ⇒ 直方图转红）。
- [x] **Step 6: 提交**：`refactor: R2 P5 Task 2——DFNode.TK 单槽化（类型面单源：OFF_DF_TK=项引用 + OFF_DF_AUX=辅码；分类表驱动派生；零行为变化）`

**风险与停条件**：分类表任一行证据不足 ⇒ **停下补测**（不得猜）；若发现某 op 的类型面**既非类型行又非辅码**（第三类）⇒ 上报（D19 需扩第三槽或另裁）；`.cir` 面若因派生顺序产生冷/暖分歧 ⇒ 停下（P4 ㉙ 用例应立刻转红）。

### Task 2 实施记录（2026-09-13；报告 `.superpowers/sdd/p5-task2-report.md`；证据 `/tmp/p5t2/`）

**形态（D19 落地）**：`OFF_DF_TK`(40) = **类型项引用**（类型面唯一真源）、`OFF_DF_TK_TERM`(64) 更名 **`OFF_DF_AUX`** = 辅码（0 = 无）；`ESZ_DFNODE` 保持 72B。**分类表代码化**（`ty_shadow.cr`：`sh_tk_is_type_face`/`sh_tk_row_state`/`sh_atom_of_term`/`sh_tk_face_of_code`/`sh_tk_split`(+`_load`)/`sh_dfn_code_of_slots`）：类型行面 = `{CONST, BINARY}` ∪ P4 六排除项再裁的 `{ALLOC, CALL, LOAD, I2F, F2I}`（P4 的「后端不按 ti 分派」= 发射面判据，不适用于语义类型面）；辅码面 = `BOUNDS_CHECK/`DEREF`/`STORE_PTR`/`SPAWN`/`HOTPATCH_ROUTE`；其余无面。写点 = `df_create_node`（仅存储；BOUNDS_CHECK 的旗标依赖改读辅码）；派生点 = `emit`/`lower_to_ccr`/`cir_cache` 装载侧共用 `sh_dfn_code_of_slots`。

**F1（裁决：修调用点）**：`ir_gen.cr:1758` 补显式第 6 实参 `0`（op39 属无面 ⇒ 显式「无类型」）；已裁决偏差 = 含 hotpatch 语料该节点码 **4→0**（全语料 3 节点，热补丁测试档；该值本是 r9 残留垃圾、零消费者）——`hotpatch_test.cr` 的 `.ccr` 相应差 3 处（逐字节定位见报告 §3/§5-①）。

**F2（裁决：分类表只覆盖原子行 + 复合行走辅码）**：复合行（ptr/ref/array/slice/optional/null/tuple 行——项不可逆）⇒ `term = -1 ∧ aux = 原行码`（Task 0 候选 (iii)+(ii)）⇒ **派生码逐字节 ≡ 旧混用码**（D22-① 全语料成立，含复合行）。**证明**（协调者要求的审计 + 探针/负控）：5 个 `ti` 消费者谓词的值敏感面 = `{TI_STR=3, TI_DEX=1, 宽度, 旗标}`，复合行码（实测仅 `ptr_ref_first` BINARY tk=10 一例 + 夹具 tk=13）**永不落入**该面；且本设计下消费者读到的是**旧值本身**（非裁决备选的 -1）⇒ 不变按构造成立（突变 M1 反证该格可抓）。

**D23 闸（新硬错）**：类型面行的可译性 —— **仅 emit 路径**计数（装载侧不计数：暖进程类型表可更小，见下「判据重定」）；`ccr_seg_prepare_save` 入口拒绝落盘（rc=1 + 计数诊断）；**全语料 0 命中**（94 档 `face_fail=0`，report-only 先行）；牙齿 = 突变 M4。

**判据重定（2 处，实测驱动）**：① 冷/暖比对由「索引全等」改为「**派生码面（op,tk,aux）逐行同 + 项内容（tag/a/b/c）等价**」——暖态类型表/项表可小于冷态（缓存命中跳过 `ir_gen` ⇒ 其 `alloc_type` 新行不存在；P4 窄清单掩盖、P5 扩面暴露；根治 = β 盘面项索引，P6）；② `--dump-tk-terms` 头部新增 `rows=`（进程类型表行数）与 `aux_nonzero=`/`face_fail=` 观测列，供上述精确形态断言。

**判据（全部本实例实跑）**：`selftest-types` **367/367**（355+12）· `test_ccr_types` **40/40**（34+6）· `test_cir_warm_path` 19/19 · `test_cache_identity` 5/5（`VER_EXPECTED=17` 未动）· `check src/compiler` rc=0 · `test_backend_bootstrap` rc=0 + `error[`=0 · **ELF canary `95084e7b…d475` IDENTICAL**（28822B）· `.ccr` 两套口径冷态四条与起点基线**逐条同** · `--dump-objects` 逐字节同（2427 行）· **派生码不变量**：94 档直方图逐节点 diff **= 仅 F1 的 3 节点**（非自源 338,207 节点余者 0）· 全枚举 **56/56** · 五 CI job 全 rc=0 · 自举链 `corec2==corec3` cmp IDENTICAL（2821374B）+ N06=0 + 冒烟 42 · 72 档 `check` 同源对拍（rc 同、日志 diff 仅 2 档自源行号位移）· 影子对拍 `decisions=agree=32767`、全分类计数 0（非自源 67 档逐字节同）· **突变控制 4 条**逐条转红（复合行入项槽 / 两槽同置 / 派生忽略辅码 / D23 越界行）。

**执行偏离（诚实登记）**：① 首轮 `hits` 复原只包住 `sh_term_of_ti`（分类查询漏网）⇒ P4 计数纪律用例转红 ⇒ 已按根因修正；② 首轮 D23 对装载侧也计数 ⇒ 暖态误拒（用例抓红）⇒ 收口为 emit-only；③ 一次自造并发（基线收尾期跑套件）⇒ 两条假红，已识别并全部串行重跑。

## Task 3：unknown 清零——命名面判定化（身份链 + 可判定面加强）

**Files:** Modify `src/compiler/ty_shadow.cr`（`sh_term_of_ti` 命名分支 :328/:330 + `sh_sig_term_of_ti` :951 **两处同步**；新增身份链构造）· `src/compiler/type_engine.cr`（AK_NAMED 可判定面规则 + 不变槽加强的**适用域**与守卫）· `src/compiler/type_selftest.cr`（`unf.nominal_*` 两例重钉 + 新例）· `src/compiler/globals.cr`（如需新归因计数）· New `tests/selfhost/test_named_face.py`（**行为断言形态**——它必须能在 shadow 下线后独立存活）。

**Interfaces:**
```
// ── 桥接侧：命名面身份链（取代空链 + b=行号 当身份）──
//   TYP_NAMED / TYP_GENERIC_PARAM : c = [sh_name_token(name_ni)]
//   TYP_GENERIC_APPLY             : c = [sh_name_token(base_ni), 实参项…]
//   （b 槽 = 行号仍是**标注**，不参与判定——引擎只比 a/c，见 type_engine.cr:281 注）
fn sh_named_identity_term(ti: int) -> int      // -1 = 不可构造（⇒ 清零不成立，上抛）
// ── 引擎侧：AK_NAMED 可判定面（**不得**用结构性展开）──
//   两条规则（适用域见下）：① 同链（逐元素同一性）⇒ 1；② 链异且**两侧链元素皆为类型项/身份令牌** ⇒ 0
//   边界：union/μ/未范化面**不适用**（保持三态 -1，登记）——加强须先满足「链元素皆类型项」不变量
```
- [x] **Step 1: 用例（红）**：命名面探针逐条断言 **`ty_equiv` ∈ {0,1}（非 -1）** 且**与 legacy 同值**：同/异名命名、两次实例化同实参的泛型应用（行不同 ⇒ 必须判等）、异实参泛型应用（判不等）、递归命名（`Node` vs `Node`；`*Node` vs `*Node`）、`[Named;N]`/`Named?`/元组含命名、别名一层、泛型形参位（`T` vs `T` / `T` vs `int`）、`AK_NAMED` vs 非命名（product/tuple/原生）。**同时用例必须落「负控」**：union/μ 未范化面**仍** -1（不得被加强误判为 0）。
- [x] **Step 2: 清零清点（report-only，**删除前置**）**：全语料 + 探针语料跑 {`check`, `shadow`} → `g_replace_unknown` = **0** ∧ `g_replace_bridge` = **0**（逐档表；同时报站点覆盖）。**判据 = 0；非 0 ⇒ 停下上报**（逐条裁决：收紧/放宽/未覆盖面）。
- [x] **Step 3: 实现**：① 桥接：两处命名构造点改为身份链（`sh_name_token` 复用，`ty_shadow.cr:244`）；泛型应用实参项走 `sh_term_of_ti`（**不展开**，只用既有身份语义）。② 引擎：`lit_implies` 的 AK_NAMED 守卫（:192/:198）改为「链比较（同一性）→ 1 / 链异且链元素皆为类型项或身份令牌 → 0」；`ak_disjoint` 的 NAMED 行**不动**（仍不断言互斥）。③ **适用域守卫**：加强**只**用于「链元素集合 ⊆ {类型项, 身份令牌}」的原子（实现为一个显式判定函数 + 注释），其余面保持 -1（含 `tt_list_variance_at` 的不变槽面——**该面本任务不动**，除非探针证明残留 -1 来自该面；动则须逐面出「链元素皆类型项」证据）。
- [x] **Step 4: 判据**：① 全语料 + 探针影子对拍 `old_stricter`/`old_looser`/`unknown*`/`replace_*` **全 0** + 站点覆盖同报；② 探针行为断言全绿；③ `selftest-types` ≥365 → ≥380（`unf.nominal_*` 两例重钉为「引擎自决 0/1 + 无回落」）；④ **ELF canary IDENTICAL**；⑤ `.ccr` 冷态逐字节同（同源基线）；⑥ 五 CI job + 全枚举 + 自举链 + `test_backend_bootstrap`；⑦ **突变控制 ≥2 条**（身份链去一个元素 ⇒ 探针红；规则放宽（链异也判 1）⇒ 探针红）。
- [x] **Step 5: 提交**：`feat: R2 P5 Task 3——unknown 清零（命名面判定化：身份链 + 可判定面加强；两构造点同步；全语料回落计数归零）`

**停条件（D-3-4 的实际边界）**：① 探针暴露**无法在不引入漏报的前提下判定**的形态 ⇒ **停下上报**（该形态 = 裁-2 备选 (b) 的真实边界；**不得**为了清零而放宽/近似）；② 若清零要求动 `tt_list_variance_at` 的不变槽面 ⇒ 单独裁决（并出「链元素皆类型项」不变量证明 + 全语料反证）；③ 若 `sh_sig_term_of_ti`（:951）与 `sh_term_of_ti` 的身份链无法保持逐位同构 ⇒ 停下（双构造点契约）。

### Task 3 实施记录（2026-09-13；报告 `.superpowers/sdd/p5-task3-report.md`；证据 `/tmp/p5t3/`）

**形态**：桥接 `ty_shadow.cr` 新增 `sh_named_identity_term`（NAMED/PARAM = `tt_atom(AK_NAMED, 本行, [名字令牌])`）+ `sh_apply_identity_term`/`sh_apply_args_chain`（APPLY = `tt_atom(AK_NAMED, **基型行**, [基名令牌, 实参项…])`——基型行 = **规范形**：两处实例化同型 ⇒ **同节点**；与 `sh_sig_term_of_ti` **两点逐位同构**，停条件③**不触发**）；`sh_term_of_ti` 三分支改写、`sh_sig_term_of_ti` 两分支同步。引擎 `type_engine.cr`：`lit_implies` 正原子分支 AK_NAMED 守卫 → `te_named_pair`（`te_is_name_token`/`te_chain_cmp`/`te_token_cmp`/`te_arg_cmp`/`te_is_arg_term` 五原语）：**同类 ⇒ 链比较**（令牌位按节点同一性 / 实参位按引擎 `ty_equiv` 三态直传；空链 ⇒ -1）；**混类 ⇒ 0**（命名侧带链 ∧ 异类侧非 NEVER/DYN），域外（空链/位置错位/实参位 μ 变元/NEVER·DYN）⇒ **-1 + uncovered**。**不接展开层**（`unf.nominal_structural_equal` 展开项结构相等 1 与桥接项 0 双断并存）。**零新全局计数**（清零证据 = 影子摘要 + 自测直断）。

**判据（全部本实例实跑）**：`selftest-types` **386/386**（367+19 新例；3 处重钉：`t3.named_face_decided_no_fallback`（原 `t3.unknown_fallback_legacy`）/`unf.nominal_equiv_atomic_decided`（原 `unf.nominal_equiv_atomic_unknown`）/`unf.nominal_type_equal_false`（零回落））· 新套件 `test_named_face.py` **18/18**（受 10 + 拒 8，**不依赖影子通道**，挂 `run.sh` selfhost-tests）· 语料 shadow **`decisions=agree=32917`、`old_stricter/old_looser/unknown*/replace_*` 全 0**（站点覆盖同报；非自源 65 档与 T0 基线**逐字节同**）· **探针可判定域 18 档 `unknown_engine=replace_unknown=0`**（RED = 1~2/档；rc/诊断逐条不变）· **ELF canary IDENTICAL**（28822B）· `.ccr` 两套口径：`ptr_arith` **逐字节同**、`generics_test` **+640B**（TYPE 段项表 52→68 行 = 身份链新增令牌/CONS 项；**发射面零泄漏**：同语料 ELF 逐字节同 + `--dump-objects` 同 + `.cir` DOT 同 ⇒ 判据⑤按「无语料命名行档逐字节同 + delta 归因」重定）· check 语料 34×0/38×1 逐档同 · **D26 同源对拍零差异**（rc + 日志）· 全枚举 **57/57** · 五 CI job 全 rc=0 · 自举链 `corec2==corec3` cmp IDENTICAL（`ba8d98b8…`）+ N06=0 + 冒烟 42 + `--help` rc=1 · 构建确定性 ×2 逐字节同 · **突变控制 3 条**（M1 令牌塌缩：自测 8 FAIL + 套件 2 例转绿；M2 链异判 1：7 + 4 例；M3 域外守卫去掉：自测 1 FAIL）逐条转红 + 源/二进制精确回滚。

**收紧面（登记，§7）**：混类判定从 -1 变 0 ⇒ 约束站点（`gen_constr_satisfied`/`gen_inst_constr_satisfied`）「命名实参 × 原生约束」由**不判**变 **TG02**（`check` rc=1；`build` rc 不变——TG02 非硬名单）。**全语料 0 命中**（rc/日志与基线逐条同 + D26 零差异）；唯一命中 = `test_generic_constr.py` 1 例登记面（已重钉 `struct_inst_named_arg_rejected`）。**提请复核**（回退面 = 混类分支回 -1）。

**停条件②（触发；按纪律未单方动）**：参数链**不变槽**元素非同形（`[NA;2]` vs `[NB;2]`、`*NA` vs `*NB`、元组含异名）仍 -1 + 回落（**可判定**——legacy 判 0，行为面早已正确拒绝）⇒ 无法判定残留在 §3 逐条证明 + 套件 `residual.*` 两例钉行为不变 + 建议修法（不变槽元素比较改三态 `te_elem_cmp`）**未落地**——**协调者裁定（2026-09-13）= 默认案 ①**：本任务只做命名面清零，该面**另立一步**（紧跟本任务派发），三件证据（适用域证明 / 全语料对拍 / 突变控制 + 负控）见报告 §6.1 交接条目。

**登记**：`¬` 面（桥接零 TT_NOT ⇒ 不可达）保持 -1；链**不区分 kind**（NAMED vs PARAM 同名 ⇒ 判等，legacy 按 kind 判否——可达性未证实）；APPLY 项 b 槽取基型行 ⇒ DF 载体面该行由 face 0 降 face 1（**派生码不变**，语料 APPLY 行不入 tk ⇒ 零可观测变化）；**预存发现另单建议**：非硬名单诊断（TA01/TG02）不阻 `build`（`main.cr:149-170` 硬名单闸；冻结基线同）。

### Task 3b：不变槽元素三态 `te_elem_cmp`（停条件② 残留面收口；2026-09-13 派发；报告 `.superpowers/sdd/p5-task3b-report.md`；证据 `/tmp/p5t3b/`）

**形态**：`src/compiler/type_engine.cr` 新增 `te_elem_cmp` 三态——① 两侧皆**身份令牌**（`te_is_name_token`：AK_UNIT ∧ b ≥ 0）⇒ **节点同一性**（不同节点 = 确定不同 ⇒ **0**）；② 两侧皆**类型项**（`te_is_arg_term`）⇒ 引擎 `ty_equiv` 三态直传；③ 其余（μ 变元/¬/裸值——**既非令牌也非类型项**）⇒ **-1 + uncovered**。`tt_list_variance_at` 不变槽分支由「非同形 ⇒ -1 + uncovered」改为「同形（`tt_type_elem_same`）⇒ 1；否则 `te_elem_cmp`：0 ⇒ 0、-1 ⇒ 上抛」。文件头注 ①（P0 登记的「不变槽非同形」未覆盖面）随之收口。

**判据（全部本实例实跑）**：残留探针 n13/n17/n18 由 `unknown_engine=1 replace_unknown=1` → **0/0**（rc/诊断逐条不变；冻结基线与改后二进制同诊断）· 语料 72 档 check rc **34×0/38×1 逐档同** + shadow **`decisions=agree=32922`**、全桶（`old_*/unknown*/replace_*`）**0**（非自源 65 档逐字节同；5 个自源档 `decisions` +1/档 = 自源码增长，与 T0→T2 的 +147 同因）· `selftest-types` 386 → **401/401**（−1 例迁出 + 13 例 t3b 段 + 3 例 witness 合取式拆项）· `test_named_face.py` **18/18**（含 `residual.*` 两例行为不变）· **ELF canary IDENTICAL** · `.ccr` 四件（`ptr_arith`/`generics_test` × `ccr`/`build --static`）与 T3 后状态**逐字节同**（本步**零新项行**——`te_elem_cmp` 不构造项）· 五 CI job + 全枚举 57/57 + 自举链（`corec2==corec3` + N06=0 + 冒烟 42）+ 构建确定性 ×2。

**三件证据（协调者裁定）**：① **适用域证明** = 逐构造点**排序表**（令牌位：AK_REF 槽 0 / AK_SUM 槽 0,1 / AK_NAMED 槽 0〔不经本函数，走 `te_named_pair`〕；类型项位：SEQUENCE/PTR/PRODUCT/FN 全槽 + AK_REF 槽 1 + NAMED 实参位）⇒ 同 (ak, slot) **不存在排序混排**；域外面（μ 变元/¬/⊤ₖ）**保持 -1**（牙齿 = 突变 M3 + 自测 `t3b.elem_out_of_domain_stays_minus1`）。**AK_UNIT 形态二义**（原生 unit 项 = `tt_atom(AK_UNIT, 行号, -1)` 与令牌同形）**登记**：令牌先判是**失效方向**要求（若先判类型项 ⇒ token × unit 走 `ty_equiv` 判**等** = 假等 = P3-T1 令牌碰撞类）；可达性 = 无构造点在同一槽混排两排序（负控 §③）。② **全语料对拍** = 上（**零翻转**）。③ **突变控制 4 条**：M1 令牌面中和（4 FAIL）/ M2 类型项面中和（7 FAIL + **残留探针复红 1/1**）/ M3 域外守卫去掉（3 FAIL）/ **M4 分派次序倒置**（3 FAIL，**复现 P3-T1 令牌碰撞 `unf.enum_variant_identity_scoped` 红** = 负控有牙）——逐条转红 + 源 sha/二进制 sha **精确回滚**（回滚后 `build/corec` 逐字节复原）。

**翻转与登记（收紧面台账）**：① `param.unknown_p0` → `param.elem_class_diff_zero`（`[int]` vs `[str]` 元素位）由 **-1 变 0**——语义加强（异原生类元素确定不同）；与 spec §2.2 的 `sequence<⊤>` 正向面**无关**（后者走 `⊤ₖ` 分支），legacy 同结论（替换门一致）；② `t3.counterexample_named_variant` 的 `ty_equiv(w, B)` 由 **-1 变 0**：空析取支里的**异变体令牌**比较由 -1（旧）变 0 ⇒ 撤掉原先掩盖 `sub_cover(B, B∩¬R) = 0` 的 -1 ⇒ **揭开预存过判**（¬ 面同类别名规则把 `B ⊆ ¬R` 判 0，值集语义应 1）——影响面实测 = 仅 witness 项（生产 `ty_exhaustive` 走 `tt_norm`+`ty_inhabited`、`gen_constr_witness_str` 走 `tt_display`；全语料零翻转）⇒ **登记为后续步**（¬ 面「异身份原子 ⇒ 不交」加强），本步不修、不近似。
**停条件② 状态**：**已关闭**；D24 的「回落计数归零 → 删 legacy」前置对本面成立（残留探针 0/0 = 硬证据）。

## Task 4：删 `type_equal_legacy` 与全部回落面（+ 桥接 legacy 副本 + 死分支/站点 6）

**前置（硬性）**：Task 3 Step 2 的清零判据成立（全语料 + 探针回落计数 0）——**否则不得开工**。本任务与 Task 5 **必须连续提交**（本任务删掉对照物后影子判定对拍停摆）。

**Files:** Modify `src/compiler/checker.cr`（删 :504-597 全函数 + 7 处递归自调 + 回落两处 :590/:599 + 包装对照 :612；`type_equal_engine` 语义改为「引擎唯一权威」）· `src/compiler/globals.cr`（删 `g_replace_unknown`/`g_replace_bridge` :421 + 复位 :246）· `src/compiler/ty_shadow.cr`（删 `sh_native_ak_legacy` :101 / `sh_base_ak_legacy` :109 + 摘要两行 :628/:630）· `src/compiler/type_selftest.cr`（5 处回落钉重钉/删：:1814/:1821/:2487/:2492/:2691；桥接副本 3 例 :1957/:1966/:1977 改走生产入口）· `src/compiler/ir_gen.cr`（:927-929 死分支）· `tests/selfhost/test_named_dedup.py`（若引用 legacy 语义的注释/断言——:7 注）· `src/ci/run.sh`（计数注释同步）。

- [x] **Step 1: 残留 -1 政策落纸（**开工前置**）**：清零后引擎仍可能回 -1 的**入口**只剩（a）预算耗尽、（b）未覆盖面（μ/空递归/dyn 位图等，Task 0 表 D 的清点结果）。政策二选一（**写进本任务 Step 1 报告，需维护者签核**）：**P-A**「-1 ⇒ 编译期硬错（新码/新措辞 + 报告显示未覆盖面 + 反例）」——需先 report-only 全语料（预期 0 命中，由 Task 3 Step 2 的 0 计数支撑）；**P-B**「-1 ⇒ 保守 false + 登记计数（保留一个最小计数器）」——**注意**：P-B 不是 legacy 长期化（无 legacy 参与），但它把「未知」静默降为 false ⇒ **与三态纪律冲突，不推荐**。**推荐 P-A**。
- [x] **Step 2: 删除（逐件）**：`type_equal_legacy` 函数体 + 7 自调 + 2 回落 + `g_replace_*` + 包装层 `ok` 计算（:610-614 区域）；`type_equal_engine` 改为「快路径 + 桥接 + 三态（1/0/P-A 硬错）」；**同提交**把影子 `sh_compare` 的调用点移除（`ty_shadow.cr` 的判定观察面停摆——注记「对照物已删，余部随 Task 5 下线」）。
- [x] **Step 3: 桥接 legacy 副本删除 + 自测三例改走生产入口**（`iface_by_ty_code`/`sh_native_ak`——P2b 已单源化，副本仅对拍用）。
- [x] **Step 4: 死分支与站点 6**（D25）：删 `checker.cr:2454-2463` 的 `EXPR_BINARY+OP_ASSIGN` 分支（含 `sh_site_begin(6)`）+ `ir_gen.cr:927-929` 对应分支；站点表 :472-483 的 6 行改注「退役（不可达，P5 D25）」；**站点 id 不重编号**。**若实施中发现任一分支可达（构造面反证）⇒ 停下上报**。
- [x] **Step 5: 判据**：`selftest-types` ≥380（5 处重钉后净变化逐例登记）· 五 CI job + 全枚举 55/55 + canary + `.ccr` 冷态 + 自举链 + `test_backend_bootstrap` · **冻结基线同源对拍（72 档 `check` 逐档 rc + 日志 diff）** · **探针全集复跑**（Task 3 的行为断言，此时代谢 legacy 已删）· 突变控制（重钉后的探针在「-1 政策被改回静默 false」时转红）。
- [x] **Step 6: 提交**：`refactor: R2 P5 Task 4——删 type_equal_legacy 与全部回落面（引擎唯一权威 + 残留 -1 政策 P-A）；桥接 legacy 副本与 EXPR_BINARY+OP_ASSIGN 死分支/站点 6 一并删除`

### Task 4 实施记录（2026-09-13；报告 `.superpowers/sdd/p5-task4-report.md`；证据 `/tmp/p5t4/`）

**Step 1（残留 -1 政策 = P-A，落纸待签核）**：清零后引擎的 -1 出口只剩（a）桥接缺口（行译不成项）、（b）未覆盖面/预算耗尽（Task 0 表 D）。**实施 P-A**（计划推荐案）：新码 **`EC_ICE_TY_INDET`（ICE04）**「type judgment indeterminate」+ 反例（两侧项文本 `tt_display`，成因两措辞「bridge gap」/「(uncovered face|budget exhausted)」）+ **入 `main.cr` 硬名单**（build 亦拒绝落盘）。report-only 依据 = 全语料 72 档零命中（本任务 Step 0 复现：`decisions=agree=32988`、全桶 0）；判定点无 AST 位置 ⇒ `--> 0:0`（定位由调用点自身诊断承担）。**⚠ 政策选择按计划 Step 1「需维护者签核」——回退 = 删 `main.cr` 硬名单一行（诊断保留）或整条回 `P-B`。**

**清零判据复现（删除前置，本任务 Step 0 自跑；二进制 = T3b 的 `f0862d70…`）**：72 档 `check` **34×0/38×1**；shadow **`decisions=agree=32988`**、`old_stricter=old_looser=unknown=unknown_bridge=unknown_engine=unknown_engine_uncovered=unknown_engine_budget=replace_bridge=replace_unknown=0`；站点覆盖 `assign-node=24998 · fn-body-ret=5905 · if-branch=2051 · struct-field-type=28 · array-elem-type=4 · generic-apply-base=2`（`assign-binary=0` = 站点 6 不可达的语料佐证）。

**删除清单（逐件 + 死亡证据）**：`type_equal_legacy`（前镜像 `checker.cr:497-573` = 注记 10 行 + 函数 67 行，含 **7 自调**）· 回落两处（:590 桥接 / :599 引擎 -1）· `g_replace_unknown`/`g_replace_bridge`（`globals.cr` 声明 + `checker.cr` 复位 + `ty_shadow.cr` 摘要两字段）· `sh_native_ak_legacy`/`sh_base_ak_legacy`（`ty_shadow.cr`）· **站点 6 分支**（`checker.cr:2459-2470` = 12 行，含 `sh_site_begin(6)`）+ `ir_gen.cr:932-996` 对应发射分支（65 行）· `type_equal` 包装层的对照计算（改纯转发）+ `sh_compare` 调用点移除（D24 停摆；函数体留 T5）。**死亡证据** = 清零判据（全语料 `replace_*=0`）+ 站点 6 三面不可达（`tok2op` 零 `OP_ASSIGN` / `T_EQ`→`EXPR_ASSIGN` / `+=` 族显式包裹 op∈{ADD,SUB,MUL,DIV}）+ 语料 `assign-binary=0`；自测 3 例（桥接副本全表对拍）改钉**冻结期望表** `ts_t4_ak_expected`/`ts_t4_native_ak_expected`（不再需要生产文件里的第二份实现）。

**判据（本实例实跑）**：`build_selfhost_native.py` rc=0（`corec` **`eae13428…`**，构建日志 `error[`=0）· `test_backend_bootstrap` rc=0（CI bootstrap-tests job）· `check src/compiler` rc=0 · `selftest-types` **404/404**（401 + 新增 `ts_t4_run` 3 例；5 处计数钉重钉为「零 ICE04 诊断」形态）· 五 CI job 全 rc=0 · 全枚举 **57/57** · **ELF canary `95084e7b…d475` IDENTICAL**（28822B）· `.ccr` 两口径四条与 T3b 后**逐字节同**（`ptr_arith` 90823B `fb4a3b59…`/90966B `592afa31…`；`generics_test` 135341B `ddec1ce6…`/135484B `cd2af565…`）· `--dump-objects` 逐字节同（静态/非静态两口径）· `.cir` DOT 逐字节同（165915B）· 语料 `check` 34×0/38×1 + 日志仅 2 档自源**行号均匀位移 −38**（纯源行位移，内容逐字同）· **冻结基线同源对拍（pre-P5 二进制 × 当前源 vs 当前二进制 × 当前源）：72 档 rc + 日志逐字节零差异** · **探针全集复跑**（T3/T3b/T0 三套探针语料）pre-T4 vs post-T4 **rc + 日志全同**（残留 -1 面全已由 T3/T3b 收口 ⇒ 无 ICE04 命中）· 自举链 `corec2==corec3` `cmp` **IDENTICAL**（`36e8f3fd…`）+ N06=**0/0** + 冒烟 **42** + `--help` rc=1 · **突变控制 3 条**：M1（P-A 报告面去掉 ⇒ 3 例转红 `t3.bridge_gap_ice04_hard_error`/`t4.ice04_bridge_gap_hard_error`/`t4.ice04_engine_uncovered_hard_error`）· M2（两因措辞合并 ⇒ 1 例转红）· M3（强制触发 ⇒ `check` rc=1 打印 `error[ICE04]: type judgment indeterminate: string vs int (uncovered face)` + `build` rc=1 **无产物** = 硬名单接线端到端证明），三条均**精确回滚**（源 sha + 二进制 sha 复原 `eae13428…`）。

**中间态口径（D24，逐条）**：① 影子判定对拍**停摆**（`sh_compare` 无调用点 ⇒ 摘要 `decisions=0/agree=0`、差异桶恒 0；站点直方图仍随挂点计数——**不作为判据**；余部随 Task 5 下线）；② 判定面回归网自本任务起 = **冻结基线同源对拍 + 行为探针 + 突变控制**；③ 本任务与 Task 5 **连续提交**、中间态不发布。

## Task 5：影子层下线 + 回归网切换（**必须紧随 Task 4 提交**）

**Files:** Modify `src/compiler/ty_shadow.cr`（删 `sh_compare`/`sh_site_begin`/ring/`sh_report`/`sh_dump_write`/`sh_kind_name`/站点表与相关 globals 引用；**保留** `sh_term_of_ti`/`sh_map_*`（生产桥接）并剥离调试计数）· `src/compiler/globals.cr`（删 `g_shadow_*` 判定面全局；桥接缓存 globals 去计数）· `src/compiler/main.cr`（删 `--type-shadow` :236 / `--type-shadow-dump` :237 / `g_shadow_on` :244 / `sh_finish`/`sh_dump_write` 调用 :212-216；**删 `--verify-named-dedup` :182/:238**（TODO #2026-09-11-8 同族清偿））· `src/compiler/checker.cr`（10 处 `sh_site_begin` 挂点删除——**判定调用一律保留**）· `src/compiler/ty_shadow.cr` 的 `sh_tk_term_of_code`（删 hits 存-复原 hack）· `src/compiler/type_selftest.cr`（站点/摘要相关例重钉或删）· `tests/selfhost/*.py`（引用 shadow 的用例与 runner）· `src/ci/run.sh`（注释/挂点同步）· `build_selfhost_native.py`（若清单有 `ty_shadow` 专用面——**corelsp 清单含影子层**，删面须三面同核）。

- [x] **Step 1: 站点挂点删除（判定点保留证明）**：逐站点删 `sh_site_begin(id)`；判据之一 = **删挂点前后的 `check` 面同源对拍逐档空**（证明「站点下线 ≠ 判定点下线」）。
- [x] **Step 2: 影子通道面删除**（ring/摘要/转储/`--type-shadow*`/`--verify-named-dedup`/`g_shadow_on`/kind 空间/`sh_kind_name`）；**桥接缓存保留**但**剥离调试计数**（`g_shadow_hits/entries` 及 `sh_map_reset` 中的计数复位；`sh_tk_term_of_code` 的存取-复原删除）；`g_shadow_map` **改名**为 `g_term_map`（+ 引用全改）——若实施时引用面 > 30 处或触及 corearch 清单面 ⇒ 改名转为 P6 登记项（**择一落纸**）。
- [x] **Step 3: 回归网切换（D26-①）**：同提交落 `回归网声明`（报告 + TODO）——「判定面回归网 = 冻结基线同源对拍 + 行为探针 + 突变控制 + 三态纪律；影子通道已下线，任何后续批次不得引用其计数/摘要/站点直方图」；把 P4 Global Constraints 的「每任务复跑影子对拍」行**在 TODO/计划继承注里改写**（避免后续批次引用已死的通道）。
- [x] **Step 4: 判据**：五 CI job + 全枚举 55/55 + `selftest-types` ≥380 + canary IDENTICAL + `.ccr` 冷态逐字节同 + `--dump-objects` 逐字节同 + 自举链 + `test_backend_bootstrap`（**三面同核**，corelsp 面必核）+ **冻结基线同源对拍（72 档 `check`）+ 探针全集** + 突变控制（在派生面故意注入错值 ⇒ 探针/对拍转红；证明网仍在）。
- [x] **Step 5: 提交**：`refactor: R2 P5 Task 5——影子层下线（判定面对拍网退役）+ 回归网切换（冻结基线同源对拍 + 行为探针 + 突变控制）；桥接层剥调试计数`

**停条件**：corelsp 清单/长驻进程面因删面出现行为变化 ⇒ 停下上报（`reset_frontend_state` 链路上的缓存重置钩子**不得**随计数一起被误删——`sh_map_reset`/`sh_unf_map_reset`/`tt_layer_reset` 的调用链保留）。

### Task 5 实施记录（2026-09-13；报告 `.superpowers/sdd/p5-task5-report.md`；证据 `/tmp/p5t5/`）

**Gate（下线前置，本实例实测）**：`sh_compare` **零调用点**（T4 已移除）+ 起点二进制 × 72 档 shadow：**70/70 档 `decisions=0 agree=0`、全差异桶 0**；消费者仅 `sh_finish`（main.cr）← 两个默认关的 CLI 旗标；无测试驱动 `--type-shadow` ⇒ **无活依赖**。

**删除面**：12 函数（`sh_site_begin`/`sh_record`/`sh_compare`/`sh_report`/`sh_dump_write`/`sh_site_name`/`sh_kind_name`/`sh_count_*`×4/`sh_map_hits`）+ `sh_finish`（main.cr，含 2 调用点）+ **17 个 `g_shadow_*` 全局** + `SHADOW_RING_CAP` + 2 CLI 旗标 + checker **9 处挂点** + hits 存-复原 hack（3 处）+ `sh_map_reset` 的 hits 复位；站点头注区改「已删除」+ 判定点保留说明。**改名**（择一条件：引用 29 ≤ 30 且不触 corearch 清单）：`g_shadow_map`/`_cap`/`_entries` → `g_term_map`/`_cap`/`_entries`；`SHADOW_MAP_INIT_CAP` → `TERM_MAP_INIT_CAP`。**保留**：桥接层全族（生产）、展开层全族（生产）、`--verify-named-dedup`（**偏离 D27，见报告 §7-1**：非影子面 + 有活消费者 `test_named_dedup.py`，删它违「只增不减」）、`--verify-evp-nodes`。

**回归网切换（D26-①）**：声明落 `src/ci/run.sh`（selfhost-tests 自述块）+ `ty_shadow.cr` 文件头 + `globals.cr` + TODO（#24 残部 + 本批条目）——**判定面回归网 = 冻结基线同源对拍 + 行为探针 + 突变控制 + 三态纪律（ICE04）**；`test_named_dedup.py` **新挂进 CI** selfhost-tests（判定面网组件，原仅在全枚举内）。

**判据（全部本实例实跑）**：`build_selfhost_native.py` rc=0（`build/corec` **`2342fd03…`**，6+ 次构建同 sha；`error[` 真实诊断 = 0）· `test_backend_bootstrap` rc=0（`error[`=0）· `check src/compiler` rc=0 · `selftest-types` **404/404**（两例**重钉**：hits 计数断言 → 查询纯度 / 缓存命中同项；不增减）· 五 CI job 全 rc=0 · 全枚举 **57/57** · **ELF canary `95084e7b…d475` IDENTICAL** · `.ccr` 四件（`ptr_arith` `fb4a3b59…`/`592afa31…` · `generics_test` `ddec1ce6…`/`cd2af565…`）与 T4 **逐字节同** · `--dump-objects`（两口径 3521 行）`cmp` IDENTICAL · `.cir` DOT（165915B）IDENTICAL · **冻结基线同源对拍 72 档 rc + 日志零差异**（`diff -rq` 空）· **探针全集 30 档 vs T4 记录零差异** · 自举链 `corec2==corec3` `cmp` IDENTICAL（`75da3610…`）+ N06=0 + 冒烟 42 + `--help` rc=1 · **突变控制 2 条**：M1（`sh_dfn_code_of_slots` 去辅码 ⇒ 自测 400/404 + test_ccr_types 35/40 + `.ccr` sha 变）· M2（`type_equal_engine` 的 `e==0` 判真 ⇒ `test_named_face` 10/18 + 自测 397/404 + **语料同源对拍 12/12 档 DIFF（3 档 rc 1→0）**）——两条均**精确回滚**（源 sha + 二进制 sha 复原 `2342fd03…`）。

**停条件核**：`sh_map_reset`/`sh_unf_map_reset`/`tt_layer_reset` 调用链保留（corelsp 面 = `test_lsp.py` 在枚举 57/57 内 rc=0）；未触发上报条件。

## Task 6：TODO #2026-09-11-14——`EXPR_LET` 判定点（独立；report-only 先行）

**Files:** Modify `src/compiler/checker.cr`（`EXPR_LET` 分支 :3079-3097 加兼容检查；**划界是否含全局声明面 :1614-1620**）· New `tests/selfhost/test_let_check.py` · `docs/error-codes.md`（若新增码）· `src/ci/run.sh`。

- [x] **Step 1: 红态复现（#32 记录复核，本任务自跑）**：`x: [int;3] = s;`（切片）/`x: [int;4] = [1,2,3];`（异长常量档）/`x: int? = 5; y: int = x;`（可选流进窄槽）逐条实测现状 rc（登记：check rc=0 等）——**旧二进制复现**（Task 0 冻结基线），确认是预存而非本批引入。
- [x] **Step 2: report-only 全语料**：加检查（`type_compat_strict(源 = val_ti, 目标 = res_type_node(type_node))` + `diag_type_incompatible`，码/措辞沿用 TF01/TA01/TC02 体系**或新码——先裁决**）→ 全语料 report-only 出命中清单（**预期命中**：泛型函数体内 `T` 注解、`[T;N]` 表示提示位、可选注入面——逐条审查后入硬名单；`x := <值>` 与 `x : .` auto 形态**不受影响**）。
- [x] **Step 3: 开门 + 用例 ≥8 例**（三红态转 rc=1 + 无产物 + 正控：合法注解/auto/泛型体内 `T`/`[T;N]` 不误报）。
- [x] **Step 4: 判据**：新套件全绿 + **`check src/compiler` rc=0**（自源零命中）+ 五 CI job + 全枚举 + canary IDENTICAL + `.ccr` 冷态 + 自举链 + 冻结基线同源对拍（**本任务预期出现 diff——收紧面**，逐档裁决入台账）+ 突变控制。
- [x] **Step 5: 提交**：`fix: R2 P5 Task 6——EXPR_LET 站点判定点（TODO #2026-09-11-14：值/注解兼容检查 + 定位诊断；report-only 全语料零/清单裁决）`

### Task 6 实施记录（2026-09-13；报告 `.superpowers/sdd/p5-task6-report.md`；证据 `/tmp/p5t6/`）

**形态**：`checker.cr::check_let_annot_compat`（新判定函数）= 豁免面单点 + `type_compat_strict` + `diag_type_incompatible`，**两调用点共用**：① `EXPR_LET` 分支（局部）；② `check_global_let`（**全局初始化器——显式划界覆盖**，注册趟 `:1614-1620` 同形缺口）。参序（源 = `val_ti`，目标 = 注解行）照 P3 T1 §3.1。**码 = TA02**（`EC_TA_DECL`；error-codes.md 既有槽位，修复前定义零 raise）→ 复用 + 入 `main.cr` 硬名单（`build` 亦拒绝）。豁免四条：无注解/auto/无初值 · 注解 `dyn` · 值 `TI_NEVER`（底部 + **错误标记 = 级联抑制**）· 注解泛型形参（照返回位点既有策略；套件同策略钉住）。

**裁决两处**（计划 Step 2/3 的「先裁决」项）：① **码** = TA02 复用（非新码；TA02 是 error-codes.md 为此面预留的槽位，且 `EC_TA_DECL` 早已定义 ⇒ 零清单扩张）；② **硬 vs 软** = **硬**（入硬名单）——依据 = 计划 Step 3 的「三红态转 rc=1 + **无产物**」+ 硬名单既有判据（「判定继续 = 产出静默错产物」：符号取注解行、后端按注解行发射，与 TS01-04/TK02 同族）；report-only 零命中（自源 + 72 档）⇒ 开门零风险。

**report-only 命中清单（全语料，实跑）**：72 档 runner `check` **rc 34×0/38×1 与 pre-P5 基线逐档同 + 诊断正文归一化逐条同 + 零 TA02**；`check src/compiler` rc=0；**58 套件全枚举**（tests/selfhost **内联源** + tests/bootstrap）⇒ **命中恰 1 例**：`tests/selfhost/test_named_face.py::named.in_container_same` 的 `p : *NA = 0;`（int 字面量 0 入指针注解）。**裁决 = 修语料**（非放松检查）：`*T ← int` 与 `*T ← null` 在**赋值位点**早已 TA01 拒绝（探针 t1，冻结基线与本任务二进制同值）⇒ 声明位点拒绝与既有语义一致；改为 `p : *NA, mut;`（无初值声明，指针身份面语义不变）⇒ 该套件 18/18、全枚举 58/58。**计划预期的三类命中（泛型体 `T` 注解 / `[T;N]` / 可选注入面）实测零命中**——均为豁免面或正控（探针 q3/q4/q5/p10/p12）。

**判据（全部本实例实跑，二进制 `f737e26b…`）**：新套件 `test_let_check.py` **24/24**（拒绝 11：四形态 + 结构双向 + 全局 + 批量 + 丢弃名 + 级联抑制 2；正控 13：精确/无注解/auto/泛型 `T` 与 `[T;N]` 与 `[T]`/可选注入/可选 None/dyn/无初值/切片注解/定长精确/命名与 string；挂 `src/ci/run.sh`）· `selftest-types` **404/404**（不增减）· `check src/compiler` rc=0 · `test_backend_bootstrap` rc=0 + `error[`=0 · **五 CI job 全 rc=0**（selfhost-tests 首跑暴露上述 1 例命中 ⇒ 修语料后复绿）· **全枚举 58/58**（57 + 本套件）· **ELF canary `95084e7b…d475` IDENTICAL**（28822B）· **`.ccr` 两口径四条与 T5 记录逐字节同**（`ptr_arith` 90823B `fb4a3b59…` / 90966B `592afa31…`；`generics_test` 135341B `ddec1ce6…` / 135484B `cd2af565…`）· **零发射面泄漏**（收紧面 = 纯诊断面）· 自举链 `corec2==corec3` cmp IDENTICAL（2854910B `5559beef…`）+ N06=0/0 + 冒烟 42 + `--help` rc=1 · 构建确定性 ×2（`f737e26b…` 两连建同）· **冻结基线同源对拍 72 档 rc + 诊断正文零差异**（`text_diffs=0 rc_diffs=0`）· **突变控制 4 条**（M1 局部调用点移除 → 15/24；M2 豁免④移除 → 23/24（`let_generic_param_annotation_not_judged` 转红）；M3 硬名单条目移除 → 20/24「静默面复活」；M4 全局面调用点移除 → 23/24 单点归因）逐条转红 + **精确回滚**（源 sha `fff7a8ba…`/`739f7f5f…` + 三二进制 sha 复原 `f737e26b…`/`544e9201…`/`4251108b…`）。

**登记（未覆盖面/不收口）**：① `-> never` 函数的**调用**被推断为 unit（非 never）⇒ `x : int = boom()` 新增 TA02 且 `return boom()` 今天即 TF01——**同一既有 `never` 类型化缺口**在本站点多暴一例，非新类（全语料零 `-> never`）；② 计划预期的三类命中面实测不存在（豁免面覆盖）——不改判；③ **#20（F3 调用位点无诊断）不并入**：本任务只落**声明位点**（计划 Task 6 的文件面仅 checker/测试/error-codes/run.sh），调用位点（实参/返回）另单。

**停条件**：report-only 命中面涉及**语义裁决**（如泛型体内 `T` 注解是否应拒）⇒ 停下上报；若命中面过大（需改自举源码）⇒ 转独立批（本批只落「清单 + 裁决点」）。

## Task 7：收官（回归 + 判据 + 统一台账 + 文档 + P6 交接）

- [x] **Step 1: 全量回归**（cwd = 仓库根、`nice -n 19`、串行、比较前 `clean-cache`）：五 CI job 全 rc=0；全枚举 55/55（+新增套件）；`check src/compiler` rc=0；`selftest-types` 终值（≥380）；构建确定性（两次构建 sha 同）。
- [x] **Step 2: 判据复验**：`clean-cache` → ELF canary IDENTICAL（28822B）；`corec2==corec3` cmp IDENTICAL + N06=0 + 冒烟 42 + `corec3 --help` rc=1；`.ccr` 两套口径（冷态、带标）+ 冷/热豁免表复测（可选程序冷≡热）；**冻结基线同源对拍 72 档**（逐档 rc + 日志 diff，diff 逐条分类）+ 探针全集 + 突变控制快照。
- [x] **Step 3: 统一台账**：逐任务列 {删除件 / 替代证据 / 收紧 / 放宽 / 修复 / 未覆盖面 / 实施偏差}——**删除件必须与 Task 4/5 的实际 diff 逐条对应**（不得用「计划写的」代替「实际删的」）；全语料命中数与基线逐条比。
- [x] **Step 4: 文档**：统一 design spec **§9 P5 行标 ✅**（含未覆盖面与继承项）+ §3.4/§6.2/§6.3 状态同步（单槽化落地形态、β 与 `atom_of` 承接面指向 P6）；format spec（`.ccr` 版本/记录不变 ⇒ 只加「D20 稳定引用形态规范」注释 + P5 段说明）；findings **§15 已由 T5 落盘**（影子层最后一次基线）+ 本任务加 **§16 终态复核（文件自冻结）**；TODO：#44 家族接 P5 收官条目（**#49**）、划销 `type_equal_legacy`/unknown/站点 6 三项、登记 P6 条目（附录 E 逐条）；CLAUDE.md 本批无文件/结构变化（不在提交面）。
- [x] **Step 5: 提交**（路径限定；逐任务提交已在各 Task 内）。
- [x] **Step 6: 附录 E 落盘**（P6 交接包；见下）。

**判据**：五 CI job + 全枚举 + canary + 自举链 + 同源对拍 + 探针 + 台账逐条可溯源 + 文档指针链闭合（spec §9 P5 行 → 本计划 Task 7 → 附录 E）。

### Task 7 实施记录（2026-09-14；报告 `.superpowers/sdd/p5-task7-report.md`；证据 `/tmp/p5t7/`）

**全量回归（本实例实跑，无并发构建窗口；`nice -n 19` 串行）**：构建 ×2 **逐字节同**（`corec f737e26bf42f…` · `corearch 544e9201af10…` · `corelsp 4251108b04cf…`；构建日志逐字节同 + 三面 `error[`=0/`GUARD clean`×3）⇒ 提交树 == 被测二进制度（且与中断实例的两次构建交叉同值 ⇒ 确定性 ×4）· **五 CI job 全 rc=0**（check/bootstrap-tests/suite/selfhost-tests/full-bootstrap）· `check src/compiler` rc=0 · `selftest-types` **404/404** · **全枚举 58/58 rc=0**（51 selfhost + 7 bootstrap；逐档日志 `/tmp/p5t7/t7_enum/`）· 自举链 `corec2==corec3` = `5559beef2abf…`（2854910B，与 T6 记录同值）+ N06=0 + 冒烟 **42**（corec 与 corec3 双通道）。

**判据复验（本实例实跑）**：ELF canary `95084e7bc68d6550d21d3d96fa3afd89c67a5d89edce5656a3d2e74fc923d475`（**28822B**）IDENTICAL · `.ccr` 两套口径冷态四条 = `ptr_arith` **90823B `fb4a3b59…`** / **90966B `592afa31…`**、`generics_test` **135341B `ddec1ce6…`** / **135484B `cd2af565…`**（与 T5/T6 记录逐字节同；差恒 143B）· `--dump-objects` 两口径 **3521 行** `cmp` IDENTICAL（vs T5 记录）· `.cir` DOT `165915B b1bdf480…` IDENTICAL · **冻结基线同源对拍 72 档**（pre-P5 `5d2b15ad…` × 当前源 vs `f737e26b…` × 当前源）：`diff -rq` **零输出**、rc 分布两侧同为 **34×0/38×1** · **行为探针全集 30 档** `diff -rq` **零差异**（vs T5 记录）· **冷/热豁免表复测（三类缓存态，本实例实测）**：① **缓存启用档**（无泛型、非可选）——`ptr_arith`（16 条目）/`chan_test`（46 条目）条目戳三跑全等 = **真命中**：ELF 冷 ≡ 暖（canary 值 / `422421f3…`），`.ccr` **冷 ≠ 暖**（`ptr_arith` 90966 → 90769B；`chan_test` 231044 → 230936B）但**各自确定性**（暖1 ≡ 暖2）⇒ D-1 勘误的「非可选冷≠热为预存」**复现**（冷/暖段级差异由 ㉙ 契约钉住：**派生码面逐行同 + 项内容等价**，`test_ccr_types` 40/40 含此例）；② **含泛型函数档**（`generics_test`）`entries=0` = **缓存整体关闭**（`main.cr` 既有策略：任一泛型函数 ⇒ `cache_enabled=0`）⇒ 冷 ≡ 暖（ELF `f1f78683…` + `.ccr` 135484B 同）；③ **可选零足迹档**（`Some`/`match` 语料）`entries=0`（表示面关缓存）⇒ 冷 ≡ 暖（ELF + `.ccr` 同；运行 rc=1 = 载荷值 1，正确）。

**突变控制（本实例实跑；仓库外实拷贝 `/tmp/p5t7/mut`，仓库源码零改动）**：**M1 派生面**（`sh_dfn_code_of_slots` 忽略辅码）⇒ `selftest-types` **400/404** + `test_ccr_types` **35/40** + `ptr_arith.ccr` sha ≠ 记录值 `fb4a3b59…`（`2f279f5c…`）；**M2 判定面**（`type_equal_engine` 的 `e==0` 判真）⇒ `test_named_face` **10/18** + `selftest-types` **397/404** + **语料对拍子集 12/12 档 DIFF**（TA01/TF01 消失面、3 档 rc 1→0 = 拒绝静默消失，正是影子对拍过去要抓的类）；回滚 = 源 sha 复原；仓库 `jj status` 干净（本轮全程未触碰）。**网的两条腿（派生面 + 判定面）都活着。** **harness 教训（本轮踩到并登记）**：`test_named_face.py` 用 `Path(__file__).resolve()` 定位仓库根 ⇒ 突变 harness 里 `tests/` 用**软链**时套件会解析回仓库二进制（假绿 18/18）；**改为实拷贝 `tests/`** 后得真值 10/18。后续批的突变 harness 必须实拷贝（`test_ccr_types.py` 用 `abspath` 无此坑）。

**衔接说明（前实例残留）**：中断实例在 `/tmp/p5t7/` 留有草稿脚本与部分输出（build ×2 + 五 CI job 全绿，止于枚举前）——本实例**全部判据重跑**（构建/CI/枚举/判据/对拍/探针/突变），旧输出存档 `/tmp/p5t7/prev_run/` 仅作旁证（其 build1/build2 sha 与本实例逐字节同）。**冻结基线 `/tmp/p5t0/base/` 当前仍在位** ⇒ 对拍腿本轮成立；跨会话复现配方 = 附录 E-13。

---

## 统一台账（T0–T6 汇总；R2 P5 Task 7 收口）

> 口径：每件给**死亡证据**（为何敢动）与**替代证据**（现在谁覆盖）；删除件与 T4/T5 的实际 diff 逐条对应（前镜像清单 = `p5-task4-report.md` §3 / `p5-task5-report.md` §2）。全语料命中数一律与各任务起点基线逐条比。

### 1. 删除 / 退役（7 件 = 6 删除 + 1 改名族）

| # | 件 | 任务 | 前镜像规模 | 死亡证据 | 替代证据 |
|---|---|---|---|---|---|
| 1 | `type_equal_legacy`（含 **7 处递归自调**）+ 两处回落（桥接缺口 / 引擎 -1） | T4 | `checker.cr` 注记 10 行 + 函数 67 行 | 清零判据成立：全语料 `replace_unknown=replace_bridge=0`（T3/T3b；72 档 + 探针）；引擎与 legacy 逐点同值 | ① 冻结基线同源对拍 ② 行为探针（`test_named_face` 18 例 + 30 档探针语料）③ 突变控制 M2 ④ **ICE04 硬错**（残留 -1 不再静默） |
| 2 | `g_replace_unknown` / `g_replace_bridge`（计数 + 复位 + 影子摘要两字段） | T4 | `globals.cr` 声明 + `checker.cr` 复位 + `ty_shadow.cr` 摘要 | 计数无对象（回落分支恒不进入） | ICE04 诊断（成因位读于第二次 `ty_budget_reset` 前） |
| 3 | `sh_native_ak_legacy` / `sh_base_ak_legacy`（桥接 legacy 副本） | T4 | `ty_shadow.cr` 两函数 | 零生产调用（仅自测 3 例引用） | 自测改钉**冻结期望表** `ts_t4_ak_expected`/`ts_t4_native_ak_expected`（对生产入口 `iface_by_ty_code`/`sh_native_ak`） |
| 4 | 站点 6 分支（`EXPR_BINARY+OP_ASSIGN`）+ `ir_gen` 对应发射分支 | T4 | `checker.cr` 12 行（含挂点）+ `ir_gen.cr` 65 行 | **三面不可达**：`tok2op` 零 `OP_ASSIGN` / `T_EQ`→`EXPR_ASSIGN` / `+=` 族显式包裹且 op∈{ADD,SUB,MUL,DIV}；语料 `assign-binary=0`（32988 次判定） | 真实赋值站点 8（`EXPR_ASSIGN`，语料 24998 次命中） |
| 5 | 影子层：12 函数（`sh_compare`/`sh_site_begin`/`sh_record`/`sh_report`/`sh_dump_write`/`sh_site_name`/`sh_kind_name`/`sh_count_*`×4/`sh_map_hits`）+ `sh_finish` + **17 个 `g_shadow_*`** + `SHADOW_RING_CAP` + **2 个 CLI 旗标** + **checker 9 处挂点** | T5 | `ty_shadow.cr` 函数 76→64；`main.cr` −2 调用点 | **Gate**：`sh_compare` 零调用点（T4 已移除对照物）⇒ 全语料 `decisions=0 agree=0`（**结构性为空**，非「恰好 0 差异」）；无测试驱动 `--type-shadow` | ① 冻结基线同源对拍 ② 行为探针 ③ 突变控制 ④ 三态纪律（D26-① 声明落 `src/ci/run.sh`/`ty_shadow.cr` 文件头/TODO #2026-09-13-9） |
| 6 | 桥接调试计数 `g_shadow_hits`（含 `sh_tk_split_impl`/`sh_tk_term_of_code`/`sh_map_reset` 的存-复原 hack） | T5 | 3 处 hack + 1 计数 | 计数消费者已删；装填守卫输入 = 真实占用（`g_term_map_entries` 保留） | 查询纯度 / 缓存命中同项的**行为断言**（`t4.tk_query_purity_and_cache_hit` + `bridge.cache_hit_same_term_no_new_entry` 重钉） |
| 7 | 改名族：`g_shadow_map`/`_cap`/`_entries` → `g_term_map`/`_cap`/`_entries`；`SHADOW_MAP_INIT_CAP` → `TERM_MAP_INIT_CAP` | T5 | 引用 29 ≤ 30（计划择一条件内）且不触 corearch 清单 | —（桥接缓存 = **生产面，保留**） | 语义等价改名，零行为变化（全量判据同批） |

**显式保留（不删，理由）**：桥接层全族（`sh_term_of_ti` 等 = 判定路径无条件经它译项，生产面）· 展开层全族（满足判定/域查询，P3 边界）· `--verify-named-dedup` / `named_dedup_verify`（非影子面 + 有活消费者 `test_named_dedup.py`，**偏离计划 D27 一项**，回退面 = 一行）· `--verify-evp-nodes`（同理）· `sh_unf_*` 展开层计数（自测读数）。

### 2. 收紧（5 类；report-only 逐批先行）

| # | 形态（旧 → 新） | 任务 | 全语料命中 | 处置 / 牙齿 |
|---|---|---|---|---|
| 1 | 类型面行不可译（桥接缺口/行越界）⇒ 静默落盘 → **`ccr_seg_prepare_save` 拒绝**（rc=1 + 诊断） | T2（D23） | 0（94 档 `face_fail=0`） | **仅 emit 路径**计数（装载侧不计数 ⇒ 缓存命中绝不误拒）；牙齿 = 突变 M4（越界行 ⇒ rc=1 + 无产物） |
| 2 | 命名 × 原生混类判定 `-1`（不判）→ **0**（确定不同）⇒ 约束站点 TG02 | T3 | 0（72 档 rc/日志逐条同）；1 例套件内联面重钉（`struct_inst_named_arg_rejected`） | 语义 = 正确（legacy 同结论）；`build` rc 不变（TG02 非硬名单）；回退面 = 混类分支回 -1（n10/n20/n21 复红） |
| 3 | 不变槽元素非同形 `-1` → 三态 `te_elem_cmp`（令牌按节点同一性 / 类型项按 `ty_equiv` / 域外 -1） | T3b | 0（72 档 check rc 逐档同 + shadow 零翻转） | 残留探针 n13/n17/n18 `1/1 → 0/0`；牙齿 = 突变 M2（残留复红 1/1） |
| 4 | 引擎 -1（桥接缺口 / 未覆盖面 / 预算耗尽）⇒ 回落 legacy → **ICE04 硬错**（入 `main.cr` 硬名单 ⇒ `build` 拒绝落盘） | T4（P-A） | 0（72 档 + 探针全集） | **⚠ 待维护者签核**；回退面 = 删硬名单一行（诊断保留）或整条回 P-B |
| 5 | `x : T = <异型值>`（局部/全局）静默 rc=0 → **TA02 硬错**（build rc=1 + 无产物） | T6（#32） | 0（自源 + 72 档 + 58 套件）；1 例套件内联面修正（`p : *NA = 0` → 无初值声明） | 豁免四条（无注解/auto · dyn · 值 NEVER · 注解泛型形参）；牙齿 = 突变 M1–M4 单点归因 |

### 3. 放宽（**0**）

本批无任何「旧拒绝 → 新接受」类变更。两处面变化不属放宽：T1 的 `.cir` 载入失败 ⇒ **miss**（三态纪律：载入失败本就不该当空表）· T5 的「站点下线 ≠ 判定点下线」（9 个判定点全保留，删的只是挂点）。

### 4. 修复（崩溃 / 静默错值 4 件）

| # | 形态（前 → 后） | 任务 | 根因 + 判据 |
|---|---|---|---|
| 1 | `.cir` 暖态 **SIGSEGV rc=139** → rc=0 且暖 ≡ 冷 | T1 | 装载侧逐函数 `read_file`（`alloc(fsize+1)`，bump 不回收）⇒ 946 条目累计 **1.1674GiB > 1GiB 堆** ⇒ `alloc` 返回 0 ⇒ `str_len(0)` 解引用；修 = 单一复用读缓冲（与写侧同构）+ 载入失败 ⇒ miss；判据 = `test_cir_warm_path` 19 例（RED 对照 15 PASS/1 FAIL）+ 自源暖态转绿 |
| 2 | `IR_HOTPATCH_ROUTE` 的 tk = **r9 残留**（UB 烘进分类表）→ 显式第 6 实参 0 | T2（F1） | bootstrap 只为实际实参写寄存器；含 hotpatch 语料 3 节点码 **4→0**（已裁决偏差，零消费者） | 自测 `p5t2.hotpatch_route_no_face` + ㉞ |
| 3 | 声明位点**静默错产物**（符号取注解行、后端按注解行发射）→ TA02 | T6 | `EXPR_LET` 与 `check_global_let` 无兼容检查（bootstrap 参考实现本有该检查 ⇒ 回归）；`EC_TA_DECL` 此前定义零 raise | 套件 24/24 + 突变 4 条 |
| 4 | `EXPR_BINARY+OP_ASSIGN` 死分支（12 + 65 行）→ 删 | T4（D25） | 三面不可达（parser 三证）+ 语料 0 命中 | 站点 8 活判定点保留；`check` 同源对拍零差异 |

### 5. 未覆盖面 / 登记（转 P6；逐条 = 附录 E）

β 盘面项索引 + `atom_of` 读回 · 暖态项表可小于冷态 · 对象布局表示位 · `MAX_*` 解除 · 可选程序收回 `.cir` 缓存 · `.csr`/TagNode · 命名消费语法/iterable/形状可写视图 · ¬ 面过判 · `-> never` 调用面 · #20/F3 调用位点 · 冻结基线复现配方 · ICE04 签核 · 非硬名单诊断不阻 `build` · `string == int` 混比 · 单条目 >1GiB 分片 · 设计先例引用面。

### 6. 实施偏差（计划 ↔ 实测；逐条）

| # | 计划文本 | 实测 | 处置 |
|---|---|---|---|
| 1 | P4 六排除项（ALLOC/CALL/LOAD/I2F/F2I）「P4 排除」 | P4 排除理由 = **发射面**判据（后端不按 `ti` 分派），不适用于**语义**类型面 | T2 再裁入类型行面（`IR_LOAD_ENUM_TAG` 保持无面） |
| 2 | F2 三候选（扩 `atom_of` / 复合行走 aux / 分类表限原子行）未裁 | 实测复合行不可逆（`ptr_ref_first` BINARY tk=10） | 裁 (iii)+(ii)：分类表只覆盖原子行 + 复合行原码进辅码槽保真 |
| 3 | 冷/暖判据「索引全等」 | 暖态类型/项表可**小于**冷态（缓存命中跳过 `ir_gen` 的 `alloc_type`） | 判据重定 = 派生码面逐行同 + 项内容等价（`--dump-tk-terms` 增 `rows=`/`aux_nonzero=`/`face_fail=`） |
| 4 | T3 接口未明写 APPLY 项 b 槽取值 | 取**基型行**（规范形）⇒ 两实例化同型同节点；双构造点逐位同构（停条件③不触发） | 落纸 + 自测 `t3n.apply_two_rows_same_args` |
| 5 | `.ccr` 判据「冷态逐字节同」 | 含命名行语料必然 +项行（`generics_test` +640B = TYPE 项表 52→68 行） | 判据重定 = 无语料命名行档逐字节同 + delta 归因（发射面零泄漏三证） |
| 6 | T5 计划 D27「`--verify-named-dedup` 一并删」 | 非影子面 + 有活消费者（`test_named_dedup.py`；删它 = 杀死既有回归网 + 违只增不减） | **未删**（偏离登记，回退面一行）；`--verify-evp-nodes` 同理 |
| 7 | T6 预期命中面（泛型 `T` 注解 / `[T;N]` / 可选注入） | 实测三类均 rc=0（豁免面/正控）⇒ 零命中；真命中 1 例在 tests/selfhost **内联源**（72 档 runner 不含内联源 ⇒ 首跑 `selfhost-tests` 才暴露） | 修语料（三条独立证据）；「58 套件全枚举」并入 report-only 语料面 |
| 8 | 冻结基线「不入库」的复现腿 | `/tmp/p5t0/base/` 本轮仍在位（对拍腿成立）；跨会话不可复现 | 转 P6：重建配方 + sha 白名单（维护者已裁定「重建，不落二进制」） |

---

## 删件 → 替代证据（安全网替代表，D26；实施时逐条勾）

| 删件 | 过去谁覆盖 | 现在谁覆盖 | 验证时点 |
|---|---|---|---|
| `type_equal_legacy`（含 7 自调 + 2 回落） | 影子对拍（旧/新逐点对账）+ 引擎 -1 兜底 | ①清零判据（全语料 + 探针回落计数 0）；②冻结基线同源对拍；③探针行为断言；④三态纪律（残留 -1 = P-A 硬错） | Task 3 Step 2（删前）→ Task 4 Step 5（删后） |
| `g_replace_*` 计数 | 回落面的「不静默」证据 | 清零后无回落 ⇒ 计数无对象；-1 面由 P-A 硬错 + 报告承担 | Task 4 |
| `sh_native_ak_legacy`/`sh_base_ak_legacy` | 全表对拍（仅自测 3 例） | 生产入口 vs 表（`iface_by_ty_code`/`sh_native_ak`）断言 | Task 4 Step 3 |
| 站点 6（`EXPR_BINARY+OP_ASSIGN` 分支） | 影子站点 6 挂点 | 不可达构造面证明（parser 三面）+ 真实赋值站点 8（`EXPR_ASSIGN`） | Task 0 表 B（删前）→ Task 4 Step 4 |
| 影子层（ring/摘要/转储/flags/站点直方图/`--verify-named-dedup`） | 判定面回归网 | 冻结基线同源对拍 + 探针全集中跑 + 突变控制 + 全枚举 + canary | Task 3（探针在影子存活期先跑 0 差异）→ Task 5 Step 4 |
| 桥接调试计数（`g_shadow_hits/entries`，`ty_shadow.cr:48`） | `sh_tk_term_of_code` 的存-复原补偿 | 计数删除 ⇒ 补偿 hack 一并删除；桥接缓存容量/装填守卫改由真实占用驱动 | Task 5 Step 2 |

> **T7 收口注**：本表六行**全部兑现**（逐条实际 diff 与替代证据 = 「统一台账」§1 七行；「验证时点」列的删前证据 = 各任务 report-only 清零判据：T3/T3b 全语料 + 探针 `replace_*=0`）。

---

## 翻转钉清单（逐条：现钉 → 本批处置）

| 钉 | 位置 | 触发任务 | 处置 |
|---|---|---|---|
| `unf.nominal_equiv_atomic_unknown`（断言 `ty_equiv(命名,命名) == -1`） | `type_selftest.cr:2487` | Task 3 | 重钉为「引擎自决 0/1（同值 legacy）」+ 无回落 |
| `unf.nominal_type_equal_false`（断言回落 +1） | `type_selftest.cr:2492` | Task 3 | 重钉为「无回落（计数不存在）」 |
| `t3.unknown_fallback_legacy` / `t3.bridge_fallback_legacy` | `type_selftest.cr:1814/:1821` | Task 4 | 删除（回落面消亡）或改断言 P-A 硬错 |
| `t1.n_face_gate_nested`（断言**不**回落） | `type_selftest.cr:2691` | Task 4 | 重钉为「零回落计数」（语义不变） |
| `param.unknown_p0`（断言不变槽 `[int]` vs `[str]` ⇒ -1） | `type_selftest.cr:1681` | Task 3b | 重钉 `param.elem_class_diff_zero`（**0** = 确定不同；uncovered 面触发改域外元素——见 plan Task 3b 台账） |
| `t3.counterexample_named_variant` 的 `ty_equiv(w, b) == -1` 合取项 | `type_selftest.cr:3106` | Task 3b | 拆项重钉：`ty_sub(w,b)=1`/可空/覆盖位命名保留；反向与等价改钉 **0**（`*_over_claim` 命名，预存 ¬ 面过判被揭开——登记后续步） |
| `t3n.invariant_slot_residual_negative`（断言残留 -1） | `type_selftest.cr:1081` | Task 3b | 删除（断言迁 `t3b.inv_slot_seq_named_diff_zero`：**0** + 零未覆盖面） |
| 桥接副本三例 | `type_selftest.cr:1957/:1966/:1977` | Task 4 | 改走生产入口（`iface_by_ty_code`/`sh_native_ak`） |
| 站点表 6 行 + `sh_site_begin(6)` | `ty_shadow.cr:479` / `checker.cr:2460` | Task 4/5 | 退役注记 + 挂点删除 |
| 影子 flags/摘要行 | `main.cr:236-237/:244` / `ty_shadow.cr:628-630` | Task 5 | 删除；回归网声明落 TODO |
| `selftest-types` 计数注释 | `src/ci/run.sh:71` | 逐任务 | 355 → 各任务实测值（只增不减） |
| `test_ccr_types.py` 计数注释 | `src/ci/run.sh` 挂点行 | Task 2 | 34 → 实测值 |
| `test_cache_identity.py:VER_EXPECTED` | `tests/selfhost/test_cache_identity.py` | Task 1/2 | **不变**（17；布局零变化——若 Task 1 归因须 bump 则此为允许面并单独记录） |
| 站点/计数相关 LSP 用例 | `tests/selfhost/test_named_dedup.py` 等 | Task 5 | 逐例核：判定面断言保留，影子面断言删除 |

---

## 自检记录

- **根因 vs 止血**：① Task 1 是**根因修复**（暖态 SIGSEGV 在 `.cir` 恢复面，见 `cir_cache.cr` 装载链），**未**采用「给长度加钳位」式吞异常；② Task 3 用「命名面**判定化**」而非「结构性展开」——后者 P3a 已实测反证（同形不同名 struct 判等 + 推翻 P2a 对拍基线，`ty_shadow.cr:678-689`），前者按 R5 的口径「term 为准、atom 派生」直达单源；③ Task 2 的单槽化 = **类型面单源 + 辅码显式分离**（D19），**不**走「把辅码也硬塞进项」的近似（那会复现 P4 T4 §2 的数值陷阱：旗标 1 = `TI_DEX` 行、宽度 8 = `TI_DEX_S` 行）；④ 删除批**先证后删**（D24：清零判据 → 删 legacy → 影子下线），并给逐件替代证据（删件→替代表）——不是「删了再补网」。
- **与既有裁决的一致性复核**：spec **R5**（双槽期漂移 ⇒ term 为准 + atom 派生）= D19/D21 的实现口径；spec **§4 步 4**（删旧分支/旧映射/双槽）= 本批 Task 2/4/5；P3a **展开层边界**（只服务满足判定/域查询）= Task 3 **不得**越界（D23 适用域守卫）；T6 判定（`MAX_*` 解除 = 记录布局迁侧表，与对象布局同源）= 裁-4（P6 合批）；裁决 2（`g_impl_for` 入段不入判定）与裁决 7（`AK_NULL` 独立槽）本批零触碰；**三态纪律**在 Task 3 的「不得断言不等价」与 Task 4 的 P-A 政策两侧同时受约束。
- **占位符扫描**：无 TBD/TODO-to-fill；三处**实施前须定的登记项**明写并给推荐与边界——裁-1/裁-2/裁-4；Task 4 Step 1 的「残留 -1 政策 P-A/P-B」给了推荐（P-A）与拒绝理由（P-B 违三态纪律）；Task 5 Step 2 的 `g_shadow_map` 改名给了「>30 处引用则转 P6」的择一条件。**未知数不猜**：全 op 分类表的实际分类结果（Task 0 表 A 实测）、暖态 SIGSEGV 的实际根因（Task 1 实测）、命名面是否存在不可判定形态（Task 3 探针实测）、清零后的实际残留 -1 入口（Task 0 表 D 实测）——全部列为实测报告项，**不预填数字**。
- **数字口径（全部实读或既有记录值）**：`selftest-types` 355（`src/ci/run.sh:71`）；`test_ccr_types` 34；全枚举 55/55（48 selfhost + 7 bootstrap）；canary `95084e7b…d475`（28822B）；`CIR_CACHE_VER=17`（`cir_cache.cr:48`）；`ESZ_DFNODE=72`/`OFF_DF_TK=40`/`OFF_DF_TK_TERM=64`（`dyn_arr.cr:119-121`）；`ESZ_NOD_DISK=36`（`ccr_io.cr:143`）；`.ccr` 两套口径（`ccr` 90823B/134701B · `build --static` 90966B/134844B，附录 D-1）；站点覆盖（T7 值，**仅作 P4 时点记录**，P5 以 Task 0 表 C 为准）；「≥N 例」一律为**计划下限**（可增不可减）。
- **风险面**：① **单槽化的分类表错一格 = 静默错码**（后端按 `ti` 分派）——缓解：Task 0 表 A 逐 op 有依据 + 派生码不变量逐节点对拍 + 直方图 diff + 突变控制；② **命名面判定化引入漏报**（把「未知」误判为「不等价」）——缓解：加强的适用域守卫 + 探针负控（union/μ 仍 -1）+ 影子清零判据（0 差异）+ 突变控制；③ **删除批的网切换空窗**——缓解：D24 顺序 + 探针必须在影子存活期与删除后**两跑** + 冻结基线同源对拍；④ **Task 1 牵出版本级联**（`.cir` bump）——缓解：停条件 + 单列裁决；⑤ **多 agent 并发**——路径限定提交 + 一构建一编译串行（P4 已有碰撞教训）；⑥ **`g_shadow_map` 改名面过大**——择一条件转 P6（避免收尾批出现大面积 diff 无收益）。
- **本计划的自我限制（诚实登记）**：本计划**未跑任何构建/测试**（纯只读侦查）；所有「现状值」均为**实读源码/文档**或**既有报告记录值**（出处已标）；凡需实测的数字（分类表结果、SIGSEGV 根因、清零后的残留面）一律留给实施任务的 RED 步，**不以估算充当**。

---

## 附录 E：P6 交接包（P5 收官产出；逐条 = 内容 / 现状证据 / 前置 / 判据）

1. **盘面项索引 + corearch `atom_of` 读回承接面**（裁-1 的 β；spec §6.3/§6.4）：`.ccr` NOD 记录承载 TYPE 段文件内项索引（D20-② 的许可形态）+ 辅码字段 ⇒ 记录 36B → 40B + `CCR_VERSION` 8→9 级联；corearch 由项索引取项 → `atom_of` → 发射码（判据 = ELF canary IDENTICAL + `.ccr` 结构断言 + `--dump-objects` 逐字节同 + 版本拒收面）。前置 = P5 Task 2 的 `atom_of` 契约 + 分类表先行落地。
2. **对象布局表示位**（T5 未覆盖面；D-3-7）：结构体字段 / 数组元素 / 全局槽 / match 结果的可选表示位（packed 位图 / element stride）——现状 = 裸值形态**响亮失败**（rc=139 双路径同）、装箱形态正确（`test_optional.py` 两例钉住）。前置 = 与 3 合并（同触 `dyn_arr.cr` 布局 + `ast.cr` 镜像）。
3. **`MAX_*` 定长槽解除**（T6 裁定；D-3-8）：`MAX_ENUM_VARIANTS`/`MAX_VARIANT_TYPES`/`MAX_STRUCT_FIELDS`（皆 16）解除 = 「记录布局迁侧表」；前置四条（读回闸同步 / `ast.cr` 镜像归位（`StructInfo.field_*` 镜像 64 vs 布局 16 **已不一致**）/ 覆盖位表示（`sh_match_bit = 2^vi` 走 int，>63 变体溢出）/ 冷热 `.ccr` 与 ELF canary 判据）；`MAX_GENERICS=4` 属另一类（写入侧有界、parse 期硬报）。**现有的 P022/P023 护栏 = 解除时的安全网**（漏改只得定位硬错）。
4. **可选程序收回 `.cir` 缓存**（D-3-9-(i)）：表示位侧表是编译期进程内状态 ⇒ 收回须给快照加表示位面**或**装载侧重派生（同 Task 2 的派生纪律）；现状 = `main.cr` 文件路径 `g_optrep_on ⇒ cache_enabled=0`（代价 = 编译耗时）。
5. **`.csr`/TagNode 图消费面**（spec §6.4；D-3-10-②）：零调用者——接消费者前**不得**把「段内数据存在」当「语义已兑现」。
6. **命名消费语法 `T: 可索引`**（P3 未开工；D-3-10-③）+ **iterable 接线**（`EXPR_FOR` 零类型检查）+ **形状可写视图**（`ref(mut=1, ⊤ₖ(SEQ))` ⇒ -1）。
7. **`tt_display` 零 `str_intern` 改造**（P4 继承；含 `.ccr` STR 段增长面）· **`g_shadow_map` → `g_term_map` 改名**（若 Task 5 择一条件触发）· **`test_type_engine.py:MIN_CASES` 由下限收紧到实测值**（P4 T4 §7-⑥ 登记）。
8. **陈旧工具/副本**（D-3-12，P4 登记不修）：`tools/module_to_ccr.py`（写 version=1 远古 `.ccr`，零 in-repo 调用者）· `pkg/` 内陈旧 `ccr_io.cr` 副本 · `docs/superpowers/specs/2026-09-11-explain-predict-incremental-design.md:371`（称 `CIR_CACHE_VER=14`，陈旧）。
9. **纪律继承（不得稀释）**：三态纪律 · 新硬错先 report-only · 清单三面同核 + `test_backend_bootstrap`（`error[`=0）· 判据口径按 TODO #2026-09-11-9（结构性断言）· **影子通道已下线 ⇒ 判定面回归网 = 冻结基线同源对拍 + 行为探针 + 突变控制**（P5 Task 5 起）· 比较前 `clean-cache`（D-1 口径）。

**P5 收官追加（T7；每项 = 现状 / 出处 / 影响面 / 建议 / 前置）**

10. **`¬` 面过判（T3b 揭开的预存缺陷）** — **现状**：`sub_cover` 的 product 规则撞 `lit_implies` 的 ¬ 面规则——`lp ⊆ ¬x` **同类原子** ⇒ `ak_disjoint` 回 0 ⇒ 判「不含」；值集语义对**异身份**同形原子（变体令牌 / 命名链）应为 1。自测以 `t3.witness_reverse_over_claim`/`t3.witness_equiv_over_claim`/`t3.product_neg_over_claim` 三例钉住过度判定 + 最小复现。**出处**：`p5-task3b-report.md` §7-1（§5-② 逐步推导）。**影响面**：仅 witness 项的 `ty_sub` 消费者（生产 `ty_exhaustive` 走 `tt_norm`+`ty_inhabited`、`gen_constr_witness_str` 只 `tt_display`）⇒ **全语料零翻转**，生产不可观测。**建议**：加强 ¬ 面「异身份原子 ⇒ 不交」，落地后三例应转 1 并重钉。**前置**：「链元素皆类型项/身份令牌」适用域证明（T3b §3.1 排序表可复用）+ 突变控制负控（M4 令牌碰撞反例）。

11. **`-> never` 调用返回型缺口（T6 登记，预存）** — **现状**：`fn boom() -> never` 的**调用**被推断为 unit（非 never）⇒ `x : int = boom()` 触发 TA02、`return boom()` 今天即 TF01——同一缺口在本站点多暴一例。全语料 `-> never` = 0。**出处**：`p5-task6-report.md` §6-①。**影响面**：调用面类型推断（返回型映射）；当前无语料命中。**建议**：调用返回型补 `TI_NEVER`（返回位点/声明位点/实参位点同源受益）。**前置**：与 12（#20）同批——同一调用推断面（`infer_gen_call`/`unify_types`）。

12. **#20 / F3：调用位点实参不匹配无诊断** — **现状**：`unify_types` 返回值被丢弃（实参位）；返回位点部分静默；T6 只落**声明位点**（显式划界）。**出处**：TODO #2026-09-16-1 · findings §6 F3 · `p5-task6-report.md` §6-③。**影响面**：调用面静默接受错型实参（符号取声明、后端按声明发射 = 静默错产物同族）。**建议**：把 `unify_types` 的 -1/0 值与诊断接线（码体系照 TA01/TA02），入硬名单前先 report-only。**前置**：调用面**错误标记**语义（级联抑制，照 T6 豁免③ 先例）+ 与 11 同批。

13. **冻结基线可复现性（T5 §7-4；维护者已裁定「重建，不落二进制」）** — **现状**：同源对拍腿依赖 `/tmp/p5t0/base/{corec,corearch,corelsp}`（= P4 收官二进制 `5d2b15ad…`/`493dc490…`/`18b94bd9…`）；`/tmp` 清理即不可复现（本轮仍在位 ⇒ T7 对拍腿成立）。**出处**：`p5-task5-report.md` §7-4 · 本计划 Global Constraints（冻结基线腿）。**影响面**：每批收尾的判定面主判据之一（跨会话失效）。**建议**：把**重建配方 + sha 白名单**落进仓库（如 `tools/baseline/REBUILD.md`：`jj` 检出 pinned revision `9bcb7083`（P4 收官）→ `python3 build_selfhost_native.py` → 三 sha 对白名单）。**前置**：维护者确认配方位置 + 白名单维护纪律（基线换代时更新）。

14. **ICE04/P-A 政策签核 + 非硬名单诊断不阻 `build`（政策对偶面）** — **现状**：（a）T4 按计划推荐案落 P-A（ICE04 入 `main.cr` 硬名单 ⇒ `build` 拒绝落盘），**签核未取**；（b）反向面：TA01/TG02 等**非**硬名单诊断不阻 `build`（硬名单闸只收 R002/TK05/TK06/TS*/TK02/TM03/ICE04/TA02；冻结基线同）。**出处**：`p5-task4-report.md` §7-7 · `p5-task3-report.md` §8-4 · `p5-task6-report.md` §6-④。**影响面**：`main.cr` 硬名单闸一处（≈:149-170）；「诊断了但产物照出」类静默面。**建议**：一次政策复核同时裁——（a）ICE04 是否保留硬名单（回退面 = 一行）；（b）非硬名单诊断是否收紧（至少 `build` 面汇总告警）；（c）新硬错入闸判据成文化（既有判据 =「判定继续 ⇒ 产出静默错产物」）。**前置**：维护者裁决。

15. **`string == int` 混比双路径分歧 + 单条目 >1GiB 分片（T1 登记）** — **现状**：`s == 0` 在 Python bootstrap 侧编成 `str_eq`（`str_len(0)` 自崩）、自举侧按 int 比较 ⇒ 两路径语义分歧；T1 以 read-EFAULT 判据绕开（语言面未修）。另：`.cir` 快照单条目 > 1GiB 无分片机制（当前最大 3.99MB）。**出处**：`p5-task1-report.md` §6-①/§6-③。**影响面**：语言面比较语义（用户可写形态）；缓存格式分片面（远期）。**建议**：语言面裁决「string 与 int 可否比较」（编译期拒绝 or 单侧转换）；分片作独立议题登记。**前置**：混比语义定案；分片须有 >1GiB 真实用例（当前零）。

16. **设计先例引用面（登记，非功能缺口）** — **现状**：`specs/2026-09-11-policy-injection-design.md:275` 与 `specs/2026-09-11-explain-predict-incremental-design.md:84/127/181/447` 以 `--type-shadow` 为**先例**（「审计通道默认关、两态产物逐字节同」）——通道已随 T5 下线。**出处**：grep 全仓（`--type-shadow` 的**非历史**引用面 = 这两份设计稿 + findings/TODO 的已更新说明）。**影响面**：未来批设计稿的先例引用（不影响当前判据）。**建议**：两 spec 下次修订时把先例载体改写为 `--verify-*` 家族（`--verify-named-dedup`/`--verify-evp-nodes`）+ 行为探针口径（原则不变）。**前置**：无（文档面顺手项）。
