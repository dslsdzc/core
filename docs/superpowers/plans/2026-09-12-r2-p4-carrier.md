# R2 P4（载体）实施计划：`.ccr` TYPE/IFACE 段 + DFNode.TK 升格 + corearch 读回

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 兑现统一设计 spec §6（载体）与 §6.4（语义保鲜兑现点）——类型/接口信息**随载体出前端**：`.ccr` 新增 **TYPE 段**（类型行表 + 类型项 DAG）与 **IFACE 段**（原生条目 + 横切形状 + 用户接口签名项 + impl 边 + 方法表）；corearch 载入两段**重建**类型项表与接口表，使引擎判定在 corearch 侧可跑（§6.3 的 `atom_of` 承接面）；DFNode.TK 进入**迁移期双槽**（原子码原样 + 类型项引用），发射面零扰动；B.7 一等缺口（`T?` 裸值 + `Some` 臂双路径 rc=139）按裁决 5 于本批（解包侧判表示）闭合。

**Architecture:** 五条主线：① **链接面纯化**（Task 0：把 `type_engine.cr` 的 iface 簇与 `iface_registry.cr` 的 `iface_kind_of` 移出，corearch 才能链接引擎核——**前置且纯搬运**）；② **段机制**（Task 1：tag/版本/加段四面/loader 闸 + 结构断言重定；两段先空壳落盘）；③ **TYPE 段**（Task 2：确定性装填 + 行表/DAG 序列化 + corearch 读回 + 判定原语跨进程同值）；④ **IFACE 段**（Task 3：扩列 + 形状命名化 + 签名项化 + 读回对拍）；⑤ **图与表示面**（Task 4 TK 双槽 + Task 5 可选表示解包侧 + Task 6 MAX_* 护栏）；Task 7 收官。

**Tech Stack:** Core 自举栈。`src/compiler/ccr_io.cr`（段表/写侧/读侧）· `type_terms.cr`（项 DAG + 哈希 + 开放寻址索引 + 扩容）· `type_engine.cr`（判定核 1-612 行 + iface 簇 613-772 行）· `iface_registry.cr`（条目表/形状表/查询 API）· `ty_shadow.cr`（桥接/展开/签名项）· `checker.cr`（类型行表 + `init_types`）· `dataflow.cr`/`ir_gen.cr`/`interp.cr`/`provenance_verify.cr`（DFNode.TK 消费面）· `cir_cache.cr`（`.cir` 快照）· `corearch.cr` + `src/lattice/ent_kernel.cr`（读回通道）。

**Spec:** `docs/superpowers/specs/2026-09-10-type-interface-unification-design.md` **§6.1**（TYPE/IFACE 段内容）、**§6.2**（DFNode.TK 升格 = 迁移期双槽 → 收尾单槽）、**§6.3**（corearch 读回）、**§6.4**（语义保鲜兑现点）；字节格式权威 = `docs/superpowers/specs/2026-09-09-lattice-ir-v7-format.md`（⚠ 统一 spec §6.1 关联行写的 `2026-09-07-lattice-ir-v7-format.md` **文件不存在**——实测目录实查，引用一律按 2026-09-09 版；P3 计划附录 C.3 已勘误）。**P4 起点文档 = P3 计划附录 C**（`docs/superpowers/plans/2026-09-11-r2-p3-capabilities.md:541-609`）。

**前置状态（已实测，非本计划目标）**：P3 全阶段（P3a + P3b）收官于 HEAD `55280aa5`；`selftest-types` **333/333**；ELF canary `95084e7b…d475` 全阶段 IDENTICAL；自举链 `corec2==corec3` + N06=0 + 冒烟 42 稳定；`ccr_io.cr` 全文件 `iface`/`impl_for`/`g_methods` 命中数 = **0**（附录 C.1 实测，本轮复核成立）；类型项 DAG **全在进程内存**（`globals.cr:265-273`）。

---

## 裁决（定案，实施时不得再议）

**维护者裁定（2026-09-12，八条；P3 计划附录 C.6 的八项待裁决面逐条收口，均按推荐项）**

| # | 议题 | 裁定 |
|---|---|---|
| 1 | 形状命名化 + `.ccr` STR 段增长 | **形状名入生产注册面；接受 STR 段增长**；判据按 TODO #26 换结构断言（`tests/selfhost/test_ccr_v7.py` 全绿 + 语义零变化 + 自举稳定，不以「与旧版逐字节同」为准） |
| 2 | 用户轴名义 vs 结构（TODO #38） | **维持结构性**（方法集包含）；`g_impl_for` 降为**声明元数据**（入段、不参与判定） |
| 3 | 本质条目表 | **扩列**（本表增行：`AK_NULL` / `AK_SUM` / `AK_FN`）⇒ 硬值 13 随之重定；`name_ni` 列按裁决 1 一同填充 |
| 4 | 整形状 `ty_sub` 路由 | **不启用**；维持逐成员判定（`iface_user_satisfies_ii`，`type_engine.cr:738`），形状项 = **签名载体** |
| 5 | 可选运行期表示 | **解包侧判表示**，与 TYPE 段同批（本计划 Task 5） |
| 6 | `MAX_*` | **先补护栏、再评估解除**（本计划 Task 6；`MAX_ENUM_VARIANTS`/`MAX_VARIANT_TYPES` 写入侧护栏 = TODO #35） |
| 7 | `never` | **给独立原子槽**（保留 `AK_NEVER` 独立原子语义，段内原子条目面显式落槽；不作 ⊥ 折叠） |
| 8 | DFNode.TK 升格 | **维持 ELF canary 不变**；变即停下上报（本计划 Task 4 的硬判据） |

**本计划裁定（D9–D18；每条 = 决策 + 理由，实施时按此执行）**

| # | 决策 | 理由 |
|---|---|---|
| D9 | **段 tag：`TYPE=7` / `IFACE=8`**；format spec §2 的「`7+` 预留」两段（驱逐标注段/证书段）**顺移 9/10**（spec 同步改一行） | 预留两段在代码中**零实现**（`ccr_io.cr` 无对应常量；驱逐机制按实例设计 §3.2 声明「不实现」，证书按 v6 spec §185「不落本文件」）⇒ 仅与注释冲突；取 7/8 保住 loader 的规范序闸 `tg == ri+1`（`ccr_io.cr:900`）与段体连续闸（`:901`）两不变式，加段成本 = 闸界 6→8 一处。**替代方案（若维护者坚持保留 7/8 给预留段）= TYPE=9/IFACE=10**，代价 = 规范序闸必须改形（`tg == ri+1` 不再成立，须换「允许集 + 单调序」双闸），本计划不采 |
| D10 | **版本 bump `CCR_VERSION` 7 → 8**（`ccr_io.cr:107`） | 若留 7，则「v7 = 恰六段规范序」语义必须放宽为「6 或 8 段」，**旧 6 段文件会被新 corearch 静默接受**（TYPE/IFACE 缺席 = 空表）——正是三态纪律要消灭的静默类（C.5-3）。bump 后旧文件在版本闸（`ccr_io.cr:875`）整类拒收，v6→v7 同日切换先例同款。**文件名/测试名不改**（`test_ccr_v7.py` 保留，引用面 40+ 处，改名收益为零；文档注明「v8 = v7 段表架构的加法扩展」） |
| D11 | **TYPE/IFACE 两段必备**（loader `have7/have8 == 0 → 拒绝`，并入 `ccr_io.cr:919` 的必备集） | 可选段 = 两种 `.ccr` 在野，任何消费者都要处理缺席 = 静默降级面；本阶段承诺即「信息恒随载体」（§6.4）。与 ENT 的可选先例（v5 兼容语义）不同源 |
| D12 | **TYPE 段 = 两小节**：①类型行表（`kind/data/extra`，24B/条）②类型项 DAG（`tag/a..d`，40B/条，**哈希不落盘**，加载时 `tt_hash5` 重算） | §6.1「DAG：tag/a..d」原文即五字段；哈希是五字段的纯函数（`type_terms.cr:35`）⇒ 落盘 = 冗余 + 第二真源。行表即 §6.1 的「原子条目扩展」（`kind/data/extra` + 用户类型名 = `TYP_NAMED` 行的 `data`（STR 索引）+ 语义域标注**不入盘** = 由 `kind/data` 经 `iface_by_ty_code`（`iface_registry.cr:234`）派生，理由 = 同一映射不得两份（P2b 刚治过的病） |
| D13 | **确定性装填**：`save_ccr` 前先做「项层规范化重建」——项表清零 + 索引失效 + 引擎 memo/预算复位 + 桥接/展开缓存复位 → 形状重注册 → 按**行序**逐行装填 → 按**接口声明序**装填签名项 → 再序列化 | 项 DAG 是**惰性**填充的（`sh_term_of_ti` 只在判定时建项）⇒ 直接落盘 = 段内容随「判定历史」变（**冷/热 `.cir` 缓存命中 ⇒ 判定次数不同 ⇒ 段不同** = 已登记的冷/热分歧类，TODO #5 末条）。重建后段内容 = 类型表/接口表的**纯函数**。前置核查：`save_ccr` 的两处调用点（`main.cr:612` / `:636`）之后不得有类型项消费者（逐站点核；`sh_finish`（`main.cr:214`）只读 `g_shadow_*` 计数，不在复位面内） |
| D14 | **IFACE 段 = 五小节**：①原生条目②横切形状（名 ni + 项）③用户接口（方法签名**存类型项索引**）④impl 边⑤方法表。**签名裸码（`TY_*`）不入段** | §6.1 明列 IFACE =「条目（原生/横切/用户）+ 满足关系」；裸码是前端**编码层**（P3b 已钉其唯一活消费者 = S6 映射面，`dyn_arr.cr:148-154`），对跨端消费零信息（非原生类型一律塌缩）；且「存 AST 节点行」对 corearch 无意义（无 AST）⇒ 必须在 save 侧解析成项 |
| D15 | **DFNode.TK 升格 = 内存双槽**：`OFF_DF_TK` 语义/字节**零变化** + 新增 `OFF_DF_TK_TERM`（类型项引用；-1 = 无）；`.ccr` NOD 36B 记录**不变**；~~`.cir` 快照**入项槽**并 `CIR_CACHE_VER` 17→18~~ **【实施修正（实测反证）：快照不落项槽、`CIR_CACHE_VER` 保持 17——装载侧在 emit 等价时点重派生。原「快照必须入槽否则冷/暖分歧」前提被 `--dump-tk-terms` 实测推翻：入槽版本暖路径 `bad_term=329`（项索引 = 进程内表索引，全数悬空）；不落盘 + 重派生后冷/暖 1298 行逐行同、`bad_term=0`。理由另含第二真源纪律。见报告 §4 + plan Step 4】** | §6.2 的「迁移期双槽」；NOD 盘记录的 `tk` 是**混用码**（基类型行 + 小旗标，如 `IR_BOUNDS_CHECK` 的 `ti=1` 动态上限），不是行号 ⇒ 无法从 `tk` 反推项 ⇒ 盘面项槽归 P5 单槽化（届时另定小节/扩记录）。~~快照必须入槽，否则 cache-hit 恢复路径与冷路径分歧（`cir_cache.cr:246-253`/`:426` 对称写读）~~ |
| D16 | **链接面纯化**：`type_engine.cr` 的 iface 簇（`:613-772`）与 `iface_registry.cr` 的 `iface_kind_of`（`:279-293`）**移出**到 corec-only 文件；corearch 清单 += `type_terms.cr`/`type_engine.cr`/`iface_registry.cr` | 实测：`type_engine.cr` 1-612 行**零** checker/桥接/注册表代码引用（非注释行 grep = 0），iface 簇是唯一耦合段（`find_iface`/`find_func`/`decl_name_of_ti`/`gen_constr_type_ti`/`sh_*`）；`iface_kind_of` 读 `get_type_kind`（`checker.cr:314`）。不拆 = corearch 构建 33×N06 式静默未定义（B.6 同族） |
| D17 | **形状名 = 6 个 ASCII 名**：`sequence` / `sequence_ro` / `sequence_rw` / `indexable` / `iterable` / `product` | 与 spec §2.2 的构造子英文名同形、与 `AK_*` 命名同族、避免非 ASCII 进 STR 段。若维护者改中文名（如 `可索引`）⇒ 改的是常量与守门用例名，段布局零影响。**命名消费语法（`T: 可索引`）不在本批**（P3 未开工清单项，另裁） |
| D18 | **段内容构造与序列化解耦**：装填/序列化（引用桥接/checker 层）落 **corec-only 新文件**（`src/compiler/ccr_types.cr`），预生成段体缓冲到 globals；`save_ccr`（`ccr_io.cr`，**corearch 也链接**）只按段表写缓冲 | `ccr_io.cr` 同时在 `backend_support_files`（`build_selfhost_native.py:103-111`）与 corec 清单里 ⇒ 其中任何对前端层符号的引用 = corearch 构建 N06 静默未定义（B.6 monomorph 教训）。解耦后该文件保持「共享层符号」纯度（可用 test_backend_bootstrap 的 `error[`=0 门持续守） |

---

## Global Constraints

- **jj only（禁 git 含只读/复合）**；**提交路径限定**（多 agent 在场：一律 `jj commit -m … <paths>`）；所有命令 `nice -n 19`；一构建一编译**串行**；cwd = 仓库根（`/tmp` cwd 触发 import 解析假失败）。
- **比较任何 `.ccr`/缓存态产物前必须 `clean-cache`**（`./build/corec clean-cache`，`main.cr:396`；相对 cwd）。冷/热缓存态 `.ccr` 分歧未修（TODO #5 末条），本批**新增**判定依据：TYPE 段内容由 D13 保证为**纯函数** ⇒ 冷/热两态 `ptr_arith` 的 TYPE 段应逐字节同（新增守门用例）。
- **ELF canary 本计划全任务预期不变**：`clean-cache` → `build tests/suite/ptr_arith.cr --static -o /tmp/…` → sha256 `95084e7bc68d6550d21d3d96fa3afd89c67a5d89edce5656a3d2e74fc923d475`。**任一任务 ELF 变 → 停下上报**（类型/载体层改动泄进发射面）。⚠ 该语料对类型层不敏感（附录 C.5-6 口径继承）——它是「发射面零泄漏」证据，**不得**当新语义正确性证据；新语义一律由行为探针承担。
- **`.ccr` 判据重定（TODO #26，本批自 T1 起生效）**：**不再以「与旧版逐字节同」为准**；改为 `test_ccr_v7.py` 结构性断言全绿 + 语义零变化 + 自举稳定（`corec2/corec3` `cmp` IDENTICAL + N06=0 + 冒烟 42）。旧记录值（P3b 报告 §4b：`ptr_arith` 88943B `54e3856b…` / `generics_test` 130974B `97be2f9e…`）自 T1 起**作废**，替换为：新尺寸/哈希（冷缓存实测报告记录）+ 结构性断言。
- **三态纪律（P0 起继承）**：引擎 -1（预算耗尽/未覆盖面）不得当 0/1 用；**载入失败/段缺失/行不可译 ⇒ 拒绝（不得静默当空表/当 0）**（C.5-3）；save 侧装填遇不可译行/签名项 ⇒ **save 拒绝（rc=1 + 诊断）**，且入硬门前先跑全语料 report-only（下条）。
- **新硬错误先 report-only**：任何把新检查升为 rc=1 门的步骤（装填失败拒绝、`MAX_*` 写入侧护栏、IFACE 段签名项不可建拒绝）必须先在**全语料**（`src/compiler/_import.cr` 自指 + `tests/suite` + `tests/selfhost` 内联源 + `src/stdlib` + `examples`）report-only 跑一遍，清单审查后再开门（`main.cr:146-165`；照 #29/TM03 先例）。语料排除项见 TODO #22。
- **清单双注册面（B.6，硬性）**：任何清单/段表改动必须**三面同核**——`build_selfhost_native.py`（`corec_files`/`corearch_files`/`corelsp_files`）↔ `src/targets/x86_64-linux/_import.cr`（project-mode）↔ `src/compiler/_import.cr`；每任务回归面**必含** `tests/selfhost/test_backend_bootstrap.py`（rc=0 + 构建日志 `error[` = 0）。
- **影子通道 = 必要不充分**（T7b concern ④ 继承）：`--type-shadow` 只覆盖 `type_equal` 站点面（本站点集 = P5 清理面，`ty_shadow.cr:252-269` 口径），**载体面（段/读回/TK/表示）零覆盖** ⇒ 载体正确性由**结构性断言 + corearch 读回对拍 + 定向探针**承担，不得以影子归零充当证据；每任务仍复跑影子对拍（**站点覆盖与差异数同报**）。
- **`.cir` 缓存面**：`CIR_CACHE_VER` 手工 bump 是格式/语义代变更的既定配方（`cir_cache.cr:14-31` 的 v14/v15/v16/v17 先例）；DF 记录布局/语义一变即**必须 bump**（v16 教训：旧快照会把坏状态原样复活成活产物）。快照对称写读 + 冷/暖两路径产物一致（`test_cache_identity.py` + `test_ccr_v7.py::test_v7_cache_hit_restore_path`）为本批守门。
- **不得回退既有收纳**：TS01-04/TK02 硬门（#29）、TM03/TG02 门（#31/#33）、F2 常量档拒绝语义与「N 不回身份」、F5 槽位契约（#25/#28）、效应/纯度判据（#26/#27）、`iface_satisfies` 轴序 A>C>B、逐成员判定——本计划一律不动；冲突 → 停下上报。
- 本语言无三元运算符；取模须非负；键比较不得依赖 i64 回绕；noclobber（`>|`）；自测用例**只增不减**（`test_type_engine.py:MIN_CASES` 与 `selftest-types` 逐任务下限见各任务判据）。
- **发射/解释器零泄漏**：`--dump-objects`（`corearch.cr:352`）与 `cir_cache` 快照在 TK 升格任务中的输出面**必须显式声明预期**（前者不变；后者随 bump 只影响缓存命中率，不影响产物）。

---

## 侦查底座（2026-09-12 实读，file:line = 当前工作树 HEAD `55280aa5`；未标注者为本轮实测）

**段表机制（ccr_io.cr）**
- `CCR_MAGIC=827474755`（`:106`）· `CCR_VERSION=7`（`:107`）· `CCR_SEG_COUNT=6`（`:108`，注「规范序 tag 1..6；预留 7+ 不占空间」）· NOD 盘记录 36B（`:113`）· EDG 8B（`:118`）· REG 24B（`:125`）。
- 尺寸：`ccr_str_seg_size`（`:261`）· `ccr_sym_seg_size`（`:272`）· `ccr_nod_seg_size`（`:322`）· `ccr_edg_seg_size`（`:326`）· `ccr_ent_seg_size`（`:330`）· `ccr_reg_seg_size`（`:334`）· `calc_ccr_size`（`:341`，`16 + CCR_SEG_COUNT*12` 起）。
- 写侧：`save_ccr`（`:470`）——`ccr_validate_i32_fields`（`:216`）形状守卫 → `populate_nod_objects`（`:452`）+ `compute_live_ranges`（`:489`）→ EDG 收集（`:495`）→ 尺寸/缓冲（`:503-505`）→ Header（`:508-511`）→ **段表 6×12B 硬编码 6 行**（`:529-534`）→ 段体 STR/SYM/NOD/ENT/REG/EDG（`:537+`）。STR 段 = 编译期 `g_strs` **全量**（`:537`）。
- 读侧：`load_ccr`（`:864`）——`fsize<16` 拒（`:865`）· magic（`:871`）· **版本闸 `ver != CCR_VERSION`**（`:875`）· `seg_cnt > (fsize-16)/12` 拒（`:880`）· 逐段三闸：`tg < 1 || tg > 6`（`:899`）· `tg != ri+1`（`:900`，规范序）· `soff != cursor`（`:901`，段体连续）· 越界（`:902`）· 去重标量 `have1..have6` + `seg_off1..6`/`seg_end1..6`（`:885-909`，**硬编码标量族**）· 必备集 `have1/2/3/5/6`（`:919`，ENT 可缺 `:920`）· 状态重置（`:923-935`）· 逐段装载（`:937+`；NOD → `g_v7_nod_sem` 对象面，`ent_kernel.cr:1010-1032`）。
- 枚举闸（读回侧）：`ccr_io.cr:1121`（`vc > MAX_ENUM_VARIANTS`）/`:1148`（`tc > MAX_VARIANT_TYPES`）——**写入侧零引用**（TODO #35）。

**类型行表与类型项 DAG**
- 行表：`alloc_type(kind,data,extra)`（`checker.cr:9-17`，24B/条）· 访问器 `get_type_kind/data/extra`（`checker.cr:314/319/324`）· `init_types()`（`checker.cr:231`）建原生 9 行（`:246-258`）并以 `iface_registry_init()`（`:262`）收尾 · 同名 `TYP_NAMED` 去重侧表 `g_named_dedup`（`checker.cr:125-140`）。
- 项 DAG：`g_type_terms`（48B/条 `{tag,a,b,c,d,hash}`，`type_terms.cr:22-24`）· 访问器 `tt_tag/tt_a..tt_d`（`:27-31`）· `tt_count`（`:32`）· `tt_hash5`（`:35`）· `tt_mod`（`:62`）· `grow_type_terms`（`:77`）· `grow_tt_index`（`:88`，容量 ≥ 2×项数）· `tt_reindex`（`:108`，重放插入）· `tt_term`（`:136`，装填守卫 + 键 = 五字段全比）· 构造 API（`:181-222`）· `tt_norm`/NNF/DNF（`:229-330`）。
- 项层全局（`globals.cr:265-273`）：`g_type_terms`/`g_type_term_count/cap`、`g_tt_index`/`g_tt_index_cap/count`、`g_tt_nil(_ok)`/`g_tt_top(_ok)`、`g_ty_memo_*`、`g_ty_steps`/`g_ty_budget_max`/`g_ty_exhausted`、`g_ty_lits_*`、`g_ty_uncovered`。

**引擎（type_engine.cr）**
- 纯核（1-612 行，**零** checker/桥接/注册表代码引用）：`AK_*`（`:18-35`，含 `AK_NULL=15`）· `tt_list_same`（`:208`）· `tt_type_elem_same`（`:228`）· `tt_list_variance_at/_variance`（`:288`/`:309`）· `ty_budget_reset`（`:428`）· `ty_exhausted`/`ty_uncovered`（`:441/442`）· `ty_sub`/`ty_equiv`/`ty_exhaustive`/`ty_inhabited`/`tt_witness`/`ty_disjoint`（`:390/405/471/414/448/606`）。
- iface 簇（613-772 行，**唯一耦合段**）：`IFACE_SAT_BUDGET`（`:632`）· `iface_satisfies`（`:634`，轴 A 形状名 → C 用户接口 → B 本质轴）· `iface_satisfies_term`（`:661`）· `iface_satisfies_ti`（`:673`）· `iface_find_method`（`:693`，唯一实现，mangling 已退役）· `iface_user_satisfies`/`_ii`（`:725`/`:738`，逐成员判定）。
- 依赖（簇外引用）：`find_iface`（`checker.cr:940`）· `find_func`（checker）· `decl_name_of_ti`（`checker.cr:954`）· `gen_constr_type_ti`（checker）· `sh_term_of_ti`/`sh_iface_shape_term`/`sh_shape_member_at`/`sh_iface_self_mode`/`sh_func_self_mode`/`sh_func_sig_term`（`ty_shadow.cr`）· `iface_shape_lookup`（`iface_registry.cr:355`）。

**接口/形状面**
- 本质条目表：`ESZ_IFACE_ENTRY=40`（`iface_registry.cr:57`）· `IFACE_ENTRY_COUNT=13`（`:61`）· 建表 `iface_registry_init`（`:85`）· 查询 `iface_count`（`:164`）/`iface_entry`（`:167`）/`iface_ops`（`:179`）/`iface_permits`（`:187`）/`iface_lit_*`（`:202-227`）· `iface_by_ty_code`（`:234`）· `ty_code_to_ti`（`:259`）· **`iface_kind_of`（`:279`，读 `get_type_kind`/`get_type_data` = checker 耦合点）**· `iface_ti_of`（`:296`）· 形状表 `iface_shape_grow/register/lookup/count/reset`（`:319-370`）· `iface_of_term`（`:375`）· 六形状 `sh_shape_seq[_ro/_rw]`/`_indexable`/`_iterable`/`_product`（`:402-433`）。
- 用户接口表：`ESZ_IFACEINFO=2712`（`dyn_arr.cr:143`）· `OFF_IF_NAME/METHOD_COUNT/GENERIC_COUNT/METHODS`（`:144-145`）· `ESZ_IFMETHOD=168` + `OFF_IFM_*`（`:155-161`）· 上限 `MAX_IFACE_METHOD_PARAMS=8`（`:162`）/`MAX_IFACE_METHODS=16`（`:167`）· parser 写点（`parser.cr:1701+`，方法签名节点 `:1762-1766`/返回节点 `:1778+`）· `g_impl_for` 写点（`parser.cr:1866-1869`）· `g_methods` 写点（`parser.cr:1855-1857`）。
- 全局（`globals.cr`）：`g_ifaces`/`g_iface_count`（`:36`）· `g_impl_for`（`:37`）· `g_methods`/`g_method_count`（`:31`）· `g_iface_entries`/`g_iface_entry_count`/`g_iface_registry_ok`（`:282`）· `g_iface_shape_names/terms/count/cap`（`:55-56`）· `IP_*` 位下标（`:298-300`）。
- 桥接（ty_shadow.cr）：`sh_native_ak`（`:79`）· `sh_base_ak`（`:94`）· 桥接缓存 `sh_map_*`（`:122-201`，`sh_map_reset` `:130`）· `sh_seq_term`（`:219`）· `sh_ref_mut_marker`（`:231`）· `sh_name_token`（`:244`）· `sh_null_term`（`:269`）· `sh_term_of_ti`（`:274`，per-ti 缓存）· 展开层 `sh_struct_term`（`:787`）/`sh_variant_term`（`:828`）/`sh_enum_domain_term`（`:841`）/展开缓存 `sh_unf_*`（`:671-738`）· 签名项 `sh_sig_term_of_ti`（`:892`，规范形）/`sh_sig_term_of_node`（`:964`）/`sh_iface_sig_param_term`（`:974`）/`sh_iface_sig_ret_term`（`:984`）/`sh_iface_self_mode`（`:990`）/`sh_func_sig_*`（`:997-1085`）· `sh_iface_shape_term`（`:1086`）/`sh_shape_member_at`（`:1119`）。

**DFNode.TK 面（全消费点清点，9 处）**
- 定义/布局：`ESZ_DFNODE=64`，`OFF_DF_TK=40`（`dyn_arr.cr:103-107`）· 建点 `df_create_node`（`dataflow.cr:92`，tk 写 `:102`）· 唯一调用点 `emit`（`ir_gen.cr:193-206`，tk 直传 `:202/:205`）。
- 读者：`df_rebuild_ir`（`dataflow.cr:448`，tk → `iri_set_tk`）· `ir_gen.cr:129`（`arr_len_lit_of` 的 `TI_STR` 判定）· `interp.cr:256`/`:546`（解释器两处）· `provenance_verify.cr:57`（宽度）· `cir_cache.cr:251`（快照写）/`:426`（快照读）· `ccr_io.cr` NOD 段（盘 36B 的 `tk` 字段）。
- `.cir` 快照：`CIR_CACHE_VER=17`（`cir_cache.cr:31`，历史 v14-v17 逐版注记 `:14-31`）；头部 = magic/ver/**编译器身份 cid**/fp/sig/name（`:220-226`），身份闸 `:367-369`；DF 记录 8 槽对称写读（`:246-253` / `:426`）。

**构建/测试面**
- 清单：`build_selfhost_native.py` `common_files`（`:35-45`：runtime + stdlib(io/fmt/cli/toml) + ast/globals/dyn_arr）· `backend_support_files`（`:103-111`：ccr_io/hotpatch/arena/goroutine/chan/sched/corearch）· `corec_files`（`:298+`，引擎三件 `:313-323`；monomorph 迁入注 `:334`）· `corearch_files = common + hit_engine + kernel + backend_support`（`:366-368`）· `corelsp_files`（`:383-418`，含引擎三件 + 影子层，**无 ccr_io**）。
- 项目模式双注册：`src/targets/x86_64-linux/_import.cr`（B.6 事发面）+ `src/compiler/_import.cr`。
- 测试：`tests/selfhost/test_ccr_v7.py`（**27 例**；`SEG_TAGS=[1..6]` `:83`·`HEADER_TABLE=88` `:88`·`seg_count == 6` 断言 `:129`·版本拒收 `:1088`）· `tests/selfhost/test_backend_bootstrap.py`（#31 未挂 CI，**本批每任务必跑**）· `tests/selfhost/test_cache_identity.py` · `tests/selfhost/test_type_engine.py`（判据下限）· `src/ci/run.sh` selfhost 挂点（`:68-90`）。
- 自测守门（本批会翻的钉）：`isat.no_str_intern_on_missing`（`type_selftest.cr:420`）· `x2.no_str_intern`（`:619`）· `ifc.mangling_zero_str_intern`（`:866`）· `x2.axis_a_registered`（`:599`）/`x2.shape_no_leak_after_reset`（`:609`）· `iface.count`（`:1362`）· `iface_entry(AK_NULL) == -1 && iface_count() == 13`（`:2362`）· `isat.axis_c_ret_code`（`:368`）· `tests/selfhost/test_iface_satisfies.py:318`（`inst_encoding_limit_named_ret_rejected`）。
- corearch 通道：`load_ccr`（`corearch.cr:339`）→ `init_backend_arrays`（`:341`）→ `build_linear_schedule`（`:347`）→ `--dump-objects`（`:352`，`dump_object_surface` `:232`，逐节点打印 `nod_*` 对象面）。

---

## Task 0（前置，纯搬运）：引擎核纯化拆分——corearch 链接面开路

**Files:** New `src/compiler/iface_axis.cr`（corec-only：iface 簇 + `iface_kind_of`）· Modify `src/compiler/type_engine.cr`（`:613-772` 整段移出，保留纯核）· `src/compiler/iface_registry.cr`（`:279-293` 移出）· `build_selfhost_native.py`（`corec_files`/`corelsp_files` 增新文件；**corearch 清单本任务不动**）· `src/compiler/_import.cr` + `src/targets/x86_64-linux/_import.cr`（双注册面同核）· `src/compiler/type_selftest.cr`（若有符号位置断言）

**Interfaces:**
```
// iface_axis.cr（corec-only；内容 = 原 type_engine.cr:613-772 逐字搬迁 + iface_kind_of 逐字搬迁）
fn iface_satisfies(t_ti: int, iface_ni: int) -> int          // 轴 A>C>B（签名/语义零变化）
fn iface_satisfies_term(t_ti: int, shape: int) -> int
fn iface_satisfies_ti(t_ti: int, cti: int) -> int
fn iface_find_method(type_ni: int, method_ni: int) -> int
fn iface_user_satisfies(t_ti: int, iface_ni: int) -> int
fn iface_user_satisfies_ii(tname_ni: int, ii: int) -> int
fn iface_kind_of(ti: int) -> int                              // 自 iface_registry.cr 移入（读 get_type_kind/get_type_data）

// 纯化后仍留在 type_engine.cr / iface_registry.cr 的面（corearch 链接目标）
//   type_engine.cr（1-612 行）：ty_sub/ty_equiv/ty_disjoint/ty_inhabited/tt_witness/ty_exhaustive/
//     tt_norm 族/tt_list_same/变型表/预算守卫/tt_list_variance*
//   iface_registry.cr（除 iface_kind_of 外全部）：条目表 + 形状表 + iface_of_term + ty_code_to_ti + iface_by_ty_code
```

- [ ] **Step 1: 红态（链接面实证，**不改仓库文件**）**：以 `/tmp` 下一次性脚本读 `build_selfhost_native.py` 的清单，**在内存中**把 `type_terms.cr`/`type_engine.cr`/`iface_registry.cr` 追加进 corearch 集后 concat 编译（等价于临时清单变更，但不落任何仓库改动——铁律 3「文件永久不允许还原」下**不得**走「改了再还原」路径）→ **期望红**：构建日志 `error[` 非零（N06 未定义：`find_iface`/`find_func`/`decl_name_of_ti`/`gen_constr_type_ti`/`sh_term_of_ti`/`sh_iface_shape_term`/`get_type_kind` 族）。**记录清单与计数**（= 拆分覆盖面，入报告）。
- [ ] **Step 2: 拆分（纯搬运）**：新建 `src/compiler/iface_axis.cr`，把 `type_engine.cr:613-772`（`IFACE_SAT_BUDGET` + 7 函数，含段头注）**逐字**移入；把 `iface_registry.cr:279-293`（`iface_kind_of` + 头注）**逐字**移入。原位留一行指路注释（`// iface 簇已移至 iface_axis.cr（R2 P4 Task 0，链接面纯化）`）。
- [ ] **Step 3: 清单三面同改**：`build_selfhost_native.py` 的 `corec_files` **与** `corelsp_files` 在 `iface_registry.cr` 之后插 `src/compiler/iface_axis.cr`（corearch 清单**本任务仍不加**——加面归 Task 2/Task 3，按需分步）；核 `src/compiler/_import.cr` 与 `src/targets/x86_64-linux/_import.cr`（project-mode 面：本任务不引入新依赖，若两文件均未列新文件则**不动**，在报告登记「本任务投射面零变化」）。
- [ ] **Step 4: 判据（零行为变化）**：`selftest-types` **333/333 不变**（用例数零增减）；`test_ccr_v7.py` 27/27；ELF canary IDENTICAL（clean-cache）；`.ccr` 逐字节同（`ptr_arith` 88943B `54e3856b…` / `generics_test` 130974B `97be2f9e…`，冷缓存）；影子对拍复跑（`decisions` 同值、全计数 0、站点覆盖同报）；全量回归（五 CI job + `test_backend_bootstrap.py` rc=0 + 构建日志 `error[` = 0）+ 自举链（`corec2==corec3` cmp IDENTICAL + N06=0 + 冒烟 42）+ 三二进制（corec/corearch/corelsp）构建 rc=0。
- [x] **Step 5: 提交**：`refactor: R2 P4 Task 0——引擎核纯化拆分（iface 簇 → iface_axis.cr；iface_kind_of 移出注册表）为 corearch 链接面开路；零行为变化`

## Task 1：段机制——TYPE=7 / IFACE=8 + 版本 7→8 + 加段四面（两段空壳）

**Files:** Modify `src/compiler/ccr_io.cr`（常量/尺寸/写侧/读侧/闸族）· `docs/superpowers/specs/2026-09-09-lattice-ir-v7-format.md`（§1 版本表 / §2 tag 行 + 布局图 / §3 新增 §3.7 TYPE、§3.8 IFACE 定义 / §4 校验规则）· `tests/selfhost/test_ccr_v7.py`（结构性断言重定）· New `tests/selfhost/test_ccr_types.py`（两段结构性断言新套件）· `src/ci/run.sh`（挂钩）

**Interfaces:**
```
CCR_VERSION   : int = 8;          // ccr_io.cr:107（7 → 8）
CCR_SEG_COUNT : int = 8;          // ccr_io.cr:108（6 → 8；tag 7=TYPE 8=IFACE）
CCR_SEG_TYPE  : int = 7;  CCR_SEG_IFACE : int = 8;   // 新增常量（数值权威 = 段序）

fn ccr_type_seg_size() -> int     // 4 + (段体缓冲长度)，与写侧同一来源
fn ccr_iface_seg_size() -> int
// 段体缓冲（D18 解耦：内容构造在 corec-only ccr_types.cr；本文件只搬运）
g_ccr_type_seg : string, mut;   g_ccr_type_seg_len : int, mut;
g_ccr_iface_seg : string, mut;  g_ccr_iface_seg_len : int, mut;
```

- [ ] **Step 1: 用例（红）**：`tests/selfhost/test_ccr_types.py` 新增 ≥6 例（本步先立「机制面」四条，内容面随 Task 2/3 增长）：① 段表 = 8 段、tag 序 1..8、offset 连续、`end == fsize`；② `seg_count` 字段 = 8、version = 8；③ loader 拒收 **6 段**（把 T1 前的旧 8 段文件截成 6 段 / 或 `seg_cnt` 改成 6）与 **缺 TYPE 段**、**缺 IFACE 段**（byte mutation 删段/改 tag）；④ 空壳两段（count = 0）时 corec `build` + corearch `--elf` 全链 rc=0、产物行为正确。**为何这几条**：加段四面的每条不变式各一正控 + 缺失拒绝（三态纪律）+ 端到端不回归。
- [ ] **Step 2: 实现（写侧）**：`:107/:108` 常量；`:341` `calc_ccr_size` 加两段；段表 6 行 → 8 行（`:529-534`，offset 链延长）；段体按规范序写两段缓冲（**本任务先写空壳：count=0**，不引入内容构造）；`ccr_validate_i32_fields`（`:216`）如需覆盖新段字段则同步。
- [ ] **Step 3: 实现（读侧）**：`:885-890` 标量族扩 `seg_off7/8`、`seg_end7/8`、`have7/8`；`:899` 闸界 `6 → 8`；`:900/:901` 规范序/连续闸**形状不变**（D9 的直接收益）；`:919` 必备集 += `have7/have8`；`:923-935` 状态重置面确认（本任务无新内存表）；`:937+` 两段解析（空壳：读 count 并**校验 == 0**，非 0 = 拒绝——内容面落地前不接受外部内容，防半成品被消费）。
- [ ] **Step 4: 测试面重定**：`test_ccr_v7.py`：`SEG_TAGS`（`:83`）→ `[1..8]`；`HEADER_TABLE`（`:88`）→ `16 + 8*12 = 112`；`V7File` 的 `seg_count == 6` 断言（`:129`）→ 8；`test_v7_loader_rejects_version_ne_7`（`:1088`）坏版本三元组 `(6,5,8)` → `(6,5,7)`（8 已是合法版本）；其余用例（ENT/REG/EDG 走查、负分支）走 `segs[tag]` 寻址，**预期零改动**——逐例实跑确认并登记改动面。**文件头 docstring 的字节真相表（`:22-38`）同步**（6→8 段、version 7→8）。
- [ ] **Step 5: spec 同步（D9/D10）**：`2026-09-09-lattice-ir-v7-format.md`：§1 表 `version 6→7` 行改为「7（magic 不变）」→ 追加 v8 行（新段集合 = 8 段）；§2 tag 行改 `1=STR 2=SYM 3=NOD 4=ENT 5=REG 6=EDG 7=TYPE 8=IFACE`（`9+` 预留：驱逐标注段/证书段——**顺移**）；布局图把 header 的 `version u32 = 7` 改 8、段体列补两段；§3 新增 TYPE/IFACE 两小节（本任务先落**空壳定义** + 「内容定义见 Task 2/3」指针）；§4 校验规则补 `seg_cnt == 8` 与两段必备。
- [ ] **Step 6: 判据**：新套件 ≥6 例全绿 + `test_ccr_v7.py` 全绿（**改动面逐例登记**：改了哪几例、为何）；ELF canary IDENTICAL；**`.ccr` 新记录值**（冷缓存 `ptr_arith`/`generics_test` 尺寸 + sha256，作废旧值并留档）；冷/热两态 `.ccr` 逐字节同（T1 起新增守门）；全量回归 + `test_backend_bootstrap.py`（`error[`=0）+ 自举链 + 影子对拍复跑（**预期零变化**：本任务不触判定路径）。
- [ ] **Step 7: 提交**：`feat: R2 P4 Task 1——.ccr 段机制：TYPE(7)/IFACE(8) 加段 + 版本 7→8 + loader 三闸与必备集扩面；两段空壳落盘 + 结构断言套件`

## Task 2：TYPE 段内容——类型行表 + 类型项 DAG + 确定性装填 + corearch 读回

**Files:** New `src/compiler/ccr_types.cr`（**corec-only**：D13 装填 + 段体构造 + 自测 helper）· Modify `src/compiler/ccr_io.cr`（读侧两小节解析 → 内存重建；写侧搬运缓冲）· `src/compiler/type_terms.cr`（如需 `tt_reset`/`tt_hash5` 暴露；**corearch 链接**）· `build_selfhost_native.py`（`corearch_files` += `type_terms.cr`；`corec_files` += `ccr_types.cr`）· 双注册面两处 · `src/lattice/ent_kernel.cr` 或 `src/compiler/corearch.cr`（读回断言/dump 通道）· New `tests/selfhost/test_ccr_types.py`（内容面用例）

**Interfaces:**
```
// ── TYPE(7) 段字节（D12；小端、offset 相对段首）──
// [row_count u32]
// [row_count × 24B {kind i32, data i32, extra i32}]         ← g_types 0..g_type_count-1 原样
// [term_count u32]
// [term_count × 40B {tag i32, a i32, b i32, c i32, d i32}]  ← 项 DAG（索引 = 行号；哈希不落盘）
// 加载侧不变量（逐条校验，违规 = 拒绝）：
//   · 子项引用（UNION/INTER 的 a,b；NOT 的 a；MU 的 b；CONS 的 a,b；ATOM 的 c 及 c 链元素）必须 < 自身行号
//     ——**ATOM 的 a（ak）与 b（ti 行号/固定性位/token 值）是标注，不得按「< 行号」校验**
//     （b = 数千行号会被误拒；见 sh_ref_mut_marker/sh_name_token/sh_seq_term 三构造点的 b 语义）
//   · tag ∈ 0..10（TT_*）；a..d ≥ -1；越界/拓扑违规 ⇒ 拒绝

// ── corec 侧装填与构造（ccr_types.cr）──
fn ccr_type_populate() -> int          // D13：规范化重建 + 行序装填 + 签名装填 + 形状重注册；-1 = 有不可译行（save 拒绝）
fn ccr_type_seg_build() -> int         // 生成 g_ccr_type_seg/g_ccr_type_seg_len；-1 = 失败
fn ccr_type_selftest_dump() -> int     // 自测/对拍通道：逐行打印（测试解析用）

// ── corearch 侧读回（load_ccr 内）──
// 段 → g_types 行（kind/data/extra）+ g_type_terms 行（tag/a..d + 重算 hash）+ g_tt_index 重建（tt_reindex）
// 失败 = load_ccr 返回 -1（不得返回 0 且留空表）
```

- [ ] **Step 1: 用例（红）≥8 例**：① 段结构（两小节计数/长度自洽、`end == fsize`）；② 行表逐行 == corec 侧 `g_types`（对拍 dump）；③ 项表重建后：同 tag/a..d 的项**命中同一节点**（去重重建正确——对 `[int;3]`/`[int;4]`/`int?`/元组等已知形态各一）；④ **确定性**：同源两次编译（**冷缓存 / 热缓存两态**）TYPE 段逐字节同（D13 的直接守门）；⑤ 拓扑违规 mutation（子项引用 ≥ 自身行号）→ loader 拒绝；⑥ 标注槽不被误拒：构造 `b = 大值行号` 的 ATOM 项，loader 接受（**防「假校验」实现**）；⑦ 段缺失/截断 → 拒绝；⑧ `--dump-types`（corearch）与 corec 侧 dump 逐行一致（跨进程同构）。
- [ ] **Step 2: 实现（corec 装填 D13）**：`ccr_type_populate()`：① 项层复位（`g_type_term_count = 0`、`g_tt_index_cap = 0`（惰性重建）、`g_tt_top_ok/g_tt_nil_ok = 0`、`g_ty_memo_ok = 0`、预算计数复位）② 桥接/展开缓存复位（`sh_map_reset()` / `sh_unf_map_reset()`）③ 形状**重注册**（`iface_shape_builtin_init()`——Task 3 落地；本任务先以现注册面为准并登记）④ 行序 0..`g_type_count-1` 调 `sh_term_of_ti`（返回 -1 ⇒ **save 拒绝 + 诊断**）⑤ 接口签名项装填（本任务先空转，Task 3 填充）。**前置核查（写进报告）**：`save_ccr` 两处调用点（`main.cr:612`/`:636`）之后无类型项消费者（`sh_finish` 只读 `g_shadow_*`）。
- [ ] **Step 3: 实现（序列化 + loader）**：`ccr_type_seg_build()` 按 D12 布局写缓冲；`save_ccr` 加段搬运（D18）；`load_ccr` 解析 → 重建（含 `tt_hash5` 重算 + `tt_reindex` 重放；`grow_type_terms`/`grow_tt_index` 前置计数）。
- [ ] **Step 4: corearch 面**：manifest `corearch_files` += `type_terms.cr`（在 `ccr_io.cr` **之前**——`ccr_io` 读侧要调 `tt_hash5`/`tt_reindex`；清单顺序 = 常量可见性约束，`iface_registry.cr:18-23` 先例）+ 双注册面两处同步；新增 `--dump-types` 测试通道（`corearch.cr`，照 `--dump-objects` `:352` 先例）。
- [ ] **Step 5: 判据**：新套件全绿；`selftest-types` **≥333 → ≥336**（新增项层/装填用例）；ELF canary IDENTICAL；`.ccr` 记录值更新（TYPE 段入盘 ⇒ 尺寸变大，实测报告）；**冷/热两态 `.ccr` 同**（D13 守门）；`test_backend_bootstrap.py` rc=0 + `error[` = 0；全量回归 + 自举链；影子对拍复跑（本任务不触判定路径 ⇒ 预期零变化，站点覆盖同报）；**尺寸实测报告**（`g_type_count`/`tt_count` 对三档语料 = `tests/suite/ptr_arith.cr` · 自源 `src/compiler/_import.cr` · 大盘，落报告，勿以估算代之）。
- [ ] **Step 6: 提交**：`feat: R2 P4 Task 2——TYPE 段（类型行表 + 类型项 DAG 序列化 + 确定性装填 D13 + corearch 读回重建）；corearch 链接 type_terms.cr`

## Task 3：IFACE 段内容——扩列 + 形状命名化 + 签名项化 + impl 边/方法表 + 读回

**Files:** New/Modify `src/compiler/ccr_types.cr`（IFACE 段构造 + 装填）· Modify `src/compiler/iface_registry.cr`（扩列：3 新行 + `name_ni` 填充 + `iface_shape_builtin_init`）· `src/compiler/ccr_io.cr`（写/读）· `src/compiler/type_selftest.cr`（硬值重定 + 零 str_intern 守门重定）· `tests/selfhost/test_ccr_types.py` · `tests/selfhost/test_iface_satisfies.py`（编码面钉评估）· `src/ci/run.sh`

**Interfaces:**
```
// ── IFACE(8) 段字节（D14；五小节，各带 count u32）──
// ① [native_count u32] [× {ak i32, ti_row i32, name_ni i32, lit_code i32, ops i64}]   = 24B/条（IFACE_ENTRY_COUNT 条）
// ② [shape_count u32]  [× {name_ni i32, term i32}]                                    = 8B/条
// ③ [iface_count u32]  [× {name_ni i32, method_count i32, generic_count i32, pad i32}
//                        + method_count × {name_ni i32, param_count i32, self_mode i32,
//                                            ret_term i32, param_terms[8] i32, param_codes[8] i32}]
//                      方法记录 = {name_ni, param_count, self_mode, ret_term} 4×4B + param_codes[8] 32B
//                                + param_terms[8] 32B = 80B（ret_term/param_terms = 类型项索引，-1 = 不可建）
// ④ [impl_count u32]   [× {trait_ni i32, type_ni i32}]                                = 8B/条（g_impl_for）
// ⑤ [method_count u32] [× {type_ni i32, method_ni i32, mangled_ni i32}]               = 12B/条（g_methods）
// 校验：name_ni/type_ni/method_ni 必须 < STR 段串数；term 索引必须 < TYPE 段项数（跨段引用域检查）

// ── 扩列（裁决 3 + 7）──
IFACE_ENTRY_COUNT : int = 16;   // 13 → 16：+AK_NULL（原生第九员，裁决 7 独立槽）+AK_SUM +AK_FN
// 新行列值：{ak, ti_row = -1（无规范行）, name_ni = 注册名, lit_code = -1, ops = 0}
//   ops = 0 的解释：null/sum/fn 无「操作许可」面（算术/逻辑/条件门对其全拒 = 现状行为，P2b 起 iface_permits(-1/未登记)=0 同值）
//   ⇒ 本三行为**纯信息面**：只改「可查询性」，不改任何判定（iface_kind_of 的行分派见 Step 3 边界）

// ── 形状命名化（裁决 1 + D17）──
fn iface_shape_builtin_init() -> int   // 六形状按 D17 名注册（zero-arg from its own idempotent path）；init_types 尾部调用
fn sh_shape_*() -> int                 // 六个构造点不变（iface_registry.cr:402-433）
```

- [x] **Step 1: 用例（红）≥10 例**：① 六形状在生产路径**名字可查**（`iface_shape_lookup(ni) >= 0`，逐名）；② 形状项与 `sh_shape_*()` 构造点同项（节点同一）；③ 注册面**泄漏守门**（`reset_frontend_state` 后 count=0 → 再 `init_types` 恢复 6，`x2.shape_no_leak_after_reset` 口径延续）；④ 扩列后 `iface_count() == 16` 且三新行 `iface_ops == 0`、`iface_entry(AK_NULL) >= 0`；⑤ **判定面零变化**：扩列前后 `iface_kind_of` 对全类型行枚举**逐行同值**（`TYP_NULL`/`TYP_OPTIONAL` 行仍回 -1 = 保守面不变）；⑥ IFACE 段结构自洽（五小节计数/长度/`end == fsize`）；⑦ 签名项化：接口方法签名**入段后逐项可读**（`ret_term`/`param_terms` 与 corec 侧 `sh_iface_sig_*` 同项）；⑧ 跨段引用域违规 mutation → 拒绝（`term` 索引越界 / `name_ni` ≥ STR 串数）；⑨ 读回对拍：corearch `--dump-ifaces` 与 corec 侧逐行一致；⑩ 签名不可建（`dyn` 位图行入签名）⇒ save 拒绝 + 诊断（**先 report-only**）。
- [x] **Step 2: 扩列实现**：`iface_registry.cr` 条目表加 3 行（`iface_put(13..15, …)`）+ `IFACE_ENTRY_COUNT 16`；`name_ni` 列按裁决 1 填注册名——16 行逐名 = 原生 8（`int/dex/string/bool/unit/never/char/dyn`）+ 结构/命名 5（`product/sequence/ref/ptr/named`）+ 新行 3（`null/sum/fn`）；`iface_registry_init` 的 `str_intern` 面 = 本次裁决允许的**唯一**初始化期驻留（判据按 TODO #26）。
- [x] **Step 3: 行分派边界（硬性）**：`iface_kind_of`（Task 0 后位于 `iface_axis.cr`）**零改动**——`TYP_NULL`/`TYP_OPTIONAL`/`TYP_DYN` 等行的分派与 P2b 钉死的域**逐格保持**（扩列只加「可查询性」不加「行→类」新映射）；`ty_code_to_ti` 零改动（`iface_registry.cr:259-269`，码 7 撞车格原样）。
- [x] **Step 4: 形状命名化**：`iface_shape_builtin_init()` 在 `init_types()` 尾部（`checker.cr:262` 之后）调用；`iface_shape_register` 保持同名覆盖幂等；D13 的规范化重建路径中**重跑**该初始化（order：reset → 形状重注册 → 行装填 → 签名装填）。
- [x] **Step 5: IFACE 段实现（写/读）**：写侧按 D14 五小节（③ 的签名项来自 `sh_iface_sig_param_term`/`sh_iface_sig_ret_term`，`param_codes` 为**信息面**保留裸码——`dyn_arr.cr:148-154` 的裸码槽语义不变；裸码不可建/塌缩照现状）；读侧重建 `g_iface_entries`（并置 `g_iface_registry_ok = 1`——**段 = corearch 侧真源**，防 `iface_ensure()` 用常量表覆盖）、`g_iface_shape_*`、`g_ifaces`（`ESZ_IFACEINFO` 布局 + `OFF_IFM_PARAM_NODES/RET_NODE` 槽：corearch 侧**无 AST 节点** ⇒ 节点槽写 -1、项槽走新字段——**布局决策**：新增 `OFF_IFM_RET_TERM` / `OFF_IFM_PARAM_TERMS` 槽（`ESZ_IFMETHOD` 168 → 168 + 8×8 + 8 = 240）**或**复用节点槽（corearch 侧节点槽恒 -1，不可复用——签名项在 corec 侧由节点派生、在 corearch 侧由段给出，二者不同域）。**取新增槽**（`ESZ_IFMETHOD` 扩至 240、`ESZ_IFACEINFO` 2712 → 16×240 + 24 = 3864。）**影响面须实测声明**：内存布局扩槽**不入盘**（盘面 = D14 紧凑形）；`g_ifaces` **不入** `.cir` 快照（快照面 = var 声明/DF 节点/SG/边/指令/串常量）⇒ **本布局变更不触发 `CIR_CACHE_VER`**——实施时以「快照字段清单逐项核对 + 冷/暖产物一致」复证并写入报告）。`g_impl_for`/`g_methods` 直填。
- [x] **Step 6: 守门重定（裁决 1 的落地面）**：`x2.no_str_intern`（`type_selftest.cr:619`）/`isat.no_str_intern_on_missing`（`:420`）/`ifc.mangling_zero_str_intern`（`:866`）→ 重定为「**判定/查询路径**零新增驻留 + 初始化/注册面固定增量（逐名登记）」；`x2.axis_a_registered`（`:599`）/`x2.shape_no_leak_after_reset`（`:609`）→ 生产注册面重钉；`iface.count`（`:1362`）与 `:2362` 复合断言 → 13 → **16**（逐行理由入注释）。
- [x] **Step 7: 签名编码面（C.4）**：`isat.axis_c_ret_code`（`type_selftest.cr:368`）与 `inst_encoding_limit_named_ret_rejected`（`test_iface_satisfies.py:318`）——**先证同构**（段内项与进程内项同节点/同判定）后才允许改走类型项；本任务只落证据，**不动用例**（改动归 P5 收尾面，登记）。
- [x] **Step 8: 判据**：新套件 ≥10 例全绿；扩列/命名化的「旧接受→新拒绝/放宽」清单（**预期空**：纯信息面 + 名字注册）逐条报告；`selftest-types` ≥336 → **≥344**；ELF canary IDENTICAL；`.ccr` 记录值更新 + **STR 段增长实测**（新增驻留串数与字节数，逐名列举）；冷/热两态同；全量回归 + `test_backend_bootstrap.py` + 自举链；影子对拍复跑（**预期零差异**，站点覆盖同报）；扩列三行的判定面零变化由 ⑤ 的全类型行枚举承担。
- [x] **Step 9: 提交**：`feat: R2 P4 Task 3——IFACE 段（原生条目扩列 13→16 + 形状命名化 + 接口签名项化 + impl 边/方法表序列化 + corearch 读回）；STR 段增长按裁决 1 接受`

## Task 4：DFNode.TK 升格——迁移期双槽 + `.cir` 快照版本 + canary 证明（裁决 8）

**Files:** Modify `src/compiler/dyn_arr.cr`（`ESZ_DFNODE` + `OFF_DF_TK_TERM`）· `src/compiler/dataflow.cr`（`df_create_node` 增参 + 存储）· `src/compiler/ir_gen.cr`（`emit` 派生项 + 传参）· `src/compiler/ty_shadow.cr`（`sh_tk_term_of_code(opcode, tk)`：允许清单制派生，其余 -1）· `src/compiler/cir_cache.cr`（快照对称写读 + `CIR_CACHE_VER` 17→18）· `src/compiler/corearch.cr`/`src/lattice/ent_kernel.cr`（如需 dump 项槽）· `tests/selfhost/test_ccr_types.py` / `test_ccr_v7.py` / `test_cache_identity.py`

**Interfaces:**
```
ESZ_DFNODE : int = 64 → 72;   OFF_DF_TK_TERM : int = 64   // 双槽：tk 原样 + 项引用（-1 = 无项）
fn df_create_node(opcode, dest, src1, src2, src3, type_kind, tk_term: int) -> int   // 唯一调用点 = emit
fn sh_tk_term_of_code(opcode: int, tk: int) -> int
//   允许清单制（见 Step 2）：仅当 op ∈ 允许清单 ∧ 0 ≤ tk < g_type_count ⇒ sh_term_of_ti(tk)
//   （项层**复用既有缓存/建项语义**，不新增第二套建项逻辑）；否则 -1
CIR_CACHE_VER : int = 18;                 // cir_cache.cr:31（v18 注记：DF 记录 +8B 项槽，布局变更）
```

- [x] **Step 1: 用例（红）**【实施记录：6 例入 `test_ccr_types.py`（㉖..㉛）+ 8 例入 `selftest-types`（`t4.tk_*`）】：① `emit` 后逐节点检查：`tk` 与升格前**逐字节同**、`tk_term` 在「tk 是合法类型行」时 = `sh_term_of_ti(tk)`、其余 -1（三类正控 + 三类负控：`IR_BOUNDS_CHECK` 的 `ti=1` 旗标语义、`tk=0` 的合法行 vs `tk` 为越界值）；② `.cir` 冷/暖两路径：命中恢复后 `tk_term` 与冷路径**同值**（快照对称写读）+ `test_v7_cache_hit_restore_path` 口径；③ `.ccr` NOD 段**36B/条不变**（记录尺寸断言 + 旧记录值复核）；④ `--dump-objects` 输出**逐字节同**（tk 列不变、不新增列）；⑤ ~~`CIR_CACHE_VER` 变 ⇒ 旧 `.cir` 条目整类失效~~**【实施修正：不 bump——见 Step 4】**。**为何这几条**：升格的三条硬约束（发射面零泄漏 / 快照对称 / 缓存失效正确）各一正控，另加双槽语义正负控。
- [x] **Step 2: 允许清单推导（先于实现；产物 = op 表入报告）**：`tk` 现为**混用槽**——同一位置既放类型行也放旗标/忽略值（实测样本：`IR_BOUNDS_CHECK` 的 `ti=1` = 动态上限旗标（`ir_gen.cr:105/:114/:140`）、比较族 `IR_BINARY` 传 `0` = 忽略）。⇒ 允许清单 = 「后端按 `ti` 分派发射的 op」（`instr.cr:460` 分发器逐分支审计）×「该 op 的 `tk` 由调用点传**类型行**」；**旗标/忽略值 op 一律 -1**（不得给 `IR_BOUNDS_CHECK` 凭空安上 `dex` 项）。清单与排除项逐 op 列报告（不猜）。**【实施实测：`instr.cr` 全文件 `ti` 消费者仅 5 处 = `IR_CONST(ti==TI_STR)` / `IR_BINARY(ti==TI_DEX)` / `IR_DEREF`+`IR_STORE_PTR`（`e2_ptr_bounds_check` 的 access_width）/ `IR_BOUNDS_CHECK(ti!=0)`；216 个 `emit(` 调用点逐 op 静态审计 + 全语料 NOD 的 (op,tk) 直方图（自源 146540 行 / `ptr_arith` 1298 行 / `dex_test`）⇒ ①×② 交集 = 恰 **{IR_CONST, IR_BINARY}**；排除面逐 op 见报告 §2（`IR_ALLOC`/`IR_CALL`/`IR_I2F`/`IR_F2I`/`IR_LOAD` 传类型行但后端不按 ti 分派 ⇒ 暂不入清单，P5 再裁）】**
- [x] **Step 3: 实现（emit 期填槽）**：`dyn_arr.cr` 布局 + `df_create_node` 增参（**唯一调用点 = `emit`**，`ir_gen.cr:193-206`，签名同步）；`emit` 内 `sh_tk_term_of_code(opcode, type_kind)` 派生后传参；`dataflow.cr` **仅存储**（不引用桥接符号——该文件虽不在 `corearch_files`（`build_selfhost_native.py:366-368` = common + hit_engine + kernel + backend_support），仍按「共享文件零前端依赖」纪律留余量，注记落文件）。**时点选择 = emit 期（非「save 前派生式」）**：快照在 IR 生成期逐函数写（`cir_cache` 路径），若填槽晚于快照 ⇒ 冷/暖恢复路径项槽分歧（D13 同类陷阱）；emit 期填 ⇒ 由构造保证一致。代价 = 每 `emit` 一次桥接查询（首次建项后为缓存命中），**须报编译耗时抽样**（照 A.3-⑩ 口径）。
- [x] **Step 4: 快照面**【**实施修正（实测反证，见 D15 注）：不落盘第 9 槽、`CIR_CACHE_VER` 保持 17**】：原设计（写点加第 9 槽 + 读点对称 + 17→18）以「不入槽则冷/暖分歧」为前提，**实测方向相反**——`--dump-tk-terms` 实测 `ptr_arith`：冷路径 `terms=13 bad_term=0`、暖路径 `terms=10` **`bad_term=329`**（恢复来的项引用**全部越出**暖进程项表）。根因 = 项表是**进程内**内容寻址表、emit 期按需追加 ⇒ 快照里的索引只在写侧进程成立；「纯函数值与其入参并存于同一记录」另违第二真源纪律。修正 = 盘记录仍 8×8B=64B（`.cir` 布局零变化 ⇒ 无 bump），装载侧在 **emit 的等价时点**（逐函数逐节点序）用 `sh_tk_term_of_code(opcode, tk)` 重派生 ⇒ 项表追加序与冷路径一致，冷/暖 (op,tk,term,tag,a,b,c) 逐行相同（㉙ 用例锁住；实测两态 `terms=13 bad_term=0` 且 1298 行全等）。**旧条目无需失效**：格式未变 + 身份字段（v17）已覆盖跨二进制面。
- [x] **Step 5: 判据（裁决 8 的硬判据）**【实测全绿：canary `95084e7b…d475` **IDENTICAL**（clean-cache 冷态）· `ptr_arith`/`generics_test` 八段**逐字节同**（升格前后，含 NOD tk 列）· `--dump-objects` 逐字节同 · 五 CI job rc=0 · 全枚举 54/54 · 自举链 `cmp` IDENTICAL（双 2754918B `55237ec1…`）+ N06=0 + 冒烟 42 · 影子对拍 diff 空 · `selftest-types` **355/355**（347 + 8）】：**ELF canary IDENTICAL**（clean-cache；变即停下上报）；`.ccr` **逐字节同**（NOD 36B 不变；TYPE/IFACE 段不受影响）——冷缓存实测；`--dump-objects` 逐字节同；解释器路径（`test_interp_parity.py`/`test_optional.py`）全绿；`test_cache_identity.py` + 冷/暖 `test_v7_cache_hit_restore_path` 绿；全量回归 + 自举链；影子对拍复跑（本任务不触判定路径）；`selftest-types` ≥344（+升格面用例）。
- [x] **Step 6: 登记面（写进报告，勿静默）**：NOD 盘面项槽归 **P5 单槽化**（理由 = `tk` 混用码不可反推项，见 D15）；`.csr`/TagNode 图消费面（§6.4 的验证消费）本批**无调用者**——项槽的价值由 ①/② 的**内容断言**兑现（不假装有外部消费者）。**【追加登记：`.cir` 快照不载项槽（Step 4 修正）；P5 单槽化时项引用须换稳定形态（内容寻址/落盘项 ID）或同款「装载侧重派生」，**不得**直接落进程内索引（本任务实测的 bad_term=329 即该形态的红证）】**。
- [x] **Step 7: 提交**：`feat: R2 P4 Task 4——DFNode.TK 迁移期双槽（tk 零变化 + 项引用槽，允许清单制）+ .cir 快照 CIR_CACHE_VER 17→18；发射面零泄漏`**【实施：`.cir` 快照保持 17（不落槽、装载侧重派生——实测反证见 Step 4）；提交信息按实际改写】**

## Task 5：可选运行期表示——解包侧判表示（裁决 5，与 TYPE 段同批）

**Files（实施记录，2026-09-13）:** `src/compiler/ir_gen.cr`（表示位侧表 + 写点置位 + 两解包点分派 + 信道）· `src/compiler/globals.cr`（侧表/信道全局 + `oe_bare/oe_boxed`）· `src/arch/x86_64/instr.cr`（**IR_CONST 全局行直写分支**——第三根因，见下）· `src/compiler/main.cr`（文件路径 `optrep_begin` + 可选面关 .cir 缓存）· `tests/selfhost/test_optional.py`（12 → **32 例**）· `src/ci/run.sh`（计数/说明同步）

**实施记录（Step 1–5 全部落地；三根因逐条实测见报告）**

- **载体裁定 = (a) 表示位侧表**（推荐项）。实现形态 = **IR 层**（非后端帧旁路字节）：每个可选槽（局部/形参/调用结果）配一个**普通 int IR 变量**当表示位（0 = 裸值 / 1 = 装箱），写点置位（静态已知 → 常量；源已配位 → 拷贝），两解包点（`match` 的 Some/None 臂、`?`）按位分派。**纯 IR ⇒ 无新 opcode、无 ABI/帧布局改动 ⇒ ELF 后端与解释器天然同源**（对照 `g_x86_mw_tag_off` 先例：后端旁路 tag 区需两后端各写一份，本任务不需要）。
- **跨函数面 = 隐藏全局信道**（本计划允许的 (a) 扩展面，纯 IR）：返回点写 / 调用点读（返回信道单元）+ 调用点写 / 被调序言读（每形参位一个单元，被调序言读点先于任何可改写信道的调用）。**读点必须紧跟真实调用 ⇒ 返回可选者的惰性 thunk 关闭**（thunk 把调用推迟到 force 点，读点悬空——实测踩过）。
- **门（零足迹）**：AST 无 `EXPR_OPTIONAL` 且无 `Some`/`None` ⇒ 整面不启用（不注册隐藏全局、不发任何表示指令）⇒ **非可选程序发射面逐字节不变**（`ptr_arith`/`generics_test` 八段 + ELF canary IDENTICAL 实测）。
- **未覆盖面（硬性登记，实测三态）**：结构体字段/数组元素/全局槽/match 结果/可选的惰性 thunk ⇒ 不配表示位 ⇒ 解包回落**既有**装箱假定：裸值形态**响亮失败**（rc=139 双路径**同**——非静默分歧）、装箱形态照常正确（`test_optional.py` 两例分别钉）。
- **第三根因（实测挖出，非计划面）**：ELF 后端 `IR_CONST` 无**全局行**分支（`g2_slot` 对全局返回帧外伪偏移 ⇒ 写栈垃圾 = 静默丢写；解释器同 IR 直写 `g_ir_vals` ⇒ **两路径分歧**）——本任务首次出现「IR_CONST 落全局行」的发射点（信道单元），逐分支修之（与 IR_LOAD/IR_STORE 的全局分派同款）。**第四根因**：Python bootstrap 的 `gen_let_decl` 只对**裸字面量**记 `constant_value`（`-1` = UnaryOp ⇒ 丢初值 ⇒ `.quad 0`）⇒ **自举链二进制的负值文件级常量静默读 0**（同类既存面 = `cir_cache.cr` 的 `CIR_CACHE_MAGIC`）——本任务改走 `oe_bare()/oe_boxed()` 函数。**第五面**：表示位侧表是编译期进程内状态、`.cir` 快照不载 ⇒ 命中恢复路径与冷路径**产物分歧** ⇒ 可选面启用即**关 .cir 缓存**（正确性优先；非可选程序照常缓存）。

**缺口事实（附录 B.7，已实测，复述以钉住红态）**：`x : int? = 5; match x { Some(v) => { return v; } None => { return 0; } }` → `check` rc=0 零诊断 / `build` rc=0 / **产物运行 rc=139** / `corec run` rc=139；纯通配臂不崩（rc=7）。根因 = `Some(1)`/`None` 走 `IR_MAKE_ENUM` 对象 + tag，而 `T?` 槽里的裸值仍是裸值 ⇒ **同一 `T?` 变量可持两种表示**；`Some` 臂的载荷解包（`IR_LOAD_FIELD`，`ir_gen.cr:1885`，偏移 `fi+1` = tag 之后）把裸值当枚举对象解引用。

- [x] **Step 1: 判别载体裁定（实施前须定，写入报告）**【实施：取推荐 (a)；承载形态改为 IR 层表示位（理由见上「实施记录」）】：候选 = **(a) 表示位侧表**（照 int 多字 tag 区先例 `g_x86_mw_tag_off`（`globals.cr:186`）+ `tag2l.cr`：`T?` 槽配 1 字节表示位，赋值/装箱侧置位、解包侧读位分派）｜(b) 值自描述（= 一律装箱，与裁决 5 相悖，否）｜(c) 静态面收窄（仅可静态证表示处直取，其余**保守拒绝**）。**推荐 (a)**：与既有 tagged-slot 机制同源、解包侧即可闭合、不改变已装箱值的表示。**未覆盖面（硬性登记）**：跨函数面（形参/返回/字段/聚合元素）的表示位传递若不可行 ⇒ 该面必须**保持响亮失败或保守拒绝**，**不得**退化为静默错值（本任务的红态对比必须逐形态实测：正确值 / 明确拒绝 / rc=139 三态，禁「静默 0」）。
- [x] **Step 2: 用例（红）**【实施：复跑确认四形态红态（L1/L5/L6/L8 双路径 139）后扩至 32 例】：`test_optional.py` 扩 ≥6 例覆盖 B.7 的四形态（裸值 + `Some` 臂 / `Some` + 通配混合 / match 作表达式 / 纯通配）× 双路径（ELF 产物 + `corec run`）；**当前 4 例红**（rc=139）为本步的失败证据（复跑确认，勿以 B.7 记录代跑）。
- [x] **Step 3: 实现**（按 Step 1 裁定）【实施：两解包点（`match` 六处臂条件/载荷绑定 + `?`）+ 写点（LET/赋值）+ 两信道（返回/形参）；构造侧 `IR_MAKE_ENUM` 逐字未动】：`Some` 臂解包路径改为「读表示位 → 裸值分支直取 / 对象分支按 `fi+1` 取载荷」；`None` 臂同源；`Some(x)`/`None` 构造侧保持 `IR_MAKE_ENUM`（不动）；如涉 `?` 解包（`EXPR_TRY`）则同规则。
- [x] **Step 4: 判据**【实测：`test_optional.py` 32/32 · ELF canary `95084e7b…d475` **IDENTICAL**（非可选程序零足迹 ⇒ 未触发允许面变化）· `.ccr` 冷/热同 + 可选程序关缓存 · 全枚举 54/54 · 自举链不变量 · `test_interp_parity` 绿；未覆盖面清单见下】；**ELF canary**：若实现触及 x86 发射（tag 区/新指令）⇒ canary **预期变**，属**允许面**但**必须报告**（与裁决 8 不相干——裁决 8 只约束 TK 升格）；`.ccr` 变化实测报告；全量回归 + 自举链 + `test_interp_parity.py` 绿；report-only 面（未覆盖形态清单）先行。
- [ ] **Step 5: 提交**：`fix: R2 P4 Task 5——运行期可选表示统一（解包侧判表示）：裸值 + Some 臂双路径 SIGSEGV（rc=139）闭合 + 覆盖集扩例`

## Task 6：`MAX_*` 护栏与解除评估（裁决 6）

**Files:** Modify `src/compiler/parser.cr`（枚举变体/载荷写入侧护栏）· `src/compiler/dyn_arr.cr`（访问器护栏，照 `fi_param_type`/`fi_set_param_type` 先例 = TODO #8 收口形态）· New `tests/selfhost/test_enum_limit.py`（或并入既有套件）· `src/ci/run.sh`

- [ ] **Step 1: 红态复现（TODO #35 记录复核）**：`enum E { V0..V16 }`（17 变体）→ 现状 `check` rc=0 零诊断（前端静默越界写：槽起点 `OFF_EI_VARIANT_COUNT` = 4360，越 `ESZ_ENUMINFO` 224B）/ `build` rc=1（读回闸 `ccr_io.cr:1121`）。≥17 载荷类型同族（`:1148`）。**逐值实测并记录**。
- [ ] **Step 2: 护栏**：写入侧硬错（照 P020 形态：`error[P0xx]` + rc=1，**非静默截断**）；访问器加范围闸（唯一写点受护）。`report-only` 先行跑全语料，清单空后开门。
- [ ] **Step 3: 解除评估（写在报告，不实施）**：`MAX_GENERICS=4`（`ast.cr:115`）· `MAX_IFACE_METHODS=16`/`MAX_IFACE_METHOD_PARAMS=8`（`dyn_arr.cr:162/167`，已有硬错护栏）· `MAX_ENUM_VARIANTS`/`MAX_VARIANT_TYPES=16`（`ast.cr:117-118`）——逐项给出「解除面 = 表迁侧表（= 段布局去定长槽）的成本 / 收益 / 风险」，结论入 TODO 与报告。
- [ ] **Step 4: 判据**：新用例（17 变体拒绝 + 16 变体通过 + 17 载荷拒绝）+ 全量回归 + `test_backend_bootstrap.py` + ELF canary IDENTICAL + `.ccr` 零变化（拒绝路径不产生产物）。
- [ ] **Step 5: 提交**：`fix: R2 P4 Task 6——枚举写入侧 MAX_ENUM_VARIANTS/MAX_VARIANT_TYPES 护栏（TODO #35，#8 同族形态）+ MAX_* 解除评估登记`

## Task 7：收官（回归 + 判据 + 统一台账 + 文档）

- [ ] **Step 1: 全量回归**：五 CI job（selfhost-tests / bootstrap-tests / suite / check / full-bootstrap）rc + 逐档计数；全枚举 selfhost + bootstrap 全部档（**含 #31 未挂 CI 档**；`test_backend_bootstrap.py` 必含）；`check` job rc=1 = 既有 red 划界（2 条 TF01 误报 `lits_copy`/`ty_memo_slot_no_grow`）——**同源对拍两侧一致**才算划界成立。【**2026-09-13 更新**：两条已修（TODO #40 TF01 收口）⇒ `check src/compiler` rc=0，本步的划界口径**作废**（改按「rc=0」判）；Task 2 §9-② 的收口路径 (a) 已兑现，`type_engine.cr` 入 corearch 清单两面、project-mode `error[`=0 门通过。】
- [ ] **Step 2: 判据复验**：`clean-cache` → ELF canary `95084e7b…d475` IDENTICAL（或按 Task 5 的允许面**带证据报告**）；`corec2==corec3` cmp IDENTICAL + N06=0 + 冒烟 42 + `corec3 --help` rc=1；`.ccr` 面（`ptr_arith`/`generics_test`/自源）新记录值 + **冷/热两态逐字节同** + TYPE/IFACE 段结构性断言；影子终态（differences/站点覆盖/回落计数**同报**，且声明其**对载体面零覆盖**）。
- [ ] **Step 3: 统一台账**：本批「旧接受 → 新拒绝」（预期：装填不可译行拒绝 / 签名不可建拒绝 / MAX_* 护栏 = 3 类，全语料 report-only 清单逐条）+ 「放宽」（预期 0 + Task 5 的 139→正确值修复单列）+ 「命名化/扩列的判定面零变化」证据（全类型行枚举）。
- [ ] **Step 4: 文档**：spec §9 P4 行标 ✅（含未覆盖面：NOD 盘面项槽归 P5 / `.csr` 消费面零调用者 / 命名消费语法未开工 / 形状可写视图不可表达等继承项）；format spec（T1 已改，此处复核一致性）；TODO：新增 P4 落地条目 + #35 划销 + #26 判据继承注更新 + P5 继承项（单槽化 + 盘面项槽 + 签名编码面收口）；findings 文档追加「P4 后复跑」节；CLAUDE.md 的 `.ccr` 段描述（`v7` 字样：§Key Conventions 的 `.ccr` 行）同步为 v8/8 段。
- [ ] **Step 5: 提交**（路径限定）。

---

## 翻转钉清单（P3 附录 C.4 逐条 → 本批处置）

| 钉 | 位置 | 触发任务 | 处置 |
|---|---|---|---|
| `.ccr` 整体段数/表长/版本断言 | `tests/selfhost/test_ccr_v7.py:83/:88/:129` | T1 | 8 段 / `HEADER_TABLE=112` / `seg_count==8` |
| 版本拒收三元组 | `test_ccr_v7.py:1088-1118` | T1 | `(6,5,8)` → `(6,5,7)` |
| `.ccr` 逐字节同记录值 | P3b 报告 §4b（`ptr_arith` 88943B `54e3856b…`；`generics_test` 130974B `97be2f9e…`） | T1 起 | **作废** → 新冷缓存记录值 + 结构性断言（TODO #26） |
| 零 `str_intern` 三守门 | `type_selftest.cr:420/:619/:866` | T3 | 重定：判定/查询路径零新增 + 初始化/注册面**固定增量逐名登记** |
| 形状注册面两例 | `type_selftest.cr:599/:609` | T3 | 生产名注册后重钉（泄漏守门口径不变） |
| 签名编码面 | `type_selftest.cr:368` + `test_iface_satisfies.py:318` | T3（**仅落证据**） | 段内项与进程内项同构证据入报告；用例改动归 P5 |
| 本质条目数硬值 | `type_selftest.cr:1362/:2362` | T3 | 13 → 16（三新行逐条理由） |
| 影子站点面 | `--type-shadow` 站点集/计数 | 本批不动 | P5 清理（B.5）；每任务复跑同报 |
| 清单双注册面 | `build_selfhost_native.py` ↔ `_import.cr`×2 | T0/T2/T3 | 三面同核 + `test_backend_bootstrap.py` 必跑（`error[`=0） |
| `.cir` 快照布局 | `cir_cache.cr:31` | T4 | `CIR_CACHE_VER` 17→18 + 版本注记 + 冷/暖同值 |

## 自检记录

- **根因 vs 止血**：① **段内容随判定历史漂移**是载体落地的真根因（惰性项 DAG 直接落盘 ⇒ 冷/热门槛差异 = 已登记的静默类）——D13 用「确定性装填 / 段内容 = 表纯函数」根治，非采样规避；② **corearch 链接面**（D16/D18）是按 B.6 的实际事故形态（清单泄漏 ⇒ N06 静默未定义）**前置**处理，非事后补门；③ Task 5 按裁决 5 走解包侧，**未**采用「先补通配臂特例」式止血（那会保留「同一变量两种表示」的根因）；④ Task 6 是 TODO #35 的写入侧护栏（读回闸前置 ≠ 写入侧护栏），非「把闸往前挪一行」。
- **占位符扫描**：无 TBD/TODO-to-fill；三处**实施前须定的登记项**明写并给了推荐与边界——Task 5 的判别载体（推荐 (a) + 未覆盖面硬约束）、Task 3 的 `ESZ_IFMETHOD` 扩槽（新增项槽，理由 = corearch 无 AST）、Task 2 的尺寸实测（**数字留待实测，不预填**）。**未知数不猜**：TYPE/IFACE 段体的实际尺寸、STR 增长字节数、`g_type_count`/`tt_count` 量级、Task 5 的 canary 是否变——全部列为实测报告项。
- **数字口径**：333（`selftest-types`，`src/ci/run.sh:71` 注释与 P3b 收官实测）· 13/`ESZ_IFACEINFO=2712`/`ESZ_IFMETHOD=168`/`ESZ_DFNODE=64`/`OFF_DF_TK=40`/`CIR_CACHE_VER=17`/`CCR_SEG_COUNT=6`/`CCR_VERSION=7`/88943B/130974B/`95084e7b…d475` = 实读或既有记录值；「≥N 例」= 计划下限（可增不可减）。16（扩列后条目数）与 240/3864（内存槽扩）为**本计划决策值**，已注明。
- **风险面**：① **`.ccr` 尺寸增长**（行表 24B + 项 DAG 40B，量级未实测）——缓解：Task 2 强制尺寸实测报告；若超预算（如自源 `.ccr` 翻倍）⇒ 停下上报（可退：项 DAG 只落「被引用闭包」= 需先裁剪再落盘，属本计划的 D12 替代面）；② **D13 的复位副作用**（save 前清项层）——前置核查 `save_ccr` 后无消费者已列步骤，若核查发现新消费者 ⇒ 停下重定（改为「序列化后重放」）；③ **corearch 二进制变大**（+type_terms/type_engine/iface_registry 纯段）——非判据面（B.4-18 先例：corearch 二进制 sha 不是计划判据），但需在报告登记；④ **STR 段增长**按裁决 1 接受后，凡「`.ccr` 逐字节同」类型的既有判据全部失效——本计划以结构性断言全覆盖，**遗留：其他批次的旧记录值需一处汇总划销**（Task 7 Step 4 落 TODO）；⑤ **Task 5 未覆盖形态**（跨函数表示位）若只能「保守拒绝」⇒ 收紧清单 + 报告；**绝不允许**以静默错值收口；⑥ **影子通道零覆盖载体面**（本计划 Global Constraints 明写）——防止后续批次把「影子归零」当载体证据复用。
- **与既有裁决的一致性复核**：裁决 2（结构口径）⇒ IFACE 段 ④ 入段但不进判定（与 `type_engine.cr:641-651` 现状一致）；裁决 4 ⇒ Task 3 的形状项仅作签名载体，**不**接 `iface_satisfies_term` 整形状路由（防线保留在 `type_engine.cr:645-650` 的注释与用例）；裁决 7 ⇒ `AK_NULL` 独立槽（`type_engine.cr:34-35` 语义零改动）；裁决 8 ⇒ Task 4 的硬判据与「停下上报」条件逐字落纸。
