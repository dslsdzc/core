# 批 8 · (甲)「调用位点实参面不校验」实施链 —— 纸面细化（2026-09-18；**未推**）

> **状态**：纸面轮（**零源码改动**；唯一写盘 = 本文件）。等 lead「开工」指令 + 2(a) 合并后进入实施。
> **2(a) 是双前置**：① 同占 `checker.cr`（文件不撞）；② **刀 4 的「自源修复」手段依赖 2(a) 的语法**
> （`@ptr_of` / `@str_of` 显式视图转换 + bootstrap 面产生式，今天不存在）——**后者更强**，见 §3 刀 4 首条。
> **lead 五项裁定已落（2026-09-18）**：见 §6；分批 = **刀 1 独立小批先落 / 刀 2–4 一个批全线一刀**（§3 抬头）。
> **取数纪律（lead 硬要求，先读）**：**oracle = 两源**——**源 A = self-hosted 全线 report-only 通道（刀 2，扩面后）的输出** ·
> **源 B = bootstrap 的 `Checker warnings` 行**（自源面有效，唯一同看实参位+返回位；配方见 §3 刀 2-bis）；
> §1.3 的 bootstrap 数字（273 等）是**代理读数**，**不得据它下结论、不得据它触发停条件**。
> 本仓先例：静态扫描报「仅 2 处」，被**被修对象自己的输出**逼出第 3 处（调用派生的传递闭包，文本扫描看不见）。
> **起点**：`develop@origin` = `41a51d8d`（本文件读数都在此点实测）；**开工前必重读**——2(a) 落 `checker.cr`
> ⇒ 行锚必漂移，**本文件全篇用符号锚**（函数名 + 唯一注释短语），行号只作参考。
> **上游口径**：`docs/superpowers/plans/2026-09-17-silence-closure.md` §1-1 的「✅ T0 回填」块（判据形态 / 「期望 7」作废 / S3 前置排期）。
> **T0 报告**：`docs/superpowers/plans/2026-09-18-b8-e1-t0.md`（PR #129）。

---

## §1 T0 侦查（本轮实测；含一条**改变工作量级**的新发现）

### 1.1 新发现：(甲) 的范围**不止泛型**——非泛型调用的实参面（个数 + 类型）**同样零校验**

| 探针 | 源码 | check | build | ELF | interp |
|---|---|---|---|---|---|
| ng1 | `fn f(a: int) -> int` ← `f("hi")` | **0（零诊断）** | 0 | **8**（静默错值） | — |
| ng2 | `fn f(a: int, b: int)` ← `f(1, "hi")` | **0** | — | — | — |
| ng3 | `fn f(a: dex)` ← `f(x)`（`x : int`） | **0** | — | — | — |
| ng4 | `fn f(a: dex)` ← `f(s)`（`s : string`） | **0** | — | — | — |
| ar1 | `fn f(a: int)` ← `f(1, 2)`（**多参**） | **0** | — | — | — |
| ar2 | `fn f(a: int, b: int)` ← `f(1)`（**少参**） | **0** | — | — | — |
| c5 | 泛型 `g[T](t: T, x: T?)` ← `g(1, "hi")` | 0 **+ 1 条 `[S1-ARG]`** | — | — | — |

**错误码现状（全仓 grep，实测）**：
- `EC_TF_ARG_COUNT`(5005) · `EC_TF_ARG_TOO_MANY`(5006)：**仅 `ast.cr` 有定义，零 raise 点**；
- `EC_TF_ARG_TYPE`(5007)：**唯一 raise 点 = `@raw_int` 内建位**（`checker.cr` 的 `@raw_int` 分支），**调用位点零 raise**；
- ⇒ **`diag.cr` 的 fail-closed 门对 `EC_TF_ARG_TYPE` 有 build 作用域豁免**（理由写的是「唯一 raise 点在 @raw_int 非调用位点」）
  ⇒ **刀 4 落地时该豁免必须同步重定**，否则调用位点的 TF07 会被门静默放行（**这是本批最容易漏的一处**）。

### 1.2 通道覆盖面（实测；**2026-09-18 复核轮更正——本节旧说法已作废**）

**两条 report-only 通道并存**（旧说法「非泛型面无任何打点」**错**，据此作的结论一并作废）：

| # | 通道 | 位置 | 覆盖面 | 门 | 形态 |
|---|---|---|---|---|---|
| 1 | 文本行 `[S1-ARG] line=… col=… pi=… p=… a=…` | `checker.cr` 的 `infer_gen_call` 实参环（S1 块末尾 `println`） | **泛型被调** | **无门**（恒打） | 文本（含 `type_display` 拼串） |
| 2 | 数字台账 `9917\|9918 <line> <col> <param_idx> <param_ti> <arg_ti> <call> @@ <源片段>` | `checker.cr` 的 `infer_expr` **`EXPR_CALL` 分支**（实参环，PR #127 落地 = 本计划起点） | **非泛型直调 + extern**（`is_ext` 分桶；`EXPR_EXTERN` 走 `9918`） | **`CORE_S1P=1`**（默认位**整块**不执行，含类型引擎调用——为保默认位零足迹，`checker.cr` 有专段注释） | **只打数值**（避免新串进 `.ccr` STR 段） |

- ⇒ 「把实参校验升硬错」在非泛型面上**有前置扫面数据**（通道 2），**但只在 `CORE_S1P=1` 下产生**；刀 2 的活因此从
  「从零建通道」改为**「扩面 + 接线 + 扫面」**（见 §3 刀 2）。
- **通道 2 的既有纪律（须继承）**：扫面锚定**只认行首**（不得 grep 裸串 `9917`——会命中源码回显，已有假阳教训）；
  输出**只打数值**（`checker.cr` 同段注释：字符串字面量与 `type_display` 的新串会写进输出 `.ccr` 的 STR 段 ⇒ 破「产物逐字节不变」）。

### 1.3 爆炸半径（**代理读数 = 273**；⚠ **不是 oracle**，只作量级直觉）
- 自源：`corec_files`（45 档）实参面告警 **273** 条 / 全部告警 387 条；`corelsp_files`（31 档）实参面 **51** 条 / 73 条。
- 语料：bootstrap **不可作 oracle**——`tests/suite/*.cr` 35 档里 24 档 bootstrap 解析器直接 `SyntaxError`（`import` 位置 / `?` 可选 / `new` 形等现代面）。
  > **⚠ 加注收窄（2026-09-18 复核轮；原文逐字保留）**：上面这句「bootstrap 不可作 oracle」**只对语料面成立**。
  > **对自源面（本批 45 + 31 档）它恰是有效域**，且是**唯一同时覆盖实参位与返回位**的现成读数 ⇒ **本批的第二 oracle**
  > （配方见 §3 刀 2-bis）。⇒ 凡把该句推广成「bootstrap 一律不可用」的引用，**作废**。
- **两个数字别混**（lead 要求分清）：**① `273`** = 我用 bootstrap 跑**另一套 checker**（Python 侧）在**语料/自源 concat** 上的**代理读数**（量级直觉用，不得进结论、不得当阈值）；
  **② 「S1P 通道已存在但尚未跑」** = self-hosted 侧的**自有**通道（§1.2 通道 2），**它的读数才是 oracle**，本批尚未产出。
  ⇒ 两者**不是一个数、也不是同一侧的读数**，不得互相替代或相加。
- ⇒ 结论（**lead 升级为硬要求；本代理 2026-09-18 复核轮二次更正**）：**以 self-hosted 侧通道（刀 2 扩面后）的输出为 oracle 的主源**；
  **它不是「从零建」**（通道 2 已存在，见 §1.2）⇒ 本刀的活 = 扩面 + 接线 + 扫面，不是建通道。
  **273 这个数不得写进结论、不得当停条件阈值**；理由 = 文本扫描看不见**调用派生的传递闭包**（本仓已有同族教训：静态扫描报「仅 2 处」而被修对象输出逼出第 3 处）。

### 1.4 判定
**(甲) 的真实工作量 = 「建通道 → 分类 → 豁免表 → 硬错 → 自源修复」五步，不是"加一行报错"。**
本循环的 `unify_types` 的 `T?` 规则（刀 1）是**其中可独立验证的一小刀**，且是 S3 的硬前置（§1-1 回填块 ④）。
⇒ **分批落（lead 裁 ③）**：**批 1 = 刀 1**（独立小批）；**批 2 = 刀 2–4**（全线一刀，types + count 同时开）。
⇒ **oracle 纪律**：批 2 的读数以**两源**为准——**源 A = 刀 2 通道输出**（§1.2 通道 2 扩面后）· **源 B = bootstrap 的 `Checker warnings` 行**（配方见 §3 刀 2-bis）；**§1.3 的 273 是代理数，不得当结论/阈值**。

---

## §2 现状读数基线（判据的对照面；实测 = `41a51d8d`）

**正例（改后必须仍绿）**

| # | 形态 | 当前读数 |
|---|---|---|
| C1 | 非泛型 `fn g(t: int, x: dex?)` ← `g(1, d)`，`d : dex, apx` | check=0 · ELF=7（3/3 稳）· interp=255（能力边界） |
| C1b | 同上但 `d : dex`（scaled） | check=0 · ELF=7 · interp=7 |
| C2 | 泛型 `g[T](t: T, x: T?)` ← `g(1, d)`，scaled | check=0 · ELF=7 · interp=7 |
| C6 | 泛型 `g[T](t: T, x: T?)` ← `g(d, d)`，apx | check=0（**1 条 S1-ARG 误报**）· ELF=7 · interp=255 |
| C7 | `fn g2[T](x: T?)` ← `g2(d)` | check=0（**1 条误报**）· `.cir` = `call g2[dex](_dxsc)` |
| J/K | `fn g[T](x: T?, t: T)` ← `g(d, d)` / `g(Some(d), d)` | check=0（各 1 条误报）· 实例 `g[dex]` |
| M | `fn g2[T](x: T?)` ← `g2(x)`（`x : int`） | check=0（1 条误报）· 实例 `g2[int]` |
| g12 | `fn g12[T](t: T, x: dex)` ← `g12(1, d)`，apx | check=0（**零** S1-ARG）· ELF=7 |

**负例（改后应 rc=1）**

| # | 形态 | 当前读数 |
|---|---|---|
| P0 | 泛型 `g[T](t: T, x: T?)` ← `g(1, d)`，apx | check=0 · interp=15 · **ELF 非确定**（97 次命中 142 一次） |
| c5 | 泛型 ← `g(1, "hi")` | check=0（仅打点） |
| ng1–ng4 | 非泛型类型不符 | check=0 |
| ar1/ar2 | 非泛型个数不符 | check=0 |
| L | `fn g2[T](x: T?)` ← `g2(Some(3))` | check=**1 · TF01**（现状已硬错，但**码不对**：返回型不符而非实参不符） |
| B/E | `f := g2(x); return f` / `return g2(x)` | check=**1 · TF01**（同族相邻缺陷，**未定位根因**） |

> **读数的用途分档（lead 更正版裁 ①）**：上表全部是 **pre-fix 读数**。其中 **P0 的 ELF 腿非确定** ⇒
> **只作 pre-fix RED 证据（弱式，N=20，出现一次 `7` 即「未复现缺陷」）**；post-fix **一律走 §4.2 强式**，
> 不得拿「没观察到旧坏值」当修复判据。

---

## §3 实施刀（**分批已裁：批 1 = 刀 1 独立小批；批 2 = 刀 2–4 全线一刀**）

> **批 1（刀 1）**：`T?` 规则·S3 硬前置·自有判据，**与 2(a) 无耦合**（唯共享 `checker.cr` 文件）⇒ 先落。
> **批 2（刀 2–4）为一个批、全线一刀**：**类型 + 个数同时开**（lead 裁 ③）。理由：分两次开会让同一形态
> 一半严一半松，第二次开还得把全部读数重跑一遍。
> **批 2 的 oracle 纪律**：扫面读数认**两源**（源 A = 刀 2 通道输出 · 源 B = bootstrap `Checker warnings`，§3 刀 2-bis），**差异逐条归因**；§1.3 的 273 不得进结论、不得当阈值。

### 刀 1 · `unify_types` 的 `T?` 规则（**独立小刀**，S3 的硬前置）

- **实施点（符号锚）**：`checker.cr` 的 `fn unify_types(pattern, concrete)`：
  现行只有三条规则（`pattern == concrete` / `TYP_GENERIC_PARAM` / `TYP_GENERIC_APPLY`）；其余落
  `type_compat_strict(concrete, pattern)` 兜底。**在兜底之前**插入 `pk == TYP_OPTIONAL` 分支：
  - ① `ck == TYP_OPTIONAL` ⇒ 递归 `unify_types(内层 pattern, 内层 concrete)`；
  - ② 否则（**裸值入可选** `T ⊆ T?`——与 `tests/selfhost/test_optional.py` 的既有语义一致）⇒ 递归 `unify_types(内层 pattern, concrete)`。
- **依据（实读）**：`test_optional.py` 记载判定点「裸值入 `T?`：`type_compat_strict` 的可选目标分支」⇒
  语义**已有**，缺的只是 `unify_types` 这一层的**内层泛型代换 + 递归**。
- **影响面（必须逐处核）**：`unify_types` 全仓 **3 个调用点**——`infer_gen_call` 的实参环（S1 块）、
  `infer_expr` 内两处（S1P 实参台账所在环 + 结构体泛型字段环；行号会漂，按符号锚）。
  **⚠ 但第三点（结构体泛型字段）机制上不受本刀影响**（复核者实读，本代理已核）：该点的守卫要求字段类型节点
  是 **`EXPR_IDENT` 泛型参数**（`is_struct_generic(si, ast_int_val(dtn))` ⇒ `alloc_type(TYP_GENERIC_PARAM,…)`），
  **`T?` 字段根本到不了该分支** ⇒ **刀 1 的实际爆炸半径 = 2 个点，不是 3 个**（另 2 点：泛型实参环 + S1P 实参环）。
- **批次可切分性（复核者两向清单，本代理已核）**：**翻红面 = 0**——全仓 `[S1-ARG]` 引用**只有 `checker.cr` 自己的 `println`**（`tests/` 零引用）⇒
  **没有任何既有用例把「`T?` 恒假/误报」钉成期望值**（与 `test_diag_gate` 的 TF07 恰成对照）；翻绿面 = 本刀预期效果。
  ⇒ **本刀属行为变更，须在批报告显式登记**（不是「无行为变化」）。
- **预期效果（判据）**：
  - 正例 C6 / C7 / J / K / M 的 **S1-ARG 误报消失**（`unify_types` 回真 ⇒ 不再打点）；
  - P0 的 arg1 走 ②：内层 `T` **已绑定** `int` ⇒ 比 `type_compat_strict(dex, int)` ⇒ **假** ⇒ 打点/（刀 4 后）硬错；
  - 实例键**不得变**（C6/J 仍 `g[dex]`、M 仍 `g2[int]`、c8 的 `g2[dex]` 仍成立、`.cir` 的 `_dxsc` 仍在场）。
- **突变自证**：撤销刀 1 ⇒ 正例五例的 S1-ARG 误报复现（可判据化：断言 `[S1-ARG]` **零命中**）。
- **风险**：三个调用点语义同改；若某处依赖"`T?` 恒假"的既有行为 ⇒ 该处须单独取裁（**停条件**见 §5）。

### 刀 2 · 全线 report-only 实参通道（**零行为**；**2026-09-18 复核轮重写：不是「从零建」，是「扩面 + 接线 + 扫面」**）

- **现状（§1.2）**：通道**已存在两条**——① 文本 `[S1-ARG]`（泛型面，无门）；② **数字台账 `9917|9918`**（`infer_expr` 的 `EXPR_CALL` 分支，非泛型 + extern，`CORE_S1P=1` 门，默认位整块不执行以保零足迹）。
- **本刀的活（三件）**：
  1. **补覆盖面（M2）**：把实参台账扩到 checker 的**四个实参承载入口**，逐形态给出「打点 / 不打点 + 理由」——
     | 入口（符号锚） | 现状 | 本刀 |
     |---|---|---|
     | `EXPR_CALL`（`infer_expr` 直调分支） | **已打点**（9917/9918） | 沿用 |
     | `EXPR_FIELD`（方法 / 模块限定调用） | **未打点** | **补**（注意接收者偏移：`arg_vars[0]`/形参链第 0 位 `self`） |
     | `EXPR_ENUM_CONSTRUCTOR` | **未打点** | **补**（构造器实参位） |
     | `EXPR_AT`（内建，含 `@raw_int` 等） | **未打点** | **补**（内建面正是 TF07 的来源位） |
     ——**打点不覆盖 = oracle 盲区 = 缺陷盲区**（M2 的核心论断）。
  2. **接线**：沿用通道 2 的既有形态（数值台账、行首可锚、默认位整块不执行），**不新增 intern 串**。
     **⚠ 硬约束（格式继承，不得改；lead 2026-09-18 补，N2）**：
     **格式是继承的锚，不得改**——**字段序 / 字段数量 / 前缀 `9917|9918` 一律照旧**。
     **既有锚定套件点名** = `tests/selfhost/test_s1p_liveness.py`（挂 `src/ci/run.sh:311` 的 `selfhost-tests`；
     该档注释载明 **20 断言**，其中 ① = **必命中探针逐字段**（`9917 4 14 0 0 2 f`）· ② = 默认位 / `CORE_S1P=0` **零行**）。
     ⇒ **增删字段、改前缀序、改列数都会直接打红它**（本代理实读：其 `HIT` 正则以
     `^(9917|9918) (\d+) (\d+) (\d+) (\d+) (\d+) (\S+)(?: @@ .*)?$` **锚行首与字段形状**，并含负例：前导空格不匹配 / 缺字段不匹配）。
     **若确需扩字段 ⇒ 同批重锁该套件并给旧值留痕**（照本仓载体换代纪律）——否则「扩面」会**静默废掉那条 CI 判据的锚**。
  3. **扫面**：`CORE_S1P=1` 跑自源（`corec` 45 + `corearch` + `corelsp` 31）+ 语料（`tests/suite/*.cr`、`tests/probes/*.cr`）+ 探针集（§2），
     逐档计数 + 逐条清单（callee / argN / param_ti / arg_ti / 行:列）。
- **判据一律经类型引擎**（`unify_types` / `type_compat_strict`），**不自写等值判定**（沿用 S1 的纪律）。
- **oracle 是两源（M1；口径已收窄）**：
  - **源 A** = 本通道输出（覆盖面见上表）；
  - **源 B** = **bootstrap 的 `Checker warnings (non-fatal)`**（`build_selfhost_native.py` 的打印行；条目形如
    `Function f argN: expected X, got Y (in caller)` / `Function f: expected return type X, got Y`）——**§1.3 旧版把 bootstrap 判为「不可作 oracle」的论断过宽**：
    那只是**语料面**（24/35 档 SyntaxError）的结论；**对自源面它恰是天然的第二 oracle**，且是**唯一同时看到实参位 + 返回位**的读数
    （补 S1P 的返回位盲区；2(a) 批实测 330→165，残留项曾点出过 self-hosted 面静默过的 bug）。
  - **最强用法 = 两源对拍**：按 **(callee, argN)** 对齐两份清单，**差异逐条解释**（一方有另一方无 ⇒ 要么是覆盖面缺口，要么是判据差异）⇒ 把「oracle 自证」升级为**两个独立前端互证**。
  - **规模校准**：用源 B 的条数替代/校准 §1.3 的 273 代理数（它比静态扫描更贴近自源实参面真值）。
- **自证**：带/不带该通道 ⇒ ELF 与 `.ccr` 逐字节相同（抄 #121 套件 C6 的形态）。**两条前置**：
  - **① 先 `clean-cache`**（或用独立缓存目录）——暖态 `.cir` 重放会**假红/假绿**，「逐字节同」在暖态下**不成立也不证伪**；
  - **② `.ccr` 的 STR 段 = 编译期全量 interned 串**：通道若在**必经路径**新增 `str_intern`，`.ccr` 会变 ⇒ **红的是判据不是缺陷**。
    **先例就在本函数内（S2）**：`checker.cr` 通道 2 的注释逐字写着「台账**只打数值**：字符串字面量与 `type_display`
    （内部拼串）会把新串写进输出 `.ccr` 的 STR 段 ⇒ 破『产物逐字节不变』」⇒ **照抄该纪律**（同函数内既有先例，强于外部举例）。

### 刀 2-bis · oracle 的第二源（配方；lead 裁：先落配方，不造工具）

- 第二源 = bootstrap 的跨面警告行：`build_selfhost_native.py` 的 `print("  Checker warnings (non-fatal):", checker.errors)`
  ⇒ 条目形如 `Function f argN: expected X, got Y (in caller)`（实参位）/ `Function f: expected return type X, got Y`（返回位）。
  ⚠ §1.3「bootstrap 不可作 oracle」**只对语料面成立**（24/35 档 SyntaxError）；**自源面（本批 45+31 档）恰是它的有效域**，
  且它是**唯一同时覆盖实参与返回位**的现成读数。
- **取数**：`nice -n 19 python3 build_selfhost_native.py` 后抓该行，按 `argN: expected` / `expected return type` 两类分组。
- **对齐键 = (callee 规范名, argN)**；三个会咬人的点（**必须照做**）：
  1. 方法调用 `recv.m(...)`：被调名在两面的形态可能不同 ⇒ **抽样 3–5 条核对后再全量对齐**；
  2. 模块限定 `m.f(...)`：同办；
  3. 泛型实例：第二源**没有实例名概念** ⇒ 用「**声明名 + argN**」对齐，实例名只作自托管侧的分组维度。
- **判据（两向，防单向恒绿）**：① 自托管有 / 第二源无 ⇒ **逐条解释**（语法面被 bootstrap 跳过？类型面它看不见？）；
  ② 第二源有 / 自托管无 ⇒ **必解释**（= 通道盲区，**真发现**）。⇒「**差异清单 + 逐条归因**」才是判据；**只比数量不算**。
- **工具纪律**：本批**只落本配方**；等 `(甲)` 实批用它跑过一次且有效，**再议**是否做成 `tools/` 脚本
  （没跑过就造 = 造一个未验证的 oracle）。

### 刀 3 · 分类 + 豁免表（扫面产出 → **逐条报 lead 批**）

- **默认处置 = 修调用方**（lead 裁 ②）。**豁免是例外**，且必须：**① 有契约证据**（文档 / 既有测试 / 两前端对齐，
  实读给出处）· **② 逐条报 lead 批** · **③ 每条自带判据**。**禁止按名字批量豁免**（先例 = `diag.cr` 的 `diag_gate_exempt` 逐条形态）。
- 需分类的**合法形**（预判，**须由刀 2 的 self-hosted 输出证伪/证实**）：
  1. **字面量多态**：`int` 字面量 → `dex` / `dex?` / `int?` 形参；
  2. **`auto_str` 面**：**按裁 ② 拆两步判**——若它声明 `-> string` 而调用方实参确实不是 string ⇒ **真错，修调用方**；
     若它是重载/泛型助手（形参面本就多态）⇒ 另议（须给出**该面的契约证据**再谈豁免）；
  3. **内建/内建体**：`w64` / `r64` / `load8` / `store8` 等——**倾向豁免，但要先交契约证据**（文档/测试/两前端对齐），
     证据到位才进豁免表（**不允许只凭"它是内建"**）；
  4. **`never` 实参**（发散表达式：`return`/`loop`/`panic`）；
  5. **方法调用接收者偏移**（`arg_vars[0]` 是接收者，形参链含 `self`）；
  6. **extern 面**（C ABI，形参面可能无类型信息）；
  7. **变参**面。
- 产出：**真错清单**（逐条修源码）+ **豁免表**（逐条：契约证据 + 理由 + 引入批号 + 该条自己的判据）。
- **纪律**：豁免必须**逐条**，禁止"整类放行"（否则 P0 一类的真错会被整类放掉 ⇒ 假绿）。
- **豁免粒度写死（S4；这是「按名字批量」的具体形态，必须堵）**：
  - **粒度 = `(callee, argN)`**，**不是 callee 名**——预判第 3 条的内建族（`w64`/`r64`/`load8`/`store8`）**天然是按名字的族**：
    若豁免行按 callee 名写一行，`w64(a,b,c)` 三个实参位**任一真错**都会被同一行放掉 ⇒ **多参内建逐位举证**；
  - **字面量多态不能写成「字面量一律豁免」**（那会让 `f(1)` 喂 `string` 形参也静默放过）⇒ 写成
    **(字面量类型 × 形参类型) 显式矩阵**，矩阵外一律不豁免。

### 刀 4 · 硬错 + 自源修复

- **真前置（lead 2026-09-18 补充；这是排期理由，强于「同占 `checker.cr`」）**：**本刀依赖 2(a) 的语法**——
  自源那批实参不匹配点（§1.3 代理读数背后那批）修起来要写**显式视图转换**，而该语法**今天尚不存在**，
  由 2(a) 造出（符号名 **`@ptr_of` / `@str_of`** + bootstrap 面产生式）。⇒ **自源修复手段按 2(a) 落地后的语法写**；
  2(a) 实施中若改名，lead 通知。**刀 1/刀 2/刀 3 不受影响**（刀 2 零行为、不写转换）。
- **范围（lead 裁 ①）**：**类型 + 个数同时开**——个数不符 `EC_TF_ARG_COUNT`(5005) / 过多 `EC_TF_ARG_TOO_MANY`(5006) /
  类型不符 `EC_TF_ARG_TYPE`(5007)（**三码全仓零 raise 点**，正等这一批；只做类型会留下**同一位置的**静默洞）。
- **成对必改（lead 升级为验收线，不许留到下一批）——TF07 撤条包（`diag.cr` 实读；2026-09-18 T0 复核后定稿）**：
  - **① 撤条不是选择，是到期条件被本批触发**：`diag.cr` 的 TF07 豁免条目自带两字段——**理由**「EC_TF_ARG_TYPE
    全仓**唯一** raise 点在 `@raw_int` 内建位、**非调用位点**」· **退出**「`#2026-09-16-1/F3` 调用位点收口后撤」。
    本批一新增调用位点 raise，**「唯一」当场被证伪 + 退出条件当场到期** ⇒ **撤条 = 本批的完成条件之一**
    （不是「记得一起改」，写法必须是**到期触发**）。
  - **② 该条目在表里重复了两遍**（`diag.cr` 中该注释块 + 其 `if (…EC_TF_ARG_TYPE) return 1;` **逐字出现两次**，
    第二条不可达）⇒ **撤净两条**（**两行一起删**），并在**批报告**登记「豁免表曾出现重复项（第二条不可达）」这一事实。
    **推论（lead 认，本代理附议）**：**过去的表审计漏过该重复** ⇒ 说明「逐条审阅」这条判据**自身没有机械校验**；
    ⇒ 「`(code, scope)` 组合唯一」这条**机械判据登记为独立项，不在本批实施**。
  - **③ TB01 必须同批复核**：`diag.cr` 的 TB01 条目**退出条件逐字写着「随根因码（N 族划界 / **TF07**）消失**」
    ⇒ TF07 撤条而 TB01 不动 = **撤一条留一条**的风险是实的（该条同时被 `ptr_ref_first` 的 TF07 级联位点引用）。
  - **④ 判据网侧（逐字可执行）**：`tests/selfhost/test_diag_gate.py`（**已从 `tests/harness/` 迁到 `tests/selfhost/`**）
    第 87 行 `build_ok("tf07_tb01", "tests/suite/ptr_ref_first.cr", ("TF07","TB01"))` = **把「豁免放行」钉成契约**
    （`build_ok` 断言 build **rc=0 且产物存在**且输出含 `error[TF07]`/`error[TB01]`）⇒ **TF07 一撤条必红**。
    **改造条目（照同档 `tf01_build_clean` 模板）**：同批把 `tf07_tb01` 改造为**负控**——
    **真 TF07 ⇒ `build` rc=1 + 零产物（`-o` 与 `<out>.ccr` 均不存在）；合法调用 ⇒ `check`/`build` rc=0**；
    格式照 `build_ok("tf01_build_clean", "tests/suite/chan_test.cr")` 从「含码且 rc=0」重锁为「**rc=0 且无该码**」的先例。
  - **⑤ `ptr_ref_first.cr` 的 TF07 是「真错」不是误报**（本代理实读；lead 未提）——
    该档 `p := &x; q := p + 1; @raw_int(q)`（**指针**），而 `@raw_int` 的守卫（`checker.cr` 的 `str_eq(name,"raw_int")` 分支）
    **只接受 `TI_DEX`/`TI_INT`/`TI_NEVER`** ⇒ 单撤豁免该档必 build 失败（正是 ④ 断言将变红的原因）。
    - **可行性闸（本代理实测，探针已清）——结论 = 「字面观测字节差在 sanctioned 面无路」**：
      | 候选观测路径 | 实测读数 | 判定 |
      |---|---|---|
      | `@raw_int(指针)` | 守卫拒绝（TF07 之源） | 当时判「契约不许」（**现已改裁，见下**） |
      | `@addr(p)`（函数地址内建） | `@addr(q) - @addr(p)` = **0** | ✗ 语义不是「取变量地址」 |
      | `q - p`（指针相减） | = **1**（**元素**差，非字节差）；且用它测缩放**同源于被测量**（循环论证） | ✗ |
      | 行为式（写穿指针）：`arr : [int; 2] = [1,2]; p := &arr[0]; q := p + 1; *q = 99;` 断言 `arr[1] == 99` + `arr[0] == 1` | `check=0 · build=0 · ELF=0`（3/3） | ✓ 命题保住，但**观测的是行为不是字节差** |
      | 2(a) 的 `@ptr_of` / `@str_of` | **守卫收 `TI_STR`**（`&int` 不过），且**须在 `unsafe` 块内** | ✗（复核者实读；lead 据此证伪 (ii)） |
    - **终裁（lead 2026-09-18，改裁）：走 (i) —— 放宽 `@raw_int` 接受指针。**
      **理由**：① (ii) 的具体形态被证伪（上表末行）；② **(i) 可逐字节证伪**——**本代理实测**：今天 `@raw_int(指针)` 在
      **build 面本来就 rc=0 + 产物照出**（TF07 被 build 面豁免放行），`check` 面则是 `rc=1（2×TF07 + 1×TB01）`，
      ELF 跑 **rc=0（3/3）** ⇒ **ir_gen / 后端 / 运行期已完整支持该形态，放宽只动诊断面** ⇒ 判据可用 **pre/post 逐字节对拍**；
      ③ 铁律 #1/#4/#5：**直接解决**（守卫自己的注释写着「**int 原值即其缩放值**才可取其原值」——**指针的原值就是它的地址**，同一条理由成立）
      优于「换掉观测手段」的替代方案；④ 放宽后该处 raise 集合**收窄** ⇒ 豁免位点清单里 `ptr_ref_first` 那一处**自然消失** ⇒ **⑤ 的闸自动解除**；
      ⑤ 原断言的「**字节差**」比行为式**更强**（精确值 vs 行为落点）。
      **pre 基线（本代理实测；判据 ① 的对拍锚——采集口径与值一并落纸，见下方基线块）**：

      > **基线块 · `ptr_ref_first` pre/post 逐字节对拍（判据 ① 的锚）**
      > - **采集口径（第三方须能按此复现）**：
      >   `cd <仓根>` → `./build/corec clean-cache` → `nice -n 19 ./build/corec build tests/suite/ptr_ref_first.cr -o <OUT> --static`
      >   （`<OUT>` 任意——**实测**两次采集用不同 `-o` 路径，产物逐字节相同 ⇒ 输出路径不影响产物）。
      > - **输入锚（两个，缺一对不上版本）**：
      >   · 编译器 = `build/corec` sha256 **`b5cdd49d4d122b633b6dc824f267c5022415e1516f3c9104b2cc3e98bdb94e68`**
      >     （**采集时的起点修订 = `41a51d8d`** 的 `nice -n 19` 构建；「起点」≠「当前 develop」，两者不同则先重建再复现）
      >   · 语料 = `tests/suite/ptr_ref_first.cr` sha256 **`6ba85a4d48e9134b3c5a079ae87ed4dd75eaed62990623af69b1b9d04d9c7e1c`**
      > - **产物值（`--static`）**：
      >   · ELF sha256 **`fdf8cad651a148692b937291ae6c30985ff54749535ed9cb95152e86ab74a4be`**
      >   · `<OUT>.ccr` sha256 **`3e27595eb64770abd529a5c7a1ec14b5386e265f1910d6071712fd589c7f6d54`**
      > - **判据 ①（post 面）= 与上述两值逐字节相同**（(i) 只动诊断面 ⇒ 产物不得变）。
      > - **可复现性自验（本代理已做）**：同口径采集**两次**（不同 `-o` 路径）⇒ 两值**逐字节相同**（同一编译器、同一语料）。
      > - 副本：`/tmp/b8e1-keep/ptr_ref_first.pre.{elf,ccr}`（**仅作临时留档**——锚以上表为准，`/tmp` 会随清理消失）。
    - **排期**：**(i) 立为独立小批，排在 `(甲)` 刀 4 之前**（它是刀 4 的前置之一）。**(a) 降为备选**——仅当 (i) 因故不能落地时启用，**(甲) 不被它阻塞**。
    - **条目四条（写进 (i) 的条目）**：
      1. **语义**：`@raw_int` 接受**指针**，返回其**地址**（值面判据：`@raw_int(p)` 与 `@raw_int(q)` 之差 = 8）；
      2. **与视图族的关系写清**：`@raw_int` = 通用「**取原值**」（`dex` / `int` / **指针**）；`@ptr_of` / `@str_of` = **串↔字视图**（且须 `unsafe`）
         ——**别让读者以为 `@raw_int` 是视图族的一员**；
      3. **该档零改动**（`ptr_ref_first.cr` 与其注释**都不用改**）——这是 (i) 相对 (ii)/(a) 的直接好处，写进选取理由；
      4. **影响面与判据改法随之变**：现态「指针入 `@raw_int` ⇒ TF07」是**硬错 + 豁免放行**；放宽后该路径**合法** ⇒
         `test_diag_gate.py:87` 的重锁**不是**「根因修好」而是「**该码在此不再产生**」——**两者判据措辞不同，别照抄 TF01 模板的字面**。
    - **判据三条（(i) 小批验收线）**：① **pre/post 逐字节**：`tests/suite/ptr_ref_first.cr` 的 **ELF + `.ccr` 必须不变**（= 只动诊断面的可证伪形式；
      基线见上）· ② **负控**：放宽后 `@raw_int("s")` / `@raw_int(true)` **仍须 `error[TF07]` + 零产物**（防「一律放行」）·
      ③ **载体单点**：该文案全仓**仅 1 处**（`checker.cr` 的 `check_error(EC_TF_ARG_TYPE, "@raw_int requires a dex (or int) expression", …)`；
      `errors.md` 命中 = 0）⇒ 改文案只动这一处字面。
    - **安全面关系（必须明写进条目，防后来者误解）**：2(a) 裁的安全面是「**视图内建须在 `unsafe` 块内**」（`@ptr_of`/`@str_of` 已强制）；
      `@raw_int` **现不要求 `unsafe`**，且它本就吃 `TI_DEX`（取位模式 = 重解释）⇒ **指针不引入新类别**。
      **「`@raw_int` 是否应当要求 `unsafe`」是预存的更大问题**（`dex` 一路今天就如此）⇒ **不在本条解决、也不作为本条的阻塞**。
    - **（备选 (a) 若启用才需的两条，现备而不用）**：
      · **突变自证（写成可判形式）**：把缩放改回按字节 ⇒ 该档**必红**——**两条断言各自为什么红（含算例，小端、`arr[0]` 在 A、`arr[1]` 在 A+8）**：
      正确（按元素）`q = A+8` ⇒ 写 8 字节落 `arr[1]` ⇒ `arr[1] = 99` ✓、`arr[0] = 1` ✓；
      按字节 `q = A+1` ⇒ 写 8 字节覆盖 **A+1…A+8**（含 `arr[1]` 低字节）⇒ `arr[0]` 字节变 `[1,0x63,0,…]` = **25345 ≠ 1**（红）·
      `arr[1]` 低字节被写 0 ⇒ **0 ≠ 99**（红）⇒ **两条断言都会红**。
      · **该档注释随改而非加注**：`ptr_ref_first.cr` 的「断言用 `@raw_int` 取地址原值求字节差」是**正在被修改的测试的一部分**，
      注释**一并改写**成新观测方式（记录类才「原文保留 + 加注」）。
    - **顺序约束（N3，已升进 §5 停条件）**：**(i) 的处置完成前** ⇒ **不动 `diag.cr`、不动 `test_diag_gate.py:87` 的断言**
      （在 `@raw_int` 仍产 TF07、豁免仍在时改那条断言 = **改完即红**）。
  - **⑧ TB01 处置 = 先量后裁（lead 2026-09-18 补裁；本批不引入闸门设计变更）**：
    - **事实**：`EC_TB_ADD` 全仓**唯一 raise** 在 `checker.cr` 的 ANY 门；TB01 豁免是**码级**，位点**不止 `ptr_ref_first`**
      （还有 **t3/t4 库单元 8 档**，那 8 档**与 TF07 无关**）⇒ **撤 TF07 行不会影响 TB01 行**。
    - **量法（逐档读数，不用代理数）**：`(甲)` 落地后对那 **8 档**逐档跑 `check` / `build`，记录 `rc` + 是否含 `error[TB01]`。
    - **分支 A（8 档仍产，且系其它根因码的合法级联）⇒ 保留 TB01 行**，做两件事：**位点字段更新**（去掉 `ptr_ref_first`、保留 8 档）+
      **理由字段重锚**（其现引 `checker.cr` 的 `:2433/:2810/:3728` **行号已漂移** ⇒ 按符号锚重写）。
    - **分支 B（8 档已不再产）⇒ 整行撤**。
    - **无论哪支**：**不把豁免表从「码级」升级为「码级 + 位点级」**（那是**闸门设计变更**，若确需**单独立项**）。
  - **⑨ 与 ⑤ 的联动（必写）**：⑤ 走 (ii) 后 `ptr_ref_first.cr` **不再产生 TF07** ⇒ **其 TB01 级联同时消失** ⇒
    `test_diag_gate.py:87` 的重锁形态 = 「**`check rc=0` 且 `TF07` 与 `TB01` 均不在输出**」+ **负控**（真 TF07 仍 `rc=1` + 零产物）；
    逐字照同档 A₂ 的 TF01 模板（`:80-86` 两段）。
  - **⑥ 两处「位点」引用均已腐**（本代理实读；M3 的「位点」半条）：
    `tests/selfhost/test_ccr_types.py:1805` —— 该文件**现无 `raw_int` 命中（0 处）**；`tests/suite/ptr_ref_first.cr:8` ——
    第 8 行现为 `p := &x;`，`@raw_int` 实际在**第 10 行**。⇒ 撤条与改造判据一律按**符号锚**
    （`@raw_int` 守卫分支 + 该档实际用法）重定位，**勿按行号**。
  - **⑦ 「成对必改」的载体已钉死**（M3 前半）：载体 = `tests/selfhost/test_diag_gate.py` 的 `build_ok("tf07_tb01", …)`；**但 PR 门只 3/5 档**——
    `tests/suite/*.cr` 语料在 PR 上**不跑** ⇒ **改造必须落在 `selfhost-tests` 门内的断言上**（本档在 `tests/selfhost/` ⇒ 已在门内），
    否则「本地绿 / 合并后才红」。
- **自源零告警**：自源里的**真错**要修（不是豁免），否则 bootstrap/CI `check` 面会红。
- **批 2 收官必做（lead 裁 ④）**：**复测两处 TF01**（`return g2(x)` / `Some(…)` 实参位，§2 末两行）——
  若被新校验**自然消化**或行为改变 ⇒ **报 lead**；本批**不并入**其修复（未定位根因，塞进来会污染归因），
  只**登记 + 最小复现入档**。
- **判据**：负例 rc=1 + 具体码 + 无产物；正例全绿；
  **自源 check 面 = 整目录 `./build/corec check src/compiler`（S3：判据面须等于范围面——范围 = `corec_files`(45) + `corelsp_files`(31)，
  不是单个 `main.cr`；与 CI `check` job 同口径）**；全量回归 + canary 5/5 + 非涉语料 `.ccr`/ELF 逐字节不变。

---

### 刀 4-bis · TB01 豁免的处置（先量后裁；lead 2026-09-18 裁）

- **机制（实读）**：`diag.cr` 的 TB01 行位点 = `ptr_ref_first`（TF07 级联）+ t3/t4 库单元 8 档（N 族级联）；
  `EC_TB_ADD` 全仓**唯一 raise** = `checker.cr:2733`；`TI_NEVER` 是**错误标记非实型**（回传点 `:2634/:2968/:3050/:3188` 一带）
  ⇒ 上游 TF07 令推断回传 `TI_NEVER` ⇒ ANY 门对双侧判「非 int/dex」⇒ 报 TB01。
  （**本代理注**：块内行号取自复核者实读；落地时按**符号锚**复核，勿按行号——本批已发现两处位点行锚已腐。）
- **豁免是码级**（`code == EC_TB_ADD`）⇒ **无法只撤 `ptr_ref_first` 一个位点**；「只撤 TF07 行、TB01 行原样」**不是可选项而是默认态**，
  且它 = **未来真 TB01 的掩体** ⇒ **不可停留**。
- **先量（本批必做）**：`(甲)` 落地后逐档跑 `check` + `build`，记录每档 `rc` + 是否含 `error[TB01]`（8 档 = 该行位点字段所列 t3/t4 库单元）。
  **判据 = 逐档读数，不用代理数。**
- **分支处置**：
  - 量出「8 档仍产 TB01 且属其它根因码的合法级联」⇒ **该行保留**，只改两字段：**位点去掉 `ptr_ref_first`**（保留 8 档清单）·
    **理由重锚**（现行行号见上；注释里的 `:2433/:2810/:3728` **已漂移**）。
  - 量出「8 档已不再产」⇒ **整行撤**（与 TF07 行一次删净）。
- **硬约束（lead 裁）**：本批**不引入「码级 + 位点级」的闸门升级**——那属**闸门设计变更**，若确需**单独立项**，不夹带。
- **成对判据**：无论哪个分支，`tests/selfhost/test_diag_gate.py:87` 那条正控都要按 TF07 条目**重锁为「根因正控 + 负控」**，且 **TF07/TB01 一起钉**。

---

## §4 判据（**lead 更正版裁 ① + 两向钉子**；本节为现行口径）

### 4.1 pre-fix RED 证据（**弱式，只用于证明缺陷在场**）

- **弱式（固定 N=20）**：`./OUT` 连跑 **20** 次，**出现一次 `7` 即视为「未复现缺陷」**——判据必须**可失败**。
- 基线（实测）：97 次运行命中 `142` 一次、`7` 零次 ⇒ 20 次内复现缺陷为预期。
- **弱式不得用作修复后的判据**（那等于把「修好了」降级成「没观察到旧坏值」——本批正在收的那类自降）。

### 4.2 post-fix 判据（**强式；两支同档共存 = 两向钉子**）

| 支 | 探针 | 强式（全部必须成立） |
|---|---|---|
| **支 1（拒）** | 本探针 `g(1, d)`（apx） | `check rc=1` **且断言到具体错误码**（不是只断 rc）· `build rc=1` · `-o` 产物**不存在** · `<out>.ccr` **不存在** |
| **支 2（放）** | `g(d, d)`（**防误杀钉子**）· `g2(d)` · 非泛型 `dex` 形参（`g12`） | `check rc=0` · **`ELF == interp == 7`** |

- **为何必须两支同档**：只写支 1 ⇒「**一律拒**」也全绿；只写支 2 ⇒「**一律放**」也全绿。
- **支 2 的 `g(d, d)` 是当场抓出误杀的那条**（T0 实测：`[S1-ARG]` 对 `T?` 恒报，含合法调用）——**保留为钉子，不得删**。
- 支 1 无非确定问题（构建失败是确定的）。

### 4.3 机械腿（确定性；**必写两项**）

- **冷态**：跑判据前 `corec clean-cache`，**或**用独立缓存目录——暖态 `.cir` 重放会假红/假绿（§0 引
  `#2026-09-17-13` 暖态假红 · `#2026-09-17-17` 键缺分量；另见记忆条目「暖态合成串陷阱」）。
- **点名仪器**（没点名的不算判据）：
  - `corec cir <f> -o <out>.cir` 的 **`call <实例名>(…)`** 行 + **`dest=_dxsc`** 计数（调用点转换在场性）；
  - `corec cir <f> --dump-params` 的 **`PARAM <实例> p<i> <name> slot=<ti> decl=<tv>`** 行（槽型直读）。

### 4.4 腿的归属（纪律）

- **`255` 只属 interp 腿**（`interp.cr` 的 `IR_I2F/IR_F2I` 能力边界：`corec run` 无 binary64 语义）——
  **不得**混进 ELF 腿口径。
- 非确定值纪律（保留）：**任何读数进判据前须先证 N≥10 次采样稳定**；非确定值域**不适用**「禁第三态」。

5. **突变自证**（每条判据须能被下列突变变红，且**命中目标**；**两向钉子两向都要能被咬**）：
   - M-α 撤刀 1 ⇒ 正例 C6/C7/J/K/M 的 **S1-ARG 误报复现**——**期望写成前态读数**（S1）：C6 **恰好 1 条** `[S1-ARG]`
     （前态实读 = 1），**不得写「非零」**（「非零」会被别处产生的告警满足 ⇒ **打空反而绿**）；
   - M-β 撤刀 4 的 raise（保留刀 2 打点）⇒ 负例 check 回 0 ⇒ 红；
   - M-γ 豁免表放宽到"dex → int 一律放行" ⇒ P0 假绿 ⇒ 判据红（证明豁免表是判据的一部分）；
   - M-δ 撤刀 2 第二遍（重定向后按实例复跑）⇒ C6/C7 的 `.cir` 机械腿红（**仪器与值判据分工**，抄 #121 M-δ）；
   - **M-ε「一律拒」突变**：把支 2 的合法调用也判错（如豁免表全删 / 校验不看转换可能）⇒ **支 2 红**（两向钉子另一向）。
6. **零足迹**：与 dex/泛型无关的语料（`tests/suite/*.cr` 全量）`.ccr` + ELF 逐字节不变。

---

## §5 停条件（命中即停、报 lead）

1. 需要改 `unify_types` **既有三条规则的语义**（而非新增 `T?` 分支）⇒ 停（会牵动 `test_optional.py` / `test_generic_constr.py` 契约）。
2. 自源**零告警不可达**——判据 = **两源读数**（源 A 通道输出 / 源 B bootstrap 警告行，§3 刀 2-bis；**不用 273**）。**「>30 处」本身不停**：
   那正是刀 4「自源修复」步骤存在的理由，按计划走；**只有在超出我判定的本批可修规模时**才停并报 lead 分批。
   （口径来源：lead 2026-09-18 对代理读数的升级要求。）
3. 硬错化使 **bootstrap 构不出 corec**（`build_selfhost_native.py` 因自源告警/错误失败）⇒ 停。
4. 需改 `src/arch/**` / `src/os/linux/callseq.cr` / `src/format/**` ⇒ 停。
5. **顺序约束（N3，lead 2026-09-18 升进本节）**：**(i)（放宽 `@raw_int` 接受指针）的处置完成前** ⇒
   **不动 `diag.cr`、不动 `test_diag_gate.py:87` 的断言**——在 `@raw_int` 仍产 TF07、豁免仍在时去改那条断言 = **改完即红**
   （该断言现钉的是「rc=0 + 产物 + 含 TF07/TB01」）。
6. `test_generic_constr.py` / `test_cache_identity.py` / `test_cir_warm_path.py` / `test_generic_param_erasure.py`
   任一由绿转红 ⇒ 停。**另两条必列（M4，复核者指出）**：
   - **`tests/selfhost/test_optional.py`** —— **刀 1 的判据依据就是它记载的「裸值入 `T?`」语义**（§3 刀 1 引用它）⇒ 必列；
   - **`tests/selfhost/test_arg_inference_gap.py`** —— 实参面收口套件（`#2026-09-16-31`）；其腿 A 前 3 例类型匹配、大概率不红，
     但**最可能被触到却没列出**的一档 ⇒ 必列。

---

## §6 已裁事项（**lead 2026-09-18 五项裁定**；本节为现行口径）

| # | 裁定 | 落点 |
|---|---|---|
| ① | **边界 = 类型 + 个数都要**（同一缺口的另一半，只做类型会留同一位置的静默洞） | §3 刀 4 范围 |
| ② | **豁免默认「修调用方」**；豁免须**契约证据 + 逐条报 lead 批 + 自带判据**；**禁止按名字批量豁免**。`w64`/`r64` 倾向豁免但**先交契约证据**；`auto_str` 按「声明 vs 实参」两分（声明 `-> string` 而实参非 string ⇒ **真错**） | §3 刀 3 |
| ③ | **分批**：刀 1 独立小批先落（与 2(a) 无耦合）；**刀 2–4 = 一个批、全线一刀（types + count 同时开）** | §3 抬头 |
| ④ | **两处 TF01 不并入**（未定位根因 ⇒ 登记 + 最小复现入档）；**但刀 4 硬错后必须复测这两例**，若被自然消化/行为改变 ⇒ 报 lead | §3 刀 4 收官必做 |
| ⑤ | **2(a) 先合** ⇒ 合并后**重读 `checker.cr`**（一律符号锚，不按行号） | 本文件全篇 |

**取数纪律（并列硬要求）**：oracle = 刀 2 通道自己的输出 **+ bootstrap `Checker warnings`（源 B，M1）**；**273 不得进结论、不得当停条件阈值**（§1.3）。

**契约冲突面（已收敛，lead 2026-09-18）**：`(甲)` 的**既存契约冲突**只剩三处 ——
① `tests/selfhost/test_diag_gate.py:87`（`tf07_tb01` 一行，**必红 ⇒ 同批改造**）；② `diag.cr` 的 **TF07 行**（含**重复项两行**）；③ `diag.cr` 的 **TB01 行**（按 ⑧ 先量后裁）。
**其余 5 处正控已有机制理由**（无「预期」字样）⇒ **停条件与验收线一律按这个收敛后的面写**（不得再泛化到全表）。

**仍待开工后逐条报批的**：刀 3 的豁免表每一条（契约证据 + 该条判据）——**逐条报 lead，不批量**。

**⚠ 开工前须先裁（新，来自 2026-09-18 实施前复核）**：**TF07 撤条包 ⑤**（§3 刀 4）——
`tests/suite/ptr_ref_first.cr` 的 TF07 **是真错**（`@raw_int(<指针>)` 撞 `@raw_int` 的 dex/int 守卫），
⇒ 单撤豁免该档必 build 失败。两条出路取裁：**(i)** 放宽 `@raw_int` 接受指针（**语义面变更**）·
**(ii)** 语料向模型对齐（改该档惯用法，照 TF01 的 A₂ 先例）。**未取裁前刀 4 不得动 `diag.cr`。**

---

## §8 实施前对抗复核吸收索引（`plan-verikernel`，2026-09-18；所核 = `cc0816bd`）

> 结论 = **必须 4 · 建议 4 · 挑不出 6**。本代理**逐条实读复核**后全部吸收（凡与复核者结论不同的，已在下表注明本代理读数）。

| 项 | 内容 | 落点 | 本代理复核 |
|---|---|---|---|
| **M1** | 刀 2 的 oracle 缺第二源；bootstrap 对**自源面**是天然 oracle（唯一同时看实参位 + 返回位） | §1.3（口径收窄）· §3 刀 2 的「oracle 是两源」 | **采纳**；源 B 的两种条目形（`argN: expected` / `expected return type`）本代理在构建日志中实见 |
| **M2** | 通道覆盖面未枚举 ⇒ 四入口（`EXPR_CALL`/`EXPR_FIELD`/`EXPR_ENUM_CONSTRUCTOR`/`EXPR_AT`）须逐形态给「打点/不打点 + 理由」 | §1.2 覆盖面表 · §3 刀 2 的覆盖面表 | **采纳**；四入口行号（`:2797`/`:3605`/`:3539`/`:4062`）本代理实读核对一致 |
| **M3** | 「成对必改」缺载体 + 缺位点处置 | §3 刀 4 的 ④（载体 = `test_diag_gate.py` 的 `tf07_tb01`）· ⑥（位点两处**均已腐**）· ⑦（须落在门内套件） | **采纳并加严**：两处位点引用**都**漂了（复核者只说了一处；`ptr_ref_first.cr:8` 现为 `p := &x;`） |
| **M4** | 停条件缺 `test_optional.py` / `test_arg_inference_gap.py` | §5 停条件 5 的追加两条 | **采纳**（前者是刀 1 判据依据所在档，确实必列） |
| **S1** | M-α 期望写成**前态读数**（C6 恰好 1 条），不写「非零」 | §4 突变自证 M-α | **采纳** |
| **S2** | §4.3 补 STR 陷阱并**引同函数内既有先例** | §3 刀 2 的「两条前置 ②」（引 `checker.cr` 台账只打数值的注释） | **采纳**；该注释本代理实读确认（含 `9917/9918` 格式行） |
| **S3** | 判据面 = 范围面 ⇒ `check src/compiler`（整目录） | §3 刀 4 判据行 | **采纳** |
| **S4** | 豁免粒度写死：`(callee, argN)`；字面量多态写成显式矩阵 | §3 刀 3 的「豁免粒度」两条 | **采纳** |
| 挑不出·甲 | 刀 1 的第 3 个 unify 调用点（结构体泛型字段）**机制上不受影响**（守卫要求 `EXPR_IDENT` 泛型参数 ⇒ `T?` 到不了） | §3 刀 1 影响面 | **采纳**（已核源码：`is_struct_generic(...)` + `alloc_type(TYP_GENERIC_PARAM,…)`） |
| 挑不出·乙 | 批次**可切分**（翻红面 = 0：全仓 `[S1-ARG]` 引用只有 checker 自己）+ 属**行为变更须登记** | §3 刀 1 末两条 | **采纳**（已核：全仓仅 `checker.cr` 一处 `[S1-ARG]`，`tests/` 零引用） |

**本代理对复核意见的**反向更正（1 条，已写进 §1.2）**：复核者与 lead 均未提——**非泛型面并非「无打点」**：
`checker.cr` 的 `infer_expr` 的 `EXPR_CALL` 分支已有**数字台账通道**（`9917|9918`，`CORE_S1P=1` 门，PR #127 落地 = 本计划起点）。
⇒ §1.2 旧说法「非泛型面无任何打点」作废；刀 2 的活相应从「建通道」改为「**扩面 + 接线 + 扫面**」。

---

## §9 lead 二轮裁定吸收（2026-09-18；与 §3 刀 4 的 ①②③④⑤⑧⑨ 互为索引）

| 裁定 | 落点 | 状态 |
|---|---|---|
| **⑤ 终裁 = (i) 放宽 `@raw_int` 接受指针**（独立小批，**排刀 4 前**；`(a)` 行为式降**备选**，不阻塞 (甲)） | §3 刀 4 的 ⑤（含可行性闸实测表 + pre 基线 + 条目四条 + 判据三条 + 安全面） | **改裁两次**：先 (ii)+闸 → 复核者证伪 (ii) 的 `@ptr_of` 形态 + (i) 可逐字节证伪 ⇒ **终裁 (i)**；顺序约束已升进 §5 |
| **TB01 = 先量后裁**（码级豁免、位点多于一处、8 档与 TF07 无关）+ **本批不引入闸门设计变更** | §3 刀 4 的 ⑧ | 待 `(甲)` 落地后逐档量 |
| **重复项两行一起删净** + 「`(code, scope)` 唯一」机械判据**独立立项** | §3 刀 4 的 ② | 已写死 |
| **契约冲突面收敛**（只剩 `test_diag_gate.py:87` + `diag.cr` TF07 行（含重复）+ TB01 行） | §6 末「契约冲突面」 | 已写死 |
| **与 ⑤ 的联动**（(ii) 落地 ⇒ TF07 与其 TB01 级联同时消失 ⇒ 重锁为「rc=0 且两码均不在」+ 负控） | §3 刀 4 的 ⑨ | 已写死 |

**形态登记（lead 要求写进批报告，勿只写「改了 `ptr_ref_first.cr`」）**：⑤ 是**同一形态的又一实例**——
「**模型与语料不一致，被一块豁免盖住**」，与 `int, apx` 白名单那次**同族**。区别 = 这次**语料是维护者写的终审回归**
（`ptr_ref_first.cr` 把 `@raw_int` 当断言工具用，本身是「预期它能吃指针」的**意图证据**）⇒ 「谁该向谁对齐」不像 apx 那次显然 ⇒ 才需要可行性闸。

---

## §10 约定（本条批报告须收进）：**推荐里的前提同样要验，不只是计划里的**

**兑现记录（第 5 次，2026-09-18）**：复核者 S1 里写着「`@ptr_of` 正是『显式视图转换』，改写语料**零语义变更**」——
**这是一句未验的前提式陈述，它没验就写进了推荐**；lead 实读 2(a) 守卫后证伪（`@ptr_of` 收 `TI_STR`、且须 `unsafe`）⇒ **⑤ 的 (ii) 路线随之作废**。
⇒ **约定**：**计划里的前提要验，推荐/复核意见里的前提同样要验**；「未验的前提」无论出自谁，一律按**待验项**处理，不按结论用。

**同族清单（本批已兑现的前提失效，供批报告引用）**：
1. 「非泛型面**无任何打点**」→ 错（S1P 台账已存在，§1.2 更正）；
2. 「bootstrap **不可作 oracle**」→ 过宽（只对语料面成立，§1.3 加注）；
3. 「`@ptr_of` 可零语义变更改写语料」→ 错（守卫不收 `&int`）；
4. 「`[S1-ARG]` 只覆盖泛型」→ 同 1；
5. 「`T?` 恒假/误报无需担心兼容」→ 需以「翻红面 = 0」的**两向清单**证（§3 刀 1，复核者已给）。

---

## §7 附：本轮读数的取数命令（可复跑）

```
# 起点 = develop@origin 41a51d8d；工作区 b8e1-ws；nice -n 19 python3 build_selfhost_native.py
./build/corec check  <probe>.cr            # rc + 诊断
./build/corec build  <probe>.cr -o OUT --static [--dump-params]   # rc + 槽型直读
./build/corec cir    <probe>.cr -o OUT.cir # 调用点机械判据（call <inst>(…) / dest=_dxsc）
./build/corec run    "$(cat <probe>.cr)"   # interp 腿（确定值）
for i in $(seq 1 10); do ./OUT; done       # ELF 腿：N≥10 采样（**非确定值不得当锚**）
```
