# R2 P6（尾批）实施计划：附录 E 十六项分诊收尾——回归网可复现化 + β 盘面项索引 + 判定缺口 + 文档/登记

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 兑现统一设计 spec §9 的**尾批**（P5 已 ✅，本批 = 类型接口化工作流的**最后一批**）：把 P5 交接包（附录 E 16 条）**逐条分诊并处置**——① **立即实施**（7 条：E-1 β / E-7 残项 / E-8 陈旧件 / E-10 ¬ 面过判 / E-11 `never` 调用面 / E-13 冻结基线复现 / E-16 先例引用）；② **待维护者裁定**（7 条：E-2/E-3/E-4 容量批归属 · E-6 能力面范围 · E-12 调用位点诊断 · E-14 政策复核 · E-15 混比语义）；③ **维持登记**（E-5 `.csr`/TagNode + E-15 分片半项）；E-9 纪律继承非任务，逐字落本计划 Global Constraints。**边界纪律（尾批）**：不做「新能力」（盘面项索引 β 除外——它是 P5 裁-1 已裁定的 **P6 承接面**，属**已裁定的**交付）；凡本批新引入的收紧一律 **report-only 先行**；凡待裁项**不得**先行实施。

**Architecture:** 七条主线：① **前置侦查与基线重建**（Task 0：**从 pinned revision `9bcb7083` 重建冻结基线**并核 sha 白名单 + 语料/探针清点 + 锚点实核；源码零改动）；② **回归网可复现化**（Task 1 = E-13：重建配方 + sha 白名单 + 语料 runner + 探针语料**入仓**——当前探针源已在 `/tmp` 内**部分丢失**，见「前置状态」）；③ **判定面缺陷收尾**（Task 2 = E-10 ¬ 面过判；Task 4 = E-11 `never` 调用返回型 + E-12 调用位点诊断（挂裁-P6-2））；④ **载体尾项**（Task 3 = E-1 β：`.ccr` NOD 盘面项索引 + `CCR_VERSION` 8→9 + corearch `atom_of` 读回）；⑤ **文档/卫生**（Task 5 = E-7/E-8/E-16）；⑥ **收官**（Task 6）。

**Tech Stack:** Core 自举栈。`src/compiler/ccr_io.cr`（`CCR_VERSION=8` :135 · `ESZ_NOD_DISK=36` :143 · NOD 写 :768 / 读 :1384-1389 / `OFF_NS_TK` :511 · 段界检 :1367/:1373）· `src/compiler/ccr_types.cr`（`ccr_type_populate` :54 / `ccr_iface_populate` :94 / `ccr_seg_prepare_save` :300）· `src/compiler/ty_shadow.cr`（分类表 :424-523 · `sh_term_of_ti` :321 · `sh_sig_term_of_ti` :885 · `tt_display_at` :539）· `src/compiler/type_engine.cr`（`lit_implies` / `ak_disjoint` / `sub_cover` / `te_elem_cmp`）· `src/compiler/checker.cr`（`unify_types` 定义 :1891 / 丢弃点 :2000 · `EXPR_FOR` :2929 · 返回位点 `body_ti != TI_NEVER` :2234）· `src/compiler/main.cr`（硬名单 :149-176 · `cache_enabled`/`g_optrep_on` :477-491）· `src/compiler/dyn_arr.cr`（`ESZ_DFNODE=72` :119 · `OFF_DF_TK=40` :122 · `OFF_DF_AUX=64` :124 · 受护访问器 :481-548）· `src/compiler/ast.cr`（`MAX_*` :115-118 · 镜像 `[.;64]` :143-145 · 错误码 :407-428）· `src/ci/run.sh`（五 job；selfhost-tests :68-109）· `tests/selfhost/test_type_engine.py`（`MIN_CASES=32` :20）。

**Spec:** `docs/superpowers/specs/2026-09-10-type-interface-unification-design.md` **§6.3**（corearch 读回：**接线 = β 批次（P6）**，前置已齐）、**§6.2/§3.4**（D20 稳定引用形态规范）、**§6.4**（语义保鲜兑现点）、**§9 P5 行**（✅ 终态与未覆盖面指针）、**§10**（YAGNI：不做项与未覆盖面登记机制）。**P6 起点文档 = P5 计划附录 E**（`docs/superpowers/plans/2026-09-13-r2-p5-cleanup.md`：1–16 条，逐条 = 现状/出处/影响面/建议/前置）+ P5 统一清单（同计划「统一清单」节）。

**前置状态（本实例实核/实读；记录值一律带出处）**：

- **工作树**：head = `5b93c9fe`（P5 Task 7 收官，纯文档 5 文件）；工作副本 `15da9397` **空**、`jj status` 干净（本实例 `jj status` 实核）。**源码零改动**（P5 T7 之后无提交）。
- **P5 判据终值**（P5 T7 记录，`p5-task7-report.md` §0/§2）：五 CI job rc=0 · 全枚举 **58/58**（51 selfhost + 7 bootstrap）· `selftest-types` **404/404** · `check src/compiler` rc=0 · ELF canary `95084e7bc68d6550d21d3d96fa3afd89c67a5d89edce5656a3d2e74fc923d475`（28822B）· 自举链 `corec2==corec3` `5559beef…`（2854910B）+ N06=0 + 冒烟 42 + `--help` rc=1 · `.ccr` 两种形式冷态四条（`ptr_arith` 90823B `fb4a3b59…` / 90966B `592afa31…`；`generics_test` 135341B `ddec1ce6…` / 135484B `cd2af565…`；差恒 143B）· `--dump-objects` 3521 行 · `.cir` DOT 165915B `b1bdf480…` · 构建确定性 ×2（`corec f737e26b…` / `corearch 544e9201…` / `corelsp 4251108b…`）。
- **冻结基线（pre-P5）**：**本实例实核仍在位且 sha 与白名单逐条同**——`/tmp/p5t0/base/corec` = `5d2b15ad746018619b01143cce400e6f3489ae72a962e12283a7afd3dcdb41e7` · `corearch` = `493dc490dfaed7774b46b74ccf1cfeca9fe5f61620e34229afb8991b1628a2a7` · `corelsp` = `18b94bd955fa204accf0753095a28f6499871566254d846333a7496c99109d49`（= P4 收官 `9bcb7083` 产物，`jj log -r 9bcb7083` 实核存在）；runner `/tmp/p3t0_run.sh` sha = `12dc571f…`（实核）。基线 rc 分布 **34×0/38×1**（P5 T0/T4/T5/T7 一致）。
- **⚠ 探针语料已开始腐坏（本实例实核，Task 1 的硬证据）**：`/tmp/p5t3b/probes/` 内**零 `.cr` 源**（仅存 `n13/n17/n18*` 的 `.log`/`.dump`——**T3b 残留探针源已丢**）；`/tmp/p5t3/probes` 18 个 `.cr` + `/tmp/p5t0/probes` 11 个 `.cr` ⇒ **全量 find 实得 29 个 `.cr`**（P5 T5/T7 记载「30 档」，**数字须在 Task 0 重清点，不得沿用 30**）；`/tmp/p5t4/run_probes.sh` 仍按 `*.cr` 通配三目录 ⇒ **对 `/tmp/p5t3b/probes` 静默空跑**。⇒ E-13 的诉求不止于冻结**二进制**，「探针语料」这条判据**已在退化**。
- **runner 影子位已死**：`/tmp/p3t0_run.sh` 的 `shadow` 模式仍传 `--type-shadow`/`--type-shadow-dump`——两旗标已随 P5 T5 删除 ⇒ **该模式不可用**；`check` 模式（无旗标）**仍可用**（本实例实读脚本确认）。入仓版必须剥掉影子分支。
- **已知未收尾面**（附录 E 之外，本批须一并看护）：`main.cr:149-176` 硬名单闸（ICE04 `:166` / TA02 `:174` 已入；**非硬名单诊断不阻 `build`**）；`g_optrep_on ⇒ cache_enabled=0`（`main.cr:488-490`）；`EXPR_FOR` 零类型检查（`checker.cr:2929-2940` 恒绑 `TI_INT`）。

---

## 附录 E 十六项分诊表（**本计划第一产物**；逐条 = 分诊 / 依据 / 处置）

> 分诊约定：**(a) 立即实施** = 形态已定、无需新裁定（实施者按本仓既有纪律自行落地）；**(b) 待裁** = 须维护者答复（逐条点名问句，见「裁决门」）；**(c) 维持登记** = 本批不做，理由写死。**不得**用本表把待裁项偷渡成实施项。

| # | 项 | 现状一句话（出处） | 分诊 | 处置 |
|---|---|---|---|---|
| E-1 | β：盘面项索引 + corearch `atom_of` 读回 | `.ccr` NOD 只有**派生码**（`ccr_io.cr:143`/`:511`），项不可跨进程解析；corearch 无 TYPE 段消费者（spec §6.3）；暖态项表可小于冷态（P5 T2 §5-④ 实测 `rows` 16→13） | **(a)** | **Task 3**（裁-1 已裁 P6；形态 = D20-② 许可形态。**两处先裁项**见 Task 3 Step 1） |
| E-2 | 对象布局表示位 | 结构体字段 / 数组元素 / 全局槽 / match 结果无表示位 ⇒ 裸值形态 **rc=139 双路径同**（响亮）、装箱形态正确（`test_optional.py` 两例锁定） | **(b)** 裁-P6-1 | 与 E-3 同批（裁-4）；**新能力面**（非尾批范围） |
| E-3 | `MAX_*` 定长槽解除 | 三上限 16（`ast.cr:116-118`）只约束进程内定长记录；解除 = 记录布局迁侧表；**四条前置未满足**（读回闸 / `ast.cr:143-145` 镜像 64 vs 布局 16**已不一致** / `sh_match_bit = 2^vi` >63 变体溢出 / 冷热 `.ccr` 与 canary 判据） | **(b)** 裁-P6-1 | 同上；护栏 P022/P023 已在位（#35 划销） |
| E-4 | 可选程序收回 `.cir` 缓存 | `main.cr:488-490`：`g_optrep_on != 0 ⇒ cache_enabled = 0`（表示位侧表 = 进程内状态、快照不载） | **(b)** 裁-P6-1 | 依赖 E-2（同批） |
| E-5 | `.csr`/TagNode 图消费面 | **零调用者**（P4 T4 §7-②；`grep csr/TagNode --include='*.cr' src/` = 0 命中，本实例实核） | **(c)** | 消费者属**验证切片轮**（spec §10「完整证明搜索归验证切片轮」）；「段内数据存在 ≠ 语义已兑现」约定保持 |
| E-6 | 命名消费语法 / iterable 接线 / 形状可写视图 | `T: 可索引` 未开工 · `EXPR_FOR` **零类型检查**（`checker.cr:2929-2940`：`infer_expr(iter)` 后恒 `def_sym(var_ni, SYM_LOCAL, TI_INT, -1)`）· 可写视图实测 `ref(mut=1, ⊤ₖ(SEQ))` ⇒ -1 | **(b)** 裁-P6-4 | 能力面（P3 未开工面遗留）；含一处**静默接受面**（`EXPR_FOR` 元素类型恒 int） |
| E-7 | `tt_display` 零 `str_intern` + 改名残项 + `MIN_CASES` | `tt_display_at` **已**只读零驻留（`ty_shadow.cr:535-536` 头注 + P5 T4 §4-9 归因实测）；`g_shadow_map → g_term_map` **P5 T5 已执行**；`test_type_engine.py:20` `MIN_CASES = 32` = 下限（实测 404） | **(a)**（仅 `MIN_CASES`）+ **前两项「现状复核」** | **Task 5**；`tt_display` 项的**原始改造意图无法确定**（见「未决项」U-1） |
| E-8 | 陈旧工具/副本 | `tools/module_to_ccr.py`（12784B；`wu32(1)` = **version 1** 远古 `.ccr`，本实例实读 `:219-220`；**零 in-repo 调用者**——本实例全仓 grep 仅命中自身 docstring/CHANGELOG/计划文档）· `pkg/` 陈旧 `ccr_io.cr` 副本（`.gitignore` 已覆盖 `pkg/` ⇒ **非入库面**）· `specs/2026-09-11-explain-predict-incremental-design.md:371` 的 `.cir` 缓存版本陈旧（旧记 `CIR_CACHE_VER`=14，实为 **17**） | **(a)** | **Task 5** ✅（2026-09-14 落地） |
| E-9 | 纪律继承 | 三态 · 新硬错先 report-only · 清单三面同核 + `test_backend_bootstrap` · 判据标准按 TODO #2026-09-11-9 · 影子通道已死 · `clean-cache` | **非任务** | 逐字落本计划 **Global Constraints** |
| E-10 | ¬ 面过判（T3b 揭开） | `sub_cover` product 规则撞 `lit_implies` ¬ 面规则：`lp ⊆ ¬x` **同类原子** ⇒ `ak_disjoint` 回 0 ⇒ 判「不含」；值集语义对**异身份**同形原子应为 1——**已收尾面 = `AK_SUM` 变体令牌**（链 = `[枚举名令牌, 变体名令牌]`）；**「命名链」半项不实施**（`AK_NAMED` 早退 -1 属 P0 终审 Important B 在册政策，翻转为政策变更；裁定 2026-09-14 = 维持 -1，待裁登记另起）。**仅 witness 项可观测**（生产 `ty_exhaustive` 走 `tt_norm`+`ty_inhabited`；全语料零翻转） | **(a)** | **Task 2 ✅**（三例 `*_over_claim` 已转 1 重新锁定 + 邻域负控 11 例） |
| E-11 | `-> never` 调用返回型 | `fn boom() -> never` 的**调用**被推断为 unit ⇒ `x : int = boom()` 误发 TA02、`return boom()` 误发 TF01（同一缺口两站点）。根因 = 声明注册域守卫 `checker.cr:1491`（NEVER→unit 钳位）。**收尾面（P6 T4a，2026-09-14）** = 直调 + 方法调用两站点（非泛型 `fn`）；**同族未修（裁④ + 新登记）** = 泛型调用（`res_call_type:1829` + 自测 `t6.never_cell_diff`）· extern（注册域 `:1574`）· 模块限定（`g_mod_func_tis`）· **`EXPR_ASSIGN` 无 never 豁免（`:3314-3336`，既有缺口，入 TODO）**。约定注：「全语料 `-> never` = 0」限 `.cr` 编译语料（72 档 + 自源 + stdlib + examples）；**58 套件内联源有 6 处**（`test_iface_ops.py`） | **(a)** | **Task 4a ✅**；**发射面零变化**（canary IDENTICAL 实测） |
| E-12 | #20/F3 调用位点无诊断 | `unify_types` 返回值被丢（`checker.cr:2000`，泛型形参位，`infer_gen_call` 内；**非泛型调用位点不是「待定位」而是「不存在」**：已知 SYM_FN 的非泛型调用分支**连实参推断都不走**（对照模块/方法/SO/外部未知/枚举构造器五处均有走查）⇒ E-12 实施 = 在该分支**新增**判定点）；`EC_TF_ARG_TYPE`（TF07，`ast.cr:423`）当前**仅 1 处 raise**（`checker.cr:3683`，且是 `@raw_int` 内建语法位置，**非调用位点**） | **(b)** 裁-P6-2 | **Task 4b（未实施，挂裁）**；**入闸判据 = 裁-P6-2(c)**；report-only 先行 |
| E-13 | 冻结基线可复现性 | `/tmp/p5t0/base/` = /tmp 产物（**本轮仍在位、sha 实核相符**）；`src/ci/run.sh:73-76` 自述「本 job 内**无法**复现——冻结二进制不入库」；维护者已裁定「**重建，不落二进制**」 | **(a)** | **Task 0**（重建 + 核白名单）+ **Task 1**（配方/runner/探针入仓） |
| E-14 | ICE04/P-A 签核 + 非硬名单诊断 | ICE04（`main.cr:166`）已按 P-A 入硬名单、**签核未取**；反向面：TA01/TG01/TG02 等**非**硬名单诊断**不阻 `build`**（`EC_TG_ARG_COUNT`/`EC_TG_BOUND` = `ast.cr:488-489`；硬名单闸实读 = R002/TK05/TK06/TS01-04/TK02/TM03/ICE04/TA02，`main.cr:152-174`） | **(b)** 裁-P6-2 | 三问同裁 + 两处**冻结面**（`*T ← 0/None`；`--verify-named-dedup` 保留=偏离 D27） |
| E-15 | `string == int` 混比 + >1GiB 分片 | bootstrap 侧对「任一侧为 string 型 var」的比较一律分派 `str_eq`（`x86_64_stack_asm.py:164`）⇒ 自举侧按 int 比较 = **双路径语义分歧**；T1 以 read-EFAULT 判据绕开。分片：单条目 >1GiB 无机制（当前最大 3.99MB，**零用例**） | 混比 **(b)** 裁-P6-3 / 分片 **(c)** | 混比 = 语言面语义定案；分片 = 无真实用例，**登记** |
| E-16 | 设计先例引用面 | `specs/2026-09-11-policy-injection-design.md:275` 与 `specs/2026-09-11-explain-predict-incremental-design.md:84/127/181/447` 以 **`--type-shadow`** 为先例——该通道已随 P5 T5 删除 | **(a)** | **Task 5**（文档顺手项，零风险） |

**分诊计数**：立即实施 **7**（E-1 · E-7 · E-8 · E-10 · E-11 · E-13 · E-16）／待裁 **7**（E-2 · E-3 · E-4 · E-6 · E-12 · E-14 · E-15 混比）／维持登记 **1 + 半**（E-5 · E-15 分片）／非任务 **1**（E-9）。

---

## 裁决门（**开工前置**；逐条 = 问句 / 影响项 / 推荐 / 未取裁时的行为）

> 纪律：**未取裁的项一律不实施**（对应任务只到「report-only / 登记」为止），且**不得**以「推荐案」冒充裁定——P5 T4 的 P-A 是**已实施待签核**（回退面 = 一行），本批不复制该形态。

| # | 问句（逐条照抄给维护者） | 影响项 | 本计划推荐 | 未取裁时 |
|---|---|---|---|---|
| **裁-P6-1** | 容量批（对象布局表示位 E-2 + `MAX_*` 解除 E-3 + 可选程序收回 `.cir` 缓存 E-4）——**仍在 P6 内实施**（裁-4 原文「合并为一个 P6 批次」），还是**转独立容量批**？ | E-2/E-3/E-4 | **转独立批**（理由：① 三者 = **新能力**（新表示语义 + 新记录布局），与「尾批只做已裁定交付/缺陷收尾」冲突；② 在野需求 = **0**（语料最大结构体 9 字段、无超限枚举、可选未覆盖面为**响亮**失败）；③ 四条前置（读回闸/`ast.cr` 镜像归位/覆盖位表示/冷热判据）**无一满足**，须先出设计） | 本批不实施；维持登记，TODO 挂「容量批（待排期）」条目 |
| **裁-P6-2** | 政策复核三问 + 两处冻结面：**(a)** ICE04/P-A 是否保留硬名单（`main.cr:166`；回退面 = 一行）？**(b)** 非硬名单诊断是否收紧（至少 `build` 面汇总告警）？**(c)** 新硬错入闸判据是否成文化为「判定继续 ⇒ 产出静默错产物」（照 R002/TS01-04/TM03/ICE04/TA02 先例）？**(d)** `*T ← 0` / `*T ← None` 是否作为 **null 初始化**放行（P5 T6 §6 的回退面 = 声明位 + 赋值位两站点同改 + `test_named_face.py` 语料回退）？**(e)** `--verify-named-dedup` 保留（偏离 D27，回退面 = 一行）是否认可？ | E-12/E-14 + P5 T4/T5/T6 的待签核面 | (a) 保留（清零判据成立、全语料 0 命中）· (b) 收紧到「`build` 面汇总」· (c) 成文化（该判据已实际指导 5 个门）· (d) **维持拒绝**（赋值位点早已 TA01 拒绝 + bootstrap 参考实现本有此检查，见 T6 §5 三证）· (e) 认可保留（活消费者 + 只增不减） | E-12 不实施（Task 4 只落 E-11）；ICE04/TA02 保持现状（已实施面不回退、不回退面不扩大） |
| **裁-P6-3** | `string == int` **能否比较**：编译期拒绝，还是单侧转换（哪一侧），还是维持现状（双路径分歧）？ | E-15 混比 | **编译期拒绝**（异型比较无良定义语义；现状 = bootstrap 侧 `str_eq` 自崩路径已被 T1 绕开而非修好） | 不实施；维持登记（T1 的 read-EFAULT 判据继续承担） |
| **裁-P6-4** | 能力面范围：`EXPR_FOR` 元素类型/非可迭代拒绝（iterable 接线）**是否入 P6**？命名消费语法（`T: 可索引`）与形状可写视图是否维持未开工？ | E-6 | **入 P6 的只有「`EXPR_FOR` 静默面收尾」**（元素类型按被迭代项推导 + 非序列 ⇒ 诊断，report-only 先行）；命名消费语法/可写视图维持未开工（须独立设计轮） | E-6 整体不实施（维持登记） |

---

## 任务总表（**本批实施面**；状态一律「待实施」，收官时按实测回填）

| 任务 | 内容 | 对应附录 E | 依赖 |
|---|---|---|---|
| T0 | 前置侦查 + **冻结基线重建**（pinned `9bcb7083`）+ 起点判据 + 守门复验 + 锚点实核（**源码零改动**） | E-13 前置 | — |
| T1 | 回归网可复现化：重建配方 + sha 白名单 + 语料 runner + 探针语料**入仓**（+ CI 面评估） | E-13 | T0 |
| T2 | ¬ 面过判收尾（`ak_disjoint` 异身份原子 ⇒ 不交；三例重新锁定 + 邻域负控） | E-10 | T0、T1（探针判据） |
| T3 | β：`.ccr` NOD 盘面项索引 + `CCR_VERSION` 8→9 + corearch `atom_of` 读回 | E-1 | T0、T1、**Step 1 两处先裁** |
| T4 | E-11 `never` 调用返回型（独立）+ E-12 调用位点诊断（挂**裁-P6-2**） | E-11/E-12 | T0、T1、裁-P6-2（E-12 半） |
| T5 | 文档/卫生批：`MIN_CASES` 收紧 + 陈旧件 + 先例引用 + 计数器锁定 + `run.sh` 自述块更新 | E-7/E-8/E-16 | T1（自述块随入仓改写） |
| T6 | 收官：全量回归 + 判据复验 + 统一清单 + 文档 + 尾批终态与剩余登记 | 全部 | 全部 |

**顺序理由**：T1 必须先于一切（后续每个任务的判定面判据都含「冻结基线同源对拍」这条判据，而该条判据当前既**不可复现**又在**腐坏**）；T3（格式级联，最大风险）**早做**以留余量；T4 的 E-12 半身挂裁-P6-2，故排在被裁项之后；T5 的 `run.sh` 自述块依赖 T1 的入仓结果。

---

## Global Constraints

- **jj only（禁 git 含只读/复合）**；**提交路径限定**（多 agent 在场：一律 `jj commit -m … <paths>`）；所有命令 `nice -n 19`；一构建一编译**串行**（每段前 `ps` 实查）；cwd = 仓库根（`/tmp` cwd 触发 import 解析假失败）。
- **基线纪律**：每任务开工先取**同任务起点**基线（binary sha + 语料计数 + 探针 rc 表），**不得沿用上一任务的计数**；T1 之后基线 = **入仓配方重建的三二进制**（sha 白名单见 T1），T1 之前 = `/tmp/p5t0/base/`（本轮在位，**任何引用须带 sha 实核**）。
- **ELF canary（硬闸，全批）**：`clean-cache` → `build tests/suite/ptr_arith.cr --static -o /tmp/…` → sha256 `95084e7bc68d6550d21d3d96fa3afd89c67a5d89edce5656a3d2e74fc923d475`。**任一任务变即停下上报**（本批**无**任何任务声明发射面变更 ⇒ 无豁免）。⚠ 该语料对类型层不敏感——它是「发射面零泄漏」证据，**不得**当新语义正确性证据（P3 附录 C.5-6 约定继承）。
- **三态纪律（不得稀释）**：引擎 -1 不得当 0/1 用；载入失败/段缺失/行不可译/项不可建 ⇒ **拒绝**；save 侧装填遇不可译行/签名项 ⇒ **save 拒绝**。**新硬错先 report-only**：本批新增的任何 rc=1 门（含 T4 的调用位点诊断）必须先在**全语料**（`src/compiler/_import.cr` 自指 + `tests/suite` + `tests/selfhost` **内联源** + `src/stdlib` + `examples` + 58 套件全枚举）report-only 跑一遍，清单审查后才开门。
- **判定面回归网（P5 T5 起，本批继承并**加固**）** = ① 冻结基线同源对拍（**T1 起 = 入仓配方重建的二进制**；72 档 `check` rc + 日志逐档 diff）② 行为探针（`tests/selfhost/*.py` + **入仓探针语料**）③ 突变控制 ④ 三态纪律（ICE04）。**影子通道已死**（`--type-shadow` / `[type-shadow]` 摘要 / 差异转储 / 站点直方图）——**任何后续批次不得引用其计数/摘要/站点直方图**（TODO #2026-09-13-9）。
- **`.ccr` 记录值引用约定（D28 继承）**：一律带**命令形式**（`corec ccr F -o O` vs `corec build F -o O --static`，差恒 143B）+ **缓存态**（冷/热；非可选程序冷≠热为**预存**豁免，各自确定性）；比较任何 `.ccr`/缓存态产物前 `clean-cache`；TYPE 段「纯函数」承诺**项面成立、行面不成立**。
- **判据标准按 TODO #2026-09-11-9**：不采用「与旧版逐字节同」当语义证据；结构性断言 + 语义零变化 + 自举稳定为本仓主判据；「≥N 例」= 下限（可增不可减）；**自测用例只增不减**（重新锁定/改名允许，删除须给死亡证据）。
- **清单双注册面（B.6，硬性）**：任何清单/段表改动必须**三面同核**——`build_selfhost_native.py`（`corec_files`/`corearch_files`/`corelsp_files`）↔ `src/targets/x86_64-linux/_import.cr` ↔ `src/compiler/_import.cr`；每任务回归面**必含** `tests/selfhost/test_backend_bootstrap.py`（rc=0 + 构建日志 `error[` = 0）。T1 新增入仓文件（`tools/baseline/*` + 探针语料）**不进**任何编译清单（非 `.cr` 编译单元；探针语料 = 测试输入）。
- **不得回退既有收纳**：TS01-04/TK02 硬门（#29）、TM03/TG02 门（#31/#33）、F2 常量档拒绝语义与「N 不回身份」、F5 槽位契约（#25/#28）、效应/纯度判据（#26/#27）、`iface_satisfies` 轴序 A>C>B、P4 全批（段机制/确定性装填/允许清单制/可选表示）、P5 全批（单槽化/命名面判定化/引擎唯一权威/ICE04/TA02/影子下线）——本计划一律不动；冲突 → 停下上报。
- **本语言无三元运算符**；取模须非负；键比较不得依赖 i64 回绕；noclobber（`>|`）；**不写 `buf == 0` 形态的空判**（string-int 混比在 bootstrap 侧编成 `str_eq` ⇒ 自崩；T1 先例 —— 见裁-P6-3）。
- **多 agent 纪律**：不并发构建；提交路径限定；实施时逐任务串行；突变控制一律在**仓库外实拷贝**（`tests/` 必须**实拷贝**而非软链——P5 T7 §4-2 的假绿教训）。

---

## 侦查底座（2026-09-14 实读实核；file:line = 当前工作树 `5b93c9fe`；未标注者为本轮实读）

**冻结基线 / 回归网**
- `/tmp/p5t0/base/{corec,corearch,corelsp}` 三 sha = 白名单值（实核，见「前置状态」）；`/tmp/p3t0_run.sh` sha `12dc571f…`（实核）；脚本 `shadow` 模式引用已删旗标 ⇒ **死码**；`check` 模式可用。
- 语料 = 72 档 = `tests/suite/*.cr` 32 + 自源 2（`main.cr`/`_import.cr`）+ 后端/内核/目标单元 15 + `src/stdlib/*.cr` 19 + `examples/*.cr` 4（P5 T0 §1.2）；`check` rc 分布 **34×0/38×1**（既有失败 = 2 档 0 字节 fixture + P21 嵌套 fn 负例 + examples 两档解析错——**不得**当回归）。
- 探针：`/tmp/p5t0/probes`（11）+ `/tmp/p5t3/probes`（18）= **29 `.cr`**（find 实核）；`/tmp/p5t3b/probes` **零 `.cr`**（源丢失，仅 log/dump）；runner `/tmp/p5t4/run_probes.sh`（三目录通配 ⇒ 对 p5t3b 静默空跑）。
- `src/ci/run.sh:68-112` selfhost-tests（**28 档 py 挂点**——`grep -c 'python3 tests/selfhost/'` 实核；原记「14 档」= 陈旧值，T0 表 D-4 勘误 + R2 P6 Task 5 修正）；`:70-80` 回归网自述块（T1 改写：冻结基线判据 → `tools/baseline/rebuild.sh` 重建 + 手工判据）；`:85` selftest 计数注（T5 后 = **415**）；`:89` `test_ccr_types` 计数注（T3 后 = **48**）。

**载体面（Task 3 = β 的对象）**
- `ccr_io.cr:135` `CCR_VERSION = 8`（load 校验 `==8`，v7 整类拒收——D10 先例）；`:143` `ESZ_NOD_DISK = 36`（7×i32 + 邻接 2×u32）；`:353` 段尺寸算式 `4 + instr_count*36`；`:1367` 装载越界核算（`(seg_end3-seg_off3)/ESZ_NOD_DISK`）、`:1373` 逐记录 `ccr_has_bytes(pos, 36, seg_end3)`。
- NOD 写：`populate_nod_objects`（:500-514，`OFF_NS_TK ← iri_tk(ii)` = **派生码**）→ `save_ccr` 的 NOD 段（:760-772：`buf_write_u32(iri_tk)`）；读：`:1384-1389`（`OFF_NS_TK ← tk`）。
- 语义对象侧表 `OFF_NS_*`/`ESZ_NOD_SEM` 定义在 `src/compiler/ent_kernel.cr`（不在 ccr_io.cr）；`OFF_NS_TK` = 派生码槽（P5 T2 后）。
- 版本面：`CIR_CACHE_VER = 17`（`cir_cache.cr:51`）——`.cir` 快照每节点 8×8B，**项槽不落盘**、装载侧重派生（P4 T4 实测裁定 + P5 T2 保持）。
- 段表：`ccr_types.cr`（`ccr_type_populate` :54 / `ccr_iface_populate` :94 / `ccr_type_seg_build` :160 / `ccr_iface_seg_build` :196 / `ccr_seg_prepare_save` :300）；`test_ccr_types.py` **40 例**（`run.sh:86`）。
- 项面：`ty_shadow.cr` 分类表（`sh_tk_is_type_face` :424 · `sh_atom_of_term` :443 · `sh_tk_face_of_code` :451 · `sh_tk_split` :474 · `sh_tk_split_load` :487 · `sh_tk_split_impl` :491 · `sh_dfn_code_of_slots` :513 · `sh_tk_term_of_code` :523）；桥接两点（`sh_term_of_ti` :321 / `sh_sig_term_of_ti` :885）。
- DF 槽：`dyn_arr.cr:119` `ESZ_DFNODE=72` · `:122` `OFF_DF_TK`（= 类型项引用）· `:124` `OFF_DF_AUX`（= 辅码）；契约注 :104-107。

**判定/诊断面（Task 2/4 的对象）**
- `unify_types` 定义 `checker.cr:1891`；**返回值丢弃点 :2000**（泛型形参位）；**使用点 :3433**（结构体泛型字段位，负控对照）；非泛型调用位点须实施时定位（TODO #2026-09-16-1 旧锚 `:1053` 已漂移）。
- `EC_TF_ARG_TYPE = 5007`（TF07，`ast.cr:423`）当前仅 `checker.cr:3683`（`@raw_int`）一处 raise ⇒ 码槽**现成**。
- `TI_NEVER` 既有豁免：`:2234`（返回位点 `body_ti != TI_NEVER`）、T6 的 `check_let_annot_compat` 豁免③（`val_ti == TI_NEVER`，`checker.cr:526` 区域）。
- `EXPR_FOR`：`checker.cr:2929-2940`（`infer_expr(iter)` 丢弃结果 + `def_sym(var_ni, SYM_LOCAL, TI_INT, -1)`）。
- 硬名单闸：`main.cr:149-176`（实读 :152-:174 八条 `hard = 1`）；`:166` ICE04、`:174` TA02。
- `tt_display`：`ty_shadow.cr:539`（`tt_display_at`）/`:608`（包装）；头注 `:535-536` 已声明「只读、零 `str_intern`」。

**`MAX_*` / 镜像面（裁-P6-1 的证据）**
- `ast.cr:115-118`：`MAX_GENERICS=4` / `MAX_STRUCT_FIELDS=16` / `MAX_ENUM_VARIANTS=16` / `MAX_VARIANT_TYPES=16`；镜像声明 `:143-145`（`field_names/field_types/field_type_nodes: [.;64]`）——**与布局 16 已不一致**。
- 受护访问器：`dyn_arr.cr:481-548`（`si_*` 9 个 + `ei_*` ~10 个，逐槽护栏；头注 :520-529 写明「唯一受护写点 + 读护栏 + P022/P023 承担用户面拒绝」）；P022/P023 护栏套件 = `tests/selfhost/test_enum_limit.py`（13 例，`run.sh:105`）。

**文档面（Task 5 的对象）**
- `src/ci/run.sh:73-76`（**T1 后必须改写**——冻结基线判据自 T1 起可复现）；`:82`/`:86`（selftest / test_ccr_types 计数注）。
- `tests/selfhost/test_type_engine.py:20` `MIN_CASES = 32`；`:37` 断言 `passed == total and total >= MIN_CASES`。
- `docs/superpowers/specs/2026-09-11-explain-predict-incremental-design.md:371`（`.cir` 缓存版本旧记 `CIR_CACHE_VER`=14 + `cir_cache.cr` 行号 `:16`，双陈旧）；`:84/127/181/447`（`--type-shadow` 先例）；`specs/2026-09-11-policy-injection-design.md:275`（同）。
- `docs/error-codes.md:100-101`（TA01/TA02）、`:114`（TF02）、`:263`（ICE04）、`:275-287`（码族统计）。
- 陈旧件：`tools/module_to_ccr.py`（12784B）；`pkg/`（`.gitignore` 覆盖，**非入库面**）。

---

## Task 0（前置，纯侦查 + 基线重建；**源码零改动**）

**Files:** 无源码改动。产物 = 报告 `.superpowers/sdd/p6-task0-report.md`（工作区，不入提交）+ 重建的冻结基线 `/tmp/p6t0/base/{corec,corearch,corelsp}` + 清点/基线原始输出（`/tmp/p6t0/`）。

**Interfaces（产物四表，全部落报告；数字一律本实例实测，未实测标「待实测」）：**
- **表 A：冻结基线重建**——`jj` 检出 pinned revision `9bcb7083`（P4 收官；**不得**用工作树直接建）→ `nice -n 19 python3 build_selfhost_native.py`（串行、无并发）→ 三 sha 对白名单（`5d2b15ad…`/`493dc490…`/`18b94bd9…`）；与 `/tmp/p5t0/base/` 副本并存则 `cmp` 逐字节核；两次构建取确定性；冒烟 42。
- **表 B：P6 起点判据基线**——五 CI job；全枚举 58/58；`check src/compiler` rc=0；`selftest-types` 404/404；构建确定性 ×2；自举链（`corec2==corec3` + N06=0 + 冒烟 42 + `--help` rc=1）；ELF canary；`.ccr` 两种形式四条（冷态、带标）；`--dump-objects` 行数；`.cir` DOT 大小/哈希。
- **表 C：语料与探针清点**——72 档清单逐档实核（**含 0 字节 fixture 与既有 rc=1 档的划界**）；探针语料**逐目录实测 `.cr` 计数**（当前 find = 29；`/tmp/p5t3b/probes` 零源 ⇒ 须从 `.log`/`.dump` 与 P5 报告内联片段判定**可复原者**）并出「可入仓 / 需重建 / 不可复原」三栏。
- **表 D：任务锚点实核**——T2/T3/T4/T5 的每个 file:line 锚逐条实读复核（本计划「侦查底座」节的锚点表即输入；**行号漂移以本轮实读为准**）。
- **守门复验（裁-4 的 P5 遗留项，本任务一并核）**：E-2/E-3 的未覆盖面**不得退化为静默错值**——实跑 `test_optional.py` 两例（结构体字段裸值 rc=139 / 装箱正确）+ `test_enum_limit.py` 13 例 + 越界样本（17 变体 / 17 字段）**仍为定位硬错 P022/P023**（不得静默通过）。

- [x] **Step 1: 基线重建**（`jj` 检出 `9bcb7083` 到临时目录 → 构建 → sha 对白名单 → 拷贝 `/tmp/p6t0/base/`；**仓库工作树零改动**，检出在仓库根之外）。判据：三 sha 与白名单**逐条同** + 冒烟 42 + 构建确定性 ×2。**不符 ⇒ 停下上报**（构建不可复现 ⇒ E-13 前提不成立，其后所有任务的对拍判据失效）。
- [x] **Step 2: 起点判据**（表 B；cwd = 仓库根、逐段 `ps` 实查无并发）。
- [x] **Step 3: 语料/探针清点**（表 C；`find` 逐目录 + runner 实跑一次 `check` 面得 rc 分布）。
- [x] **Step 4: 锚点实核**（表 D）。
- [x] **Step 5: 守门复验**（`test_optional.py` / `test_enum_limit.py` / 越界样本）。
- [x] **Step 6: 报告**（不入提交；`jj status` 应收官为空）。

**判据**：四表齐 + 基线三 sha 与白名单逐条同 + 源码零改动（`jj status` 干净）+ 报告内数字全部可追溯（命令 + 原始输出片段）。

**停条件**：重建 sha 不符白名单 ⇒ 停下（不得改白名单迁就）；`/tmp/p5t0/base/` 与重建物不一致 ⇒ 停下（两者必有其一非源出 `9bcb7083`）。

### Task 0 实施记录（2026-09-14；**R2 P6 Task 6 补记**——T0 当批只出报告、未回填本计划）

> 补记理由：T0（`p6-task0`）的报告 `.superpowers/sdd/p6-task0-report.md` **不入版本控制**（`.superpowers/` 被 `.gitignore` 覆盖）⇒ 计划内 T0 面**零留痕**（六步未勾、无实施记录）。T1 报告 §9-5 已登记该事实；**归属裁定 = T6 补**（章程边界：T5 的章程 = E-7/E-8/E-16；同类归口：与「统一清单」同为跨任务收编面；避免计划文件被两批各改一次）。本记录内容 = T0 报告 §1–§5 的实测数字逐条摘录（**非重跑**，出处已标）。

**表 A（重建）**：`jj workspace add /tmp/p6t0/src --revision 9bcb7083` → 构建 ×2（日志逐字节同，54565B）⇒ `corec 5d2b15ad…` / `corearch 493dc490…` / `corelsp 18b94bd9…`（= 白名单**逐条同**）；与 `/tmp/p5t0/base/` `cmp` **IDENTICAL ×3**；冒烟 rc=42。**表 B（起点判据）**：五 CI job rc=0 · 全枚举 **58/58** · `check src/compiler` rc=0 · `selftest-types` **404/404** · 构建确定性 ×2（`f737e26b…`/`544e9201…`/`4251108b…`）· 自举链 `corec2==corec3` `5559beef…` + N06=0 + 冒烟 42 + `--help` rc=1 · canary `95084e7b…d475`（28822B）· `.ccr` 四条（90823/90966/135341/135484）· `--dump-objects` 3521 行 · `.cir` DOT 165915B `b1bdf480…`。**表 C（清点）**：72 档 = 32+2+15+19+4，rc 分布 **34×0/38×1**（与 P5 T7 `parity_post` `diff -rq` 零差异）；**探针官方清点 = 29 `.cr`**（11+18+0）——「30」= 29 + 迁移 runner 的**字面 glob 伪条目**（`_tmp_p5t3b_probes_*.cr`，rc=1，两态恒等 ⇒ 该条对拍空洞；**U-4 收尾**）；`/tmp/p5t3b/probes` = T3b **输出**目录（27 件 = 21 log + 6 dump）⇒ **源未丢**（T1 报告 §9 的「源已丢」初判据此更正）。**表 D（锚点）**：逐条实读——5 条漂移（`ent_kernel.cr` 实为 `src/lattice/ent_kernel.cr` · `main.cr` 硬名单为 6 语句行覆盖 11 常量且 `return 1` 在 `:178` · `dyn_arr.cr` 契约注为 `:103-118` · `run.sh` 挂点实为 **28** 非 14 · 探针 29 非 30）+ 1 条码名约定（护栏用户面实印 `error[P22]`/`error[P23]`）。**守门复验**：`test_optional.py` **34/34**（未覆盖面裸值 rc=-11 双路径同 = 响亮）；`test_enum_limit.py` **13/13**；越界样本（17 变体 / 17 字段）= **P22/P23 定位硬错 rc=1 + 无产物**（未退化为静默）。**停条件**：两条均未触发。

---

## Task 1：回归网可复现化（E-13）——配方 + sha 白名单 + runner + 探针语料**入仓**

**Files:** New `tools/baseline/REBUILD.md`（配方 + 白名单 + 纪律）· New `tools/baseline/rebuild.sh`（可执行重建：`jj` 检出 pinned revision → 构建 → sha 校验 → 落到指定目录；**不含**任何二进制）· New `tools/baseline/parity_run.sh`（72 档 `check` 面 runner；**剥掉影子分支**；cwd 参数化）· New `tools/baseline/probes_run.sh`（**实施时增补**：29 档探针 runner——T0 表 C 揭示迁移期 runner 的**字面 glob 伪条目**，探针判据若不连 runner 一起入仓则仍是空洞证据）· New `tests/probes/README.md` + `tests/probes/*.cr`（**29 档实拷**；来源表逐档落 README——**不进 `.cr` 源内**，保逐字节保真）· Modify `src/ci/run.sh`（`:73-76` 自述块改写：冻结基线判据**自本任务起可复现**，指向 `tools/baseline/`）· Modify `TODO.md`（#48 登记① 收尾 + 新条目）。**未加** `tests/selfhost/test_baseline_recipe.py`（Step 4 结论 = 不挂钩 CI ⇒ 无需该套件）。

**Interfaces：**
```
tools/baseline/REBUILD.md      # pinned revision / 三 sha 白名单 / 构建步骤 / 换代纪律（基线换代 = 更新白名单 + 记旧值）
tools/baseline/rebuild.sh <outdir>     # jj 检出 → build_selfhost_native.py → sha256 对白名单 → 拷贝三二进制
tools/baseline/parity_run.sh <corec> <outdir>   # 72 档 check：逐档 clean-cache + rc/logs（影子分支删除）
tools/baseline/probes_run.sh <corec> <outdir>   # 29 档探针：显式清单 + 计数断言（无字面 glob 伪条目）
tests/probes/…                 # 探针语料（29 档实拷；逐档来源表在 README——**源内不加注**以保逐字节保真）
```
- [x] **Step 1: 配方 + 重建脚本**（Step 0 的重建**手跑配方固化**；脚本须幂等、失败即非零、**不落二进制进仓库**）。判据：在**干净临时目录**跑通一次 ⇒ 三 sha 与白名单同。**实测**：41s，三 sha 逐条同 + `cmp` 与 `/tmp/p6t0/base`、`/tmp/p5t0/base` IDENTICAL ×3 + 冒烟 rc=42 + 收工 `jj workspace list` 无残留。
- [x] **Step 2: runner 入仓**（剥影子分支 + cwd 参数化 + 语料清单以表 C 为准）。判据：入仓 runner × 重建基线 × 72 档 = 与 `/tmp/p3t0_run.sh check` 面**逐档 rc + 日志 `diff -rq` 零差异**（同源同比）。**实测**：零差异（72 档 rc 分布 34×0/38×1 两侧同）；另：冻结基线同源对拍（vs 当前二进制）亦零差异。
- [x] **Step 3: 探针语料入仓**——**前提更正（T0 表 C/§3.3 实测）**：`/tmp/p5t3b/probes` 是 T3b 的**输出**目录，**29 档源全在位**（无需重建源；亦不"源已丢"）⇒ 处置 = `/tmp/p5t0/probes`(11) + `/tmp/p5t3/probes`(18) **实拷** + 逐档来源表。判据：入仓探针 × 重建基线 × 当前二进制，逐档 rc + 日志在两态间**零差异**（对拍判据自证）+ 与 P5 记录逐条同。**实测**：两态 `diff -rq` 零差异；rc/log **29/29 与 `/tmp/p5t7/probes_post` 逐字节同**；入仓 29 档 sha 与 T0 清单 29/29 同。
- [x] **Step 4: CI 面评估**（**默认不加挂钩**）。**实测结论 = 不挂钩**：`.github/workflows/core-ci.yml`（`actions/checkout` + `fetch-depth: 2` 浅检出）内**无 `jj`** ⇒ pinned revision 不可得；增量实测（rebuild 41s + 对拍 19s + 探针 1s）虽小，但不解决 jj/浅检出前置 ⇒ 保持**手工判据 + 入仓配方**（`run.sh` 自述块已写明）。
- [x] **Step 5: 判据**：五 CI job rc=0 · 全枚举 58/58 · `selftest-types` 404/404 · `check src/compiler` rc=0 · **ELF canary IDENTICAL** · 自举链 · `test_backend_bootstrap` rc=0 + `error[`=0 · **冻结基线同源对拍 72 档零差异**（新旧 runner 互证）· 探针全集（29）零差异 · 构建确定性 ×2 · 突变控制 ≥1 + 精确回滚。**实测全绿**（数字见下方实施记录）；**突变控制实际 5 条**（M1/M2/M4/M5/M7 转红 + M3 **未转红**，诚实登记）。
- [x] **Step 6: 提交**：`feat: R2 P6 Task 1——回归网可复现化（冻结基线重建配方 + sha 白名单 + runner/探针语料入仓；探针源腐坏段重建并登记）`（提交信息按实际改写，见实施记录）。

**停条件**：① 重建脚本在干净环境复跑不出白名单 sha ⇒ 停下（**不得**改白名单对齐）；② 探针重建件与 P5 记录的 rc/诊断不符 ⇒ 停下（说明重建失真，须逐档归因）；③ runner 入仓版与 `/tmp` 版出现差异且无法归因 ⇒ 停下；④ 若入仓需要动任何 `.cr` 编译清单（三面同核）⇒ 停下（本任务应零清单面）。**四条均未触发**。

### Task 1 实施记录（2026-09-14；报告 `.superpowers/sdd/p6-task1-report.md`；证据 `/tmp/p6t1/`）

**形态**：`tools/baseline/` 四件（`REBUILD.md` 配方/白名单/换代纪律 · `rebuild.sh` 重建（`jj workspace add --revision 9bcb7083` → `build_selfhost_native.py` → sha **硬闸** → 冒烟；`trap` 收工 forget+删目录）· `parity_run.sh` 72 档 runner（影子分支剥除 + `nullglob` + **计数断言 72**）· `probes_run.sh` 29 档 runner（显式清单 + **计数断言 29**））+ `tests/probes/`（README 逐档来源表 + **29 档实拷**）+ `run.sh` 自述块改写 + TODO #2026-09-14-2 新条目 / #48① 闭合。

**判据（全部本实例实跑）**：`rebuild.sh` **41s** rc=0 → 三 sha = 白名单 + `cmp` vs `/tmp/p6t0/base`、`/tmp/p5t0/base` **IDENTICAL ×3** + 冒烟 42 · 入仓 runner vs 迁移 runner（**同一二进制**）：72 档 `diff -rq` **零输出**，rc 分布两侧 34×0/38×1 · **腿① 主判据**：冻结基线 × 当前源 vs 当前二进制 × 当前源（T0 `parity_start`）**零差异** + 与 P5 T7 `parity_post` 零差异 · **探针**：两态（基线/当前）`diff -rq` 零差异；rc/log **29/29 逐字节同 P5 T7 记录**；rc 分布 17×0/12×1；入仓 sha 与 T0 清单 29/29 同 · 五 CI job 全 rc=0（`check` 16s · `bootstrap-tests` · `selfhost-tests` **108s** · `suite` 21 档 · `full-bootstrap`）· 全枚举 **58/58**（92s）· `selftest-types` **404/404** · canary **`95084e7b…d475`（28822B）IDENTICAL** · 自举链 `corec2==corec3` `5559beef…` + N06=0 + 冒烟 42 + `--help` rc=1 · `test_backend_bootstrap` rc=0 + `error[`=0 · 构建确定性：五 job 内多次构建同 sha（`f737e26b…`/`544e9201…`/`4251108b…` = P5 T7 记录值）· 仓库改动面 = 36 路径（`src/` 仅 `run.sh` 注释块；`src/compiler/checker.cr` sha `fff7a8ba…` = P5 T6 记录值 ⇒ 源码零改动）。

**突变控制（5 条；全部在 `/tmp/p6t1/mut/` 副本，仓库零改动）**：**M1** 语料掉一档 ⇒ runner **rc=1**（`CORPUS COUNT 71 != 72`）+ diff 报缺档 · **M2** 撤 `nullglob` + 零匹配目录 glob（复现 U-4 形态）⇒ **rc=1**（`PROBE COUNT 30 != 29`）· **M4** cwd 陷阱（`cd /tmp`）⇒ rc=1 + 缺档 · **M5** rc 通道打哑 ⇒ 72×0 vs 34×0/38×1 转红 · **M7 源级突变**（仓库外实拷贝重建：`checker.cr:592` `e == 0` 判真 = 判定放宽，`corec 527ca428…`）⇒ **12 档日志 + rc DIFF、rc 分布 39×0/33×1（5 档 1→0 = 拒绝静默消失）**——腿① 对**真行为变更**有牙（同 P5 T5 M2 的类别）。**M3（计划建议的「去掉逐档 clean-cache」）实测仍绿 ⇒ 未转红**：`check` 面不读 `.cir` 缓存 ⇒ 该突变在本判据不可检出（诚实登记；clean-cache 纪律对 `build`/`.ccr` 面仍是必需的）。

**执行更正（T0 表 C/表 D 的直接后果，逐条写进文档）**：① 探针正式清点 **29**（U-4；「30」= 29 + 字面 glob 伪条目）——本任务 runner 以显式清单 + 计数断言消灭该形态；② 「探针源已丢」**按下述更正**：源档全在位，`/tmp/p5t3b/probes` 是输出目录（⇒ **Step 3 不含任何源重建**；计划原写的「重建件」不存在）；③ `run.sh` selfhost-tests 挂点实为 **28** 档 py（计划写 14 是陈旧计数——但**本任务未改该计数注**：属 T5 卫生批的面，避免扩面）；④ 探针语料**源内不加注**（逐字节保真优先；逐档来源表落 `tests/probes/README.md`）。

---

## Task 2：E-10 ¬ 面过判收尾（引擎判定面）

**Files:** Modify `src/compiler/type_engine.cr`（`lit_implies` 的 ¬ 面规则 + `ak_disjoint` 的原子相斥判据；**须先出**「异身份原子 ⇒ 不交」的适用域守卫 + 头注）· Modify `src/compiler/type_selftest.cr`（三例 `t3.witness_reverse_over_claim` / `t3.witness_equiv_over_claim` / `t3.product_neg_over_claim` **转 1 并重新锁定**，另加邻域负控 **11** 例，子 runner `ts_t2_neg_run`）· Modify `src/ci/run.sh`（selftest 计数注 404 → 415）。

- [x] **Step 1: RED 复现（本任务自跑）**：三例现状 = 过判（`B ⊆ ¬R` 判 0）；用 `tt_display` 打印两侧项 + 手工最小复现（`q3 = B ∩ ¬R ∩ ¬G` 单点）落报告。**冻结基线复证**（`/tmp/p6t0/base/corec`）。
- [x] **Step 2: 适用域守卫（**先写证明再动码**）**：可加强面 = 「两侧皆为同类 **`AK_SUM` 变体项**（identity atom）」——T3b §3.1 排序表只可复用「链元素可判域」的**事实**，**不可复用其结论方向**（`te_elem_cmp` 判的是不变槽**元素**是否等价；¬ 面判的是**两个整原子的值集是否相交**——元素「不等价」≠ 整原子「不交」）。域外逐条：**`AK_SUM` 链元素皆令牌**（`[枚举名令牌, 变体名令牌]`）⇒ 异链 1 / 同链 0；**AK_NAMED ⇒ 维持 -1**（P0 终审 Important B 在册政策，本批不翻转）· **参数化构造子（SEQUENCE/PTR/REF/PRODUCT/FN）⇒ 维持 0 且不得扩域**（反例 `[int]` vs `[int ∪ string]`；REF 的 mut 标记位是令牌，无类守卫会被误判「异身份」）· **`⊤ₖ` 显式排除 ⇒ 0**（`tt_top_k` 的 c 槽 = 0 非 -1）· **空链 / 链元素域外 ⇒ 0**（P1-a 已裁定：域外逐位不变，零新增未覆盖面）· **μ 变元/裸值走既有路径不变**（逐条写进文档，不得猜）。
- [x] **Step 3: 实现**（落点经 T2 前置侦查修正）：`ak_disjoint` **只收类 id、看不到项**（`type_engine.cr:69`；调用面 `:118` / `:327` / 自测均只传类 id）⇒ 「同类 ∧ 异身份」**不可**落在该函数（照字面改 = 同类一律不交 = **假不交**）。**落点 = `lit_implies` 的 ¬ 面**：新增 `atom_same_class_disjoint`（域守卫）+ `sum_chain_same`（AK_SUM 链**逐元素令牌同一性**、三态），在 `:326`（`AK_NAMED` 早退）之后、`:327`（`ak_disjoint` 调用）之前插一行 `if pk == xk { return atom_same_class_disjoint(lp, x); }`；**`ak_disjoint` 本体与正×正路径（`lit_empty_pair:118`）零改动**（后者经 `ty_inhabited → ty_exhaustive` 泄进 match 穷尽性 TM03 生产面，本批不碰，**单列候选**）。**不得**近似、不得把「未知」当 0。
  - ⚠ **实现教训（已修正并补用例）**：首版链比较用 `te_chain_cmp`（命名面槽位语义：槽 0 = 令牌 / 槽 ≥1 = **类型项**）⇒ AK_SUM **槽 1 的变体令牌**走 `te_arg_cmp → ty_equiv`，两个不同令牌在结构比较下**判等**（令牌的 b 槽只是标注）⇒ 误判「同链」⇒ 三例 `t3.*` 仍判 0，而当时 9 例负控**全过**（N5 跨枚举在槽 0 被令牌规则救回）。补 **N10「同枚举异变体」** 后暴露；改用 `sum_chain_same` 修复。
- [x] **Step 4: 用例**：三例重新锁定为 1；**邻域负控 11 例**（子 runner `ts_t2_neg_run`，**不**内联进 `type_selftest_run`——巨型函数内联新用例有 rc=139 前科）：① 同身份 ⇒ 仍相交（0）；② μ 变元作**字面** ⇒ 仍 -1 + 未覆盖面；③④ `⊤ₖ` **两个方向** ⇒ 0（**语义断言**——既有 `neg.supertype*` 走 **¬×¬** 分支，不覆盖 `:327` 同类路径）；⑤ 跨枚举**同名**变体 ⇒ 1 + 同项 ⇒ 0；⑥ AK_UNIT 令牌字面 ⇒ 0（类域锁定）；⑦ `AK_NAMED` ⇒ **维持 -1**（政策锁定）；⑧ 空链 / 链元素域外 ⇒ 0（P1-a 锁定）；⑨ 参数化面 `[int]` vs ¬`[int∪string]` 双向 ⇒ 0；⑩ **同枚举异变体 ⇒ 1**（暴露首版缺陷的那条）；⑪ `&T` vs `&mut T` ⇒ 0（mut 标记是令牌 ⇒ 类域守卫的**稳定牙齿**）。
- [x] **Step 5: 判据**：`selftest-types` ≥404（**只增**）· `check src/compiler` rc=0 · **ELF canary IDENTICAL**（判定面改动**不得**泄进发射面）· `.ccr` 两种形式冷态**逐字节同** · 五 CI job + 全枚举 58/58 + 自举链 + `test_backend_bootstrap` · **冻结基线同源对拍 72 档零差异** · **探针全集零差异** · **突变控制 ≥2**（M1 撤销加强 ⇒ 三例复红；M2 域外守卫去掉 ⇒ 邻域负控红）+ 精确回滚。
  - **实测（2026-09-14；隔离 workspace `e5ce8f9e` + 本任务两文件，**不经主树**）**：`selftest-types` **415/415**（404 + 11）· `check src/compiler` rc=0 · canary `95084e7b…d475`（28822B）**IDENTICAL** · `.ccr` 四条 = T1 记录值（90823 `fb4a3b59…` / 90966 `592afa31…` / 135341 `ddec1ce6…` / 135484 `cd2af565…`，差恒 143B）· `--dump-objects` 3521 行 = T0 记录 · `.cir` DOT 165915B = T0 记录 · 五 CI job 全 rc=0 · 全枚举 58/58 · 自举链 `corec2==corec3` IDENTICAL + N06=0 + 冒烟 42 + `--help` rc=1 · **同源对拍 72 档零差异**（rc 分布两侧 34×0/38×1）· **探针 29 档逐档内容同 T0 记录**（rc 17×0/12×1）· 对 T0 起点的两档自源日志差异 = **纯行号偏移 +146**（= 本任务两文件净增行数，内容逐字同）。
  - **突变控制实测（判据 = M1/M2a/M2c′/M3；M2b 仅探索记录）**：M1 撤销加强 ⇒ **5 FAIL**（三例 t3 + N5 + N10）；M2a 类域守卫去掉 ⇒ **N11 转红**；M2c′ 不可判出口 `return 0 → return 1` ⇒ **N8 转红**；M3 次序对照（新行移到 `ak_disjoint` 之后）⇒ **零差异**（最小性）；M3b 同类条件去掉 ⇒ 既有 `exhaust.witness_bool` 转红（条件必要性）。回滚逐条源 sha 复原 ✓。**注**：调用侧空链守卫（`if cp < 0 || cq < 0`）单独删除 ⇒ **零差异**（与 `sum_chain_same` 内层 `p < 0 || q < 0` 行为等价 ⇒ 该行**无牙齿**，保留作显式域注释）。
- [x] **Step 6: 提交**（路径限定：`src/compiler/type_engine.cr` · `src/compiler/type_selftest.cr` · `src/ci/run.sh` · 本计划）

**停条件**：① 加强会翻转**任何生产可观测面**（`ty_exhaustive` / 诊断 / rc）⇒ **停下上报**（E-10 的「生产不可观测」前提被推翻）；② 域证明里出现**排序混排**（同一 (ak, slot) 既放令牌又放类型项）⇒ 停下（照 T3b 停条件）；③ 若收尾需要动 `te_elem_cmp`/`te_named_pair` 自身语义（非 ¬ 面）⇒ 停下（超章程）。

**实施记录（2026-09-14；隔离 workspace `e5ce8f9e` + 本任务两文件执行，主树零改动）**

- **停条件核查**：① 生产可观测面**未翻转**（72 档 rc + 日志零差异 / 探针 29 档内容同 / canary IDENTICAL / `.ccr` 四条同）✓；② 域内**无排序混排**（AK_SUM 槽 0/1 皆令牌，逐元素同一规则）✓；③ **未动** `te_elem_cmp` / `te_named_pair` 自身语义，亦**未复用** `te_chain_cmp`（其槽 ≥1 是类型项语义，对 AK_SUM 是错的槽位分派）✓。
- **勘误三条（已按此落地）**：① Step 3 落点 = `lit_implies` ¬ 面（`ak_disjoint` 只见类 id，按字面改 = 假不交）；② Step 2 域收窄 = **仅 AK_SUM 变体项**（T3b 排序表只可复用「链元素可判域」的事实，不可复用结论方向；参数化构造子/`⊤ₖ`/空链/`AK_NAMED` 各自维持原语义）；③ 附录 E-10 的「命名链 ⇒ 1」= **政策变更**，本批**维持 -1**（P0 终审 Important B + `named.not_disjoint` 在册）。
- **登记（本任务发现，未修）**：① `tt_display_at` **参数链分支**对带链原子渲染退化为 `>`（实测量：`sum1`/`seq1`/`ptr` 皆 `>`，裸原子 `int`/`unit` 正常）⇒ ICE04/反例诊断中参数化类型显示退化（**预存缺陷**，建议 TODO）；② 调用侧空链守卫 `if cp < 0 || cq < 0 { return 0; }` 与 `sum_chain_same` 内层检查**行为等价**（单独删除 ⇒ 零差异）⇒ **无牙齿行**，保留作显式域注释；③ 正×正（`lit_empty_pair:118`）同类异身份加强 = **单列候选**（经 `ty_inhabited → ty_exhaustive` 达 match 穷尽性 TM03 生产面，本批不碰）。
- **执行面**：主树当时含他任务在途改动（树不可编译）⇒ 本任务在**仓库外隔离 workspace**（`jj workspace add -r e5ce8f9e`）完成；提交内容 = 本任务两文件 + `src/ci/run.sh` 计数注 + 本计划勘误。

---

## Task 3：E-1 β——`.ccr` NOD 盘面项索引 + `CCR_VERSION` 8→9 + corearch `atom_of` 读回

**Files:** Modify `src/compiler/ccr_io.cr`（版本常量、`ESZ_NOD_DISK`、NOD 写/读、段界检）· `src/compiler/ccr_types.cr`（save 前把项索引装填进 NOD 语义对象；`ccr_seg_prepare_save` 的 D23 复用）· `src/compiler/ent_kernel.cr`（`ESZ_NOD_SEM`/`OFF_NS_*` 若扩槽）· `src/compiler/main.cr`（corearch 侧读回路径：项索引 → 取项 → `sh_atom_of_term` → 派生码；**若** corearch 发射路径读的是 `iri_tk`，则加「装载期解析一次」的时点）· `src/compiler/cir_cache.cr`（**仅当** Step 1 裁 (ii) 时才动——见下）· `tests/selfhost/test_ccr_types.py`（结构断言重锁：版本 9 / NOD 40B / 项索引落盘/读回对拍 / 版本拒收）· `tests/selfhost/test_ccr_v7.py`（若记录值受影响）· `src/ci/run.sh`（计数注）· `docs/superpowers/specs/2026-09-09-lattice-ir-v7-format.md`（NOD 记录 §3.3 + 版本族）。

**Interfaces（**Step 1 必须先裁两处，不得沿用附录 E 的数字当已决**）：**
```
// ── 先裁项 U-2：NOD 记录布局（附录 E-1 写「36B → 40B」= +4B 只容一个字段，
//    而该项同时点名「项索引 + 辅码」两个量）——
//    L-a: 既有 tk 字段保持「派生码」（辅码面节点即靠它）+ 新增 4B「项索引」（-1 = 无）⇒ 40B
//    L-b: 既有 tk 字段语义改为「项索引 ∪ 辅码」单槽（依 D22-② 互斥，一槽可承载二者之一；分类表判读）⇒ 36B 不变
//    判据：D20「纯函数值与其入参并存 = 第二真源」——L-a 存「码 + 索引」两者 ⇒ 须给 D20 合规论证；
//         L-b 无并存但读侧须能判定槽内是项还是辅码（依赖分类表 + 行可译性）
// ── 先裁项 U-3：本任务是否一并收口「暖态项面」（P5 T2 §5-④：缓存命中跳过 ir_gen ⇒ 暖态项表更小 ⇒ 项不可重建）
//    (i) 只落盘面/corearch 面（不动快照）⇒ 暖态项面继续登记；
//    (ii) 同批给 .cir 快照加项索引面 ⇒ 必须 bump CIR_CACHE_VER 17→18 + 记失效面
//    推荐：(i)（(ii) 收益 = 暖态项槽完整，无在野消费者；且 (ii) 把格式批从 .ccr 扩到 .cir 双格式）
```
- [ ] **Step 1: 两处先裁 + 布局表 + 版本拒收面**（U-2/U-3 写进文档 + 逐字段布局表 + `load_ccr` 的 `!=9` 拒收语义（照 D10：v8 整类拒收，不静默当空表）+ 失效面清单：在野 `.ccr`/缓存条目整体作废）。判据：布局表逐字段有依据 + D20 合规论证 + 失效面登记。
- [ ] **Step 2: 用例（红）≥10 例**：① 版本闸（v8 文件拒收 rc=1 + 诊断，**不得**静默）；② NOD 记录尺寸/字段序（读回逐字段）；③ **项索引落盘/读回对拍**（同一 `.ccr` 的 NOD 项索引 → TYPE 段取项 → `sh_atom_of_term` → 码 ≡ 盘上派生码）；④ 辅码面节点（`IR_BOUNDS_CHECK` 旗标 1 / `IR_DEREF` 宽度 8）在版式下的保真；⑤ 复合行节点（`ptr_ref_first` BINARY tk=10）保真；⑥ 类型面节点 `atom_of` 回读 ≡ 旧派生码（全语料逐节点）；⑦ 越界项索引 ⇒ 拒绝（三态，不得静默）；⑧ 段界检（记录增长后的 `seg_end3` 核算）；⑨ corearch `--dump-objects` 逐字节同；⑩ `CIR_CACHE_VER` 按 U-3 裁定（不动 17 / 或 18）。
- [ ] **Step 3: 实现**（写侧装填 + 读侧解析 + corearch 读回；**单源**：项索引与派生码同出 `emit` 期的那一次拆分，不得二次构造）。
- [ ] **Step 4: 判据（硬闸）**：**ELF canary IDENTICAL**（变即停下）· **`--dump-objects` 逐字节同**（两种形式）· `.ccr` 两种形式四条**预期变更**（版本/NOD 记录）⇒ **重锁记录值 + 逐段 delta 归因**（照 P3a T4/T5 的「逐段实测」先例）· `test_ccr_types` ≥40 → 实测值 · `test_ccr_v7` 全绿 · `selftest-types` ≥404 · 五 CI job + 全枚举 + `check src/compiler` rc=0 + 自举链 + `test_backend_bootstrap` · **冻结基线同源对拍 72 档零差异**（判定面不受格式面影响——**这是本任务最关键的隔离判据**）· **突变控制 ≥3 条**（项索引写错槽 / 越界项不拒 / corearch 读回退化为直接读盘上码 ⇒ 各自转红）+ 精确回滚。
- [ ] **Step 5: 文档**：format spec（NOD 记录 + 版本族 + D20 形态说明）· spec §6.3 状态行（**β ✅**）· `docs/error-codes.md`（若新增拒绝码）· TODO 条目。
- [ ] **Step 6: 提交**：`feat: R2 P6 Task 3——β：.ccr NOD 盘面项索引 + CCR_VERSION 8→9 + corearch atom_of 读回（发射面零泄漏）`

**停条件**：① U-2 两候选**都无法同时满足** D20 合规与读侧可判定 ⇒ 停下上报（须维护者裁布局）；② 版本 bump 若**被迫**同时动 `.cir` 快照格式（U-3 = (ii)）而超出批准面 ⇒ 停下（单独裁决）；③ canary 或 `--dump-objects` 变化 ⇒ 停下（发射面泄漏）；④ 段界检/读回出现任何「静默当空表」形态 ⇒ 停下（三态纪律）。

### 实施记录（R2 P6 Task 3，2026-09-14；**本批已实施**）

**先裁（Step 1 门）**：
- **U-2 = L-a**（40B = 32B 语义区 + 8B 邻接；新字段 `item i32` @+28，邻接域顺移 +32/+36）。L-b **排除**（单槽「项索引 ∪ 辅码」+ 分类表判读 = 读侧不可判定——分类表恰需被丢弃的派生码；反例 = `tests/suite/ptr_ref_first.cr` 的 `IR_BINARY tk=10`）。L-a 的 D20 合规论证三条：① 跨边界引用零新增（只引入文件内项索引 = D20-② 唯一许可形态）；② 「码 = 派生量」不变（两值同源于 `ccr_types.cr:ccr_nod_item_populate` 单次装填）；③ **分歧即硬拒**（`load_ccr` 的 TYPE 段后一致性校验：域外 / `atom_of(项) ≠ tk` ⇒ 拒绝 rc=1——消灭「盘面值静默胜出」的危害模型）。**登记未取**：更严单源形态 **L-c**（旧槽改载辅码、码完全装载侧重派生——无并存，但改发射路径输入 ⇒ canary/`--dump-objects` 由构造性降为须实测）；若维护者取 L-c，本轮写侧装填/校验/回读通道可整体复用（改动面 = 写侧旧槽写 0 + corearch 重派生 + 拒绝面收窄）。
- **U-3 = (i)**（协调者已裁）：只落盘面/corearch 面，`.cir` 快照不动、**`CIR_CACHE_VER` 保持 17**。依据 = 项索引只在 `ccr_type_populate()` 重建后的**文件空间**成立，而 `.cir` 写点（`main.cr:553`）早于该空间 ⇒ (ii) 只能落进程内索引 = D20-③ 禁令 + `bad_term=329` 红证；且快照已载派生码，保存期重派生即得 ⇒ (ii) 零收益。

**Files 勘误（实施面实际 = 计划 Files ✚ 补）**：
- ✚ `src/compiler/type_terms.cr`——`tt_atom_of_term` 自 `ty_shadow.cr` **整函数迁入**（corearch 清单不含判定衔接层，而 `load_ccr` 校验与回读通道需要它；B.6 三面核：两清单**均已**含本文件 ⇒ 无清单增删）。
- ✚ `src/arch/x86_64/regalloc.cr`——**L-a 下零改动**（corearch 读回实体 = `corearch.cr:243`（`--dump-objects`）+ `regalloc.cr:877`（`iri_set_tk(ni, nod_tk(ni))` 发射侧码落点）；计划把读回挂在 `main.cr` 是**文件归属错**）。
- ✚ `tests/selfhost/test_region_cfg.py`（`:283` 硬算式 36→40 + `:316` 版本断言 8→9——计划漏列的第三/四个格式断言点）。
- ✚ `src/compiler/ccr_types.cr` 的 D18 **强制**承载（写侧装填必须在 corec-only 文件；`ccr_io.cr` 在 corearch 清单内，不得直调判定衔接层——新增静态断言 `test_p6t3_d18_purity_static` 守护）。
- ✚ `src/compiler/corearch.cr`（`--dump-nod-items` 通道）。
- **路径勘误**：计划写的 `src/compiler/ent_kernel.cr` 实为 **`src/lattice/ent_kernel.cr`**。

**Step 3 时序改写（实读矛盾 ⇒ 落地形态）**：计划原文「**单源：项索引与派生码同出 `emit` 期的那一次拆分，不得二次构造**」**不可照字面实施**——`ccr_type_populate()`（`ccr_types.cr:54-81`）在 save 前 `tt_layer_reset()`+`sh_map_reset()` 并 按行序重建 ⇒ emit 期（`sh_tk_split`/DF 两槽）的项引用保存前**整体作废**；文件空间只在 `ccr_seg_prepare_save()` 之后成立。**实施约定**：写侧 = **保存期**由盘上派生码经**同一桥接函数**（`sh_tk_face_of_code` 分类 + `sh_term_of_ti` 建项）重派生（分类/建项单源，无第二套逻辑）；与 `.cir` 装载侧重派生（`cir_cache.cr:43-46/:521-523`）同构。**突变 M3 即此改写的机器抓手**（改用 `OFF_DF_TK` 活表索引 ⇒ loader 拒 ⇒ 转红）。

**锚点漂移（实施时实读，行号以实施树为准）**：`run.sh` 计数注 `:82/:86 → :85/:89`（T1 改写后）· `dyn_arr.cr` 契约注 `:104-107 → :103-118` · 计划 Task 3 Files 的 `main.cr`（见上）· `ent_kernel.cr` 路径（见上）。

**判据（全绿，逐条实测见 TODO #2026-09-14-3 / `.superpowers/sdd/p6-task3-report.md`）**：canary IDENTICAL · `--dump-objects` 两种形式逐字节同 · `.ccr` 四条重锁 + **delta = 4B×节点数**（pa +5192 = 4×1298；gt +7452 = 4×1863；形式差恒 143B）· 72 档对拍零差异 · 探针 29 档零差异 · 五 CI job + 枚举 58/58 + selftest 404/404 + `check src/compiler` rc=0 + 自举链 · `test_ccr_types` 40→48 / `test_ccr_v7` 27/27 / `test_region_cfg` 22/22 · 突变 M1–M5 转红 + 精确回滚。

**残留（登记）**：暖态项面为**部分**（缓存命中跳过 ir_gen ⇒ 暖进程未分配的类型行落 `item = -1`，码由 `tk` 保真）；冷态完整、冷/热各自确定性不变。

---

## Task 4：E-11 `never` 调用返回型（独立）+ E-12 调用位点诊断（挂裁-P6-2）

**Files:** Modify `src/compiler/checker.cr`（调用推断的返回型映射：被调返回行 = `TI_NEVER` ⇒ 调用推断 `TI_NEVER`；E-12 半：`unify_types` 返回值接线 + 非泛型调用位点定位 + 错误标记/级联抑制，照 T6 豁免③先例）· Modify `src/compiler/main.cr`（E-12 硬名单行——**仅在裁-P6-2(c) 批复后**）· Modify `docs/error-codes.md` · New `tests/selfhost/test_call_check.py`（若裁-P6-2 批复）/ Modify `tests/selfhost/test_let_check.py`（`never` 面）· Modify `src/ci/run.sh`。

- [x] **Step 1: RED（E-11，本任务自跑，双侧同证）**：`fn boom() -> never { loop { } }` + `x : int = boom();` ⇒ 现状 **TA02 误发**（机制：调用推断 = unit（`:1491` 钳位）⇒ 声明位豁免③ `:526` 不触发 ⇒ `type_compat_strict(TI_UNIT, TI_INT)` = 0 ⇒ `:530`）；`return boom();` ⇒ 现状 **TF01 误发**（机制：`EXPR_RETURN` 推断返调用值 = unit ⇒ `body_ti = TI_UNIT` ⇒ `:2234` 的 `body_ti != TI_NEVER` 成立 ⇒ `:2240`）；冻结基线**同值**（预存）。**另记**：本改动**必破**一条既有套件锁定（`test_iface_ops.py` 的 `t6_S1_never_unit` 今期望 TF01@2）⇒ 裁① 重新锁定。**实测（RED，合流树 `9cfa6246` + 未打补丁二进制）**：直调声明位 rc=1 + `error[TA02]: Variable declared as int, got unit` · 直调返回位 rc=1 + `error[TF01]: Function return type mismatch` · 变量传播位同 TF01 · 方法站点 rc=1 + TA02 · 死分支 build rc=1 + 无产物。**E-12 面**：`fn take2(n: int)->int` 以 `take2("s")` 调用 ⇒ 现状 rc=0 零诊断（TODO #2026-09-16-1 实证复跑；**锚点实核**：非泛型调用位点**今天不存在**——`EXPR_CALL` 的 SYM_FN 非泛型分支连实参推断都不走，E-12 实施 = 在该分支**新增**判定点）。
- [x] **Step 2: E-11 实现 + 用例 ≥6**：返回型映射补 `TI_NEVER`（**只改推断结果，不改发射**）；用例 = 两站点误报消除 + 正控（真异型仍报）+ 级联（`x : int = nosuchfn()` 仍只发 N06）。**实测（T4a 约定）**：落点 = 直调站点 + 方法姊妹站点两处守卫（后者含 `fi_generic_count == 0` 对齐；extern 面由 `EXPR_FN` 限定排除——裁④ 必需件）；**14 条用例**（主判据 5：声明位/返回位/变量传播/死分支 build+run/方法站点；正控 5：真异型 2 既有 + near-miss 负控 + 级联 N06 既有 + 语句位宽面；登记断言 4：泛型调用/extern/泛型方法/`EXPR_ASSIGN` TA01 保持）+ **重新锁定 1**（`t6_S1_never_unit` 改判 rc=0）。套件计数：`test_let_check` 23→（实读 24，注释陈旧自纠）→ **28** · `test_tf01_fallthrough` 19→ **22** · `test_iface_ops` 76→ **78**。
- [x] **Step 3: E-11 判据**：新/改套件全绿 · **ELF canary IDENTICAL**（**`never` 调用后的可达性/代码消除 = 本轮不做**；若 canary 变 ⇒ 停下，说明改到了发射面）· `.ccr` 四条冷态逐字节同 · 五 CI job + 全枚举 + 自举链 + `check src/compiler` · 冻结对拍**逐档裁决**（本任务预期出现**收紧面的反向** = 误报消失类 diff，逐条入清单）· 突变控制 ≥2（去掉 `TI_NEVER` 透传 ⇒ 两站点复红）。**实测**：canary `95084e7b…d475`（28822B）IDENTICAL · `.ccr` 四条 = T3 新锁值（`ptr_arith` 96015B `680a6f98…` / 96158B `76f36e6a…`；`generics_test` 142793B `41e9d845…` / 142936B `704316c8…`；差恒 143B）· 三套件 28/28 · 22/22 · 78/78 · **72 档对拍零差异**（误报消失类 diff 的载体 = **套件 leg**，不在 72 档语料；`parity_run.sh` 覆盖面实读）· 突变 **Mu1–Mu5**（见下方实施记录）。
- [ ] **Step 4: E-12 前置门（**须裁-P6-2**）**：报 report-only 清单（全语料 + 58 套件内联源 + examples），零命中 ⇒ 按裁定开门；非零 ⇒ 停下（清单审查：放宽 / 修语料 / 停）。**未取裁 ⇒ 本步止于 report-only 报告**（不发诊断、不开门），登记于 TODO。**实施锚点（T4a 侦查实读）**：新增判定点 = `EXPR_CALL` 的 SYM_FN 非泛型分支内（方法面同形块同款）；参数型取法二选一——`fi_param_type(fi, pi)`（自带越界护栏）或 EXPR_PARAM 链导航（照 `infer_gen_call` + 前扫，注意「形参不连续」教训 #21）；泛型面丢弃点与使用点维持实读值（零漂移）。
- [x] **Step 5: 提交**（分段）：`fix: R2 P6 Task 4a——never 调用返回型（两站点误报消除；发射面零变化）` / `fix: R2 P6 Task 4b——调用位点实参判定接线（TF07 复用 + report-only 清单；裁-P6-2 批复面）`。**本批只落 4a**（4b 挂裁-P6-2，未取裁 ⇒ 不实施）。

**停条件**：① E-11 使 canary 变化 ⇒ 停下（可达性变更超章程；归因入口 = `ir_gen.cr:1662-1663` 的 `new_ir_var("call", call_ti)`）；② E-12 的 report-only 命中面涉及**语义裁决**（如泛型体内实参位是否判）⇒ 停下上报；③ 命中面过大（需改自举源码）⇒ 转独立批；④ **E-11 落成后发现任何一处「泛型 / extern / 模块 / 赋值位」面被一并改动**（裁④ 越权）⇒ 停下（尤其 extern：守卫④ 缺失时 `t6_S3_never_unit` 会静默转绿——**该转绿是越权信号，不是收益**）。

---

## Task 5：文档/卫生批（E-7 残项 + E-8 + E-16）

**Files:** Modify `tests/selfhost/test_type_engine.py`（`MIN_CASES` 32 → **实测值 415**）· Modify `tools/module_to_ccr.py`（**文件头标废弃**：写 version=1 的远古 `.ccr`、零 in-repo 调用者、当前 corearch 整类拒收 ⇒ 使用该工具产出的文件必被拒）· Modify `docs/superpowers/specs/2026-09-11-explain-predict-incremental-design.md`（**5 处**：`:371` 三陈旧——`CIR_CACHE_VER` 旧值 14 → **17**、`cir_cache.cr` 行号 `:16` → **`:51`**、**magic `C1C1…` 分句删去**（该断言与实测不符：自举二进制落盘 magic 全 0，P4 T5/TODO #2026-09-10-10 登记）；`:84`/`:181` 旗标计数 `8 → 7`（`--type-shadow-dump` 已删）；`:127`/`:447` 先例载体改写）· Modify `docs/superpowers/specs/2026-09-11-policy-injection-design.md`（`:275` 同款改写）· Modify `TODO.md`（#53 条目 + 删除候选登记）· Modify 本计划（本节 + 侦查底盘陈旧锚，见下）。**`src/ci/run.sh` 零改动**（实施时逐条实测复核：其全部 `N 例` 注与实测一致——T2/T3/T4a 已各自同步；T1 提到的「14 档挂点」陈旧计数在**本计划**，不在 `run.sh`）。

- [x] **Step 1: `MIN_CASES` 收紧**：`:20` → **415**（实测）+ 注释写「只增不减（收紧为下限=实测；新增用例须同步下界）」。判据：`test_type_engine.py` 绿。**实测**：`PASS test_selftest_types_all_pass`（rc=0）。
- [x] **Step 2: 陈旧件**：`tools/module_to_ccr.py` 头注废弃（**不删**——删件属独立决定；登记「删除候选」入 TODO #2026-09-14-5）· `pkg/` **不属入库面**（`.gitignore` 覆盖 ⇒ 仅报告登记，**不进提交**）· 两 spec 的陈旧数字/行号/magic 分句修正。
- [x] **Step 3: 先例引用改写（E-16）**：`explain-predict` 4 处（`:84`/`:127`/`:181`/`:447`）+ `policy-injection` 1 处（`:275`）→ 「`--verify-*` 家族（默认关）+ 行为探针/结构性断言」，原则不变（审计通道默认关 / 两态产物同）；每处**保留历史陈述**（通道曾存在 + 已被 P5 T5 下线），只换现行载体。
- [x] **Step 4: 复核**（T5 已实跑，结论见下「实施记录」）：`type-shadow` **无活跃引用**（`src/` 8 + `tests/` 3 = 全为「已下线/历史」注；`docs/` 64 中仅本任务两 spec 的 5 处为修靶）；陈旧版本值在 `docs/ src/ tests/` **零引用**（修靶后）。
- [x] **Step 5: 判据**：五 CI job rc=0 + 全枚举 58/58 + `check src/compiler` rc=0 + canary IDENTICAL + `.ccr` 四条 = T3 新锁值 + 72 档同源对拍零差异 + `test_type_engine.py` 绿 + `test_backend_bootstrap` rc=0 + `error[`=0；**重建 ⇒ 三二进制逐字节同**（= 零行为变化的构造性证据，见下）。
- [x] **Step 6: 提交**：`docs: R2 P6 Task 5——文档/卫生批（MIN_CASES 收紧至 415 + module_to_ccr 废弃标注 + 两 spec 陈旧值与 --type-shadow 先例载体改写 + 计划勘误）`

**停条件**：① 若 `grep` 出 `--type-shadow` 的**非历史**活跃引用（代码/CI）⇒ 停下（并行的对照实现下线有残留）；② 若改 `MIN_CASES` 暴露实测值 < 32 ⇒ 停下；③ 若 `test_purity.py` 的 `MIN_CASES`（同形面）与本任务产生耦合（维护者要求同批收紧）⇒ 单独裁决（它不在 E-7 点名内）。**三条均未触发。**

### Task 5 实施记录（2026-09-14；工作树 = 合流树 `/tmp/p6mrg`（head `9bdbdf16`）；证据 `/tmp/p6t5/`）

**形态**：`MIN_CASES` 32 → **415**（+「只增不减」注）· `module_to_ccr.py` 头注废弃块（版本 1 / `ccr_io.cr:135` `CCR_VERSION = 8` 整类拒收 / 零 in-repo 调用者 / 保留不删）· `explain-predict` **5 处**（含 **magic 分句删去**）· `policy-injection` 1 处 · `TODO.md` #53 + 删除候选 · **计划勘误**（本节 Files/Steps 实况化 + 侦查底座陈旧锚 + T1 记录一句不精确）。

**判据（全部本实例实跑）**：**重建 ⇒ `corec ae01de75…` / `corearch 228f82e9…` / `corelsp 90eb19c6…` 与编辑前逐字节同**（T5 改动零 `.cr` ⇒ 二进制度不变 = 零行为变化的构造性证据）· 五 CI job 全 rc=0 · 全枚举 **58/58**（66s）· `selftest-types` **415/415** · `check src/compiler` rc=0 · canary **`95084e7b…d475`（28822B）IDENTICAL** · `.ccr` 四条 = T3 新锁值（96015B `680a6f98…` / 96158B `76f36e6a…` / 142793B `41e9d845…` / 142936B `704316c8…`，差恒 143B）· **腿① 同源对拍 72 档零差异**（冻结基线 `/tmp/p6t1/base/corec` × 当前源 vs 当前二进制 × 当前源；rc 分布 34×0/38×1 两侧同）· `test_type_engine.py` 绿（MIN_CASES=415）· `test_backend_bootstrap` rc=0 + `error[`=0 · 自举链 `corec2==corec3` IDENTICAL（`0b2e06d0…`）+ N06=0 + 冒烟 42 + `--help` rc=1。

**grep 复核（本实例实跑）**：`type-shadow`：`src/` 8 条 + `tests/` 3 条**全为「已下线/历史」注**（无旗标注册/无调用点——注册面三处实核 = `main.cr` / `corearch.cr` / `targets/x86_64-linux/main.cr`，`--type-shadow*` 零在位）；`docs/` 64 条中 5 处为本任务修靶、其余为历史计划/冻结记录（`type-shadow-findings.md` 已自 §16 冻结）。陈旧版本值（14）在 `docs/ src/ tests/` **零引用**；`MIN_CASES` 修靶后 = `test_type_engine.py:20`（415）+ 邻域 `test_purity.py:27`（26，**登记不并入**）。

**登记（不静默）**：① `tools/module_to_ccr.py` **删除候选**（本任务只标废弃；删除 = 独立决定）· ② `test_purity.py` 的 `MIN_CASES = 26`（同形面，未纳入 E-7）· ③ **T0 缺失实施记录归 T6**（T5 只登记该事实，**不动 T0 节**——理由：章程边界 / 与 T6 的「统一清单」同类 / 避免计划文件被两批各改一次）· ④ **T2 无 TODO 条目**（#50=T1 / #51=T3 / #52=T4a；建议 T6 补或由 T2 自补）· ⑤ 「14 档挂点」陈旧文本在**计划**（已随本任务勘误），`run.sh` 内无此计数（实核 `grep -n '\b14\b'` = 0）。

---

## Task 6：收官（回归 + 判据 + 清单 + 文档 + 尾批终态）

- [x] **Step 1: 全量回归**（cwd = 仓库根、`nice -n 19`、串行、比较前 `clean-cache`）：五 CI job 全 rc=0；全枚举 58/58（+ 本批新增套件）；`check src/compiler` rc=0；`selftest-types` 终值；构建确定性 ×2。**实测**：五 CI job 全 rc=0 · 全枚举 **58/58** · `check src/compiler` rc=0 · `selftest-types` **415/415** · 构建 ×2 **逐字节同**（`corec`/`corearch`/`corelsp` sha 两次比对 IDENTICAL）。
- [x] **Step 2: 判据复验**：**用入仓配方重建的基线**（T1 产物）跑同源对拍（72 档 rc + 日志逐档 diff）· 探针全集（入仓语料）· 突变控制快照 · ELF canary · 自举链（`cmp` IDENTICAL + N06=0 + 冒烟 42 + `--help` rc=1）· `.ccr` 两种形式（冷态、带标；T3 后为新锁值）· `--dump-objects` · `.cir` DOT。**实测**：`tools/baseline/rebuild.sh` rc=0 ⇒ 三 sha = 白名单（+ 冒烟 42）· 腿① 72 档`diff -rq` **零输出**（两侧 34×0/38×1）· 探针两态 **零差异**（29 档，17×0/12×1）· canary **IDENTICAL** · 链 `0b2e06d0…` + N06=0 + 冒烟 42 + `--help` rc=1 · `.ccr` 四条 = T3 新锁值 · `--dump-objects` **3521 行** · `.cir` DOT **165915B** `b1bdf480…` · `test_backend_bootstrap` rc=0 + `error[`=0。
- [x] **Step 3: 统一清单**：逐任务列 {实施 / 删除 / 收紧 / 放宽 / 修复 / 登记 / 偏差}——**与各任务的实际 diff 逐条对应**；四裁定（裁-P6-1..4）的**取裁结果与落地情况**单列一节；未取裁项列「已登记、未实施」。**交付** = 本计划「统一清单」节（7 小节；KPI = 实施 6 · 删除 0（+1 候选）· 迁移 1 · 收紧 3 · 放宽 1 · 修复 1（+1 并记）· 登记 19 · 偏差 9 · 未取裁 7）。
- [x] **Step 4: 文档**：spec §9 加 **P6 行（尾批）**：实施项 / 判据 / 剩余登记；spec §6.3（β ✅ 或「未实施」按 T3 实际）；format spec（版本族按 T3 实际）；TODO 新条目（P6 收官条目 + 四裁定结果 + 转独立批登记）；`CLAUDE.md`（若 T1 引入 `tools/baseline/` 或 `tests/probes/` ⇒ 在相应节补一行；`.cr` 清单无变化则不写）。**实测**：§6.3（β ✅）与 format spec 版本族（v9 行）**T3 已落**，本任务只加 **§9 P6 行** + **TODO #2026-09-14-7** + `CLAUDE.md` 一行（`tools/baseline/` + `tests/probes/` 已入仓，属测试/工具面新增 ⇒ 补一行；`.cr` 编译清单零变化）。
- [x] **Step 5: 尾批终态陈述（一页）**：类型接口化工作流 = P0–P6 全阶段终态；「已实施面 / 已裁定面 / 剩余登记面（含四裁定的未取裁者）」三栏；**下一批 = 容量批或验证切片轮**（按裁-P6-1 结果指向）。**交付** = 本计划「尾批终态陈述（一页）」节。
- [x] **Step 6: 提交**（路径限定；逐任务提交已在各 Task 内）。

**判据**：五 CI job + 全枚举 + canary + 自举链 + 同源对拍 + 探针 + 清单逐条可溯源 + 文档指针链闭合（spec §9 P6 行 → 本计划 Task 6 → 统一清单）。**全部满足**（实测见 Step 1/2）。

### Task 6 实施记录（2026-09-14；工作树 = 合流树 `/tmp/p6mrg`（head `4b909996`）；证据 `/tmp/p6t6/`）

**形态**：T6 收尾（**零 `.cr` 改动**——纯文档/清单面）：全量回归 + 判据复验 + 统一清单（7 小节）+ spec §9 P6 行 + TODO #2026-09-14-7 + `CLAUDE.md` 一行 + **Task 0 记录回填**（补记）+ 尾批终态陈述。

**判据（全部本实例实跑，证据 `/tmp/p6t6/`）**：构建 ×2 **逐字节同**（`sha1.txt` == `sha2.txt`，`cmp` IDENTICAL）· 五 CI job 全 rc=0（`check`/`bootstrap-tests`/`suite`/`selfhost-tests`/`full-bootstrap`）· 全枚举 **58/58**（66s）· `check src/compiler` rc=0 · `selftest-types` **415/415** · **ELF canary `95084e7b…d475`（28822B）IDENTICAL** · **入仓配方重建**（`tools/baseline/rebuild.sh`）rc=0 ⇒ 三 sha **逐条 = 白名单**（`5d2b15ad…`/`493dc490…`/`18b94bd9…`）+ 冒烟 42 + 工作区零残留 · **腿① 同源对拍 72 档零差异**（`diff -rq` 零输出；两侧 rc 分布 34×0/38×1）· **探针 29 档两态零差异** · `.ccr` 两种形式四条 = **T3 新锁值**（96015 `680a6f98…` / 96158 `76f36e6a…` / 142793 `41e9d845…` / 142936 `704316c8…`；差恒 143B）· `--dump-objects` **3521 行** · `.cir` DOT **165915B** `b1bdf480…` · 自举链 `corec2 == corec3` IDENTICAL（`0b2e06d0…`）+ N06=**0** + 冒烟 **42** + `--help` rc=**1** · `test_backend_bootstrap` rc=0 + `error[`=**0**。

**突变控制快照（继承 + 本批实跑，不重跑全表）**：T1 5 红（M1 语料掉档 / M2 伪条目 / M4 cwd / M5 rc 通道 / **M7 源级判定放宽 ⇒ 12 档 DIFF、5 档 rc 1→0**）+ 1 诚实未红（M3 去 clean-cache：`check` 面不读 `.cir` 缓存 ⇒ 本判据不可检出）· T2 M1/M2a/M2c′/M3（M2b 不作判据、M3b 仅记）· T3 M1–M5（写槽填派生码 / 删校验 / 活表索引 / 版本闸放水 / 只删交叉校验）· T4a Mu1–Mu5（两站点守卫 + 条件必要性 + extern 越裁探测）——**四任务各自的突变集逐条转红 + 精确回滚**，出处 = 各任务报告。

**停条件核**：**零条触发**——全部判据与协调者给定基线逐条相符（canary / `.ccr` 四条 / 链 sha `0b2e06d0…` / 415/415 / 58/58 / 72 档零差异）。

**待维护者处置项（不在本任务职权内，只登记）**：① **主树 `/home/DslsDZC/core` 工作副本 9 未提交文件**（`ccr_io.cr`/`ccr_types.cr`/`corearch.cr`/`globals.cr`/`ty_shadow.cr`/`type_engine.cr`/`type_selftest.cr`/`type_terms.cr`/`ent_kernel.cr` = T2 的两份过时草稿 + T3 抢跑残留）；② **主树 `build/*` 为 T3 味**（`corec aa7bdd5d…`/`corearch 28c5d669…` ≠ P6 起点白名单）⇒ 任何判据不得引用；③ **最终整合**（让主树采用本批终态 `4b909996` 链）涉及工作副本还原 ⇒ **须维护者明确许可**（铁律 #3）。

---

## 统一清单（P6 全批 T0–T5；**Task 6 收尾**）

> 约定（继承 P5）：每件给**证据**（为何敢动）与**替代/覆盖**（现在谁覆盖）；删除/收紧件与实际 diff 逐条对应；全语料命中数一律与**同任务起点**基线逐条比。**KPI**：实施 **6** · 删除 **0**（+1 候选）· 迁移 **1** · 收紧 **3 类** · 放宽 **1 类** · 修复 **1 件**（+1 并记）· 登记 **19** · 偏差 **9** · **未取裁 7**。

### 1. 实施（6 件；逐件 = 交付 / 判据 / 出处）

| # | 件 | 提交 | 交付 | 判据（实测） |
|---|---|---|---|---|
| 1 | **T0 前置侦查 + 冻结基线重建**（源码零改动） | 计划定稿 `bc4f0a43` 内 | 四表（A 重建 / B 起点 / C 语料与探针清点 / D 锚点）+ 守门复验 + `/tmp/p6t0/base` | 重建三 sha = 白名单 + `cmp` 与 `/tmp/p5t0/base` IDENTICAL ×3 + 冒烟 42；**U-4 = 29 档**；表 D 5 漂移 + 1 码名约定；守门复验（`test_optional` 34/34 · `test_enum_limit` 13/13 · 越界 P22/P23 硬错） |
| 2 | **T1 回归网可复现化（E-13）** | `e5ce8f9e` | `tools/baseline/{REBUILD.md,rebuild.sh,parity_run.sh,probes_run.sh}`（配方 + 白名单 + 双 runner；**零二进制入库**）+ `tests/probes/`（29 档实拷 + README 来源表）+ `run.sh` 自述块 + TODO #2026-09-14-2 | rebuild 41s ⇒ 三 sha = 白名单；入仓 vs 迁移 runner（同二进制）72 档零差异；腿① 零差异；探针两态零差异 + rc/log 29/29 同 P5 T7；五 CI job + 58/58 + canary + 链；**突变 5 红 + 1 诚实未红**（M3 去 clean-cache：`check` 面不读缓存 ⇒ 不可检出） |
| 3 | **T2 ¬ 面过判收尾（E-10）** | `911c692d`（兄弟；经 `9bdbdf16` 落地） | `type_engine.cr` +59 行（`atom_same_class_disjoint` + 接线；`ak_disjoint` 本体零改动）；`selftest-types` 404 → **415**（+11） | canary IDENTICAL · `.ccr` 四条 = T1 值 · `--dump-objects` 3521 行 · `.cir` DOT 165915B · 五 CI job + 58/58 · 链 · **72 档/探针零差异**（含对 T0 起点仅两自源档行号偏移的可归因）· 突变 M1/M2a/M2c′/M3（M2b 不作判据，M3b 附加证据） |
| 4 | **T3 β（E-1）** | `0819d506`（兄弟；经 `9bdbdf16` 落地） | NOD 记录 **36B → 40B**（+28 = TYPE 段文件空间项索引 `item i32`；`tk` 槽仍 = 派生码）+ `CCR_VERSION` **8 → 9**（旧版整类拒收）+ loader **一致性硬校验**（域外 / `tt_atom_of_term(项) ≠ 盘上码` ⇒ rc=1）+ corearch `--dump-nod-items`；`sh_atom_of_term` → 共享层 `type_terms.cr::tt_atom_of_term`；`test_ccr_types` 40 → **48** | canary IDENTICAL · `--dump-objects` 两种形式同 · `.ccr` 四条**重锁**（96015 `680a6f98…`/96158 `76f36e6a…`/142793 `41e9d845…`/142936 `704316c8…`；delta = 4B×节点数；形式差恒 143B）· 72 档/探针零差异 · 三格式套件 48/27/22 · 突变 M1–M5 五条转红 |
| 5 | **T4a `-> never` 调用返回型（E-11）** | 与合流同在 `9bdbdf16` | `checker.cr` 两站点守卫（直调 + 方法调用；四条守卫含 `EXPR_FN` 限定）；用例 +9（`let_check` → **29** · `tf01` → **22** · `iface_ops` → **78**）；重新锁定 1（`t6_S1_never_unit` 由误报 rc=1 改判 rc=0） | canary IDENTICAL · `.ccr` 四条 = T3 值 · 72 档零差异（**载体在套件 leg**）· 探针零差异 · 五 CI job + 58/58 · 链 `0b2e06d0…` · 突变 Mu1–Mu5 五条 |
| 6 | **T5 文档/卫生（E-7/E-8/E-16）** | `4b909996` | `MIN_CASES` 32 → **415**；`module_to_ccr.py` 废弃头注（**不删**）；两 spec **8 处**（三陈旧 + magic 分句删去 + 旗标 8→7 + 先例载体 ×3）；TODO #2026-09-14-5 + 计划勘误 | **重建 ⇒ 三二进制与编辑前逐字节同**（零 `.cr` 改动 = 零行为变化的构造性证据）· grep：`--type-shadow` 无活跃引用 · 陈旧版本值零引用 · 五 CI job + 58/58 + canary + `.ccr` + 腿①/探针零差异 |

### 2. 删除 / 迁移（0 + 1 + 1）

| # | 件 | 任务 | 证据 |
|---|---|---|---|
| — | **净删除 0 件**（本批无删件——P6 是「落地 + 收尾」批，非删旧批） | — | — |
| 1 | **迁移**：`sh_atom_of_term` 整函数 → `type_terms.cr::tt_atom_of_term`（共享层；因 corearch 清单不含判定衔接层） | T3 | 调用面机械改名（`type_selftest.cr` 7 处）+ 构建 rc=0；突变 M3 守护「save 期重派生」硬约束 |
| 2 | **删除候选（登记未删）**：`tools/module_to_ccr.py` | T5 | 写 version=1 远古 `.ccr`（`wu32(1)`）⇒ 现行 corearch 整类拒收；零 in-repo 调用者 |

### 3. 收紧（3 类；全语料命中逐条 = 0）

| # | 形态（前 → 后） | 任务 | 全语料命中 | 牙齿 |
|---|---|---|---|---|
| 1 | ¬ 面：同类原子**异身份**「判不含(0)」→「**确定不交(1)**」（仅 `AK_SUM` 变体项；`AK_NAMED`/参数化/`⊤ₖ` 面**维持原语义**） | T2 | **0**（72 档 rc/日志零差异 + 探针 29 档零差异；生产 `ty_exhaustive`/`ty_disjoint` 不受影响——正×正路径未动） | 突变 M1（撤销加强 ⇒ 三例 `*_over_claim` + 两例转红）· M2a（类域守卫去掉 ⇒ `&int` vs `&mut int` 误判）· M2c′（不可判出口改判 ⇒ 空链误判） |
| 2 | `.ccr` 版本闸：v8 → **v9**（`CCR_VERSION 8→9`）⇒ 旧版**整类拒收**（照 D10 先例，不静默当空表） | T3 | 0（语料/探针在 v9 下自洽；`test_ccr_types` ㊳ 锁定拒收面） | 突变 M4（版本闸放水 `!=`→`>` ⇒ 转红） |
| 3 | `.ccr` loader **一致性硬校验**：项索引域外 / `tt_atom_of_term(项) ≠ 盘上码` ⇒ **拒绝 rc=1**（把 D20 的「第二处定义」从静默分歧降级为可证伪不变量） | T3 | 0（L-a 布局下写侧单一来源 ⇒ 构造上一致） | 突变 M2（删校验 ⇒ 越界/不一致项被静默接受）· M5（只删交叉校验 ⇒ 域内非原子项被接受） |

### 4. 放宽（1 类）

| # | 形态（前 → 后） | 任务 | 全语料命中 | 覆盖 |
|---|---|---|---|---|
| 1 | `-> never` 函数的**调用**：推断 unit → **`TI_NEVER`** ⇒ 声明位 `x : int = boom()` 不再误发 TA02、返回位 `return boom()` 不再误发 TF01 | T4a | 72 档**零差异**（误报类 diff 的载体 = **套件 leg**：`t6_S1_never_unit` 由 rc=1+TF01@2 改判 rc=0） | 用例 +9 三套件；突变 Mu1–Mu5；裁④ 明确不动的同族面（泛型调用 / extern / 模块限定）登记 |

### 5. 修复（1 件 + 1 件并记）

| # | 形态（前 → 后） | 任务 | 根因 + 判据 |
|---|---|---|---|
| 1 | 两站点**误报**（TA02@声明位 / TF01@返回位）→ 零误报 | T4a | 声明注册趟的域守卫把 NEVER 钳成 unit（`never` 在 parser 侧走裸码路径）⇒ SYM_FN 类型 = unit ⇒ 两站点误报；判据 = 三套件 +9 例 + 突变 Mu1/Mu4 单点归因 |
| 2 | **并记**：¬ 面过判（witness 项 `B ⊆ ¬R` 判 0 → 1） | T2 | 语义过判；**生产不可观测**（T3b §5-② 推导）⇒ 归「收紧/正确性加强」（§3-#1），不单列修复件 |

### 6. 登记（19 件；逐条 = 归属 / 状态）

| # | 件 | 归属 | 状态 |
|---|---|---|---|
| 1 | `tools/module_to_ccr.py` 删除候选 | T5（TODO #2026-09-14-5） | 登记未删 |
| 2 | `test_purity.py` `MIN_CASES = 26`（与 E-7 同形面） | T5（TODO #2026-09-14-5） | 登记未改 |
| 3 | **T2 无 TODO 条目**（#50=T1/#51=T3/#52=T4a） | T6（本清单） | 待补（建议 #55；或并入 #54 两行指针） |
| 4 | `tt_display_at` **参数链渲染退化**（带链原子一律渲染 `>`；裸原子正常；影响 ICE04/约束反例诊断的参数化类型显示） | T2 报告 §6-2（**预存缺陷**，本批不改——`ty_shadow.cr` 非任何任务面且改诊断文本会动对拍面） | 建议入 TODO |
| 5 | `EXPR_ASSIGN`（赋值位）无 never 豁免 | T4a → TODO #2026-09-14-4 | 已登记 |
| 6 | 暖态项面**部分**（缓存命中跳过 ir_gen ⇒ 未分配行 ⇒ `item = -1`，码由 `tk` 保真；冷态完整） | T3 → TODO #2026-09-14-3 | 已登记 |
| 7 | E-5 `.csr`/TagNode 图消费面（零调用者） | 验证切片轮 | 维持登记 |
| 8 | E-15 单条目 >1GiB 分片（零真实用例） | 本批 | 维持登记 |
| 9 | **E-12 / #20** 调用位点诊断（「非泛型调用位点」实为**不存在** ⇒ 实施 = 新增判定点；锚点已入计划 Task 4） | 裁-P6-2 | 已登记、未取裁 |
| 10 | E-6 iterable 接线 / 命名消费语法 / 形状可写视图 | 裁-P6-4 | 已登记、未取裁 |
| 11 | 容量批（E-2 对象布局表示位 + E-3 `MAX_*` 解除 + E-4 可选程序收回 `.cir` 缓存） | 裁-P6-1 | 已登记、未取裁（推荐转独立批） |
| 12 | E-15 `string == int` 混比语义（双路径分歧） | 裁-P6-3 | 已登记、未取裁 |
| 13 | 非硬名单诊断不阻 `build`（TA01/TG02 等） | 裁-P6-2(b) | 已登记、未取裁（T0 语料内已有活证据：`ptr_arith` check rc=1 B04 而 build 出 canary） |
| 14 | `--verify-named-dedup` 保留（偏离 P5 D27） | 裁-P6-2(e) | 已登记、未取裁 |
| 15 | `*T ← 0` / `*T ← None` 是否作为 null 初始化放行（P5 T6 回退面 = 两站点） | 裁-P6-2(d) | 已登记、未取裁 |
| 16 | **T2 正×正面候选**（`lit_empty_pair`：正×正同类异身份 ⇒ 不交；语义成立但经 `ty_inhabited → tt_witness → ty_exhaustive` 达 **TM03 生产面**） | 裁（单列候选） | 已登记、未取裁 |
| 17 | **U-1** `tt_display` 零 `str_intern`「改造」的原始意图（源码已满足；P4 文档无出处） | 本批 | **无法追溯**（建议维护者补一句，否则按「已满足」关闭） |
| 18 | **U-2 的 L-c 备选形态**（更严的单源形态） | T3 §1.2 / 本计划 | L-a 已裁（授权面内，**可被维护者推翻**）；L-c 登记未取 |
| 19 | **主树遗留**：工作副本 9 未提交文件（T2 过时草稿 + T3 抢跑 7 文件）+ `build/*` 为 T3 味（`aa7bdd5d…`/`28c5d669…` ≠ 起点白名单） | **待维护者处置**（涉还原 ⇒ 铁律 #3 须明确许可） | 已登记（本清单 + T6 报告） |

### 7. 偏差（9 条；逐条 = 现象 / 处置）

| # | 现象 | 处置 / 出处 |
|---|---|---|
| 1 | **事故：T3 抢跑**（未取裁前把 T3 实作落进共享工作副本 7 文件；停手指令后仍在写 ⇒ `TaskStop` 硬停） | 零提交、零还原；T2 隔离到独立 workspace 收尾；T3 冻结后重跑。**护栏缺失（任务卡未写「先裁前不得落码」）记在协调者账上** ⇒ 后续含「先裁」的卡把护栏写进正文。出处 = `.superpowers/sdd/p6-incident-2026-09-14-t3-preempt.md` |
| 2 | **提交形态**：T2 `911c692d` 与 T3 `0819d506` 为**兄弟提交**，经合流态 `9cfa6246` 由 T4a 的 **`9bdbdf16` 双亲合并提交**一次性落地（未拆「合并 + T4a」两段） | 协调者已裁决**接受**（树内容正确，不做 rebase/split）。出处 = T4a 报告 §6-1 |
| 3 | **T4a 过程事故**：CI job **执行期间**改 `src/ci/run.sh` ⇒ bash 增量读取截断（`line 137: unexpected EOF`，rc=2） | 源**冻结**后重跑全部五 job（§4 的 5/5 = 重跑结果）。出处 = T4a 报告 §6-2 |
| 4 | **T3 计划偏离三处**：① Step 3 时序（计划「同出 emit 期拆分」→ 实为**保存期重派生**，突变 M3 守护）；② Files 漏项（`type_terms.cr` / `regalloc.cr` / `test_region_cfg.py`）；③ corearch 读回**归属错**（计划挂 `main.cr`，实为 `corearch.cr:243` + `regalloc.cr:877`） | 逐条写进文档；T3 报告 §6-1/§6-2 |
| 5 | **T1 计划偏离四处**：① `probes_run.sh` 为计划外增补（探针判据无 runner 则仍空洞）；② 「14 档挂点」陈旧计数在**计划**而非 `run.sh`；③ **CI 不挂钩**（浅检出 `fetch-depth: 2` + 无 `jj`）；④ 「探针源已丢」初判被 T0 更正（`p5t3b/probes` 是输出目录） | 逐条写进文档并已修正；T1 报告 §9 |
| 6 | **T5 计划偏离**：计划 Files 写「Modify `src/ci/run.sh`（计数注；若有变）」——实测**无变**（T2/T3/T4a 已各自同步） | 按「无变则不动」执行；改在计划文件（陈旧锚的真实所在） |
| 7 | **T0 记录未回填**（只出报告、不入库 ⇒ 计划内零留痕） | **T6 补记**（本计划 Task 0 实施记录 + 六步勾选；归属裁定 = T6） |
| 8 | **T2 执行面隔离**（主树当时含 T3 在途改动、树不可编译 `Undefined name: sh_atom_of_term` ×9）⇒ 全程在仓库外 workspace（`jj workspace add -r e5ce8f9e`） | 主树零改动/零提交/零还原；协调者合流。T2 报告 §6-6 |
| 9 | **T2 首版实现缺陷**（实错与修正：首版加强的负控全绿而缺陷未暴露 ⇒ 靠**新增 ⑩** 才抓出；负控由下限 4 提到 **11**） | 写进文档并强化用例；T2 报告 §3.3/§6-5 |

---

## 未决项与登记（**不猜**；Task 6 收尾后状态）

- **U-1（`tt_display` 零 `str_intern` 改造的原始意图）**：附录 E-7 首项写「`tt_display` 零 `str_intern` **改造**（P4 继承）」，但当前源码 `tt_display_at` **已**是只读零驻留（`ty_shadow.cr:535-536` 头注 + P5 T4 §4-9 的实测归因），P4 计划/报告中**未检出**该「改造」条目的原始出处 ⇒ **本计划无法确定**它要改什么。**处置**：Task 5 只做「现状复核 + 守门用例（`x2.no_str_intern` / `isat.no_str_intern_on_missing` / `ifc.mangling_zero_str_intern`）复核」，**不预设改造**。**状态 = 无法追溯**（维护者若知原意请补一句；否则按「已满足」关闭）。
- **U-2（T3 布局先裁）**：**已裁 = L-a**（40B = 旧 `tk` 槽保留派生码 + 新增 4B 项索引 + 装载期一致性硬校验），该先裁在授权面内（计划 Step 1 原文即「L-a … 须给 D20 合规论证」）。**可被维护者推翻**；更严的 **L-c 形态登记未取**（事故记录 §4 + T3 报告 §1.2 末）。
- **U-3（暖态项面是否同批收尾）**：**已裁 = (i)**（只落盘面/corearch 面；`.cir` 快照不动、`CIR_CACHE_VER` 保持 17）⇒ **暖态项面为部分**（未分配行 ⇒ `item = -1`，码由 `tk` 保真）——残留登记（TODO #2026-09-14-3）。
- **U-4（探针「30 档」的账）**：**已收尾 = 29 `.cr`**（T0 表 C：11+18+0；「30」= 29 + 迁移 runner 的字面 glob 伪条目）。「30」此后不得引用（T1 已修 runner + 入仓语料）。
- **四裁定门（裁-P6-1..4）状态**：**全部未取裁**（逐条 = 「裁决门」节；未取裁项一律未实施）。
- **登记维持（不做，理由写死）**：E-5 `.csr`/TagNode（消费者属验证切片轮）· E-15 分片（零真实用例）· `tools/module_to_ccr.py` 删除（本批只标废弃；候选登记）· `ty_shadow.cr` 文件名与 `sh_` 前缀改名（P5 T5 §7-2 登记，收益仅命名）· `test_purity.py` `MIN_CASES`（同形面，未纳入 E-7）。

---

## 尾批终态陈述（一页；P0–P6 全阶段；维护者先读）

**一、已实施面（本批交付，全部实测判据）**

| 面 | 终态 | 出处 |
|---|---|---|
| 回归网（判定面） | **四判据齐备且有载体**：① 冻结基线同源对拍（**入仓配方 `tools/baseline/rebuild.sh` + sha 白名单**；三 sha 重建可复现）② 行为探针（**入仓 29 档** `tests/probes/` + runner）③ 突变控制 ④ 三态纪律（ICE04/D23/版本闸） | T1 + 本计划「Global Constraints」 |
| 载体（`.ccr`） | **v9**：NOD 40B 承载 **TYPE 段文件空间项索引**（+ `tk` 派生码保持）+ loader 一致性硬校验 + corearch `--dump-nod-items` 读回 ⇒ 项引用**跨进程可解析**（spec §6.3 兑现） | T3 / spec §6.3 |
| 判定引擎 | ¬ 面同类原子异身份 ⇒ **确定不交**（`AK_SUM` 变体项）；`AK_NAMED`/参数化/`⊤ₖ` 面维持原语义（政策不动） | T2 |
| 调用位点推断 | `-> never` 调用 ⇒ `TI_NEVER`（两站点误报消除；同族三面按裁④不动） | T4a |
| 回归判据终值 | `selftest-types` **415/415** · 全枚举 **58/58** · canary `95084e7b…d475`（28822B）IDENTICAL · 自举链 `0b2e06d0…` + N06=0 + 冒烟 42 · `.ccr` 四条 = 96015/96158/142793/142936（差恒 143B）· 72 档对拍零差异 · 探针 29 档零差异 | T6 §Step 1/2 |
| 文档/工具 | `MIN_CASES` 收紧至 415（只增不减）· `module_to_ccr.py` 废弃标注 · 两 spec 陈旧值与先例载体改写（`--type-shadow` → `--verify-*` + 行为探针） | T5 |

**二、已裁定面**：P5 裁-1（β 归 P6）**已兑现**（T3）· 裁-4（容量批 P6 合批）→ **现提请改判**（见下）· P5 D20/D22/D23/D28 与 TODO #2026-09-11-9 判据标准**全批未动**（继承有效）· U-2 = L-a（授权面内，可推翻）· U-3 = (i)。

**三、剩余登记面**（19 件，逐条 = 「统一清单」§6；其中影响下一批指向的四条）

1. **裁-P6-1 容量批**（E-2 对象布局表示位 + E-3 `MAX_*` 解除 + E-4 可选程序收回 `.cir` 缓存）：**推荐转独立批**（= 新能力面；在野需求 0；四条前置无一满足）。**下一批指向 = 容量批（若裁「实施」）或验证切片轮（若裁「转批」）**。
2. **裁-P6-2 政策五问**：ICE04/P-A 签核 · 非硬名单诊断是否收紧 · 入闸判据成文化 · `*T ← 0/None` · `--verify-named-dedup` 保留——**全未取裁**（E-12 的落地挂此）。
3. **裁-P6-3 / 裁-P6-4**：`string == int` 混比语义 · `EXPR_FOR`/iterable 是否入批——**全未取裁**。
4. **待维护者处置（涉铁律 #3 还原面）**：主树工作副本 9 未提交文件 + 主树 `build/*` T3 味 ⇒ **最终整合须明确许可**。

**四、多 agent 事故与护栏（记在账上）**：T3 抢跑（`.superpowers/sdd/p6-incident-2026-09-14-t3-preempt.md`）——**零提交、零还原、零文件丢失**；护栏措施 = 「含先裁的任务，护栏写进任务卡正文」；T4a 的 CI 期间改源事故（rc=2）⇒ 处置 = 源冻结后重跑五 job（已入清单 §7）。

---

## 自检记录

- **分诊的诚实性**：16 项**未**凑数——7 项实施中有 2 项（E-7 的 `tt_display` 子项、E-8 的 `pkg/`）**实测后缩水为「复核/登记」**（前者已满足、后者非入库面），1 项（E-15 分片）明确降为登记（零用例）；7 项待裁**逐条点名问句**（裁-P6-1..4），未用「推荐」冒充裁定；唯一「新能力」性质的实施项（β）**有既有裁定背书**（P5 裁-1 + spec §6.3）并附两处**先裁项**（U-2/U-3）。
- **根因 vs 止血**：① T1 是把判据从 `/tmp` 里**救出来**（实测证据 = `p5t3b/probes` 的 `.cr` **已丢**、runner 对空目录**静默空跑**——不修则「零差异」会退化为**空洞证据**，违三态纪律）；② T2 直达 `ak_disjoint` 的相斥判据（不在 `sub_cover` 加特例）；③ T3 走 D20 的**许可形态**（TYPE 段文件内项索引），**不**落进程内索引（`bad_term=329` 红证）；④ T4a 只改**推断结果**、明令发射面零变化（不做可达性消除——那是另一个语义变更）。
- **与既有裁决的一致性复核**：spec §6.3（β 前置已齐 → T3）· §6.4/§10（E-5 归验证切片轮）· P5 裁-1/裁-4/裁-2/裁-3 的边界（β 属 P6、容量批转独立批、命名面判定化不得回退、并行的对照实现不得复活）· D20（稳定引用禁令）· D28（两种形式 + 冷/热豁免）· TODO #2026-09-11-9（判据标准，不得回退「逐字节同旧版」）· #24/#30/#32/#35/#43/#44/#45–#49 的已收尾面**一律不动**。
- **占位符扫描**：无 TBD；「待实测」标签逐处给出测量命令或出处；三处**先裁/未决**（U-1 无法追溯、U-2/U-3 需 Task 3 内先裁、U-4 需 Task 0 清点）明写，**未预填数字**。
- **数字约定（全部实读或既有记录值；出处已标）**：P5 判据终值（`p5-task7-report.md` §0/§2）· 冻结基线三 sha（**本实例 sha256sum 实核**）· runner sha `12dc571f…`（实核）· 探针计数 29 `.cr`（本实例 find 实核）+ `p5t3b/probes` 零源（实核）· `CCR_VERSION=8`/`ESZ_NOD_DISK=36`（`ccr_io.cr:135/:143`）· `CIR_CACHE_VER=17`（`cir_cache.cr:51`）· `ESZ_DFNODE=72`/`OFF_DF_TK=40`/`OFF_DF_AUX=64`（`dyn_arr.cr:119/:122/:124`）· `MIN_CASES=32`（`test_type_engine.py:20`）· `MAX_*`/镜像 64（`ast.cr:115-118/:143-145`）· 硬名单八条（`main.cr:152-174`）· `unify_types` 丢弃点（`checker.cr:2000`）· TF07 单 raise（`checker.cr:3683`；`ast.cr:423`）· `EXPR_FOR`（`checker.cr:2929-2940`）。**凡「≥N 例」皆为下限**。
- **本计划的自我限制（诚实登记）**：本计划**未跑任何构建/测试**（纯只读侦查 + `sha256sum`/`find`/`ls`/`jj status` 级实核）；所有「现状值」均为实读源码/文档或既有报告记录值；需实测的数字（重建 sha、探针账、β 的记录值 delta）一律留给实施任务的 RED 步。**U-1 无法确定**（原始意图不可追溯）、**U-2/U-3 留给 Task 3 先裁**、**U-4 留给 Task 0 清点**——均已显式登记，未猜。
- **风险面**：① T3 的格式级联（本批最大风险）——缓解 = Step 1 先裁 + 版本闸（整类拒收）+ 隔离判据（判定面对拍零差异）+ `--dump-objects` 逐字节同 + 突变三条；② 基线重建不可复现（⇒ E-13 前提崩，T0 停条件）——缓解 = T0 首步实测 + 与 `/tmp` 副本 `cmp` 交叉；③ 探针重建失真——缓解 = 逐档「重建依据」注 + 与 P5 记录 rc/诊断逐条比 + 对拍判据自证；④ 收紧面（T4b）命中非空——缓解 = report-only + 裁-P6-2 前置 + 停条件；⑤ 多 agent 并发——路径限定提交 + 一构建一编译串行。
