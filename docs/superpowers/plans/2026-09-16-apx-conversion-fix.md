# apx（dex）形式转换缺口族——写点/调用面覆盖 + 声明型触发（#80/#81 同批修，⑦a 顺带）

**日期**：2026-09-16 · **性质**：**计划**（本轮只读侦查 + 落纸 + 裁决门；**未取裁前不得落码**）
**起点**：`develop = 69cf3d09`（本分支基点；分册 bookmark `feature/apx-conversion-fix`）
**输入**：`docs/superpowers/specs/2026-09-16-apx-conversion-audit.md`（设计面）· `TODO.md` **#80 / #81 / #78**（登记面）· 上批计划 `docs/superpowers/plans/2026-09-16-global-operand-seams.md`（体例母本）
**铁律**：禁止 `git`（一律 `jj`）· 路径限定提交 · 写文件后立刻 `jj status` · 长编译 `nice -n 19` · 先裁后码 · 三态纪律

**Architecture:** ① **T1 前置侦查 + 冻结基线 + 插桩实证**（零源码改动：逐条实测表 B 的**当前行为**，含审计 B-2 的读码反例裁定实验与 #81 的插桩点 A/B）② **T2 先裁落纸 + 四表完备性枚举**（转换调用点 / 写点 / 读点 / 调用形态，**零未分类**）③ **T3 唯一漏斗 + 写点接线**（聚合四类 + 全局初值）④ **T4 调用环重写**（直调 + 方法调用；extern 保持）⑤ **T5 探针套件 + 突变控制** ⑥ **T6 判据复验**（Global Constraints 全绿）⑦ **T7 收官**（台账 + 报告 + 路径限定提交）。

---

## §0 形式模型与契约（**实读**）

| 形式 | 常量 | 语义 | 默认世界 |
|---|---|---|---|
| 精确（scaled） | `TI_DEX_S = 8`（`src/compiler/ast.cr:274`） | 定点缩放整数，S = 10⁶ | **跨函数/跨存储边界统一形式**（`ir_gen.cr:971-972`） |
| 近似（apx） | `TI_DEX = 1`（`ast.cr:262`） | binary64 位模式（快路径） | 仅 **`apx` 标签的 LET 槽**（`parser.cr:819`：apx 是 **LET 专属标签**） |

**关键推论（本批的立论基石）**：`apx` 是 **LET 专属标签** ⇒ **形参 / 返回值 / 结构体字段 / 数组·切片元素 / 元组元素 / 枚举载荷 / 全局槽** 这些面**一律不存在 apx 形式**，其规范存储形式**恒为精确（scaled）**。因此：
- 这些面的写点规则可**不依赖声明面**：任何 `TI_DEX` 值写入聚合/全局槽 ⇒ 必须转 scaled；
- 调用实参面**依赖形参声明**（`TI_DEX` 形参在 callee 被强制为 `TI_DEX_S`，见下）⇒ 用 `fi_param_type` 即可，无需类型表。

**转换机构（4 个 helper，全在 `src/compiler/ir_gen.cr`）**：`dex_bits_to_scaled`（`:982`）· `dex_scaled_to_bits`（`:1016`）· `dex_scale_int`（`:1033`）· `dex_store_adjust`（`:1045-1051`，**按目标槽 IR 型**分派前两者）。契约自注释在 `:973-976`：「**存储/边界处按槽位形式转换**」。

**转换调用点全集（11 处，逐条实读命中）**：`1253 / 1259 / 1272 / 1273`（二元操作数对齐）· `1349`（局部赋值）· `1366`（全局赋值）· `1703`（`@raw_int`）· `1827 / 1832`（**调用实参环**）· `2424`（LET）· `2495`（返回点）。

**两后端在调用边界零 dex 逻辑**（实读）：ELF `cs_args_dispatch`（`src/os/linux/callseq.cr:66-89`）按 `irv_type` 分类进 XMM/int；`cs_arg_on_stack`（`:95-107`）按**类型类计数**判落栈；解释器 `interp.cr:240-245` **按位置拷贝实参、无任何 dex 判断** ⇒ **环漏转一个实参 ⇒ 双路径必同错**（同一个坏 IR）。

---

## §1 审计坐标校正（**以实读为准**）

> 纪律：审计 spec 的坐标**逐条实读复核**；不符者标「审计坐标已校正」，并给实读值。

| # | 审计给的坐标 | **实读值** | 差异 |
|---|---|---|---|
| `dex_scale_int` | `:1027` | **`:1033`**（`dex_bits_to_scaled` 982 · `dex_scaled_to_bits` 1016 · `dex_store_adjust` 1045 均命中） | **校正 +6** |
| 调用实参环门 | `:1806` `ast_kind(func_node)==EXPR_IDENT` | **`:1806`**（环体 `:1806-1848`；bits→scaled 在 `:1832`；extern scaled→bits 在 `:1827`） | 命中 |
| 方法调用 self 入参 | `:1779-1786` | **`:1776-1786`**（`EXPR_FIELD` 分支；self 写入 `arg_vars[0]` 在 `:1783-1785`） | 命中（范围微调） |
| 形参在 callee 物化 | `:2863-2867` | **`:2855-2867`**（`param_type := ast_type_val(pn)` `:2863`；`if param_type == TI_DEX { = TI_DEX_S }` **`:2866`**） | 命中 |
| 返回点转换门 | `:2494` | **`:2494-2496`**（转换调用 `:2495`） | 命中 |
| 结构体字段**读** | `:2520`（`new_ir_var("field", TI_INT)`） | **硬定型在 `:2513`**（`v := new_ir_var("field", TI_INT)`）；`IR_LOAD_FIELD` 发射在 `:2520` | **校正**：型丢失点 = `:2513` |
| 数组/切片元素**读** | `:2583`（`IR_LOAD_INDEX`）· `:2591` | **硬定型在 `:2578`**（`v := new_ir_var("elem", TI_INT)`）；发射 `:2583` / `:2591` | **校正**：型丢失点 = `:2578` |
| 结构体字面量字段写 | `:2643-2646` | **`:2646`**（可选装箱）**`:2647`**（`IR_STORE_FIELD` 发射） | 校正 +1..+3 |
| 字段赋值写 | `:1344-1346` | **`:1374-1381`**（分支头 `:1374`；发射 `:1380`；`:1344-1349` 是**局部赋值**，非字段面） | **校正**：审计把局部赋值与字段赋值混列 |
| 数组字面量元素写 | `:2690-2700` | **`:2690-2695`**（发射 `:2695`） | 命中 |
| 下标赋值写 | `:1362` | **`:1383-1413`**（发射 `IR_STORE_INDEX` **`:1403`** / `IR_STORE_INDEX_VAR` **`:1410`**） | **校正**：审计给 `:1362` 是全局赋值的 `find_global` 邻域 |
| 元组元素写 | `:2818-2825` | **`:2821-2822`**（发射 `:2822`） | 命中 |
| 枚举载荷写 | `:2556-2560` | **`:2610-2611`**（分支头 `:2598`；发射 `:2611`，`fi+1` tag 偏移） | **校正 +54**（`:2556-2560` 是 `IR_SLICE` 邻域） |
| 枚举载荷读（match 绑定） | 「`:2520` 同形」 | **另有点：`:2316`**（`fv := new_ir_var("fld", TI_INT)`；发射 `:2318` / `:2335`） | **补全**：审计未列此独立硬定型点 |
| 全局初值写 | `:3099-3101` | **`:3093-3098`**（可选装箱 `:3096`；`emit(IR_STORE, …)` **`:3097`**） | **校正** |
| `reg_one_global` | `:3114+` | **`:3106-3155`**（dex 型判定 `:3119-3131`；apx 静态初值取节点 a `:3138-3151`） | 命中（范围锚定） |
| `as` 转换 | `:2749-2767` | **`:2749-2767`**（`IR_LOAD` 纯拷贝 `:2765`） | 命中 |
| `@raw_int` | `:1703` | **`:1699-1708`**（bits→scaled 在 `:1703`） | 命中 |
| 二元对齐 | `:1248-1273` | **`:1246-1273`**（TI_DEX 支头 `:1249`；TI_DEX_S 支头 `:1269`） | 命中 |
| 调用环结果定型 | `:684-690` | **`:679-695`**：该函数名是 **`ir_call_return_type`**（`:666`），**非审计所称 `fn_return_ti`** | **命名校正（关键，见下）** |
| `frame.cr` 落位公式 | `:129/131` | **`:133`**（`caller_off := 16 + (pi - 6) * 8`，9+ binary64 支）与 **`:139`**（`pi >= 6` 通用支）；`:129-130` 是 XMM 保存的 movsd 编码 | **校正** |
| `g_cur_ret_ti` 来源 | （审计未给） | **`:2843`**（`ret_ti := fi_return_type(fi)`）→ **`:2927`**（`g_cur_ret_ti = ret_ti`） | **新增坐标（决定 §2 的裁定）** |
| 元组下标语法 | `t . 0` | **`t.0`**（`parser.cr:359-366`：`T_DOT` 后 `T_INT` ⇒ `tv = iv + 1`） | **探针语法校正** |
| 泛型函数语法 | （审计未给） | **`fn name[T](…)`**（`tests/suite/generics_test.cr:11/21/27`） | **探针语法补充** |

### 1.1 **两条实质性的审计结论校正**（不是坐标漂移，是结论）

**校正 A —— 审计表 A 行 ②a（B-2「返回点转换恒不触发」）读码判定不成立。**
审计断言：门 `:2494` `g_cur_ret_ti == TI_DEX`，而 Core `-> dex` 使 `fn_return_ti` 返 `TI_DEX_S`（`:684-690`）⇒ 门恒假。
**实读**：`:684-690` 属于 **`ir_call_return_type`**（调用结果定型用，与返回点门无关）；而 `g_cur_ret_ti` 取自 **`:2843 ret_ti := fi_return_type(fi)`** → `:2927`。`fi_return_type` 存的是 **`rtv`（`TY_*` 码）**（`parser.cr:1434-1461`：`add_func(fn_name, pc, rtv, fn_node)`，`rtv := unpack_type(rt)`），而 **`TY_DEX == TI_DEX == 1`**（`ast.cr:106` / `ast.cr:262`，两组常数同序编号）⇒ **`g_cur_ret_ti == TI_DEX` 为真**，返回点 `dex_bits_to_scaled` **会触发**。
**旁证（强）**：`tests/suite/dex_test.cr:63-66`（`dex_apx_ret_fn`：`v : dex, apx = 1.0; return v / 2.0;`）与 `:53-54`（断言 `b2 == 0.5`）**正是 B-2 同形**，且该档在 `suite` job 每轮实跑 ⇒ 若门恒假，该档早红。
⇒ **B-2 预期 = 现状已绿**，本批降级为 **P0-confirm（非回归钉子）**，不进 RED 集合；T1 用一行探针终裁（**停条件见 §9**）。

**校正 B —— 审计 B-7 的探针零区分力（语言语义未定之外的第二问题）。**
审计 B-7 探针 `d : ., apx = 1.5; x := d as dex; return @raw_int(x) / 1000000;`：
- 读法甲（`as` = 恒等拷贝）⇒ `x` 槽型 `TI_DEX` 持 bits ⇒ `@raw_int` 内部 `dex_bits_to_scaled` ⇒ 1500000 ⇒ `1`；
- 读法乙（`as` = 形式转换）⇒ `x` = scaled 1500000 ⇒ `@raw_int` 原样 ⇒ `1`。
**两种读法同答 1** ⇒ 该探针**无法区分 `as` 语义**。本计划给出替换探针（§4 B-7′：**源为精确值**，两读法答 1 vs 1500000），并保留「待维护者问用户」标注。

---

## §2 现状根因（一句话 + `file:line`）

> **dex 形式转换契约（「存储/边界处按槽位形式转换」）只在 5 类落点接线，而跨函数边界的唯一转换通道——调用实参环 `ir_gen.cr:1806-1848`——以 `ast_kind(func_node) == EXPR_IDENT`（`:1806`）为门，方法调用（`EXPR_FIELD`）整段跳过（#80）；两后端在调用边界只做运输、零 dex 逻辑（`callseq.cr:66-107` 按 `irv_type` 分类/计数；`interp.cr:240-245` 按位置拷贝）⇒ 漏转的实参在**两条路径上一致地错**（#81）。同时聚合四类写点（字段字面量 `:2647` / 字段赋值 `:1380` / 下标赋值 `:1403`·`:1410` / 数组字面量 `:2695` / 元组 `:2822` / 枚举载荷 `:2611`）与全局运行期初值（`:3097`）**无漏斗**，聚合读点硬定型 `TI_INT`（字段 `:2513` / 元素 `:2578` / match 绑定 `:2316`）⇒ 一切「按 IR 值型触发」的下游转换连锁失效（#78，放大器）。**

**逐条支撑（全部实读）**：
1. **环是唯一实参转换通道**：转换调用点全集 11 处（§0），实参面只有 `:1827`（extern scaled→bits）与 `:1832`（Core bits→scaled），二者同在 `:1806` 的门内。
2. **环门拒方法调用**：`:1806` 要求 `ast_kind(func_node) == EXPR_IDENT`；方法调用走 `:1776-1786` 的 `EXPR_FIELD` 分支 ⇒ `:1806` 判否 ⇒ 整段 `:1812-1847` 不执行。
3. **callee 恒精确**：`:2866` 把 `dex` 形参一律改 `TI_DEX_S` ⇒ callee 侧**没有任何 binary64 类形参**（推论：`frame.cr:119-137` 的 XMM 形参保存支对声明 dex 形参**不可达**，见 §9 U5）。
4. **后端按实参 IR 型分类**：`cs_args_dispatch:70-86`（`TI_DEX` → XMM0-7 / 否则 int 寄存器）、`cs_arg_on_stack:95-107`（**按类型类计数**判落栈）、`cs_stack_args:122-138`（`TI_DEX` → `e2_sd_ld_var` + `push xmm0`）——三者**全部按 `irv_type`**。callee 侧按 **形参序号** 定位栈参（`frame.cr:133/139`：`caller_off := 16 + (pi-6)*8`）⇒ **caller 按类型类、callee 按序号**，两类判据不同源（#81 的次错候选，§9 U3）。
5. **聚合/全局写点无漏斗**：见上「逐条」列出的 7 个发射点，**全部无 `dex_store_adjust`**；`inject_global_inits`（`:3067-3103`）只有可选装箱（`:3096`）。
6. **聚合读丢型（#78）**：`:2513` / `:2578` / `:2316` 三处 `new_ir_var(..., TI_INT)` ⇒ 读结果的 IR 型恒 `TI_INT` ⇒ 下游「按值型触发」的判定（二元对齐 `:1249`、LET/赋值的 `dex_store_adjust` `:1045`、返回门 `:2494`）**全部不触发**。

---

## §3 活点清单（逐条 = 坐标 / 形态 / 可达性 / 现象 / 修复归属）

| # | 坐标（**实读**） | 形态 | 可达性 | 现象 | 归属 |
|---|---|---|---|---|---|
| **L1** | `ir_gen.cr:1806`（门）→ `1812-1847`（环体） | 调用实参环门 = `EXPR_IDENT` | **可达**（口径方法调用是主语法） | 静默错值 · 双路径同错 | **本批**（T4） |
| **L2** | `ir_gen.cr:1380` | 字段赋值写点无漏斗 | 可达（`s.f = d`，d = apx） | 静默错值 | **本批**（T3） |
| **L3** | `ir_gen.cr:2647` | 结构体字面量字段写点无漏斗 | 可达 | 静默错值 | **本批**（T3） |
| **L4** | `ir_gen.cr:1403` / `:1410` | 下标赋值（常量/动态）写点无漏斗 | 可达（`a[0] = d` / `a[i] = d`） | 静默错值 | **本批**（T3） |
| **L5** | `ir_gen.cr:2695` | 数组字面量元素写点无漏斗 | 可达（`[d, 1.0]`） | 静默错值 | **本批**（T3） |
| **L6** | `ir_gen.cr:2822` | 元组元素写点无漏斗 | 可达（`(d, 1.0)`） | 静默错值 | **本批**（T3） |
| **L7** | `ir_gen.cr:2611` | 枚举载荷写点无漏斗 | 可达（`V(d)`） | 静默错值 | **本批**（T3） |
| **L8** | `ir_gen.cr:3097` | 全局运行期初值写点无漏斗 | **有条件可达**：apx 全局 + 运行期初值（`g : dex, apx, mut = f()`，f 返回 scaled）/ 非 apx 全局 + extern 初值（**静态无 `.so` 不可真跑**） | 静默错值 | **本批**（T3；extern 形判据降级到发射字节） |
| **L9** | `ir_gen.cr:2513` / `:2578` / `:2316` | 聚合读硬定型 `TI_INT` | 可达（一切经聚合读的 dex 值） | 静默错值（下游不触发） | **#78（后续批）**；本批只做**作用域内的比较点声明面查表**（见裁-APX-1） |
| **L10** | `ir_gen.cr:1249-1268` | 二元对齐「有 `TI_DEX` 操作数 ⇒ 快路径」 | 可达（经 L9 读入的操作数**唯一**错误来源） | 静默错值（B-8） | **本批**（T3 的比较点声明面查表） |
| **L11** | `ir_gen.cr:2749-2767` | `as` = 纯 `IR_LOAD` 拷贝 | 可达，但**语义未定** | 未定 | **待裁（裁-APX-3 / U1）**，**不进 RED** |
| **L12** | `frame.cr:133` vs `callseq.cr:101-105` | caller 按类型类 / callee 按形参序号 | **机理未实证**（须 T1 插桩） | 可能次错 | **T1 裁定**（裁-APX-6） |
| **L13** | `frame.cr:119-137` | XMM 形参保存支 | **读码判定 = 不可达**（`:2866` 恒把 dex 形参改 `TI_DEX_S`） | —（死码） | 登记（U5），本批不动 |

---

## §4 探针设计表（表 B′：审计 10 条 + 本批新增 4 条）

**通用判据网（**替换「逐例钉值」的通用面**）**：
- **C1 双形对拍**：同一数值经 **apx 形** 与 **精确形** 两条路写入/读出 ⇒ 可观测结果必须**一致**；
- **C2 面覆盖对拍**：同一值经**未覆盖面**（字段/元素/元组/载荷/全局初值/方法实参/栈参）与经**已覆盖面**（局部 LET / 直调实参 / 返回）⇒ 必须一致；
- **C3 期望值锚**：断言锚在 `@raw_int` 表达的 scaled 整数（`@raw_int` 是**唯一显式形式通道**，`ir_gen.cr:1699-1708`；不用它做锚 ⇒ 比较点自己就踩 L10）；
- **C4 非回归对照（B-10）**：已覆盖面必须保持正确（防「修法把对的搞坏」）。

**两条硬约束（探针头注必须写，来源 = 上批 N-4 教训）**：
1. 涉全局**必须 `mut`**——不可变 + 字面量初值的全局被 `find_global_const_node`（`ir_gen.cr:592-611`）折叠成 `IR_CONST`，读点不碰全局行 ⇒ **探针假绿**；
2. **`apx` 算术无解释器腿**（`interp.cr:263-266`/`:702-705` 对 `IR_I2F`/`IR_F2I` **显式报错 rc=255**）——一切经 `@raw_int`/边界转换的 apx 探针**没有 `corec run` 腿**（能力边界、非缺陷）⇒ 主判据 = **ELF 侧双形/双面对拍 + 期望值**。

> 源码块为**设计稿**（未编译验证语法；语法按 §1 校正后的实读形：元组下标 `t.0`、泛型 `fn f[T](…)`）。T5 落地时先过一遍 `check`。

| # | 机理一句话 | 最小探针（≤10 行） | 期望 | 优先级 |
|---|---|---|---|---|
| **B-1** | 方法调用实参不走环（门 `:1806`） | ```fn main() -> int { d : ., apx = 7.0; s : ., mut = S1 { v = 0 }; return s.m(d); }``` + `struct S1 { v: int }` + `impl S1 { fn m(self: S1, x: dex) -> int { return @raw_int(x) / 1000000; } }` | 7 | **P0 / RED** |
| **B-1b**（新） | 方法调用的 **self 偏移**（`arg_vars[0]` = 接收者） | ```fn main() -> int { d : ., apx = 7.0; s : ., mut = S2 { v = 0 }; return s.m(1, d); }``` + `impl S2 { fn m(self: S2, k: int, x: dex) -> int { return @raw_int(x) / 1000000; } }` | 7 | **P0 / RED** |
| **B-2** | 返回点门（**本计划判为已绿**，审计判为恒不触发） | `fn f() -> dex { d : ., apx = 7.0; return d; }` + `fn main() -> int { v := f(); return @raw_int(v) / 1000000; }` | 7 | **P0-confirm / 预期非 RED** |
| **B-3a** | 结构体**字面量**字段写点无漏斗（`:2647`） | ```fn main() -> int { d : ., apx = 7.0; s : ., mut = S3 { f = d }; return @raw_int(s.f) / 1000000; }``` + `struct S3 { f: dex }` | 7 | **P0 / RED** |
| **B-3b** | 结构体字段**赋值**写点无漏斗（`:1380`） | ```fn main() -> int { d : ., apx = 7.0; s : ., mut = S3 { f = 1.0 }; s.f = d; return @raw_int(s.f) / 1000000; }``` | 7 | **P0 / RED** |
| **B-4a** | 数组字面量元素写点无漏斗（`:2695`） | ```fn main() -> int { d : ., apx = 7.0; a : [dex; 2] = [d, 1.0]; return @raw_int(a[0]) / 1000000; }``` | 7 | **P0 / RED** |
| **B-4b** | 下标赋值 + 动态下标（`:1403` / `:1410`） | ```fn main() -> int { d : ., apx = 7.0; a : [dex; 2] = [1.0, 1.0]; i : ., mut = 0; a[i] = d; return @raw_int(a[i]) / 1000000; }``` | 7 | **P0 / RED** |
| **B-4c** | 切片元素（`:1403` 同点，源为切片） | ```fn main() -> int { d : ., apx = 7.0; a : [dex; 3] = [1.0, 1.0, 1.0]; s := a[0..2]; s[1] = d; return @raw_int(s[1]) / 1000000; }``` | 7 | P1 / RED |
| **B-5** | 元组元素写点无漏斗（`:2822`） | ```fn main() -> int { d : ., apx = 7.0; t := (d, 1.0); return @raw_int(t.0) / 1000000; }``` | 7 | **P0 / RED** |
| **B-6** | 枚举载荷写点无漏斗（`:2611`） | ```fn main() -> int { d : ., apx = 7.0; e := V(d); return match e { V(x) => { return @raw_int(x) / 1000000; } }; }``` + `enum E1 { V(dex) }` | 7 | **P0 / RED** |
| **B-7′** | `as` 语义（**替换审计原探针：原探针零区分力**，§1.1 校正 B） | ```fn main() -> int { e : dex = 1.5; y := e as dex; return @raw_int(y) / 1000000; }``` | **待语言定案**：恒等 ⇒ 1500000；转换 ⇒ 1 | **P1 / 不进 RED**（**待维护者问用户**） |
| **B-8** | 聚合读丢型 ⇒ 比较不对齐（L9+L10） | ```fn main() -> int { d : ., apx = 1.5; s : ., mut = S3 { f = 1.5 }; if s.f == d { return 1; } return 0; }``` | 1 | **P0 / RED**（本批走「比较点声明面查表」作用域修法；完整修归 #78） |
| **B-9** | 第 9 个 binary64 栈参（#81；机理未实证） | 方法带 **8 个 `dex` 形参** + 第 9 参 `lx : dex, apx, mut = 1.5`；callee 直返该形参。**须同时记** 插桩点 A（环内每轮 `cpi` / `ast_type_val(cpn)` / `irv_type(arg_vars[cpi])`）与 B（`callseq.cr:66-105` 每实参 `irv_type` + 类计数 + 落栈判定；`frame.cr:116-147` 每形参 `pty` + `caller_off`） | 7 | **P0 / RED**（机理 T1 裁定） |
| **B-10** | **反证对照**：已覆盖面应当正确 | `fn g(x: dex) -> int { return @raw_int(x) / 1000000; }` + `fn main() -> int { d : ., apx = 7.0; return g(d); }` | 7 | P1 / 非 RED（**回归钉**） |
| **B-11**（新，⑦a） | 全局**运行期初值**写点无漏斗（`:3097`） | `fn sc() -> dex { return 7.0; }` + `g11 : dex, apx, mut = sc();` + `fn main() -> int { return @raw_int(g11) / 1000000; }` | 7 | **P0 / RED** |
| **B-12**（新） | 泛型实例化后 dex 形参（审计 ①d「部分/未实证」） | `fn g12[T](t: T, x: dex) -> int { return @raw_int(x) / 1000000; }` + `fn main() -> int { d : ., apx = 7.0; return g12(1, d); }` | 7 | P1（可达性 T1 实证后定） |
| **B-13**（新） | 全局 apx 形参经方法调用（对上批 seam 批 `need_pack` 掩蔽面的**值面**复核） | `g13 : dex, apx, mut = 7.0;` + `fn main() -> int { s : ., mut = S1 { v = 0 }; return s.m(g13); }` | 7 | P1 |
| **B-14**（新） | **C1 双形对拍通用形**（非钉值）：同程序 `d : ., apx = 7.0` 与 `d : dex = 7.0` 两版 ⇒ 逐条结果必须同 | 两版同值 | P1 |

**对照（每例必配）**：① **精确形对照**（`d : dex = 7.0`，同一写点/读点 ⇒ 应已正确）；② **已覆盖面同值对照**（C2，如 `d` 经直调实参/局部 LET）；③ **非可选负控**（无 apx 的程序字面面须零变化）。

---

## §5 候选方案（三条；逐条 = 改动面 / 风险 / 可证伪判据 / 零足迹）

### (i) 唯一漏斗 + 声明型触发（**维护者背书方向；推荐主体**）

- **改动面**：
  1. 新增 **`slot_form_of_decl(declared_node_or_code) -> int`**：把「声明面」映到**规范槽形式**（`TI_DEX` ⇒ `TI_DEX_S`；`apx` 标记 ⇒ `TI_DEX`；非 dex ⇒ `-1`）。**输入契约见裁-APX-2（节点级、零 `alloc_type`）**。
  2. 新增 **`dex_slot_adjust(declared_ti, val, val_node)`** = 现 `dex_store_adjust` 的**声明型版**；`dex_store_adjust(target, val, node)` **退化为薄壳**（`slot_decl_ti(target)` + apx 位）⇒ **既有 5 个调用点行为等价、零字节**。
  3. **写点接线（7 处，L2–L8）**：字段赋值 `:1380` · 结构体字面量 `:2647` · 下标赋值 `:1403`/`:1410` · 数组字面量 `:2695` · 元组 `:2822` · 枚举载荷 `:2611` · 全局初值 `:3097`。
     其中**聚合面无 apx**（§0 推论）⇒ 规则可退化为「**`TI_DEX` 值 ⇒ 转 scaled**」，**不需要声明面查表**（⇒ 零 `alloc_type` 风险，见裁-APX-2）。
  4. **调用环重写（L1）**：抽 `dex_convert_call_args(callee_ni, first_arg, arg_vars, ac, is_method, is_extern)`：
     - 直调：形参节点链（现状）**或** `fi_param_type(cfi, i)`；
     - 方法调用：**跳过 `arg_vars[0]`（self）**，被调形参链不含 self（`ir_gen.cr:1776-1786` + TODO #80 已记）；
     - extern：保持 `scaled → bits`（现状正确，`callseq.cr` 的 C ABI 面不动）。
  5. **比较点声明面查表（L10，作用域内替代 #78）**：`ir_gen.cr:1246` 处，操作数节点为 `EXPR_FIELD`/`EXPR_INDEX` 时经**节点级声明面**取其声明型参与分流判定（**不改 IR 型、不改读点**）。
- **风险**：
  - **R1**：任何新增 `res_type_node`/`alloc_type` 调用 = 给 ir_gen 路径新增类型表写入 ⇒ 破 warm-cache「不可写条目」不变式（`test_warm_cache_gate.py` 的实锤：warm 命中跳过 `ir_gen_func` ⇒ `alloc_type` 行不重建）⇒ **裁-APX-2 强制节点级零 alloc**。
  - **R2**：环重写改变直调面的发射序 ⇒ 若 `fi_param_type` 与形参链结果不一致（泛型实例化 / 形参超 64 / checker 未登记）⇒ 新错值。**缓解**：T2 枚举 + 保底「链不可用 ⇒ 回落旧路径」。
  - **R3**：聚合法规约（「槽恒精确」）若与某既有语料的实际依赖冲突（例如某语料依赖「聚合元素可持 bits」）⇒ 行为变更。**缓解**：T1 全语料 `check` 对拍（腿①）零差异为硬判据。
- **可证伪判据**：B-1/B-1b/B-3a/B-3b/B-4a/B-4b/B-5/B-6/B-8/B-11 由「静默错值」转**精确值 7/1**；B-10 与精确形对照保持绿；C1 双形对拍全同。
- **零足迹**：非 dex 程序（无 `TI_DEX` 值、无 `dex` 声明）⇒ 漏斗恒 `return val` 且**不经任何类型表查询** ⇒ 发射面逐字节不变。

### (ii) 逐调用形态补转换环（审计已指出「逐点补必再漏」）

- **改动面**：只给 `EXPR_FIELD` 分支加一段复制自 `:1812-1847` 的转换（不抽函数）。
- **风险**：形参链导航（`:1839-1844`）与 self 偏移**各复制一份**⇒ 漂移双源；聚合写点仍全漏（B-3..B-6/B-11 不动）。
- **判据**：只能让 B-1 单项转正；对 B-3..B-6 **零效果** ⇒ 覆盖面必然不闭合。
- **结论**：**不推荐**（无覆盖面闭合论证；本仓已有「`IR_REF` 修完仍漏 `IR_SLICE`/`IR_AWAIT`」的同类实证）。

### (iii) (i) + 后端不对称一并修（`frame.cr` 按类型类定位栈参）

- **改动面**：在 (i) 之上改 `frame.cr:133/139` 的落位公式为「按类型类计数」，或改 `callseq.cr` 的分类使**实参 IR 型在环后恒等于 callee 形参型**。
- **风险**：改**后端发射序** ⇒ canary / `.ccr` / 自举链 全在弹道上；且 §0 推论说 callee 恒无 binary64 类形参 ⇒ 该不变量**当前不可达** ⇒ 改动属「加固一个不可达路径」。
- **判据**：B-9 转正；canary IDENTICAL。
- **结论**：**保留为 L12 的退路**（当且仅当 (i) 修完后 B-9 仍红时启用，见裁-APX-6）。

### 推荐

**主体 = (i)**，理由：① 覆盖面有**可机械枚举的闭合判据**（T2 四表零未分类），逐点补无此性质；② 漏斗化后「聚合面」不需要声明面查表（§0 推论）⇒ 零 `alloc_type` 风险，可**逐字节证明**零足迹；③ 环重写把形参类型来源从「AST 链前扫」换成 `fi_param_type`（O(1) 槽区读）⇒ **顺带消灭审计触发模式②**；(iii) 留作 L12 退路。

---

## §6 裁决门（**开工前置**；逐条 = 问句 / 影响 / 推荐 / 未取裁时）

| # | 问句（逐条照抄给维护者） | 影响 | 本计划推荐 | 未取裁时 |
|---|---|---|---|---|
| **裁-APX-1** | **批序：#78（聚合读丢型）与本批谁先？** 三选：**(a) 本批先修**（值面/覆盖面先到位；#78 单列后续批）· **(b) #78 先修**（读点恢复声明型，转换仍按值型）· **(c) 同批**。 | 批次边界 + RED 集合 + canary 风险面 | **(a)**。理由：① 写点/环覆盖是**值正确性的必要条件**，#78 单独修**不产生任何正确值**（读点型准了但存储形式仍错 ⇒ 更早暴露垃圾值，可能把「双路径同错」变成新的错值面）；② 本批写点修复**不依赖** #78（§0 推论：聚合槽规范形式恒精确，由声明面独立确立）；③ #78 改动面 = **每个聚合读点的 IR 型** ⇒ 波及比较分派 / `ptr_analysis` / regalloc 类型门（`regalloc.cr:680-681`）/ 可选表示 / `.cir` 快照 / `.ccr` ⇒ 须独立立项 + 独立 RED；④ 唯一需要 #78 的条目是 **B-8**，本批用**作用域内的比较点声明面查表**（不改 IR 型）替代。 | **停下报 lead**（批序决定 T1 边界，不得默认） |
| **裁-APX-2** | **漏斗的输入契约（声明型从哪取）？** 三选：**(a) 节点级 + 零 `alloc_type`**（`si_field_type_node` / `ei_variant_type_node` / `ast_b(param|let)` / `fi_param_type`，判 `ast_kind(n)==0 && ast_type_val(n)==TY_DEX`）· **(b) 复用既有声明面 API**（`field_ti_of_node` `:358` / `struct_row_field_ti` `:367` / `enum_payload_ti` `:389` / `global_decl_ti` `:373` / `elem_ti_of` `:352`）· **(c) 混合**。 | `.ccr` TYPE 段 / `.cir` 快照 / warm-cache 不变式 / 移植成本 | **(a)**。**关键事实**：候选 (b) 的 5 个 API **全部经 `res_type_node` ⇒ 会 `alloc_type`**（`checker.cr:957+`：`EXPR_ARRAY`/`EXPR_IDENT`/`EXPR_REFTYPE` 分支均 `alloc_type`；`alloc_named_type`）。它们今天只在 `if g_optrep_on != 0` 下被调（`ir_gen.cr:1379/1396/2610` 等），而 `g_optrep_on` 由 AST 是否含 `EXPR_OPTIONAL`/`Some`/`None` 决定（`:3257-3265`）⇒ **对非可选程序当前零调用**。本批若无条件引入 ⇒ **给 ir_gen 路径新增 `alloc_type` 站点** = 直接撞上 warm-cache 实锤教训（`test_warm_cache_gate.py`：warm 命中跳过 `ir_gen_func` ⇒ `alloc_type` 行不重建 ⇒ 共享类型面漂移）+ `.ccr` TYPE 段风险。**节点级通道的先例已在仓内**：`reg_one_global:3124`（`ast_kind(ltn)==0 && ast_type_val(ltn)==TY_DEX`）、`agg_elem_count_of:3004`（读类型节点不 alloc）。**注**：聚合面依 §0 推论**根本不需要**声明面（槽恒精确）⇒ 需声明面的只剩「调用实参 / 全局槽 apx 位 / 比较点」三处，均可节点级零 alloc。 | **(a)**（(b) 须同批把新 `alloc_type` 站点纳入 warm-cache 判据并重锁 `.ccr`——**不推荐**） |
| **裁-APX-3** | **`as` 语义：`d as dex` 是恒等还是形式转换？**（`ir_gen.cr:2749-2767` 现状 = 纯 `IR_LOAD` 拷贝） | 语言语义定案 + B-7′ 的期望值 | **不能由代理取裁**——**待维护者问用户**。给出两候选读法与判别探针 B-7′（**审计原 B-7 探针零区分力，已替换**，§1.1 校正 B） | **标「待用户裁」**；B-7′ **不进 RED 集合**；`as` 行为冻结不动 |
| **裁-APX-4** | **覆盖面口径**：本批覆盖哪些调用形态/写点/读点，哪些留后续？ | 批次范围 | **本批覆盖**：① 调用环（直调 + 方法调用；extern 保持现状）② 4 类聚合写点（字段赋值/结构体字面量 · 下标赋值/数组字面量 · 元组 · 枚举载荷）③ 全局运行期初值 ④ 比较点声明面查表（仅 dex 面）⑤ 返回点（保持 + 加固判据）。**留后续**：`as`（待裁）· #78 聚合读定型 · 泛型实例化后形参（T1 实证后定）· `IR_SPAWN`/`IR_HOTPATCH_ROUTE` 实参面（若 T2 枚举命中则评估） | 按推荐执行；枚举若发现表外活点 ⇒ **停下报** |
| **裁-APX-5** | **调用环的形参类型来源与 self 偏移**：类型来源 = 形参 AST 链（现状）还是 `fi_param_type`（FuncInfo 槽区）？方法调用如何跳过 self？ | 环重写正确性 + 触发模式②是否顺带消灭 | **`fi_param_type(cfi, i)` + 跳过 `arg_vars[0]`**（O(1)、不依赖 AST 链连续性 ⇒ 顺带消灭审计触发模式②）；**保底**：`func_ni < 0` 或 `cfi < 0` ⇒ 回落 AST 形参链（现状路径，逐字保留） | 按推荐；T1 须实证 `func_ni = ast_data(node)`（`ir_gen.cr:1777`）在方法调用点可用 |
| **裁-APX-6** | **#81 的修法与判据**：**(a)** 只修环覆盖面 + **强制实测** B-9 是否随之转正 · **(b)** 同批一并改后端（按类型类定位栈参/统一分类）。 | 后端发射面 / canary 风险 | **(a) + 强制实测**。理由：§0 推论「callee 恒无 binary64 类形参」⇒ (b) 是在维持一个**当前不可达**的不变量，却付出**改后端发射序**的 canary 风险。**T1 须先跑插桩点 A/B 留证**：若 A 显示环**确已转换**第 9 实参（⇒ 模式②证伪）⇒ 转查落位公式 ⇒ 此时才评估 (b)。 | 取 (a)；**若 (a) 后 B-9 仍红 ⇒ 停条件**（§9） |
| **裁-APX-7** | **探针入仓形态**：py 套件 + `run.sh` 挂钩 + suite 语料 + 通用判据？ | 回归网覆盖面 | **全做**：① `tests/selfhost/test_apx_conversion.py`（逐条 RED/精确形对照/期望值 + **C1 双形对拍通用腿**）② `src/ci/run.sh` 的 `selfhost-tests` 挂钩 ③ **`tests/harness/ci_hook_allowlist.txt` 同步**（否则挂点覆盖率判据 `test_ci_hook_coverage.py` 必红）④ 一档 suite 语料 `tests/suite/apx_conversion_test.cr`（**只放已绿形态**；并入腿① **73 → 74** 计数，须显式更新） | 只入 py 套件 + `run.sh` 挂钩（+ 白名单同步） |
| **裁-APX-8** | **#78 登记面的处置**：本批是否改 `TODO.md #78` 文本？ | 台账一致性 | **不改结论，只加交叉引用**：批末在 #78 条目追加一行「RED 语料 = 本批 §4 探针 B-3a/B-3b/B-4a/B-4b/B-5/B-6/B-8 的子集；修复时须同改点 = `ir_gen.cr:2513 / :2578 / :2316` + 消费者（二元对齐 `:1249` / `ptr_analysis` / regalloc 类型门）」。若裁-APX-1 选 (b)（#78 先修）⇒ 本门作废 | 加交叉引用（不改 #78 结论） |

---

## §7 任务表（T1..T7；状态「待实施」，收官时回填）

| # | 任务 | 源码改动 | 关键判据 |
|---|---|---|---|
| **T1** | **前置侦查 + 冻结基线 + 插桩实证**（**零源码改动**；插桩仅临时、事后撤销，不得落盘） | 无 | ① 冻结基线重建（`nice -n 19 bash tools/baseline/rebuild.sh`）三 sha 命中 + 冒烟 42；② B-1/B-1b/B-2/B-3a/B-3b/B-4a/B-4b/B-4c/B-5/B-6/B-7′/B-8/B-9/B-10/B-11/B-12/B-13 **逐条当前行为实测表**（ELF rc + 期望 + interp 腿能力标记）；③ **B-2 一行裁定实验**；④ **B-9 插桩点 A/B 记录 + 判定**（模式②坐实 / 证伪 → 转落位公式）；⑤ B-12 泛型可达性实证 |
| **T2** | **先裁落纸 + 四表完备性枚举**（零源码改动；**未取裁前不得落码**） | 无 | 四表**零未分类**：① 转换调用点（`dex_store_adjust`/`dex_bits_to_scaled`/`dex_scaled_to_bits`/`dex_scale_int` 全部调用点，逐条分类）② **写点表**（全仓 `IR_STORE` / `IR_STORE_FIELD` / `IR_STORE_INDEX` / `IR_STORE_INDEX_VAR` / `IR_STORE_PTR` / 全局初值发射点，逐条判「能否为 dex 槽 + 有无漏斗」）③ **读点表**（`IR_LOAD_FIELD` / `IR_LOAD_INDEX*`，逐条判声明面可得性与是否硬定型）④ **调用形态表**（`EXPR_CALL` 的 `func_node` 形态 × {直调 / 方法 / 模块 / extern / 泛型实例} × 环是否覆盖）+ 裁决门回填 |
| **T3** | **实现：唯一漏斗 + 写点接线**（L2–L8） | `src/compiler/ir_gen.cr` | RED 转正（B-3a/B-3b/B-4a/B-4b/B-4c/B-5/B-6/B-11）+ **精确形对照全绿** + **非可选程序零字节**（`.ccr` 四条 / canary IDENTICAL） |
| **T4** | **实现：调用环重写**（L1；含方法调用 self 偏移） | `src/compiler/ir_gen.cr` | B-1/B-1b 转正 + B-9 裁定落纸（转正 ⇒ #81 与 #80 同一修复；仍红 ⇒ 报 lead，裁-APX-6 转 (b)）+ B-10 非回归 |
| **T5** | **探针套件 + 突变控制** | `tests/selfhost/test_apx_conversion.py`（新）· `tests/suite/apx_conversion_test.cr`（新）· `src/ci/run.sh` · `tests/harness/ci_hook_allowlist.txt` | 套件全绿 + 通用判据 C1/C2 在仓 + **突变双向红**（漏斗关掉 ⇒ 未覆盖面探针红；方法跳过删掉 ⇒ B-1 红）+ 腿①计数 73→74 显式更新 |
| **T6** | **判据复验** | 无 | §8 Global Constraints 全绿（canary / `.ccr` 四值 / 腿① / 探针 29 / 枚举 / selftest-types 415 / 五 CI job / 自举链 `corec2==corec3` + N06=0 + 冒烟 42） |
| **T7** | **收官**（台账 + 文档 + 路径限定提交） | `TODO.md`（#80/#81 状态 + #78 交叉引用按裁-APX-8）· 批报告 | `jj status` 干净 + 报告归档 + 三态纪律全程 |

**T1 步骤细化（前置侦查）**：
1. `jj git fetch` + 确认 `develop` 基点；冻结基线重建（`tools/baseline/rebuild.sh`，**nice -n 19**，收工 `jj workspace forget` + 删 tmp）。
2. 起点判据：canary + `.ccr` 四条（`bash tools/baseline/canary_check.sh`）⇒ 与 `canary_values.tsv` 逐条比。
3. 逐条探针：每例写临时 `.cr`（**必须改注释前缀避 `.core/cache/cir/` 增量缓存命中**，照 `test_apx_tag.py:31` 形），`corec check` → `corec build --static` → 跑产物记 rc；`corec run` 记「拒收 rc=255」或真实值（**能力边界标记，不得当错值**）。
4. B-2 裁定：`fn f() -> dex { d : ., apx = 7.0; return d; }` ⇒ 期望 7。**若实测 ≠ 7（=我的读码判定反了）⇒ 停条件触发**（§9）。
5. B-9 插桩：临时在 `ir_gen.cr:1815-1845` 与 `callseq.cr:66-105`/`frame.cr:116-147` 加打印 ⇒ 跑 B-9 探针 ⇒ 记录表 ⇒ **撤销插桩**（`jj` 侧不得留下这些改动）。
6. B-12 泛型可达性：`fn g12[T](t: T, x: dex)` 实测。

---

## §8 Global Constraints（判据清单，**逐条必须全绿**）

1. **ELF canary IDENTICAL**：`95084e7bc68d6550d21d3d96fa3afd89c67a5d89edce5656a3d2e74fc923d475`（**28822B**）。
2. **`.ccr` 四条当前锁值**（真源 = `tools/baseline/canary_values.tsv`，**本批实读**；**勿抄旧值**）：
   | name | artifact | sha256 | size |
   |---|---|---|---|
   | `pa_ccr` | `pa.ccr` | `680a6f9843747b521213c3bcca1cf8724657943410f182e97ed63dcc11c7cd7a` | 96015 |
   | `pa_static_ccr` | `pa.st.bin.ccr` | `76f36e6a6b6eb2f18fa5541550d09e5e73bb0e8dfc7df2ebbe3d747c382f329c` | 96158 |
   | `gt_ccr` | `gt.ccr` | `d92a2727d0c51aa887d7053305a26bdf85451303e08d268898cf1de76bae2c95` | 142765 |
   | `gt_static_ccr` | `gt.st.bin.ccr` | `a1f7b99c68c7d433834593100a776b74f883de291482f20762fe7399ec5182ea` | 142908 |
   **本批预判 = 四条全 IDENTICAL**（实读依据：`tests/suite/ptr_arith.cr` 与 `tests/suite/generics_test.cr` **零 `dex`/`apx` 命中** ⇒ 漏斗恒早退、环不触发）。**若变且归因不出 ⇒ 红**（两级纪律：值变须「显式归因 + 同批重锁 + 旧值留痕」）。
3. **腿① 冻结基线同源对拍 73 档**（`tools/baseline/parity_run.sh`；本批若新增 suite 语料 ⇒ **74**，须显式更新计数）。
4. **行为探针 29 档**（`tools/baseline/probes_run.sh`）。
5. **枚举 62/62**（口径 = 上批台账计数；**真源 = 当批实测重算**，见 U7）。
6. **`selftest-types` 415/415**。
7. **五 CI job 全 rc=0**（`check` / `bootstrap-tests` / `selfhost-tests` / `suite` / `full-bootstrap`）。
8. **自举链**：`corec2 == corec3` IDENTICAL + **N06=0** + 冒烟 42。
9. **三态纪律**：不得把「未判」当 0/1，不得静默（引擎 `-1` ⇒ ICE04 硬错口径不变）。
10. **先裁后码**：§6 八门取裁落纸后方可进入 T3。
11. **长编译限 CPU**：一切构建/测试 `nice -n 19`（或 `cpulimit -l 10`）。
12. **本批新增（warm-cache 不变式）**：**禁止**在 ir_gen 路径新增 `alloc_type` 站点（`res_type_node`/`alloc_named_type`/`alloc_type` 调用）——新增 = 与 `test_warm_cache_gate.py` 的实锤冲突（warm 命中跳过 `ir_gen_func` ⇒ 缺行不重建）；若不得不新增 ⇒ **停下报 lead**（须同批扩 warm-cache 判据 + 可能重锁 `.ccr`）。
13. **本批新增（挂点纪律）**：新增 `tests/selfhost/*.py` **必须**同时进 `src/ci/run.sh` 或 `tests/harness/ci_hook_allowlist.txt`（否则 `test_ci_hook_coverage.py` 红）。
14. **本批新增（探针硬约束）**：涉全局探针**必须 `mut`**；`apx` 探针**无解释器腿**（`rc=255` 是能力边界，非缺陷）。

---

## §9 未决项 / 停条件

### 未决项（**不许猜**；逐条 = 内容 / 阻塞对象 / 解除条件）

| # | 未决项 | 阻塞 | 解除条件 |
|---|---|---|---|
| **U1** | **`as` 语义**（`d as dex` 恒等 vs 转换）——语言定案问题 | B-7′ 的期望值、T3 是否含 `as` | **维护者问用户**（裁-APX-3）；在此之前 `as` 行为冻结、B-7′ 不进 RED |
| **U2** | **#78 与本批的边界**（批序） | T1 的枚举边界与 T3 范围 | 裁-APX-1 |
| **U3** | **#81 的机理**（模式②「环未命中形参」 vs 落位公式不对称） | T4 的判据与裁-APX-6 | T1 插桩点 A/B 实测 |
| **U4** | **泛型实例化后 dex 形参**（审计 ①d「部分/未实证」）可达性 | B-12 是否进 RED | T1 步骤 6 实证 |
| **U5** | **`frame.cr:119-137` XMM 形参保存支 = 死码**（读码判定：`:2866` 恒把 dex 形参改 `TI_DEX_S` ⇒ `pty == TI_DEX` 不可达） | 无（登记） | 若将来出现「callee 可持 binary64 类形参」的形态 ⇒ 本条作废并须补判据 |
| **U6** | **⑦a 的 extern 形态**（`g : dex = <extern 调用>`）判据**只能到发射字节级**（静态无 `.so` 不可真跑） | L8 的判据强度 | 同裁-SEAM-2 口径（显式标注「无运行期腿」） |
| **U7** | **「枚举 62/62」的计数口径**（上批台账值；本批实读未能定位其生成脚本） | §8 第 5 条的复现 | T6 当场实测重算并在报告写明计算口径（**不得自造计数**） |
| **U8** | `IR_APPROX`（apx 标记指令，`ir_gen.cr:2436`/`:2465`）与本批转换面的交互 | 无 | 读码判定 = 纯标记、无运算语义（`test_apx_tag.py`）⇒ 登记，本批不动 |
| **U9** | 探针语料的**新 suite 档是否引入暖/冷分歧**（新档进 `run_suite` 循环） | T5 | 照 `test_warm_cache_gate.py` 口径核一遍；有分歧 ⇒ 该档只入 py 套件 |

### 停条件（**命中即停 + 报 lead，不得自行放宽**）

1. **B-2 实测 ≠ 7**（= §1.1 校正 A 的读码判定被证伪）⇒ 停：说明 `TY_`/`TI_` 常数面被我误读，§2 根因须整体重审。
2. T1/T2 枚举发现**表外活点**（未列入 §3 L1–L13 的静默错值面）⇒ 停并报（是否并入本批由 lead 裁）。
3. 漏斗/环重写导致 **canary 或 `.ccr` 四条变化**且**归因不出** ⇒ 停（= 零足迹被破）。
4. 方法调用点 `func_ni`（`ast_data(node)`）不可用 **且** AST 形参链回落路径也不可用 ⇒ 停（环无法覆盖方法调用）。
5. **裁-APX-6 (a) 后 B-9 仍红** ⇒ 停并报（给「落位公式不对称 + 后端改动面 + 风险 + 替代」），**不许自行放行到 (b)**。
6. 实现中判定改动面**超出本批预算**（如：需动 `checker.cr`/后端发射序）⇒ 停并报。
7. 任何「未判」被当成 0/1，或判据出现静默跳过（SKIP 当 PASS）⇒ 停（三态纪律）。

### 待维护者问用户（**一次一问**）

- **Q1（唯一）**：`d as dex`（`d` 为 `dex` 值，源/目标同为 `dex`）的语义是 **恒等拷贝** 还是 **形式转换（bits ↔ scaled 归一）**？——判别探针 B-7′ 已备（§4）。**在用户答复前 `as` 面冻结。**

---

## 附：本计划的自证边界（**诚实声明**）

- 本计划**零构建、零测试运行、零源码改动**：全部结论为**读码判定**（逐条 `file:line` 实读），唯一例外是引用**既有绿档**（`tests/suite/dex_test.cr:63-66` 的 `dex_apx_ret_fn`）作为 §1.1 校正 A 的旁证。
- **最强的新结论**（§1.1 校正 A：B-2 已绿）**必须由 T1 的一行实验终裁**，已列为停条件 1。
- **未实证项**已逐条进 §9（U1–U9），**未填空**。
