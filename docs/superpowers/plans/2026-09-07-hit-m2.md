# HIT M2 实施计划（运行时核事件集 + 全表驱动收口）

> **状态：待执行（2026-09-07 计划定稿）**
> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 表模式从「直线数据子集」升级为可运行完整 Core 程序（含函数调用/控制流/系统边界）的真实后端路径,并把 instr.cr 硬编码发射器数据化收口——「表 = 唯一平台依赖」形态达成,验收 = 表模式自举（corec 经表模式编译自己成功运行）。

**Architecture:** 运行时核 9 事件（4 数据事件保持 + jump/branch/call/ret/extern 控制/调用/系统事件）;投影模板语言 schema v2（前缀/REX/opcode/ModRM/disp/imm/rel32/两步序列）;lower_to_core 全 op 覆盖（直通优先、4 核合成兜底、标签→事件序回填）;发射收口 = 单指令编码形态入表、序列算法保留（prologue/参数拷贝/2L 慢路径 = 模板解释器代码调模板）;运行时条目最小描述 + x86 ABI 一档。执行分四段 M2a-d（依赖序强,每段可独立提交评审）。

**Tech Stack:** Core 自举编译器（src/arch/hit/hit.cr + lower_to_core.cr + core-x86.toml、src/arch/linux/ld/instr.cr + elf.cr、src/compiler/corearch.cr）、Python 测试。

**设计 spec:** `docs/superpowers/specs/2026-09-07-hit-m2-design.md`（方案拍板 + 节 1-3 逐节确认 + M2a-d 分段——本计划按段展开任务;执行时 spec 为字段/接口的权威定义源）。

## Global Constraints

- 版本控制 `jj`（铁律 #2,git 被 hook 拦截）。每任务 `jj commit -m '<msg>'`;不动 bookmark
- 构建 `nice -n 19 python3 build_selfhost_native.py`（每次改动后重建,约 2-3 分钟;只改 corearch 侧也可全量）;测试 `nice -n 19` 前缀（铁律 #6）
- **行为等价硬约束**：旧路径（无 --table）产物在任何阶段零变化——数据化/模板迁移判据 = 同源程序旧路径 vs 表路径 ELF **逐字节一致**（M1 判据延续）;回归面 = mw1-6 + live_ranges + ccr_v6 + region_cfg + compile + backend_bootstrap + hit_table(11) + slice_bounds + bootstrap 三套
- 表模式恒 O0 全栈发射（spec §7.3 拍板——优化 = 格形态层;O2×表组合恢复挂账 M3）;`--table` × `--link/--shared` 拒绝保持（M1 偏差 #2,extern 重定位解锁 = M2d 专项前不放开）
- 每次任务验证 = 构建 + 该任务判据 + hit_table 回归（表路径专项套件）;段末 = 全量回归
- IR op 全集 / 函数边界（REG root span）/ 事件流现状 = 源码真源（ast.cr 常量、ccr_io.cr 段布局、hit 层实现）;Task 0 盘点先行,后续任务按盘点清单执行

---

## Task 0: 盘点（形态族 + 表化边界 + 降低面）——计划锚点

**Files:** 分析产出（无代码改动）
- 读 `src/arch/linux/ld/instr.cr` 全 2381 行:分类全部 e2_*/sz_* 函数族——(a) 单指令编码形态（REX/ModRM/SIB/disp/imm/rel 组合清单——schema v2 字段集按此收敛,spec §3 字段若有缺在此补）;(b) 序列算法（prologue/epilogue/参数拷贝/2L 慢路径块/调用点序列——数据化边界外,清单标注）;(c) IR op → 发射块映射全集（表直通候选清单）
- 读 `src/arch/hit/hit.cr` + `lower_to_core.cr` + `core-x86.toml`:事件流/池/回填机制现状（M2a 扩展的接缝）
- 读 `elf.cr` 发射循环 + 函数帧布局（g2_slot 公式/2L tag 区/join 块回填）:控制流/调用事件与帧的交互面
- 产出清单（三张表）:(1) 形态族清单 → schema v2 字段定稿确认;(2) 序列算法清单（保留代码边界）;(3) IR op → 事件/合成映射初稿（直通/合成/保留旧路径三分类——M2b 直通表与 M2c 迁移块划分依据）
- 交付:清单并入本 plan 附注（文末「Task 0 盘点结果」节）,M2a-c 任务按清单执行;差异（spec §3 字段缺形态）→ 字段扩展先于 Task 2

---

## M2a 段:事件集 + 模板语言 v2

### Task 1: core-x86.toml schema v2 + 表加载扩展（hit.cr）

**Files:**
- Modify: `src/arch/hit/core-x86.toml`（4 数据事件模板迁移到 v2 字段 + 控制/调用/系统事件条目 5-9 声明——先以「形态占位未实现 → 加载通过、发射拒绝」两阶段落地）
- Modify: `src/arch/hit/hit.cr`（记录布局/解析扩展:多 proj 条目、step 新字段、runtime 小节）
- Test: `tests/selfhost/test_hit_table.py`（追加 v2 加载 walker + 字段解析断言）

**Interfaces:**
- Produces: `fn hit_proj_count(event_slot: int) -> int`（事件 proj 形态数）;`fn hit_proj_step_count(event_slot: int, proj_i: int) -> int`;`fn hit_proj_step_get(event_slot: int, proj_i: int, step_i: int, out: ptr) -> int`（out 指向 v2 step 记录——字段布局 = spec §3 字段序;返回 0 = 成功、-1 = 越界）;`fn hit_runtime_get(key: string, out: ptr) -> int`（运行时条目查询,runtime 小节字段化）
- 旧接口 `hit_proj_step`（M1 单 proj 单步）迁移到新接口之上或保留别名（hit 调用方 = emit_instr_tabled/测试——按 Task 0 盘点实际调用面定,文档同步）
- 验证:表加载（事件数含 9 + 直通可选）、字段解析逐项、缺事件/坏字段拒绝;corearch `--table` 加载打印事件计数变化测试同步更新

- [ ] **Step 1:** 盘点结果定稿 schema v2 字段集（Task 0 清单表 1;spec §3 字段缺形态在此补齐并回填 spec）
- [ ] **Step 2:** 写失败测试（v2 加载 walker:解析 core-x86.toml v2 全字段;坏文件拒绝;事件 5-9 存在）
- [ ] **Step 3:** 跑测试确认失败（hit.cr 未支持 v2 字段）
- [ ] **Step 4:** 扩展 `hit.cr` 记录布局与解析（事件/步记录加宽 + proj 多条目 + runtime 小节）;toml 解析按 v2 键读取;旧字段兼容读取（M1 表形态 = v2 子集）
- [ ] **Step 5:** 更新 `core-x86.toml` 到 v2（4 数据事件迁移 + 事件 5-9 声明 + runtime 小节 x86 档）——事件 5-9 的模板先声明真实 x86 投影（jump = E9 rel32;branch = cmp + jcc 序列;call = E8 rel32;ret = C3;extern = E8 + 外部重定位标记）,但发射器未支持前 `emit_instr_tabled` 对 5-9 返回拒绝（同 M1 'needs more events' 通道）
- [ ] **Step 6:** 构建 + 测试绿（加载/字段/拒绝面）
- [ ] **Step 7:** 提交 `feat: HIT schema v2——投影模板语言扩展 + 运行时核事件声明（M2a Task 1）`

### Task 2: 模板解释器扩展——新形态字段发射

**Files:**
- Modify: `src/arch/linux/ld/instr.cr`（emit_instr_tabled 侧:按 v2 step 字段发射;多 proj 形态选择;rel32 回填登记）
- Modify: `src/arch/linux/ld/elf.cr`（回填表:事件流内部目标/函数起点/外部符号三表——登记点与回填时机）
- Test: `tests/selfhost/test_hit_table.py`（每新形态定向对照:同源程序表路径 vs 旧路径逐字节）

**Interfaces:**
- Consumes: Task 1 `hit_proj_step_get`/`hit_proj_count`
- Produces: `fn emit_instr_tabled` 扩展（支持字段:prefix/rex{w,r,x,b}/opcode≤3/modrm 双角色/rm_mode 0-2/disp/imm/rel/cond——逐形态一个对照测试）;回填登记 `fn hit_rel_add(kind: int, pos: int, target: int)`（kind: 0=事件流位置、1=函数索引、2=外部符号序）
- **事件流注入测试通道**（本任务新增——模板对照与降低器解耦的关键）:corearch 隐藏 flag `--hit-events-file <path>`——文本事件流文件（`--dump-events` 输出的逆格式:每行 `ev <id> dst=<n> s1=[pool]n s2=[pool]n` + 池行）→ 构造 g_hit_ev_stream/g_hit_pool,跳过 lower 直接 emit。测试专用;真实构建路径不启用（与 --inject-* 通道同哲学）。此后 Task 3/6 的对照与 dump 断言全部经此通道（无需降低器先支持）
- 形态选择规则:proj 条目顺序 = 优先序;发射前按操作数形状（槽寻址=rm_mode1/2、寄存器对=0;branch 双目标=rel 两角色）选首个可匹配 proj;无匹配 → 拒绝（'no projection for shape'）
- 验证:数据形态扩展（disp/imm——经注入通道构造）+ jump 事件逐字节对照;错误面（坏 rex 组合/越界 opcode）拒绝

- [ ] **Step 1:** 写失败测试:注入通道读文件 → 跳过低 lower 发射（jump 事件程序 vs 旧路径对照——现无通道无 v2 发射 → FAIL）
- [ ] **Step 2:** 实现注入通道（文本解析 = dump-events 逆格式;~40 行;corearch 接线同 --dump-events 三态 dispatch）
- [ ] **Step 3:** 实现 v2 step 发射（每字段按 instr.cr 现有编码辅助——REX 位组装/ModRM/SIB 复用旧路径函数,Task 0 清单表 2 标注可复用族）
- [ ] **Step 4:** 实现 rel32 回填登记 + elf.cr 回填（事件流内部目标先落地;函数/外部目标接口留空登记——Task 4/5 消费）
- [ ] **Step 5:** 对照测试绿（注入通道:jump + disp/imm 形态）+ 旧路径回归零变化
- [ ] **Step 6:** 提交 `feat: 模板解释器 v2 形态 + 事件流注入测试通道——REX/ModRM/SIB/disp/imm/rel（M2a Task 2）`

### Task 3: branch/call/ret/extern 定向模板对照

**Files:**
- Modify: `src/arch/linux/ld/instr.cr`（cond 角色发射——cmp/test 序列 + jcc;call/ret 模板;extern 重定位登记）
- Modify: `core-x86.toml`（事件 6-9 模板细化:branch = cmp(cond 槽,rm 形态按操作数) + jcc 序列;call/ret/extern 定稿）
- Test: `tests/selfhost/test_hit_table.py`（每事件定向对照:含分支程序/含调用程序/ret/extern 拒绝面）

**Interfaces:**
- Produces: 事件 6-9 完整发射;`hit_rel_add(kind=2)` 外部符号 → 与旧路径 ext_rel 表同源（M2d 解锁 --link 的前置登记面;本任务 extern 发射 = 仅静态下可解析符号?——**M2 拍板:extern 模板先落地登记,`--table`×`--link` 解锁 = M2d 组合验证专项（Task 17）,此前 extern 程序在 --table 下拒绝**）
- 验证（全部经 Task 2 注入通道——不依赖降低器）:branch（条件 + 双目标回填）、call/ret（无参函数——**call 事件语义 = 跳转至函数事件流位置 + ret 返回;参数传递/栈序列 = Task 5 降低器配套,不在本任务对照面**）、extern（登记面——发射到 ext_rel 表;`--table`×link 解锁 = Task 15）

- [ ] **Step 1:** 写失败测试:branch（条件分支含双目标回填,经注入通道）逐字节对照
- [ ] **Step 2:** 实现 branch 发射（cond 槽装载 + cmp + jcc 双目标——模板序列 step1=cmp step2=jcc;rel 双角色回填）
- [ ] **Step 3:** 对照绿 + 回归
- [ ] **Step 4:** 写失败测试:call/ret（无参函数,注入通道）逐字节对照
- [ ] **Step 5:** 实现 call（E8 rel32,目标 = 函数事件流位置,Task 2 回填 kind=1 消费）/ret（C3）
- [ ] **Step 6:** 对照绿 + 回归;extern 登记面测试（拒绝保持——解锁 Task 15）
- [ ] **Step 7:** 提交 `feat: 控制/调用事件模板——branch 双目标 + call/ret + extern 登记（M2a Task 3）`

### M2a 段末验收

- [ ] hit_table 全绿 + 每事件定向对照绿 + 旧路径回归零变化（compile/ccr_v6/mw1-6 快子集）
- [ ] 提交（如段内已逐任务提交则跳过）;plan 状态行 M2a 完成

---

## M2b 段:降低层全 op（控制流/调用/合成完备）

### Task 4: 降低器控制流——标签映射 + 事件流回填

**Files:**
- Modify: `src/arch/hit/lower_to_core.cr`（IR_LABEL/JUMP/BRANCH → jump/branch 事件;标签 → 事件流位置表;函数序 → 事件流段排布 + call 目标回填）
- Test: `tests/selfhost/test_hit_table.py`（控制流程序 end-to-end:if/while/嵌套——行为 exit 断言 + 混合过渡态对照）

**Interfaces:**
- Produces: `fn hit_label_pos(id: int) -> int`（IR label id → 事件流位置;降低期填表）;lower_to_core 对 IR_JUMP/IR_BRANCH/IR_LABEL 不再拒绝;两遍法（遍 1 记位置、遍 2 经 Task 2 回填登记写 rel 目标）
- Consumes: Task 2/3 rel 回填（kind=0 事件流内部）
- 验证:if-else/while/break/continue 程序 exit 正确（行为判据——控制流无旧路径逐字节可比?**有**:同源程序旧路径 ELF exit 相同 + 关键跳转位置 dump 断言（--dump-events 扩展显示 rel 目标事件序））

- [ ] **Step 1:** 写失败测试:if/while 程序表模式 exit 断言（现拒绝 'needs more events' → FAIL）
- [ ] **Step 2:** 实现标签→位置两遍降低 + jump/branch 事件生成 + 回填
- [ ] **Step 3:** 测试绿（if/while/break/continue/嵌套）
- [ ] **Step 4:** 提交 `feat: 降低层控制流——标签映射 + jump/branch 事件 + rel 回填（M2b Task 4）`

### Task 5: 函数调用降低——call/ret + 参数/返回序列

**Files:**
- Modify: `src/arch/hit/lower_to_core.cr`（IR_CALL/IR_RETURN 值 → call/ret 事件 + 参数传递序列事件化;callee 帧布局 = 现有 g2_slot 全局 var 行语义）
- Modify: `src/arch/linux/ld/elf.cr`（函数事件流段排布 + call 目标回填 kind=1 消费;callee 事件流 = .ccr 函数序展开）
- Test: `tests/selfhost/test_hit_table.py`（调用程序:带参/返回值/递归/多层——行为断言）

**Interfaces:**
- Produces: call 事件语义落地——参数拷贝序列（实参槽 → callee 参数槽,按 x86 ABI 一档栈/寄存器传参由运行时条目参数化,Task 15 前 = 现行 ABI 固定序列）、返回值槽约定、callee-saved 保存（复用旧路径 prologue 算法——Task 0 清单表 2 标注的序列算法,表模式下以「序列模板」调用同一算法函数）
- 自举面注意:corec 自用 IR_CALL 密集（递归/多态包装）——Task 5 完成后函数调用语言面即通
- 验证:fib(10)=55 表模式（旧路径对照 exit）;递归/参数多档程序

- [ ] **Step 1:** 写失败测试:fib 表模式 exit 55（现 call 拒绝/无序列 → FAIL）
- [ ] **Step 2:** 实现调用降低（参数序列 + call + 返回序列 + ret）——序列算法复用旧路径函数（Task 0 清单表 2）
- [ ] **Step 3:** 测试绿（fib/参数/递归/多层调用）+ 旧路径回归
- [ ] **Step 4:** 提交 `feat: 降低层函数调用——call/ret + 参数序列（M2b Task 5）`

### Task 6: 合成规则补齐 + 直通表

**Files:**
- Modify: `src/arch/hit/lower_to_core.cr`（数据 op 合成规则全表 + 直通优先查询）
- Modify: `core-x86.toml`（数据直通扩展事件声明——add/and/or/xor/not/mul/div/cmp/shift 等 x86 直通投影）
- Test: `tests/selfhost/test_hit_table.py`（合成 vs 直通双路径对照:同一 op 合成表 vs 直通表产物——直通 = 扩展表加载时生效、合成 = 仅 4 核表）

**Interfaces:**
- Produces: `fn hit_synth(ir_op: int, dst/s1/s2: int) -> int`（合成规则:返回 0 = 已合成（事件流已改写）、-1 = 不可合成（拒绝））;直通查询 = 事件 id 10+ 存在 → 直发（hit_event_lookup 已有）;nand 真语义 = and+not 两步序列（Task 2 序列发射消费）;const 池负值/宽值（hit_pool_intern 扩 16B 槽 + 借位链——M1 偏差 #3 落地）
- 规则覆盖清单（Task 0 清单表 3 定稿）:add 反减已有;and/or/xor/not/移位/比较/mul/div/select 按 IR op 全集逐项落规则或直通
- 验证:位运算程序（& | ^ ~ << >> 语义 = Core 源无这些运算符?——**按 M1 偏差 #0:IR OP_AND 等无源码载体,规则以 IR 层测试载体（合成事件流 dump 断言）验证**）;const 负值/宽值程序（M1 偏差 #3 载体——hit_pool_w64 借位链 + 16B）

- [ ] **Step 1:** 写失败测试:合成规则直通表加载 + 事件流 dump 断言（add 反减链/and→nand 两步）
- [ ] **Step 2:** 实现合成规则表 + 直通优先查询（lower_to_core 数据 op 分支重构——Task 0 清单表 3）
- [ ] **Step 3:** nand 两步序列模板落地（core-x86.toml + 发射）+ const 池负值/宽值
- [ ] **Step 4:** 测试绿 + dump-events 断言 + 回归
- [ ] **Step 5:** 提交 `feat: 降低层数据 op 完备——合成规则全表 + 直通优先 + nand 真语义 + 池负宽值（M2b Task 6）`

### Task 7: suite + 自写覆盖全绿（混合过渡态验收）

**Files:**
- Create: `tests/hit/m2_*.cr` 自写覆盖（控制流/调用/内存/打印/位运算组合）
- Test: `tests/selfhost/test_hit_table.py`（suite 表模式跑批:编译 tests/suite/*.cr → --table → exit 断言对照旧路径）
- Modify: `src/arch/hit/lower_to_core.cr`（残余拒绝面按 suite 实证清点——仍有 op 拒绝 → 补规则或显式挂账）

**Interfaces:**
- Consumes: Task 4-6 全部
- 验证:suite 全集（tests/suite/*.cr 编译 + 表模式运行 exit == 旧路径）;自写覆盖组;syscall3/extern 面（suite 用 print/syscall → extern 拒绝面冲突?——**处置:suite 跑批分两级:纯数据/控制程序全跑;extern 程序（io 族）在 M2d extern 解锁前跳过并清单记录**）——验收判据 = 非 extern 面 suite 全绿

- [ ] **Step 1:** 跑批脚本（suite 编译 → 双路径 exit 对照;extern 面跳过清单）
- [ ] **Step 2:** 清点拒绝面（未覆盖 op 清单 → 补规则/挂账;挂账 = 注释 + TODO 条目）
- [ ] **Step 3:** 全绿 + 回归
- [ ] **Step 4:** 提交 `feat: M2b 验收——suite 混合过渡态全绿 + 拒绝面清点（M2b Task 7）`

### Task 8: 自举冒烟（混合态先行）

**Files:**
- Test: `tests/selfhost/test_backend_bootstrap.py` 或新 `tests/selfhost/test_hit_bootstrap.py`:corec 源码经表模式编译出 ELF → 运行冒烟（打印/编译小文件自验）
- Modify: `src/compiler/main.cr`（`build` 命令 `--table` 透传——M1 挂账「corec build 透传 --table」落地;`-O` 表模式恒 0 强制）

**Interfaces:**
- Produces: corec `build --table <path>`（透传给 corearch --table）;表模式自举产物 = 混合态（未覆盖 op 落旧路径——Task 0 清单表 3 的「保留旧路径」类）
- 验证:表模式编译 corec 源码 → 产物可运行（打印版本串/编译一个冒烟 .cr）;extern 面（io 密集）依赖 M2d 解锁——**自举冒烟先行判据 = 能跑通「corec check/run 小样本」或打印级冒烟;全功能自举 = M2d 终验**

- [ ] **Step 1:** corec build --table 透传 + -O 强制 0
- [ ] **Step 2:** 自举冒烟脚本（表模式编译 corec → 运行冒烟样本）
- [ ] **Step 3:** 清点混合态残留（自举语料中落旧路径的 op/序列清单——M2c 数据化优先级输入）
- [ ] **Step 4:** 提交 `feat: M2b 自举冒烟（混合态）+ corec --table 透传（M2b Task 8）`

### M2b 段末验收

- [ ] suite 非 extern 面全绿 + 自举冒烟绿 + 拒绝面清单更新;hit_table + 全量回归快子集
- [ ] plan 状态行 M2b 完成

---

## M2c 段:发射数据化收口

### Task 9: 形态族迁移第 1 批（数据直通事件族）

**Files:**
- Modify: `core-x86.toml`（直通事件模板由 Task 6 占位升级为完整 v2 形态——按 Task 0 清单表 1 分批）
- Modify: `src/arch/linux/ld/instr.cr`（对应形态族的旧硬编码块改为读表——**每族迁移 = 删旧块前先以「双源对照」验证**:表模板产物 == 旧块产物逐字节,再删旧块）
- Test: `tests/selfhost/test_hit_table.py`（每族对照用例 + dump 断言）

**Interfaces:**
- Consumes: Task 0 清单表 1（形态族分块）+ Task 2 解释器
- 验证:每块迁移 = 旧路径 byte-identical（全套 compile 回归逐块绿）;表路径产物不变
- 批次划分（示例,按 Task 0 清单定稿）:imm 族/寄存器对算术族/disp 槽寻址族/SIB 索引族/cond 族/rel 族

- [ ] **Step 1:** 批 1 族清单 + 双源对照测试（写失败测试:表模板 vs 旧块逐字节）
- [ ] **Step 2:** 模板定稿（core-x86.toml 批 1 条目）
- [ ] **Step 3:** 解释器读表发射批 1 形态 + 旧块删
- [ ] **Step 4:** 对照绿 + compile/backend_bootstrap 回归
- [ ] **Step 5:** 提交 `refactor: 形态族批 1 数据化（族名）——byte-identical 对照（M2c Task 9）`

### Task 10-12: 形态族迁移批 2-4（同 Task 9 模式,每批一提交）

- [ ] 批 2（按 Task 0 清单）:提交同上模式
- [ ] 批 3:同上
- [ ] 批 4:同上
- 每批判据:双源对照绿 + 旧路径 byte-identical + 全量回归;批次总数与边界以 Task 0 清单表 1 为准（4 批为上限估计,清单定稿可增减——增减时同步本计划状态行注记）

### Task 13: 汇合——emit 单路径（混合模式消灭）

**Files:**
- Modify: `src/arch/linux/ld/instr.cr`（emit_instr 分发 → 表查询 + 模板解释器;旧路径 e2_* 残留族删净——Task 0 清单表 2 的「序列算法」族保留为解释器调用的序列函数）
- Modify: `src/arch/linux/ld/elf.cr`（发射循环单路径——表模式概念消失于发射层:表 = 唯一发射真源;`--table` flag 语义 = 强制加载核心表 + 拒绝缺失事件（无表默认 = 内置 x86 表?——**拍板:无 --table = 内置默认表（编译期嵌入 core-x86.toml 数据）;--table 加载失败 = 拒绝;旧「表模式/非表模式」二元性消灭**））
- Test: 全量回归 + backend_bootstrap + 自举（汇合后产物 == 旧路径产物逐字节——最强判据）

**Interfaces:**
- Produces: 内置默认表（embed core-x86.toml v2 数据）;`--table` = 覆盖/自定义表;发射路径零分支（每 op = 表条目/合成/序列算法三选一,无「落旧路径」）
- 验证:全套回归绿;自举（混合态已冒烟）在汇合后重跑 = 行为不变;逐字节判据（backend_bootstrap stage 链）

- [ ] **Step 1:** 内置默认表接入（无 --table 路径 = 加载内嵌数据）
- [ ] **Step 2:** emit_instr 汇合（分发 → 表;残留旧族删/转序列函数）
- [ ] **Step 3:** 全量回归 + 自举重跑
- [ ] **Step 4:** 提交 `refactor: 发射汇合——表 = 唯一发射真源,混合模式消灭（M2c Task 13）`

### M2c 段末验收

- [ ] 全量回归绿（旧路径产物逐字节不变 = 最强判据）+ plan 状态行 M2c 完成

---

## M2d 段:运行时条目 + 纯表自举收口

### Task 14: 运行时条目数据化 + call/ret 序列参数化

**Files:**
- Modify: `core-x86.toml`（runtime 小节定稿——spec §6 字段:call 机制/param 传递/save 集/extern 入口）
- Modify: `src/arch/linux/ld/instr.cr`（call/ret 序列函数按运行时条目参数化——x86 档实例;校验:事件集必备性/runtime 字段完整性）
- Test: `tests/selfhost/test_hit_table.py`（运行时条目加载 + 校验 + x86 档序列行为）

**Interfaces:**
- Produces: `fn hit_runtime_param_mode() -> int` 等查询（序列函数分支按条目——x86 一档实现,非 x86 条目 = 校验拒绝 'unsupported runtime profile'）
- 验证:调用程序行为回归;坏 runtime 条目拒绝

- [ ] **Step 1:** 写失败测试（runtime 加载/坏条目拒绝）
- [ ] **Step 2:** runtime 小节解析 + 校验 + x86 档查询
- [ ] **Step 3:** call/ret 序列按条目参数化（行为不变回归）
- [ ] **Step 4:** 提交 `feat: 运行时条目数据化——runtime 小节 + 序列参数化 x86 档（M2d Task 14）`

### Task 15: extern 解锁 + 组合验证

**Files:**
- Modify: `src/arch/linux/ld/instr.cr`/`ld.cr`（extern 事件重定位 → 旧路径 ext_rel 同源面;`--table` × `--link/--static` 解锁——M1 偏差 #2 清除）
- Modify: `src/compiler/corearch.cr`（拒绝分支删除）
- Test: `tests/selfhost/test_hit_table.py`（extern 程序静态/链接两形态 exit 对照;`--table` × `--link` 组合用例;HIT × 2L 组合（2L 快路径走表程序 + 慢路径块）;O2 × 表拒绝保持（表模式恒 O0 注记测试））

**Interfaces:**
- Consumes: Task 3 extern 登记 + Task 13 汇合
- 验证:io/print/syscall 程序（suite extern 面补跑）;2L 溢出程序表模式行为 = 旧路径;组合矩阵用例全绿

- [ ] **Step 1:** extern 重定位接通（ext_rel 同源）
- [ ] **Step 2:** --table × link/static 组合测试绿（拒绝删除）
- [ ] **Step 3:** HIT × 2L 组合用例（M1 偏差清点）
- [ ] **Step 4:** suite extern 面补跑全绿
- [ ] **Step 5:** 提交 `feat: extern 解锁 + 组合验证（HIT×link/2L）——M1 偏差清点（M2d Task 15）`

### Task 16: 纯表自举收口（终验）

**Files:**
- Test: `tests/selfhost/test_hit_bootstrap.py`（正式化:表模式编译 corec 全量 → 产物编译冒烟样本 + 自举链（stage0→stage1 表模式））
- Modify: 清点面修复（纯表自举暴露的残余:Task 8 混合态清单逐项消除——未覆盖 op → 合成/直通补齐;序列算法残留确认在边界内）

**Interfaces:**
- Consumes: 全部
- 验证:corec（经表模式编译的产物）成功编译冒烟程序并运行正确;stage 链 byte 判据（表模式自举产物 vs 旧路径自举产物行为等价）;全量回归

- [ ] **Step 1:** 纯表自举脚本（全量 corec 表模式编译）
- [ ] **Step 2:** 清点修复（混合态残留 → 补齐,循环直至零残留编译）
- [ ] **Step 3:** 自举链验证（stage0→1 表模式）
- [ ] **Step 4:** 全量回归
- [ ] **Step 5:** 提交 `feat: 纯表自举收口——表模式编译 corec 全链绿（M2d Task 16）`

### Task 17: 文档收尾 + M1 偏差清账

**Files:**
- Modify: `docs/superpowers/specs/2026-09-05-hardware-interface-table.md`（§2 事件模型修订回填:运行时核 9 事件表述;§6 M2 状态完成 + M2a-d 记录;§3.3 运行时条目）
- Modify: `docs/superpowers/plans/2026-09-05-hit-minimal-core-m1.md`（偏差四项清账注记:nand 真语义 ✓/混合模式消灭 ✓/常量池负宽值 ✓/--link 解锁 ✓）
- Modify: `TODO.md`（HIT 节 M2 挂账逐项划除;新挂账:表模式 O2 恢复（发射器汇合后自然解锁）挂 M3、第二张表 M3）
- Modify: `docs/superpowers/specs/2026-09-07-hit-m2-design.md`（状态:实施完成;执行记录回填）

- [ ] **Step 1:** 文档更新
- [ ] **Step 2:** 全量回归终跑
- [ ] **Step 3:** 提交 `docs: HIT M2 收官——spec/TODO/M1 偏差清账（M2d Task 17）`

### M2d 段末验收（= 计划终验收）

- [ ] 纯表自举绿（corec 经表模式编译自己成功运行 = 用户拍板验收锚）
- [ ] 全量回归绿 + 文档清账;plan 状态行全部完成

---

## 风险注记（执行评审）

- Task 0 盘点产出驱动 M2a schema 字段与 M2c 批次数——盘点不实 → 后段计划漂移（段内以清单为准,段界差异回填本计划状态行）
- 自举为终极验收:混合态冒烟（Task 8）先行降险;纯表终验（Task 16）暴露面 = 语言全特性（IR op 全集固定——降低器按 op 全集覆盖即可,与源特性正交）
- 2L/多字 × 表组合:2L 快路径 = 表直通/合成;慢路径块 = 序列算法（Task 0 清单表 2 标注边界）——组合验证 Task 15
- 逐字节对照判据在 M2c 最强（旧路径产物零变化）;M2b 行为判据（exit 对照）宽松——控制/调用程序以 dump-events + exit 双断言
- 表模式恒 O0:分配器/发射器表化后 O2 解锁 = M3 挂账（不扩本计划）

---

## Task 0 盘点结果（执行时回填）

> 本节约占位——Task 0 完成后回填三张清单（形态族/schema 字段定稿、序列算法边界、IR op → 事件/合成映射),M2a-c 任务按此执行。回填后本注删除。
