# (乙) 泛型实例形参擦除 —— 实施计划（规划轮）

> 轮次：2026-09-18 规划轮（只读侦察 + 落纸）。**零构建、零源码改动**；本文件是唯一新增物（不碰 `TODO.md` ⇒ 不取号）。
> 起点：`develop = 3e461edd`（本轮实读）；本工作区 `core-cachebloat-ws` 的 `@` = 空提交于 `f9ff306a`。
> 定位：**(乙) = 表示面**（槽型/寄存器类/ABI）；与 **(甲)/F3 = 检查面** 的边界见 §6。

## 0. 证据分级（本文件每句机理都带标签，无标签即缺陷）

| 标签 | 含义 |
|---|---|
| 【实测·转述】 | `plan-optdex` 的实测读数，由 team-lead 转述；**本轮未复核**（零构建 ⇒ 无法复核运行面） |
| 【实测·本轮只读】 | 本轮我以只读手段（读盘/读文件头/grep）取得的读数，命令可复现 |
| 【读码】 | 本轮我实读 `develop = 3e461edd` 的源码行；锚点均为 file:line |
| 【推断·未验证】 | 由读码外推、**未实测**；实施前必须先测 |

**硬约束（本轮）**：零构建 · 零源码改动 · 只新增本文件。
**提交纪律（实施轮）**：一批一链一 bookmark；路径限定提交；推前跑对应判据（见 §4.1）。

---

## 1. 机理

### 1.1 根因链（全部【读码】，行号 = develop 3e461edd）

1. **实例创建只深克隆函数体**。`monomorph.cr:600`：
   `if k == EXPR_FN { d2 := gen_clone_tree(d); n := ast_alloc(k, a, b, c, iv, tv, d2, ln, cl); ... }`
   —— `EXPR_FN` 的 `a`=名、`b`=首形参、`c`=形参数 **原样复制**；只有 `d`（函数体）走克隆。
   ⇒ **实例与声明共用同一个形参节点链**。
2. **`EXPR_PARAM` 的克隆分支存在但走不到**：`monomorph.cr:604`
   `if k == EXPR_PARAM { d2 := gen_clone_tree(d); n := ast_alloc(k, a, b, c, iv, tv, d2, ln, cl); ... }`
   —— 克隆 `d`（类型节点）的能力已有；但 `EXPR_FN` 分支从不访问 `b`，该分支对**实例的形参**恒不可达。
3. **形参序言按「声明面节点」定槽型**。`ir_gen.cr:3071-3081`：
   ```
   3071  param_type : ., mut = ast_type_val(pn);      // 泛型形参 = 0
   3072  if param_type < 0 { param_type = TI_INT; }   // ⇒ TI_INT（GP 类）
   3073  if param_type == TI_DEX { param_type = TI_DEX_S; }
   3080  else if dex_opt_type_node(ast_data(pn)) != 0 { param_type = TI_DEX_S; }
   3081  pvar := new_ir_var(pname, param_type);
   ```
   泛型形参 `t: T` 的节点 `ast_type_val` = 0 ⇒ **槽型恒 `TI_INT`**；`t: T?` 的 `dex_opt_type_node` 见下条 ⇒ 恒假。
4. **`dex_opt_type_node` 看不穿泛型形参**。`ir_gen.cr:1127-1134`：要求内层节点 `ast_kind(inner) == 0`；泛型形参引用是 `EXPR_IDENT`（kind ≠ 0）⇒ 对 `T?` 恒返回 0 ⇒ 声明面的 dex 门恒假。
5. **调用点对齐环按「声明面参数链」判门，且发生在实例重定向之前**。`ir_gen.cr:1964-2008`：
   `cfi := find_func(func_ni)`（= **声明**）→ 环门 `ir_gen.cr:1984-1985`：
   `if ast_type_val(cpn) == TI_DEX || (cfn_ext == 0 && dex_opt_type_node(ast_data(cpn)) != 0)`
   ⇒ 泛型形参两个子判据皆假（第 3/4 条同因）⇒ **不转换**。
   实例重定向在 `ir_gen.cr:2072-2077`（`spec_ni := gen_find_or_create_*` → `func_ni = fi_name(spec_ni)`），**在环之后**。
6. **ABI 分类由实参 var 的 IR 型决定，与被调形参槽型是两个独立面**。`callseq.cr:66-89`（`cs_args_dispatch`）：
   `pt := irv_type(fa + ai); if pt == TI_DEX { →XMM(fr_cnt) } else { →GP(ir_cnt) }`；栈参判定同规则（`callseq.cr:95-106`）。
   ⇒ 调用点按 `TI_DEX` 放 XMM，而被调侧（第 3 条）按 `TI_INT` 读 GP ⇒ **寄存器类失配**。
7. **`fi_param_type` 救不了场**：实例的形参型是**逐位抄声明**（`monomorph.cr:844-850`，`fi_set_param_type(new_fi, pi, fi_param_type(func_ni, pi))`）。
   【实测·本轮只读】全仓 grep：`fi_param_type(` 的**唯一读取点**就是这条拷贝自身（`monomorph.cr:849`）；其余全是 `fi_param_count`。
   ⇒ 后端 ABI 不读 FuncInfo 形参型（只读实参 var 型）⇒ 「把 FuncInfo 抄对」**不是**承重面。

**合起来**：实例的形参「类型」在三面各说各话——节点面（`ast_type_val` = 0）、类型行面（未解析）、FuncInfo 面（抄声明）。
调用链上唯一把两面拴在一起的是**调用点对齐环**（第 5 条），而它对泛型形参**恒不生效**。

### 1.2 实测读数（【实测·转述】，本轮未复核）

| 探针 | 前态 ELF / interp | 读法 |
|---|---|---|
| 泛型 `h(ds)` | **77 / 85** | 两腿分歧 |
| 泛型 `h(da)` | 两腿均偏离期望 | 两腿同错（非仅 ELF） |
| 非泛型 `f_i(d)` | **2 / 0** 且 `check` = 0 诊断 | 静默（零诊断） |
| `h[dex]` 体内 `@raw_int(t)` | `error[TF07]` | 实例体从未按 `dex` 检查（检查面，见 §6） |

另（【实测·转述】）：ELF 侧读数随构建漂移（63→247）⇒ 读的是**未定义寄存器**，与第 1.1-6/7 条的失配解释自洽。
**归因待复核项**：`h(ds)` 的实参形态（是否为 `dex` 族 var）本轮**未能核实**（探针源码不在本工作区）；§4 的判据必须自建最小探针，不引用这两个数当锚。

### 1.3 已核读点清单（谁看形参链 / 形参型）

| 读点 | 锚点 | 现状 | 修 A1 后 |
|---|---|---|---|
| 形参序言（槽型 + 表示位/decl_ti） | `ir_gen.cr:3071-3091` | 声明面 | 自动跟随（实例链已具体化） |
| 调用点 dex 对齐环 | `ir_gen.cr:1964-2008` | 声明面 + 重定向前 | **不自动**（须 A2，见 §2） |
| 检查器登记形参符号 | `checker.cr:2169-2210` | 用 scope 符号解析（泛型形参注册为 `TYP_GENERIC_PARAM` 行，`checker.cr:2177-2187`） | 不受影响（检查的是声明） |
| 对齐环之外的另一 `ast_b` 读点 | `checker.cr:2016` | 调用检查（声明面） | 不受影响 |
| 实例 FuncInfo 形参型拷贝 | `monomorph.cr:844-850` | 抄声明 | 不受影响；**无读取点**（§1.1-7） |
| mangling（实例名） | `monomorph.cr:836-841`（`orig_name + "[" + type_args + "]"`） | 由**调用点**类型实参拼 | 不受影响 |
| 实例缓存键 | `inst_key_of_ti`（`monomorph.cr:177-230`） | 由**调用点**类型实参行拼 | 不受影响 |

### 1.4 未验证面（实施前必须先测，不得据本文件推定）

- 【推断·未验证】泛型**结构体**实例是否有同族面（`g[dex]` 之类的结构实例）；本文件只覆盖 `EXPR_FN` 实例路径。
- 【推断·未验证】复合形参面的代入完备性：`[T; N]` / `&T` / `*T` / 元组形参 在 `gen_clone_tree` 下的产物（`inst_type_node_of_ti` 对元组/复合实参已知回 -1 ⇒ 有**未覆盖面**，见 `monomorph.cr:160-175` 契约注释）。
- 【推断·未验证】`-O 1` 与 `run` 两形态下同一实例的行为是否与 `build` 一致（对齐环只在 `build`/`run` 共用的 ir_gen 路径上，理论上同源，未测）。
- 【推断·未验证】`.ccr` TYPE 段是否新增行（实例代入出的类型行多半**已由调用点存在**；须实测段计数）。

---

## 2. 修法候选（≥2）与影响面矩阵

### 候选 A（推荐）：实例侧形参链具体化 + 调用点按实例签名对齐（A1 + A2 成对）

- **A1（被调面）**：在 `gen_create_instance`（`monomorph.cr:812-870`）克隆体之后，**同一趟**克隆声明形参链：
  逐形参克隆 `EXPR_PARAM`（复用 `monomorph.cr:604` 分支语义 ⇒ 类型节点经 `EXPR_IDENT`/`EXPR_OPTIONAL` 分支代入），
  组成新的参数 run，`ast_set_b(new_fn_node, <新首形参>)`（`c` 保持 = 形参数）。
  **必须同时重算 `param.type_val`**：现有 `EXPR_PARAM` 分支把 `tv` 原样拷贝 ⇒ 泛型形参的 `type_val` 仍是 0，
  §1.1-3 的槽型仍会是 `TI_INT`。做法对齐 parser 的写点（`parser.cr:1472-1476`：基类型时 `param.type_val` = 基类型码）。
- **A2（调用面）**：让调用点对齐环看到**实例**签名。两条实现路线（择一，**不得两都做**）：
  - A2a 后置第二遍：在重定向后（`ir_gen.cr:2076` 之后、IR_CALL 发射前）用实例 `cfi` **再跑一遍**对齐环；
    前提 = 环已抽成 cfi 参数化、幂等的函数（**这正是 `aaa054fe` 半成品的形态**，见 §7）。
  - A2b 前置换序：把泛型解析（`ir_gen.cr:2072`）**移到对齐环之前**，使 `cfi` 一开始就是实例。
    注意 `type_args` 的回落路径（`ir_gen.cr:2048-2066`）按实参 var 型取名字串，`TI_DEX`/`TI_DEX_S` **同映射 "dex"**
    ⇒ 换序对键无影响【读码】；但换序会改动**所有调用**的路径（面大、回归风险高）。

**A 的代价**：A1 增 AST 节点（每实例每形参 + 类型子树）；A2 触及调用发射前的公共段。**A1 单独无效**——调用点仍按声明面判门 ⇒ 实参仍以 `TI_DEX` 走 XMM（§1.1-5/6），失配只是**换向而不是消失**。

### 候选 B：消费点按实例绑定解析（不改 AST）

- 实例创建时把**代入后的形参类型行**持久化（现表 `g_gen_instances` 只存 `(源 fi, 键串, 新 fi)` 三元组，`monomorph.cr:861-866` ⇒ 需新表或键串回解）；
- 新增单入口 helper（如 `inst_param_slot_ti(fi, pi)`），把 `ir_gen.cr:3071-3081` 与 `ir_gen.cr:1984-1985` **两处**都改为经它取值。

**B 的代价**：替换逻辑要在**没有 `gen_clone_tree` 代入机制**的情况下重写一遍（`T`、`T?`、`[T;N]`… 都要自己解），
且「两处读点都要改」——漏一处即静默类（正是本条要消灭的形态）。收益 = 零 AST 扰动。
**结论：机制上可行、工程上比 A 贵且更易漏；只在前者被实测证否时启用。**

### 候选 C：只修 dex 族（分层最小面）

只对「代入后为 `dex` 族」的形参做具体化（序言 + 对齐门）。面最小、最快拿到红→绿，但：
① 概念上只治 ABI 面的一半（`T=string` 等槽型仍错，语义保鲜哲学里 IR 型不忠实）；
② 若作为**终态**交付即是变通（铁律 1）。**定位 = 可选的落地分期（先 dex 族收口 → 再一般化），不是替代方案**；是否分期由维护者裁。

### 影响面矩阵（每格：该候选是否改变 / 需要注意什么）

| 面 | A（A1+A2） | B | C |
|---|---|---|---|
| 形参槽型（`new_ir_var` 的 IR 型） | 改（T=dex ⇒ `TI_DEX_S`，其余按代入型） | 改（同左，经 helper） | 仅 dex 族改 |
| 寄存器类 / ABI（`callseq.cr:66-89`） | **必须 A2 成对才改**（实参由 `TI_DEX`→`TI_DEX_S` 转 GP） | 同左（同样须改对齐门） | 仅 dex 族改 |
| `.ccr` TYPE 段 | 预计**不新增行**（代入型行由调用点已建）【推断·未验证】；NOD/instr 段必变（实例体槽型） | 同左 | 仅 dex 族变 |
| `.cir` 快照 | **不涉**（含泛型声明的单元整体关缓存，见 §3） | 同左 | 同左 |
| `inst_key_of_ti`（实例键） | 不改（键自调用点类型实参拼） | 不改 | 不改 |
| mangling | 不改（`orig_name[type_args]`） | 不改 | 不改 |
| 需要同步的读点数 | 2（序言 + 对齐环）；A2a 增量式、A2b 改公共路径 | 2（同上，且要自建代入） | 2（门内加白名单） |
| 主要风险 | 参数 run 的**布局契约**与 dedup 交互（见下） | 漏读点 ⇒ 静默；键串回解脆 | 半修被当成修完（口径风险） |

### A1 的实现陷阱（实施轮必须逐条核）

1. **参数 run 的扫描契约**：序言与对齐环都靠「`pn = pn+1` 起向前扫到下一个 `EXPR_PARAM`」（`ir_gen.cr:3116-3120`、`ir_gen.cr:2003-2007`）定位下一形参；
   克隆必须保持「形参节点与其类型子树相邻、且按形参顺序分配」，否则扫描会跨过形参或串位。
2. **dedup 交互**：`gen_dedup_add` 按**源节点**去重（`monomorph.cr:479-483`）。两个形参共享同一类型节点时，第二次克隆命中 dedup
   ⇒ 类型子树不会重复分配（扫描仍能落在下一个形参上，**但须实测确认**）。
3. **`type_val` 重算**（§2-A1）：不重算 = 槽型仍 `TI_INT`，整个修复白做——**这是最易漏的一步**。
4. **`ast_data(pn)` 的消费点**：`ir_gen.cr:3086-3091`（`ptn := ast_data(pn)` → `irv_set_decl_ti` / optrep 形参信道）在 `g_optrep_on` 下走 `res_type_node(ptn)`；
   代入后的类型节点必须能 `res_type_node` 成功（`inst_type_node_of_ti` 合成的节点满足「根节点占单槽」契约，`monomorph.cr:170-175`）。

---

## 3. 换代问题（**按实测决定**；禁止写「实施时必须换代」）

### 3.1 结构性事实（【读码】）

- **两道缓存门都关**：`main.cr:484-490` 扫描全部函数，任一 `fi_generic_count > 0` ⇒ `cache_enabled = 0`；`main.cr:495-497` 可选表示面开启 ⇒ 同样关。
  ⇒ **任何含泛型声明的编译单元整体不读不写 `.cir`**（实例是在 IR 生成期由泛型声明派生的 ⇒ 无声明则无实例；扫描在 IR 生成之前 ⇒ 声明必被看到）。
- **身份自动失效**：`.cir` 头含运行中编译器 ELF 的**全文件 FNV-1 哈希**（`cir_cache.cr:142-168`），装载时比对
  （`cir_cache.cr:500-502`：`if cid == 0 || entry_cid != cid { return -1; }`）⇒ 重建后的编译器**天然拒绝**旧二进制写的条目。

### 3.2 本轮只读实测（【实测·本轮只读】）

| 缓存目录 | 条目数 | 版本分布 | 名字含 `[`（实例形） |
|---|---|---|---|
| `/home/DslsDZC/core/.core/cache/cir` | 81 | 全 17（≠ 现行 20） | **0** |
| `/home/DslsDZC/core-optdex-ws/.core/cache/cir` | 95 | 全 20 | **0** |
| `/home/DslsDZC/core-cachebloat-ws/.core/cache/cir` | 0 | — | — |

复现命令（只读、无构建）：
```bash
python3 -c "
import glob,struct
for f in sorted(glob.glob('.core/cache/cir/*')):
    b=open(f,'rb').read(64); m,v,i,fp,s,nl=struct.unpack_from('<qqqqqq',b,0)
    print(v, b[48:48+nl].decode('utf8','replace'))" | sort | uniq -c | sort -rn | head
```
**读数结论**：现存快照中**零条**实例 IR（`[` 名条目为 0）。与 §3.1 的读码结论一致。

### 3.3 判据（实施轮照此执行，**不得**预先承诺结论）

1. 实施后、重新构建后，**再数一次**上表三列（含「当次构建产出的新条目」）。
2. **> 0**（出现实例名条目）⇒ 必须 `CIR_CACHE_VER` 19→20 式换代**并给出归因**（哪条路径写出的、为何门没挡住）；
   同时按批 5/批 7 的载体清单纪律 grep 全量枚举并重锁版本载体。
3. **= 0** ⇒ 登记「**无复活通道，故不换代**」，并把 §3.1 两条结构性事实写进归因（不是「忘了换」）。
4. 无论 0 与否：**禁止**在计划/实现里写「实施时必须换代」这类预置结论（本轮口径，逐字遵守）。

---

## 4. 判据要件

### 4.1 先扫「既有判据网会不会挡路」（实施前第一步；【本轮只做清单，未跑】）

必须**逐条实跑并分类**（红 = 三类之一：① 真被修好的缺陷 ② 预期重锁 ③ 停，见 §5）：
`src/ci/run.sh` 五档 job（`check` / `bootstrap-tests` / `selfhost-tests` / `suite` / `full-bootstrap`）+
`tools/baseline/` 回归网（canary / parity / probes / warm）+
高相关套件：`tests/selfhost/test_ccr_v7.py` · `test_ccr_types.py` · `test_optional.py` · `test_apx_conversion.py` · `test_generic_constr.py` · `test_compile.py` · `tests/suite/generics_test.cr` · `tests/probes/*`。
**特别注意**：`tests/harness/test_diag_gate.py:77` 一类**正控**（要求某诊断必现）——本修复若改动诊断面必咬；
载体清单（版本/哈希锁）必须**grep 全量枚举**，与批 5 §7ter 同法，不得凭印象列。

### 4.2 新判据（承重件）

- **P1（两腿同判 + 机理无关锚）**：最小泛型探针（`T=dex` 族），锚定值由**规范/算纸**给出（不是「ELF 给什么就是什么」）；
  `interp` 与 `ELF` 两腿都必须等于锚定值；**且检查面（`check`）零诊断**（区分 §6 的甲）。
- **P2（ABI 面显式断言）**：对实例形参槽型断言（IR 面读回：`dex` 族形参槽 = `TI_DEX_S`），
  并对**调用点**断言实参转换（转换后实参 var 型 = `TI_DEX_S`）——两面同判，防「只改一侧」。
- **P3（不变性 / 零产物断言，落 build 面）**：**不含泛型实例**的语料，ELF 与 `.ccr` **逐字节不变**
  （判据落在 `build` 产物面，不是 `check`/`cir` 面）；含泛型的语料允许变，但必须能逐条归因到本修复。
- **P4（三态安全）**：本修复不涉缓存，但仍须覆盖 `check`（诊断）/ `build`（ELF）/ `run`（interp）三态同源；
  另加 `.cir` 缓存开关两态（冷/暖）——**暖态只对「不含泛型」的语料要求**（含泛型本就关缓存）。
- **P5（突变自证 / 可证伪）**：预置至少 2 个突变，每个必须让 ≥1 条判据转红：
  ① 去掉 A1 的 `type_val` 重算（§2-A1-3）⇒ P2 槽型断言必红；
  ② 去掉 A2（保留声明面环）⇒ P1 两腿分歧必红。
  **突变不红 = 判据无效，不得交付**。

### 4.3 与既有判据网的关系

本修复会改变**含泛型单元**的 IR/产物 ⇒ 任何以「该语料字节哈希」为判据的条目都可能需要**重锁**（带归因）。
重锁清单要在实施轮用 grep 全量枚举（载体纪律），不得只改跑红的那条。

---

## 5. 停条件（机械可判）

- **S1 · 路径集**：出现「本批路径集之外」的改动需求
  （路径集 = `src/compiler/monomorph.cr` + `src/compiler/ir_gen.cr`（序言段/调用点段）+ 判据文件 + 计划文件）⇒ **停**、回报。
- **S2 · 套件转红**：任一既有套件转红且**不能**归入「预期重锁清单」⇒ **停**、回报。
- **S3 · 引文逐字比对**：实施后把 §1.1 引用的每一行与代码**逐字**比对；行漂移或语义不符（如 `monomorph.cr:600` 的 `b/c` 复制被改过、门位置变了）⇒ **停**，先校正机理段再继续。
- **S4 · 单根因 vs 双根因**：A1+A2 落地后判据仍红 ⇒ 判「是否第二条独立根因」：
  若第二条根因**不属于**表示面（例如落入检查面 §6 的甲）⇒ **停**，登记为独立条目，**不顺手扩面**。
- **S5 · 突变未红**：任一预定突变未使判据转红 ⇒ **停**，判据无效，重做判据。

---

## 6. 与 (甲)/F3 的边界（可独立验证 + 哪条判据区分）

| 维度 | (乙) 表示面（本计划） | (甲)/F3 检查面 |
|---|---|---|
| 定义 | 实例形参的**槽型/寄存器类/ABI** 与 IR 型保真 | 实例体**检查/诊断**（按 `T` 未解析检查，`h[dex]` 体内 `@raw_int(t)` 报 TF07【实测·转述】） |
| 现象面 | `interp` ↔ `ELF` **值分歧**（检查面零诊断） | `check` 面**诊断有无/归属**错（运行值可正确） |
| 修复触碰面 | `monomorph.cr` + `ir_gen.cr`（序言/调用点） | checker/诊断侧（不改 IR 槽型） |
| 区分判据 | **P1**（两腿同判 + check 零诊断） | 甲侧判据 = `check` 面诊断断言（正控 `test_diag_gate.py` 形态） |
| 交叉风险 | 若 (乙) 修完 P1 仍红且 `check` 面出现诊断 ⇒ 可能触发甲的联锁 | 甲若用「实例体复检」实现，需确保不改变 IR 槽型（否则越界到乙） |

**独立性判据**：两批各自的判据都必须在**对方未落地**时能红/绿自证（乙看值、甲看诊断）；两批共用同一探针语料但断言面不同。

---

## 7. `aaa054fe` 半成品（环抽函数 `dex_align_call_args`）评估

**它是什么**【读码 `aaa054fe`】：
- `ir_gen.cr:1254-1300`：把原内联的对齐环抽成 `dex_align_call_args(cfi, ac, arg_vars, arg_nodes, arg_nodes_cap)`，
  **cfi 参数化 + 幂等**（只对 `irv_type(av) == TI_DEX` 生效 ⇒ 重定向前后各跑一遍安全）；
- 两个调用点：`aaa054fe:2178`（重定向前，原位置）与 `aaa054fe:2246`（**重定向后第二遍**——正是 (乙)-A2a 需要的缝）；
- 同一提交还带 `tests/selfhost/test_opt_eq.py`(+174) 与 `src/ci/run.sh`(+12)。

**结论（回答 lead 的第 7 问）**：
1. **形态上正是 A2a 的一半**（「重定向后按实例签名再跑一遍」的机制已就位，且幂等性已论证）；
2. **对 (乙) 自身零效果**（【读码确证】）：第二遍的门（`aaa054fe:1274-1275`）读**实例形参链**，
   而实例链 = 声明的共享链（§1.1-1）⇒ 门恒假（`T` 的 `ast_type_val`=0；`T?` 的 inner kind≠0）——与 plan-optdex 实测一致；
3. **不可原样复用**：① 它在**另一条链**上（未进 develop）；② **热路径遗留调试打印** `println("DBG gate …")`（`aaa054fe:1276`）必删；
4. **建议**：若批 8 先合入 develop ⇒ 以 develop 上的形态为基线，只补 A1；若未合 ⇒ **在新链上重做等价抽取**（不带 DBG），
   并**不**把 `aaa054fe` 当依赖 cherry-pick（跨链 + 未过判据）。两条路线都须在计划里登记**批次顺序依赖**。

---

## 8. 未决（交维护者裁）

1. A2 走 **A2a（增量第二遍，需先有环抽）** 还是 **A2b（换序，面大但无前置依赖）**？
2. 是否按候选 C **分期**（先 dex 族 → 再一般化），还是直接上 A（一般化）？
3. §1.4 的未验证面（泛型**结构体**实例、`[T;N]` 等复合形参）是否纳入本批范围，还是各自独立条目标记？
4. 是否需要在本批一并把 `fi_param_type` 更新为代入后代码（无读取点，纯语义保鲜；成本低但会动 `monomorph.cr:844-850`）。

### 裁定（2026-09-18，lead。**原文未决四条保留在上述不动**；本节 = 后加的裁定层）

- **裁 1｜A2 走 A2a（增量第二遍），A2b 不做。**
  依据：A2a 是**加法式**改动（重定向前后各跑一遍，环幂等）；A2b 把实例键计算面挪到环前，可能动 mangling/实例键，面大且无必要。
  **前置条件（一票否决）**：动手前须在改前构建上实测「泛型 `d : dex, apx` 调用点的 `.cir` 中**不含**转换指令（`dex_opt_dex`/`IR_APPROX` 类）」——
  证实「第一遍被门 `ir_gen.cr:1984-1985` **整个跳过**」⇒ 第二遍是**唯一**一次转换，无叠加风险；
  若实测发现第一遍**已发过**某种转换 ⇒ **停**、报 lead（性质变为撤销/幂等处理，另裁）。
- **裁 2｜直接上 A（一般化），不做 C 分期。**
  依据：C = 给已知普遍缺失打专属补丁（(甲) 批次已被同一理由否掉）。
  **前置条件**：先做一遍**全语料影响面扫面**（照 F3 的 S1 形态）——枚举「实例形参链被具体化」的影响面
  （哪些语料 / 哪些函数 / 多少实例 / 分别落在 GP 类还是 XMM 类），**报 lead 之后**才进实现。
- **裁 3｜复合形参不入批**（泛型**结构体**实例、`[T;N]` / `&T` 等）：未验证面入批 = 让判据建立在假设上
  （§1.4 三项未被验证）；各自**登记为独立条目**，本批只做**已验证的标量形参链**。
- **裁 4｜`fi_param_type` 不动**：无读取点 ⇒ 无观测面 ⇒ 没有判据能抓住「改错的时刻」；
  在 (乙) 条目内注明**可选语义保鲜项**，本批不碰（见 §1.1-7、§2 矩阵）。
- **裁 5｜判据面补一条（幂等/不重复转换）**：构造「第一遍已正确」的用例，断言第二遍是**无操作**
  （`.cir` 逐字节或指令计数不变）——与 §4.2-P5 的突变配套（突变不红 = 判据无效）。

**⚠ 时序依赖（硬）**：A2a **依赖批 8 的环抽 `dex_align_call_args`**（`aaa054fe`，见 §7）。
⇒ ① **实施链等批 8 合入后再开**（lead 已令 `plan-optdex` 摘掉 `aaa054fe:1276` 的 DBG `println`、**保留**环抽，
并在其提交信息注明「(乙) 的 A2a 依赖此抽取」，防被当死码删）；
② **在此之前不得在任何分支复制一份环抽**（两条链各带一份必然在合并时打架）。

**执行序（不许压缩）**：先测后改，两测都是一票否决 —— ① A2a 前置实测（第一遍是否整个被门跳过）② 全语料影响面扫面
⇒ **报 lead** ⇒ 才进 A1 + A2a。纪律依据：本仓两次翻车（§1-1 / §1-5 机理）都是「先信机理、后补实测」。

---

## 附录 A：本轮实读锚点清单（file:line，develop = 3e461edd）

- `src/compiler/monomorph.cr:600`（EXPR_FN 克隆：只深克隆 `d`）· `:604`（EXPR_PARAM 分支，实例路径不可达）
- `src/compiler/monomorph.cr:177-230`（`inst_key_of_ti`）· `:836-841`（mangling）· `:844-850`（FuncInfo 形参型抄声明）· `:858`（`fi_set_generic_count(new_fi,0)`）
- `src/compiler/ir_gen.cr:1127-1134`（`dex_opt_type_node` 契约）· `:1964-2008`（调用点对齐环；门 `:1984-1985`）· `:2048-2066`（键回落路径）· `:2072-2077`（实例重定向）· `:3071-3091`（形参序言）· `:3116-3120`（形参链扫描）
- `src/compiler/checker.cr:2016` / `:2169-2210`（形参链读点；泛型形参注册 `:2177-2187`）
- `src/compiler/dyn_arr.cr:462/466/472`（`fi_param_count`/`fi_param_type`/`fi_set_param_type`）· `src/compiler/parser.cr:1472-1476`（parser 写形参型）
- `src/os/linux/callseq.cr:66-89`（寄存器分派按实参 var 型）· `:95-106`（栈参同规则）
- `src/compiler/main.cr:484-490`（泛型关缓存）· `:495-497`（optrep 关缓存）· `:539`/`:573`（读/写调用点）
- `src/compiler/cir_cache.cr:80`（`CIR_CACHE_VER = 20`）· `:142-168`（身份 = 全文件 FNV-1）· `:500-502`（身份拒绝）
- `aaa054fe`：`src/compiler/ir_gen.cr:1254-1300`（环抽）· `:1276`（DBG println）· `:2178` / `:2246`（两遍调用点）
