# `(甲)` 刀 4 ＋ 两句语言裁定：**联合路线图**（纸面，未实施）

- **产出**：`b8-e1`，2026-09-19
- **基线**：`develop@origin` = **`c1044645`**（落盘时**现查**；引用一律用**符号锚**，行号只作示例）
- **状态**：**纸面**。本轮**不改 `checker.cr` 行为**、**不碰 `ld.cr`**（`plan-optdex` 的 B/C 迁移在飞）、**不推**。
- **本文件的两句裁定来自维护者（lead 转述，原话级）**——见 §A。

---

## §0 结论前置（红旗：**A 类的「排除」证不出来**）

**A 类（49 点）无法写出满足两向钉子的判据式**——它与真异型**在类型面上逐字段同形**，本仓**也没有可用来区分的 handle 型**。

| 来源 | 台账行（同一台二进制、同一格式） | 形状 |
|---|---|---|
| **A 类**（语料真样本，`tests/suite/conc_test.cr`） | `9917 523 23 0 3 0 sched_enqueue` | **`p=3(string) ← a=0(int)`** |
| **真缺陷**（对照探针 `fn takes(s: string)` ← `n : int = 7`） | `9917 5 11 0 3 0 takes` | **`p=3(string) ← a=0(int)`** |

**三型分类（实据）**：
1. **45 点**：实参 = `cur_g := sched_get_curg()` / `recv_wait := r64(ch, 48)` ⇒ **普通 int 变量，AST 看不出 handle**（`r64` 按 `-5` 是设计旁路、其 int 返回有契约）；
2. **4 点**：`ch := go square(21)` ⇒ **变量中介**（`EXPR_GO` 的 int 先落变量再传 `chan_recv`）⇒ 与 `#2026-09-19-4` 的 `await` 那条**同型**；
3. **无 `TI_HANDLE` 常量**：`grep 'TI_HANDLE\|TI_CHAN\|TI_GOROUTINE' src/compiler/ast.cr` = **空**（handle 就是 `string`，A₂ 定的模型）。

**推理链**：抑制 49 点的唯一可实现规则 = 「按 (callee, param) 列全」（`sched_enqueue(g)` · `chan_recv/chan_send/chan_close(ch)` · `g_free(g)`）⇒ 该规则**必然让「int 变量 → `chan_recv`」不再报** ⇒ **反向钉子按构造不可能存在**。
⇒ **且 (丙) 也退化**：要「刀 4 不覆盖 A 面」**也需要一条抑制规则** ⇒ 同样回到 (乙)。
⇒ **唯一出路 = (甲)：把 A 的产出侧真正改型** ⇒ **这正是维护者裁定一**（§A）。

---

## §A 两句维护者裁定（原话级）与各自的性质

### 裁定一（**新语言特性**）

> `go f(x)` 的型 = **引入句柄类型 `Future<T>`**（携带元素型；`await` 从它取值型）。

- **性质 = 新语言特性**（不是修补）：新类型构造子 + `await` 取值型规则 + 引擎（`ty_shadow` / `type_terms` / 变型表）+ `res_call_type` 分支 + 语料。
- **连带待定**：`chan_recv` / `chan_make` / `g_new` / `sched_go` 这一族**要不要一起改成 `Future<T>` / `Chan<T>`**——**A₂ 当时把它们对齐成了 `string`**（裁定原文：「**(A)：语料/stdlib 向模型对齐**（**(C) handle 兼容明令禁止；(B) 改 `alloc` 模型亦否**）」）⇒ **那条对齐与本次裁定冲突，必须显式处理**（§7 第 7 项，本计划最大未决点）。
- **⇒ 刀 4 押后**：前置从「三条」变为「三条 + 本特性的实现」。

#### 裁定一的**追加裁定**（2026-09-19，维护者**原话级**）：**A₂ 的对齐作废，那一族一起改**

> **A₂ 对齐作废，那一族一起改。**

**A₂ 的裁定原文**（PR **#118** / `cc8f1b836669`，**逐字保留、标记作废**——本仓惯例：**只增不删，不静默替换**）：
> **(A)：语料/stdlib 向模型对齐**（**(C) handle 兼容明令禁止；(B) 改 `alloc` 模型亦否**）

**⇒ 作废**（2026-09-19 维护者裁定）：**(C)「handle 兼容明令禁止」不再成立**——改法改为「**引入携带元素型的句柄类型，那一族一起改**」。
⇒ **A 面 49 点可全收（含那 45 点）**；§7 第 7 项由「最大未决点」**解封**（见 §7.1/§7.2/§7.3）。

**留痕形态**：A₂ 原文**原文保留在上**，作废标记与今日裁定附于其后——**不是替换**。

### 裁定二（**改契约**）

> `&x` **改判 `TYP_REF`**（不是「调用点拒」）。

- **性质 = 改契约**：`&x` 产出 `*int` 是**文档化语义**——**编译器自己的用户可见诊断**就这么写（`error[TA02]: Variable declared as &int, got *int`）⇒ 按本仓口径**必须单独立项**。
- **`#2026-09-19-1` 的状态随之变更**：**「定性待裁」→「已裁：改判 `REF`，待实施」**（该条目即本裁定的载体）。
- **它独立可做**，且**消掉了 F 类那 2 点**：改判后 `&x` 与 `&T` **同 kind** ⇒ F 的「REF 形参 ← PTR 实参」自然不再存在 ⇒ **F 的临时排除不必写**（§2.2 因此从「排除」改为「由裁定二消解」）。

---

## §B 依赖序（含本代理意见）

**结论：`&x` → `TYP_REF` 先做；`Future<T>` 大而独立，可并行。**（与 lead 倾向一致。）

**支持理由（我补三条）**：
1. **体量**：裁定二 = 五层逐处 + 三处载体 + `(i)` 白名单，**自足且可在单个小批闭合**；裁定一 = 新类型构造子 + 引擎 + 两处定型 + IR 槽 + 语料，量级大一个数量级。
2. **它消掉的不对称正是实参面校验要依赖的东西**：F 类 2 点**因改判自然消失**（不必写临时排除）⇒ 先做它，刀 4 的未落环排除**从两条减到一条**。
3. **它解锁 `#2026-09-19-1` 的验收**（该条已「待实施」）。

**⚠ 我的一条保留（不是反对排序，是同批必须处理的）**：裁定二在**安全面是「减少检查」**——`region_check` 三处 + `provenance_verify` 一处**只认 `TYP_PTR`**，改判后**对 `&x` 派生的值不再生效**（**静默**，不会红）。⇒ **必须与裁定二同批补等价检查**（或显式登记「该面覆盖率下降」），否则是**用一次静默换一次不对称**。

---

## §C 对刀 4 三条前置的影响 + 刀 4 的位置

| # | 原前置 | 现状 |
|---|---|---|
| ① | 台账跳 never（+ 三条哨兵族 skip） | ✅ **仍成立**（`#142` / `c1044645`） |
| ② | 63 点逐类处置表 | **被替换**：A 归**裁定一** · F 归**裁定二** · D **已修**（`#140`）· E **已排除**（`#142`）⇒ 表**只剩 B/C**（迁移，`plan-optdex` 在做） |
| ③ | 刀 2 落地 + 重跑 `0 / 6 / 63` | ✅ **仍成立**（`#139`）；真面现 = **57**（`63 → 61 → 57`） |

**⇒ 刀 4 的位置**：**排在「裁定二」之后、与「裁定一」的完成解耦**——
- 裁定二落地 ⇒ F 面消失 ⇒ 刀 4 的未落环排除**只剩 A**；
- 但 **A 仍受裁定一制约** ⇒ **刀 4 的实参硬错要覆盖 A 面，必须等裁定一**；
- **可选中间态**：若要更早拿到硬错价值，可**先覆盖 B/C/D/E/F 面**（不含 A），并把「A 面不在覆盖内」**写进触发条件 + 登记**（这是 (丙) 的**降级版**，只在裁定一未完成期间成立；**不要在裁定一完成后继续保留**）。

**⚠ 排期更新（2026-09-20，维护者确认 `Buf` 后）**：
- **裁定二 → 「拆 `string`」整批（`Buf` + `Chan<T>` + `Future<T>` + `G`，**必须同批**）→ 刀 4**；
- **「拆 `string`」不得拆开做**：中途会出现「有的句柄是新类型、有的还是 `string`」的双模型并存（§7.3 排期注）；
- **`(i)` 的最终语义并入该批**（与 `Buf` 一起定），**在此之前不再单独改**（§7.3 连锁 3）。

---

## §1 触发条件的最终形态（逐项写死）

```
报错 ⟺  调用位点有 callee
      ∧ ¬内建回退（fi ≥ 0）
      ∧ pti ≥ 0
      ∧ ¬skip族                       /* 见 §1.1 八条（已全部落环） */
      ∧ ¬A句柄产出通道                 /* 由裁定一处理；未落地前 = 中间态排除（丙降级版） */
      ∧ ¬unify_types(pti, ati)
```

**F 的项已删除**——裁定二落地后 `&x` 与 `&T` 同 kind，F 面自然消失（§A）。

### §1.1 已落环的八条 ⇒ **不会**产生硬错（**勿再列一遍**）

`pti < 0` · `ati == TI_UNIT` · 整型字面量 · `None → T?`（既有四条）· `ati == TI_NEVER` · `ati == TI_DYN` · 泛型形参型（`#142` 三条）· **内建回退**（无 `FuncInfo` ⇒ `fi < 0`，`-5` 的免检契约）。

---

## §2 未落环的排除（**只剩 A，且它归裁定一**）

### §2.1 A —— **不走「排除」，走裁定一的产出侧改型**（§0 已证排除不可能）

### §2.2 F —— **由裁定二消解，不写排除**
改判后 `&x` 是 `TYP_REF`，与 `&T` 形参**同 kind** ⇒ `n19_ref_named.cr` 的 2 点**自然归零**。
**判据（裁定二的验收）**：该档 `CORE_S1P=1` 台账 **2 → 0**；**反向钉**：真正的 REF/PTR 混用若仍存在（如 `*T` 形参 ← `&x` 的值经变量中介）**仍须报**。

---

## §3 语料影响预测 + 验法（**先写死，再取数**）

| 面 | 预测（裁定二落地后） | 理由 |
|---|---|---|
| `tests/probes/n19_ref_named.cr` | **2 → 0** | F 由改判消解 |
| 闭包外清洁档 | **57 → 55**（F 的 2 点消失；A 的 49 点在裁定一前**保持**） | 逐档实量 |
| `src/compiler`（40 档闭包） | **0** | 自源在实参面本就 0 点 |
| `targets`（24 档闭包） | **0**（**前提 = B/C 已合**） | 未合 ⇒ B/C 6 点转红 |
| `ptr_ref_first.cr` / `test_diag_gate.py` / `test_ccr_types.py` | **转红**（须同批改，见 §8） | 三处载体依赖「`&x` = PTR」 |
| canary 五条 + `.ccr` 四条 | **不变**（须实测） | 改判是**类型面**、不进发射面——**但 `@raw_int` 若改白名单会动判定面** ⇒ 逐字节对拍照旧 |

**验法**：逐档 `CORE_S1P=1 check` 点集比对 · 两 project 单元（rc + 未定义双零）· `CI_JOB_NAME=suite` · `canary_check.sh` 5/5（对未改动值表）· 同输入 pre/post **逐字节**（`out` + `out.ccr`）。

---

## §4 判据档骨架（**先清单不写码**）

**A 正向（到具体错误码，不只断 rc）**：`string` 形参 ← `int` 变量 ⇒ **rc=1 + `error[TF07]` + 定位行号 + build 面零产物**；换 `bool`/`dex` 各一；泛型实例化后实参位各一；**方法/模块限定/枚举构造三形各一**（刀 2 的四形态覆盖 ⇒ 硬错须同样覆盖）。

**B 反向（防误杀）**：`g(d, d)`（`T?` 规则）rc=0 · `chan_recv(go work())` rc=0（**取决裁定一**）· `@str_of(p)` 显式视图（`unsafe` 内）rc=0。

**C 对照（防「一律红」）**：完全无关两档 rc=0。

**D `skip` 规则 × 钉子对照表（七条整张）**：`pti < 0` · `TI_UNIT` · **整型字面量（钉子必须变量形）** · `None→T?` · `TI_NEVER` · `TI_DYN`/泛型形参 · **内建回退**——逐条问「我这颗钉子会不会落在它里面」。

**E 突变自证（强制形状）**：`re.subn` 计数 == 1 · 打印命中上下文 · 剥注释/去串类预处理**先单独自证**。

**⚠ F 撤条的连带（**这条很重要，是刀 4 的一部分、不是顺手**）**：`diag.cr` 的 **TF07 build 豁免自带退出条件写着「退出 = #2026-09-16-1/F3 调用位点收口后撤」**——**刀 4 就是那个调用位点收口**。要素：
1. **撤条本体**：`diag.cr` 的 TF07 build 豁免（`if scope == GATE_SCOPE_BUILD && code == EC_TF_ARG_TYPE { return 1; }`）——**且那里重复两份**（同一 `if` 写了两遍，`:186` 与 `:190`）；
2. **连带改**：`tests/selfhost/test_diag_gate.py`（**正控 5 条之一 = TF07**；头注还写着「TF07 是 build-scope 豁免码 ⇒ build 面恒过」）· `tests/selfhost/test_iface_ops.py`（**3 处** TF07 计数断言）；
3. **关系**：本仓既有口径「`diag.cr` 的 TF07 build 豁免须随硬错化重定」——**现在有了确证的退出条件（代码里的原话）**。

---

## §5 自伤面核查

`src/compiler` 全闭包在实参面 **0 点**（四形态下实测）⇒ 硬错**不应**自伤自举。**但必须实测**：`check src/compiler`（rc=0 且仍 0 点）· `check src/targets/x86_64-linux` · **`full-bootstrap`**（三阶段 `cmp` 逐字节；串行化 + 跑完清 `/tmp/corec2`、`/tmp/corec3`）。

### §5.1 ⚠ **工作区 stale 处置：强制前置（2026-09-19 由「配方」升为「前置」）**

**凡本计划涉及的任何 worktree，stale 处置一律：先 `cp -a`（或逐档 `cp`）到仓库外 ⇒ 再 `update-stale` ⇒ 再复核 sha。**
- **依据（当日第三次实证）**：`update-stale` 实测 `modified 1 files, removed 1 files`——**它会把工作副本里未快照的内容换掉**；那次正是**守卫副本接住**（`/tmp/guard-blade4-plan-v2.md` 213 行，恢复后 sha 逐字相同）。
- **判据**：`update-stale` 之后，**逐档 sha256 与守卫副本对拍**（不是「文件还在盘上」——本仓有四次前科）。
- ⇒ 这条**同样适用于 §8 施工序的每一步**（改判期间会频繁切基点）。

### §5.2 ⚠ **独立验收项：「安全面不得净减」**（裁定二专属，**不是附注**）

> **改判前生效的检查，改判后要么仍生效，要么显式登记为「已知不再生效」并给理由。**

- **背景**：`region_check`（`rc_return_escape` / `rc_store_escape` / `region_check_func`）与 `provenance_verify`（TU03 门）**只认 `TYP_PTR`** ⇒ 裁定二后**对 `&x` 派生的值不再生效且不报（静默）**。
- **为什么必须独立立项**：**「减少检查」不会红**——它是**静默面**，正是本批一路在收的形态（用一次静默换一次不对称）。
- **判据**：逐处列出「改判前是否生效 / 改判后是否生效」，**任一处从「生效」变「不生效」且无显式登记 ⇒ 本项红**。
- **处置二选一**：**(a)** 扩展该处接受 `TYP_REF`（等价检查）；**(b)** 显式登记「已知不再生效 + 理由 + 覆盖面损失」，并在批报告写明。

### §5.3 ⚠ 裁定二的影响面**三分**（2026-09-20 实核；**§5.2 只覆盖类 1**）

**扫面**：`src/` 全扫 `TYP_PTR` = **51 处**（`type_selftest.cr` 11 处为测试构造 ⇒ 生产 `.cr` **39 处**）。按「会不会因改判失效」三分——**处置方式不同，不得混为一谈**：

| 类 | 处数 | 站点（符号锚） | 改判后 | 可否「登记了事」 |
|---|---|---|---|---|
| **1 安全门** | 4 | `region_check.cr:63`（`rc_return_escape`）· `:99`（`rc_store_escape`，变量名 `val_ti`，非 `ptr_ti`）· `:119`（`region_check_func`）· `provenance_verify.cr:67`（TU03 门） | 早退 ⇒ **静默**失去覆盖 | **可以**（= §5.2 的 (a)/(b)） |
| **2 `@raw_int` 实参门** | 1 | `checker.cr:4239` | **硬错**（非静默） | **不可以**——归刀 4 实参面家族 |
| **3 类型身份 / 实例键 / mangling** | 见下 | `monomorph.cr:207`/`:214` · `ty_shadow.cr:349`/`:903` · `iface_axis.cr:206`/`:207` | 类型项变 ⇒ **产物字节变** | **不可以**——须一次 byte 对拍 |

**类 2 的实据（代码里自带教条 + 受害者点名）**：`checker.cr:4239` 的分支注释原文写着「`TYP_REF`/`TYP_SLICE`/`TYP_ARRAY` 等**仍报错**……⇒ **没证据就不扩**」，并在注释里点名 `tests/suite/ptr_ref_first.cr`（`@raw_int(q) - @raw_int(p)` 取字节差）。
- **逐档实读（2026-09-20）**：该档 = `p := &x; q := p + 1; …@raw_int(q) - @raw_int(p)…` ⇒ **确证会硬错**（`p` 改判后是 REF）。其 `q := p + 1` 另落空 `checker.cr:2903-2910` 的 PTR 分支——**那半条归 S3 裁决（指针算术纳入 REF）**，而 `@raw_int` 这半条**无人认领**。
- **同形语料共 4 档，但确证者只 1 档**：`ptr_arith.cr`（`*p` / `*q = 99`）· `opt_dex_test.cr:56,64`（`*p = d` + `match *p`）· `apx_conversion_test.cr:59`（`*p = d`，且 `@raw_int` 施加于 **`px` 而非 `p`**）——这 3 档的 `&x` 都用在**解引用 / 写**上（REF 的本职）⇒ **待实测；不得从「形式相同」推结论**（本批纪律：观察 ≠ 已核验机理）。

**类 3 的实据**：`monomorph.cr:207` 与 `:214` 分别产 `"&"+inner` / `"*"+inner` ⇒ **实例键变**；`ty_shadow.cr:349`/`:903` + `iface_axis.cr:206`/`:207` 把 `&T`/`*T` 落成 **`AK_REF`(11) 与 `AK_PTR`(12) 两个不同原子**（`type_engine.cr:29` 原文）⇒ **在引擎里本就不相等** ⇒ 语义包含判定会变 ⇒ **必然动产物字节**（`.ccr` interned 串面）⇒ 归「登记」不成立。
- **范围收窄（避免虚报）**：生产 39 处里，**已按 `PTR ‖ REF` 同族处理**的不受影响（实测例：`ir_gen.cr:790/989/1009/1036` · `checker.cr:392/1733` · `ty_shadow.cr:903`）⇒ 该族**本来就承认这两者同族**，**唯安全门那 4 处没跟上**（这恰是 §5.2 的由来）。**只认 PTR 的**须逐处定 / 对拍，**计数在实施时现取**。

**⚠ 同构陷阱（「(a) 必须逐处谓词、不得 blanket」的**第二处**依据）**：`ast.cr:295-296` 的 `extra` 槽不对称（`TYP_REF.extra` = mut 标记 · `TYP_PTR.extra` = 地址空间）**在引擎侧同构存在**——`type_engine.cr:461`/`498`/`502` 里 `AK_REF` 槽 0 = **mut 标记**（参与身份）、`AK_PTR` 槽 0 = **元素**（不变）⇒ 凡「统一二者」的改动，**`checker`/`ir_gen` 侧与 `type_engine` 侧都要逐槽处理**。

---

## §6 回退条件（满足任一 ⇒ 停，不上报为完成）

1. 任何 `tests/suite/*.cr` 在 `build`/`run` 面转红，且**不属 §3 预测面**；
2. `src/compiler` 出现任何新点；
3. canary 五条任一变（且非「D1 绿 + 真实码面变化」的正当重锁路径）；
4. 语料点集与预测不符 ⇒ **不符本身不是回退理由，是归因对象**（多出的点逐个读，判「真缺陷」还是「排除漏了一条」；**不许把新增点一律当修好的证据**）；
5. **B/C 未合** ⇒ 不得开硬错（`targets` 会炸）；
6. **§5.2 安全面出现净减且未登记**；**§5.3 类 2 / 类 3 未处置就开硬错**（类 3 无 byte 对拍记录 ⇒ 同上）。

---

## §7 裁定一（`Future<T>`）落地面清单（施工图）

| # | 面 | 现址（符号锚） | 要做什么 |
|---|---|---|---|
| 1 | **类型构造子** | `ast.cr` 的 `TYP_*` 表（已用 0/1/2/3/4/7/8/9/10/11/12/13 ⇒ **5、6 两个空号**） | 新增 `TYP_FUTURE`（`data` = 元素型 ti）——**或**复用 `GENERIC_APPLY` + 内建类型名（两者取舍见下） |
| 2 | **引擎·kind→原子** | `iface_axis.cr::iface_kind_of`（现覆盖 `BASE/DYN/NAMED/GENERIC_PARAM/GENERIC_APPLY/ARRAY/SLICE/REF/PTR/TUPLE`） | **新增一个 `AK_*` 原子**（变型表要给槽位语义：`Future<T>` 对 `T` 应**不变**还是协变） |
| 3 | **引擎·形状层** | `ty_shadow.cr::sh_term_of_ti` / `sh_sig_term_of_ti` · `type_terms.cr` 的 tag 表 | 新分支（形状项/签名项各一） |
| 4 | **`EXPR_GO` 定型** | `checker.cr::infer_expr` 的 `EXPR_GO` 单发分支 `return body_ti` | 改为 `alloc_type(TYP_FUTURE, body_ti, …)`；**range 形（返回数组）须同步** |
| 5 | **`EXPR_AWAIT`** | `checker.cr::infer_expr` 的 `EXPR_AWAIT`（现「`TYP_ARRAY` ⇒ elem，否则原样返回」） | 新增「`TYP_FUTURE` ⇒ `data`」；array 分支保留 |
| 6 | **`ir_gen` 同改** | `ir_gen.cr:1922-1924`（`new_ir_var("_go_ch", TI_INT)` + `emit(…, TI_INT)`） | 与 #4 **必须同改**；**IR `type_kind` 槽变 ⇒ `.ccr` 字节面变** ⇒ **先看 D1**（红⇒非确定性不得重锁；绿⇒真实码面变化走正常重锁） |
| 7 | **`chan_*` / `g_*` / `sched_*` 那一族**（**2026-09-19 解封**：A₂ 对齐作废 ⇒ **一起改**） | 见 **§7.1 逐条清单** | 逐条给「**改 / 不改 + 理由**」；含 **`alloc` 一条待维护者确认**（§7.3）；11 例**重定不删**（§7.2） |
| 8 | **A 的 45 点另需** | `sched.cr::sched_get_curg() -> int` + `bi_add("g_get_curg", TI_INT)`（`checker.cr:302`） | **两处同改**（`-4` 产出侧 #3）——**否则即使 `go` 改型，45 点仍异型**（实参来自 `sched_get_curg()`/`r64`） |
| 9 | **语料影响** | A 49 = 45（`sched_enqueue`）+ 4（`chan_recv`） | 见 #7/#8：**两族都改型 ⇒ 49 → 0**；只改一族 ⇒ 归零不全 |
| 10 | **判据（两向钉子）** | —— | **正**：`go f(x)` 的型 = `Future<T>`、`await h` 取 `T`（运行值 + `--dump-types` 双读）；**反**：**把 `Future<T>` 当 `int` 用必须报**（防「新类型不可判」）；**对照**：非 `go` 的普通 `int` 变量仍按原样 |

### §7.1 「那一族」逐条清单（**禁止用「那一族」当枚举**；逐条给改/不改 + 理由）

**全仓枚举方法（可粘可跑）**：
```
grep -rn '^fn .*(\(handle\|chan_\|g_new\|g_free\|sched_go\|sched_enqueue\|fiber_\)' src/stdlib/*.cr src/compiler/*.cr
```
⚠ **静态枚举只是起点**：本仓口径「扫描只作起点，收敛判据必须是被修对象自己的输出」（`verify_command_traps` 第 9 条：静态扫描会漏**经调用派生的传递闭包**，A₂ 当年「仅 2 处」实为 3 处就是栽在这里）⇒ **改完须以 TF01 / 台账读数逐档清点收敛**。

| 函数（符号锚） | 现签名 | 改 / 不改 | 理由 |
|---|---|---|---|
| `src/stdlib/chan.cr::chan_make` | `(elemsize: int, cap: int) -> string` | **改**（返回型 → `Chan<T>`） | 通道**句柄产出方**；只改 `go` 不改它 ⇒ `chan_recv(ch)` 4 点仍异型 |
| `chan.cr::chan_send` | `(ch: string, val: int)` | **改**（第 1 形参） | 收句柄 |
| `chan.cr::chan_recv` | `(ch: string) -> int` | **改**（第 1 形参）；**返回型另议** | 第 1 形参 = 句柄；**返回型 = 元素的值**，性质不同（不是句柄）——须单独定 |
| `chan.cr::chan_close` | `(ch: string)` | **改**（第 1 形参） | 收句柄 |
| `goroutine.cr::g_new` | `(entry_fn: int, arg: int, arg_type: int) -> string` | **改**（返回型） | **G 句柄产出方** |
| `goroutine.cr::g_free` | `(g: string)` | **改**（第 1 形参） | 收 G 句柄 |
| `sched.cr::sched_go` | `(fn_ptr: int, arg: int) -> string` | **改**（返回型） | 产出**结果通道**；**必须与 `go` 表达式的型一致**（否则两面继续不一致——刀 4 的 A 类正是这个形状） |
| `sched.cr::sched_enqueue` | `(g: string)` | **改**（第 1 形参） | A 类 **45 点**的形参侧 |
| `scheduler.cr::fiber_*`（`fiber_init` 等 6 个） | 多为 `(fi: int)` 索引 | **不改**（**显式列出**） | ① 该档**不在任何构建清单**（`CLAUDE.md` 明载；`build_selfhost_native.py` 逐档核定）；② `fiber_init` 名字碰撞是**已知潜伏项**（`#2026-09-19-6`）⇒ **本族不含它**，但**必须在这里显式写「不改」**，否则读者会以为漏了 |
| `dyn_arr.cr::alloc` | `(n: int) -> string`（`bi_add("alloc", TI_STR)`） | **不动**（**待维护者确认**，见 §7.3） | 它产出的是**裸缓冲**、不是句柄 |

### §7.2 `test_chan_handle_model.py` 的 **11 例：重定 ≠ 删掉**

- **机械核验**：该套件 = `CORPORA(5) × 2 + 1 = **11**`，**实测 `11/11 通过`**（`CORPORA = chan_test · conc_test · go_e2e_test · go_final_test · go_parallel_test`）。
- **它编码的是 A₂ 的契约**：其头注**逐字记录**了「3 处返回型 + 5 处 handle 形参」与四条判据（5 档 `check` rc=0 且无 TF01 / `build` rc=0 / `run` rc=0 / 对齐形态自证）。
- **⇒ 该契约已作废**（今日裁定），但**删掉这 11 例 = 丢掉「曾经有过这条契约」的证据**。
- **重定 + 留痕（两组并存）**：
  1. **原 11 例整体保留**（可在组头加一条注：「**本组前提已由 2026-09-19 裁定作废**，保留供追溯」）；
  2. **新语义另写一组**（`Chan<T>` / `Future<T>` 形态：`ch := chan_make(8,1)` 的型 = `Chan<int>`；`chan_recv(ch)` 收 `Chan<int>`；`go f(x)` 的型 = `Future<T>`；`await h` 取 `T`）；
  3. **读者能看出契约怎么变的**——这是「留痕」的目的，不是「多留一份垃圾」。

### §7.3 `alloc` —— **已裁：换成自己的缓冲类型 `Buf`**（2026-09-20 维护者原话级）

> **取候选 (i)：`alloc` 换成「自己的缓冲类型 `Buf`」。**

**形态写死**：**`alloc(size: int) -> Buf`**（**与 `string` 解耦**）。
⇒ **本代理的推测被确认**：本意就是**把 `string` 这个「万能不透明类型」拆开**——**缓冲归缓冲、句柄归句柄、通道归通道**。

#### ⚠ `Buf` **不是孤立的一条**——它与 `Chan<T>` / `Future<T>` / `G` **同属一个批**（「拆 `string`」）

**⇒ 排期（lead 定，2026-09-20）：**
1. **裁定二（`&x` → `TYP_REF`）仍先动**——自足、体量小、且消掉 F 面；
2. **「拆 `string`」整批**（`Buf` + `Chan<T>` + `Future<T>` + `G`）**排在裁定二之后，且必须同批**——
   否则中途会出现「**有的句柄是新类型、有的还是 `string`**」的**双模型并存**，**那正是本批在收的东西**。
3. ⇒ **§7.3 不得被写成「一个独立的小改动」**：它是**那一整批的第一块**。

#### 连锁三条 —— **各给「要定什么」**，并**在本节给答案**（不留实施者临场决定）

1. **`@ptr_of` / `@str_of` 的立足点（**本节内定**）**
   - **取向：保留 `string` 作为「裸字视图的另一端」**——即**视图内建语义不动**，`Buf → string` 走**一次显式转换**。
   - **理由（本代理实读）**：2(a) 已按「裸字 ↔ `string`」立了 **63 处先例**（`@ptr_of` 27 + `@str_of` 36），且 **B/C 的迁移刚按该范式合入**（`79e0395c`）⇒ 不动视图面**就不必重做那 6 处**。
   - **⚠ 状态 = lead 的倾向（写入计划待确认，不是结论）**——若维护者裁「视图另一端也改 `Buf`」，则连锁 2 的第二种后果成立。
2. **B/C 的 `79e0395c` 是否重做 —— 两条后果都写死（不写的后果就是「将来没人知道要不要重做」）**
   - **若视图内建不动**（连锁 1 取上）⇒ **不必重做**；那 6 处（`ld.cr` 的 `ctx_add_so`/`ctx_add_plt`/`ctx_emit_static` 等）**维持现状**；
   - **若视图语义随 `Buf` 变** ⇒ **那 6 处必须在 `Buf` 的新语义下重做**（成本 = 一次已合并迁移的返工，**如实计入本批**）。
3. **`(i)` 白名单 —— 列为**联合决策**，本批不再单独改它**
   - `(i)` 已因 S2（加 `TYP_REF`）要改一次；若 `Buf` 再动类型面，**同一个门会被连续改第三次** ⇒ **判据与理由各自漂移**。
   - ⇒ **写死为联合决策**：**`(i)` 的最终语义**（收哪些类型、理由是什么）**在「拆 `string`」批里与 `Buf` 一起定**；**在此之前不再单独改 `(i)`**（S2 只做「加 `TYP_REF` + 重写理由」这一件，且**理由里要写明它还会随 `Buf` 再变一次**）。

### 拆的条件
裁定二落地 ⇒ **F 的「临时排除」不存在了**（§2.2）⇒ 刀 4 的未落环排除**只剩 A**（A 归裁定一）。

### §8.10 S1/S2 判据档骨架（**先清单不写码**；与 S3/§7.3 无关，故可先行）

**档名（拟）**：`tests/selfhost/test_ref_retype.py`（挂 `selfhost-tests` 门内——`suite` 腿不在 PR 门内，本仓有「glob 收编 ≠ 进门」前科）。

**S1（定型点改判）判据**
| # | 用例 | 期望 | 反例（防真空） |
|---|---|---|---|
| S1-1 | `p := &x;` 后 `--dump-types` 读 `p` 的变量行 | kind = **`TYP_REF`** | 改判前该行 = `TYP_PTR` ⇒ 若改判后仍读 `TYP_PTR`，本项红 |
| S1-2 | `tests/probes/n19_ref_named.cr` 台账（`CORE_S1P=1`） | **2 → 0** | 若仍 2 行 ⇒ 红 |
| S1-3 | `fn g(a: &int)` ← `&x`（**直给**，不经变量） | 台账 **0 行** | 改判前此处 1 行 ⇒ 若仍 1 行 ⇒ 红 |
| S1-4 | `r : &int = &x;` 的既有 `error[TA02]` | **消失**（同 kind 不再报）——**改判成功的正向证据** | **改判前基线已实测写死**：本档 `check` ⇒ **`error[TA02]: Variable declared as &int, got *int`**（该文案即「`&x` = `*int`」的**文档化语义**原话）⇒ **改判后该诊断必须不再出现**；若仍出现 ⇒ 红（说明 `&x` 没真的变成 REF） |

**S2（`(i)` 白名单整体重写）判据——三颗钉子同批**
| # | 方向 | 用例 | 期望 |
|---|---|---|---|
| S2-1 | **正（防误杀）** | `@raw_int(&x)`（**传 REF**，`unsafe` 内） | **接受**（rc=0；且 `ptr_ref_first.cr` 仍 rc=0） |
| S2-2 | **反（防漏放）** | `string` / `bool` / **array** / **slice** 各一 | **逐类各红**：`error[TF07]` + **rc=1** + `build` 面**零产物** |
| S2-3 | **对照（防「一律」）** | `@raw_int(n : int)` · `@raw_int(d : dex)` | **仍接受** |
| S2-4 | **前提已死的登记** | 源码注释里不再出现「`&x` 解析到 `TYP_PTR`」 | 结构判据（剥注释后 grep）——**原前提的措辞必须被替换**，不是留着 |

**突变自证（强制形状，逐条）**：`re.subn` 计数 == 1 · 打印命中上下文 · 剥注释预处理先单独自证。
- S1 突变：把 `UOP_REF` 改回 `alloc_ptr_type(inner, 0)` ⇒ **S1-1/S1-2/S1-3 必红**；
- S2 突变①：白名单**只加常量不改理由** ⇒ **S2-4 必红**（措辞未换）；
- S2 突变②：白名单「一律放宽」⇒ **S2-2 必红**；
- 对照突变：S2-3 的 `int`/`dex` 路径不受 S2-1/S2-2 影响。

**⚠ 一条**已填**的期望值 + 一处**自纠**（2026-09-19，S3 裁定到达后）**

- **S1-4 的期望值已填**（原留白）：**`error[TA02]` 改判后必须消失**。**依据 = 裁定二本体**（`&x` 改判 `TYP_REF` ⇒ 与 `&int` 注解**同 kind**）⇒ 与 S3 **无关**。
- **⚠ 自纠**：我先前把这条标注为「**与 S3 绑定**」——**那是错的**。S3 管的是**指针算术**（`ptr±int` 三点），**不管注解兼容**。
  ⇒ **S3 真正的耦合点是另一处**：`ptr_ref_first.cr` 的**算术断言**（`p+1` 按元素缩放），不是 TA02。
- **S3 两向钉子的「改判前」基线**（**实测写死**，避免将来把「改判前本来就通」当成「放宽成功」）：
  - `q := &x + 1;` ⇒ **今天 0 诊断**（`&x` 现为 PTR，算术门接受它）⇒ 改判后**同一源必须仍 0 诊断**；
  - `a + b`（`int + int`）⇒ **今天 0 错误** ⇒ 改判后**仍须不受影响**（这是「扩面没把非地址型一起放进来」的对照）。

## §9 两句裁定各自的**两向钉子**（改契约类尤其要防误杀）

| 裁定 | 正向（该变的变了） | 反向（防误杀） | 对照（防「一律」） |
|---|---|---|---|
| **一**（`Future<T>`） | `go f(x)` 的型 = `Future<T>`；`await h` 取 `T`（**运行值 + `--dump-types` 双读**） | 合法 `await` 链**不报**（`p_go.cr` 形） | 非 `go` 的普通值**不受影响** |
| **二**（`&x`→REF） | `&x` 的 kind = `TYP_REF`（`--dump-types`）；`g(p)`（`&NA` 形参 ← `&x`）**不报** | **`@raw_int(&x)` 仍须可用**（§8.3 的抉择结果）；**`*T` 形参 ← REF 值仍须报** | 同 kind 的 REF/REF 与 PTR/PTR **不受影响**；`int → string` 真异型**仍报** |
