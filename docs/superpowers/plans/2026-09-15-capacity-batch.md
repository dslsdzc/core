# 容量批（CAP）实施计划：E-2 对象布局表示位 + E-3 `MAX_*` 定长槽解除 + E-4 可选程序收回 `.cir` 缓存

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 兑现 P6 计划「裁决门」裁-P6-1 所指的三项（维护者已裁 = **转独立批**，本批 = 该三项的落地批）：① **E-2**——把可选运行期表示面从「局部 / 形参 / 返回 / 调用结果」扩到**聚合面**（结构体字段 / 数组元素 / 全局槽 / match 结果）：现状 = 裸值形态 ELF 与解释器**双路径同 rc=-11**（响亮失败，非静默），装箱形态正确；② **E-3**——解除 `src/compiler/ast.cr:115-118` 三上限（`MAX_STRUCT_FIELDS` / `MAX_ENUM_VARIANTS` / `MAX_VARIANT_TYPES`，皆 16）：解除 = **记录布局迁侧表**（P5 附录 E-3 登记的方向），四条前置须逐条收口；③ **E-4**——收回「可选程序关 `.cir` 缓存」的临时门（`src/compiler/main.cr:488-490`），依赖 E-2。

**边界纪律（容量批 = 新能力面）**：本批引入**新表示语义**（聚合面可选值）与**新记录布局**（进程内符号表迁侧表）。凡未取裁的裁决门项**一律不实施**（对应任务止于 report-only / 登记）；任何「顺带」的新能力（iterable / 命名消费语法 / `.csr` 消费面 / `MAX_GENERICS` / 盘面格式改版）**出界**。**本批不做清单**见「本批不做」节。

**Architecture:** 四条主线：① **T1 前置侦查 + 冻结基线重建 + 起点判据 + 三面守门复验**（源码零改动；含 E-2/E-3/E-4 的现状 RED 复现与锚点实核）；② **T2 = E-2**（表示面聚合扩展；**先裁 = 运行期表示选择律**，见裁-CAP-1）；③ **T3 = E-3**（记录布局迁侧表；**先裁 = 上限策略**，见裁-CAP-2）；④ **T4 = E-4**（`.cir` 缓存收回；**先裁 = 收口面**，见裁-CAP-3；**机制须先 RED 实证**——本计划实读**未能复现** P4 报告所述的分歧机制，见裁-CAP-3 的「机制存疑」条）；⑤ **T5 收官**（全量回归 + 统一台账 + 文档 + 终态）。

**Tech Stack（实施面锚点，行号 = 本工作树实读，非 P6 计划转抄）**：`src/compiler/ir_gen.cr`（表示面全域：侧表 `g_ir_var_rep` :214-241 · `opt_rep_slot` :251-259 · `global_decl_optional` :261-277 · `callee_ret_optional` :279-295 · `rep_enc_of_expr` :297-330 · 未覆盖面自陈注 :207-208 · 写点 :1147-1184 / :2105-2172 / :2209-2210 / :2312 / :2346 / :2369 / :2488 · 解包点 :1987-2018 / :2044-2064 / :2442-2461 · `optrep_begin` :2835-2846 / `optrep_prescan` :2848-2861 / `reg_optrep_globals` :2878-2900 · match 结果 :1960/:2076/:2087）· `src/compiler/globals.cr`（`g_ir_var_rep` :63 · `g_optrep_on` :67 · 信道单元 :70/:73 · `g_cur_ret_opt` :75 · 表示码注 :76-80）· `src/compiler/ast.cr`（`MAX_*` :115-118 · 镜像 struct :130-165 · 错误码 `EC_P_TOO_MANY_PARAMS` :370 / `EC_P_ENUM_LIMIT` :372 / `EC_P_STRUCT_LIMIT` :373）· `src/compiler/dyn_arr.cr`（记录尺寸 :56-58 · FuncInfo 偏移 :90-94 · StructInfo 偏移 :97-100 · 枚举偏移 :231-243 · 受护访问器 :462-549）· `src/compiler/parser.cr`（P020 :1448-1461 · struct 写入 :1634-1640 + P023 :1648-1652 · enum 写入 :1695-1716 + P022 :1724-1733）· `src/compiler/ccr_io.cr`（`CCR_VERSION` :135 · SYM 布局注 :312-332 · struct/enum 写 :706-747 · 读回闸 :1203/:1245/:1272 · 清零循环 :1208-1219/:1250-1265）· `src/compiler/ty_shadow.cr`（`sh_match_bit` :1204-1213 · `sh_match_first_missing` :1217-1226）· `src/compiler/checker.cr`（覆盖位消费 :3012-3040，位运算 :3036）· `src/compiler/main.cr`（缓存门 :473/:477-490/:532/:553）· `src/compiler/cir_cache.cr`（`CIR_CACHE_VER=17` :51 · 版本族注 :7-49 · 身份字段 :57-90 · 头部 :214 · 装载闸 :436-468）· `tests/selfhost/test_optional.py`（34 例；未覆盖面 :347-352）· `tests/selfhost/test_enum_limit.py`（13 例）· `tests/selfhost/test_cir_warm_path.py`（21 例；可选面 C 组 :277-287）· `tools/baseline/{rebuild.sh,parity_run.sh,probes_run.sh,REBUILD.md}` · `tests/probes/`（29 档）· `src/ci/run.sh`（selfhost-tests 挂点 28 档）。

**Spec / 起点文档**：`docs/superpowers/specs/2026-09-10-type-interface-unification-design.md` **§9 P6 行**（:288 末句「**下一批指向 = 容量批 或 验证切片轮**（按裁-P6-1 结果）」）· **P5 计划附录 E**（`docs/superpowers/plans/2026-09-13-r2-p5-cleanup.md:472-474` = E-2/E-3/E-4 原文）· **P4 计划附录 D-3-7/8/9**（`docs/superpowers/plans/2026-09-12-r2-p4-carrier.md:387-389`）· 判定面纪律继承 = **P6 计划 Global Constraints**（`docs/superpowers/plans/2026-09-14-r2-p6-tail.md:80-93`）逐字有效，本计划据实改写为容量批版本。

---

## 前置状态（本实例实核 / 实读；未标注者 = 本实例实读）

- **工作树 = `/tmp/p7wt`**（jj workspace；`.jj/repo/` 为空目录 ⇒ 共享主仓 store）。**本实例实核**：无并发构建/编译/测试（`ps aux | grep -Ei 'corec|corearch|build_selfhost|python3 .*test'` 零命中）。
- **本计划未跑任何构建 / 测试 / 编译**（硬纪律；见「自检记录」末条）。所有「现状」= 实读源码/文档或既有报告记录值；一切需实测的数字标「须 T1 实测」。
- **⚠ 修订面勘误（本实例实读证据）**：提示词给定「基线 = P6 终态提交 `97f4394f`」，但本工作树**含 P6 记录之后的修复**，逐条证据：`tests/selfhost/test_params_limit.py`（TODO #2026-09-10-4：`src/compiler/dyn_arr.cr:89` `MAX_FN_PARAMS : int = 64;` + `/tmp/p7wt/src/compiler/parser.cr:1452` `if pc > MAX_FN_PARAMS {`）· `tests/selfhost/test_tuple_slots.py`、`test_agg_slots.py`、`test_agg_checks.py`（TODO #2026-09-16-2/#28/#29 族）· `tests/selfhost/test_cache_identity.py` + `src/compiler/cir_cache.cr:26-29`（v17 = 编译器身份字段，TODO #2026-09-10-1）· `tests/selfhost/test_nested_fn.py`（TODO #2026-09-10-12）· `tests/selfhost/test_interp_parity.py`（TODO #2026-09-10-7）。⇒ **P6 计划/报告的记录值（`.ccr` 四条、`selftest-types` 计数、自举链 sha、构建确定性 sha）不得直接当本批起点**（见「起点基线值」节：逐条标「须 T1 重测」）。本实例**无法**以 `jj` 核验工作树修订与 `97f4394f` 的一致性（硬纪律：不得跑 jj）⇒ **留给 T1 实核并登记**。
- **报告位置漂移**：提示词写报告在 `/tmp/p7wt/.superpowers/sdd/`——**该目录在本工作树不存在**（`.superpowers/` 被 `.gitignore` 覆盖 ⇒ 不入 jj workspace 检出）。报告实体在 **`/home/DslsDZC/core/.superpowers/sdd/`**（本实例实读在位：`p6-task0-report.md`、`p6-task1-report.md`、`p6-task3-report.md`、`p6-task4-report.md`、`p6-incident-2026-09-14-t3-preempt.md` 等）。本计划的报告产物同样落该处（不入提交）。
- **既有安全网（本批起点不变量，不得弱化）**：E-2 的未覆盖面当前 = **响亮失败**（`tests/selfhost/test_optional.py:347` `case_dual_rc("uncovered_field_bare_is_loud", UNCOVERED_FIELD_BARE, -11)`）；E-3 的越界写入当前 = **定位硬错 + 无产物**（`tests/selfhost/test_enum_limit.py:189-210`，用户面实印 `error[P22]`/`error[P23]`）。两者都是「不得退化为静默错值」的判据承载。

---

## 附录 E 三项分诊表（**本计划第一产物**；逐条 = 现状 / 影响面 / 依赖 / 前置 / 处置）

> 分诊口径：**(a) 实施** = 本批实施面（逐任务落地）；**(b) 待裁** = 须维护者答复（逐条点名问句见「裁决门」）；**(c) 维持登记** = 本批不做，理由写死。**不得**用本表把待裁项偷渡成实施项。

| # | 项 | 现状（出处 = 本实例实读） | 影响面 | 依赖 | 前置 | 处置 |
|---|---|---|---|---|---|---|
| **E-2** | 对象布局表示位 | 表示位机制存在且仅覆盖四类槽：局部（写点 `ir_gen.cr:2105-2108`/`:2171-2172`/`:1147-1149`）· 形参（`:2537-2546`）· 返回与调用结果（`:1745-1762`/`:2209-2210`/`:2578-2598`）· 解包点 = match 臂（`:1987-2018`/`:2044-2064`）与 `?`（`:2442-2461`）。**聚合面未配位**（代码自陈注：`ir_gen.cr:207-208`「未覆盖面（结构体字段/数组元素/全局槽/match 结果/惰性 thunk）：不配表示位 ⇒ 解包走**既有**装箱假定」；`:261-262`「globals 无表示位 ⇒ 视作未覆盖面」）。逐站点：字段写 `:1160-1164`（EXPR_ASSIGN）/`:2346`（EXPR_STRUCT）/枚举载荷 `:2312`；字段读 `:2230`；数组元素写 `:1177`/`:1184`/`:2369`/`:1314`，读 `:2287`/`:2295`；全局槽写 `:1152-1155`/`:2735`，读判定 `:262-277` + `:322-327`（可选全局恒 `oe_boxed()`）；match 结果 `:1960`（`result_var` 无 `irv_set_rep`）/`:2076`（臂写）/`:2087`（merge），消费端落到 `:329` `return oe_boxed();` | ir_gen 写点约 8 处 + 解包点 2 处 + 类型判定辅助 1 处；**纯 IR 面 ⇒ ELF 与解释器天然同源**（`globals.cr:57-60` 明撰，须保持）；若取裁为 packed 位图/stride 案 ⇒ 再触**目标程序**数据布局（`type_size`/`type_align`/ELF 发射/解释器/`@sizeOf` 面），发射面可能变 | 无（独立）；与 E-3 共享「容量」裁决面但**不共触**同一布局（见「锚点漂移 #6」） | **成立且是起点安全网**：未覆盖面 = 双路径同 rc=-11 响亮失败（`test_optional.py:347`），非静默 | **(a) 实施**（挂**裁-CAP-1**） |
| **E-3** | `MAX_*` 定长槽解除 | 三上限 = `ast.cr:116-118`（`:115` `MAX_GENERICS=4` 属另一类）；**只约束进程内定长记录**——`ESZ_STRUCTINFO=440`/`ESZ_ENUMINFO=4408`（`dyn_arr.cr:57-58`），字段槽 16（`OFF_SI_FIELD_NAMES=8`→`OFF_SI_FIELD_TYPES=136`→…，`:97-100`），变体槽 16×272（`OFF_EI_VARIANT_COUNT=4360`，`:237-243`）；写入侧 = `parser.cr:1634-1640`（struct）/`1695-1716`（enum）+ 末尾硬错 P023 `:1648-1652` / P022 `:1724-1733`；读回侧 = `ccr_io.cr:1203`/`:1245`/`:1272`（`> MAX_* ⇒ rc=1`）+ 清零循环 `:1208-1219`/`:1250-1265`（硬编码 16/4）；覆盖位 = `ty_shadow.cr:1207-1213`（`sh_match_bit` 循环乘 2）+ `checker.cr:3036`（`m_cover` 单 int） | 访问器调用点 **54 处**（本实例 grep 计数：`ty_shadow.cr` 16 · `checker.cr` 19 · `ir_gen.cr` 9 · `lsp/analysis.cr` 6 · `interp.cr` 2 · `type_selftest.cr` 2 · `ccr_io.cr` 10 · `provenance_verify.cr` 1 · `arch/x86_64/instr.cr` 1）——**受护访问器单源 ⇒ 签名不变则调用点零改动**；真改动面 = `dyn_arr.cr`（记录 + 访问器 + 侧表）· `parser.cr`（写入循环）· `ccr_io.cr`（读回闸/填充）· `ty_shadow.cr`+`checker.cr`（覆盖位）· `ast.cr`（常量 + 镜像）；判据面 = `test_enum_limit.py`（13 例）重钉 | 无（可独立于 E-2 实施） | **四条逐条结论见下节**（结论：①未满足 ②未满足且比登记值更宽 ③未满足 ④须重锁/重设） | **(a) 实施** 或 **(c) 维持登记**（在野需求 0）；按**裁-CAP-2** |
| **E-4** | 可选程序收回 `.cir` 缓存 | `main.cr:484-487` 注 + `:488-490` `if g_optrep_on != 0 { cache_enabled = 0; }`（文件路径专用：批处理路径 `ir_gen_all` 不经缓存——本实例 grep 实核 `load_cir_cache`/`save_cir_cache` 仅 `main.cr:532`/`:553` 两调用点）；快照面 = `cir_cache.cr`（头部 48B+name :214 · vars 24B :215 · NOD 64B :219 · edges 32B :220 · instrs 48B :221 · SG 48B :232；版本 `CIR_CACHE_VER=17` :51；五闸 :446-457）；判据面 = `test_cir_warm_path.py`（可选面 C 组 `:277-287` = 两跑产物同 + **零条目**） | 缓存命中面（编译耗时）；若快照格式变 ⇒ 版本 bump + `tools/baseline` 判据面同步 | **E-2**（表示面定案后才知道哪些状态须跨进程可重建） | **未验证**：P4 报告陈述「命中恢复的函数解包点回落装箱路径」——本实例静态枚举**未能复现该机制**（`irv_rep`/`irv_set_rep` 全部读取点 = `ir_gen.cr:304`/`:1148`/`:1987`/`:2443`，**均在同函数生成内**）；⇒ 机制**须实施时 RED 实证或证伪**（见裁-CAP-3） | **(a) 实施**（挂**裁-CAP-3**）；机制证伪且无分歧 ⇒ 按 **(c)** 收口并登记 |

**分诊计数**：实施 **3**（E-2 · E-3 · E-4，各自挂裁决门）／待裁 **0 项整条**（但三条各含设计裁决，见「裁决门」3 主问 + 3 附问）／维持登记 **0**（E-3 若取裁 (c) 则转登记）。**本计划不把任何一条提前判死**：三条皆有在野收益（E-2 = 消除响亮失败面；E-3 = 解除能力上限；E-4 = 可选程序编译耗时）与明确成本（见各任务）。

---

## E-3 四条前置逐条实核（**逐条结论**；P5 附录 E-3 登记的「前置四条」）

| 前置 | 登记内容（P5 附录 E-3） | 本实例实核结论 | 缺什么 |
|---|---|---|---|
| ① 读回闸同步 | 「读回闸（`ccr_io.cr` 的 `> MAX` ⇒ rc=1）」 | **未满足**。实读三闸：`:1203` `if fc > MAX_STRUCT_FIELDS { println("error: .ccr struct field count exceeds max"); return 1; }` · `:1245`（`vc > MAX_ENUM_VARIANTS`）· `:1272`（`tc > MAX_VARIANT_TYPES`）；且清零循环硬编码槽数（`:1208`/`:1250`/`:1256`/`:1260` 的 `>= 16`、`:1219`/`:1265` 的 `>= 4`）。解除后若不改：**读回侧仍整类拒绝** >16 的文件、填充仍只覆盖 16 槽 | 三闸改为**动态填充**（或按新上限）+ 清零循环改由侧表形态决定；**注**：盘面格式**已是变长**（`ccr_io.cr:324`「structs: struct_count + {name, field_count, fields×8B}」/`:332`「enums: … variants×{name, tc, types×4B}」；`ccr_sym_seg_size` 用 `si_field_count(sti)` 动态求和，`:309-336`）⇒ 盘面**无需**为解除改版 |
| ② `ast.cr` 镜像归位 | 「`StructInfo.field_*` 镜像 64 vs 布局 16 **已不一致**」 | **未满足，且实读比登记值更宽**。逐条：`ast.cr:143` `field_names: [string; 64]` / `:144` `field_types: [int; 64]` / `:145` `field_type_nodes: [int; 64]` vs 布局 16 槽（`OFF_SI_FIELD_TYPES-OFF_SI_FIELD_NAMES = 128B = 16×8`）**✗**；另**三处登记外不一致**：`ast.cr:147` `StructInfo.generic_names: [string; 16]` vs 布局 4 槽（`OFF_SI_GENERIC_COUNT-OFF_SI_GENERIC_NAMES = 32B`）**✗**·`ast.cr:154` `EnumVariant.types: [int; 64]` vs 布局 16 **✗**·`ast.cr:164` `EnumInfo.generic_names: [string; 16]` vs 布局 4 **✗**。（相符者：`ast.cr:133` `param_types: [int; 64]` = `MAX_FN_PARAMS=64`；`:136` FuncInfo generic 16 = 布局 16；`:162` `variants: [EnumVariant; 16]` = 布局 16） | 镜像归位（或**删除候选**——本实例全仓 grep：`FuncInfo`/`StructInfo`/`EnumInfo` 在 `ast.cr` 之外**仅注释命中**，无类型使用点 ⇒ 是否为死声明**须实施时实证**，含 bootstrap 侧对 struct 声明的处理） |
| ③ 覆盖位表示 | 「`sh_match_bit = 2^vi` 走 int，>63 变体溢出」 | **未满足**。`ty_shadow.cr:1204-1213` 头注自陈「覆盖位：bit(vi) = 2^vi……vi < 63 由 MAX_ENUM_VARIANTS = 16 保证不溢出——**该前提自 R2 P4 Task 6 起由写入侧护栏强制**」；实现 = 循环乘 2。消费点 `checker.cr:3033-3040`：`bit := sh_match_bit(ci)` · `m_cover = m_cover + bit`（**单 int 累积**）· 冗余臂判据 `(m_cover / bit) % 2 != 0`；下游 `sh_match_first_missing(cover_bits, count)`（`ty_shadow.cr:1217-1226`）按 `(bits / b) % 2` 逐位判 | 覆盖位**表示更换**（位图/侧表/多字）+ 两消费点（`:3036` 与 `sh_match_first_missing`）+ 冗余臂判据同批；**>63 变体在现表示下必然溢出** ⇒ 即使只把上限提到 64 也不可（63 = i64 位宽边界，非 16 的问题） |
| ④ 冷热 `.ccr` 与 ELF canary 判据 | 「冷热 `.ccr` 与 ELF canary 判据」 | **未满足（= 判据须重锁/重设）**。本批若只迁**进程内**记录：`.ccr` 四条**预期不变**（盘面逐字段照写，`ccr_io.cr:706-747`）；canary **预期 IDENTICAL**（记录布局不进发射面）。**两值都必须以 T1/T3 实测为准**，不得预填；若实测有变 ⇒ 逐段 delta 归因 + 走裁-CAP-2 附问 3（版本面） | 判据重锁表 + 归因链；冷/热口径按 D28（两命令口径差恒 143B + 冷/热各自确定性；比较前 `clean-cache`） |

**结论汇总**：四条前置**无一满足**（与 P5/P6 登记「四条前置未满足」一致）；其中 ② 的实际缺口**大于**登记值（多 3 处镜像不一致），③ 的边界**严于**登记印象（不是「>16 才有问题」，而是「上限一旦越过 63 必溢」，故上限策略与位表示改造**耦合**）。**但注意**：四条前置是「解除」的前置，**不是**「本批启动」的前置——T1/T2 可先行，T3 实施前必须先取裁（裁-CAP-2）并落纸设计。

---

## 裁决门（**开工前置**；逐条 = 问句 / 影响项 / 推荐 / 未取裁时的行为）

> 纪律继承 P6：**未取裁的项一律不实施**（对应任务只到「report-only / 登记」为止），且**不得**以「推荐案」冒充裁定；含「先裁」的任务，**护栏写进任务卡正文**（P6 事故教训：`.superpowers/sdd/p6-incident-2026-09-14-t3-preempt.md` §2）。

| # | 问句（逐条照抄给维护者） | 影响项 | 本计划推荐 | 未取裁时 |
|---|---|---|---|---|
| **裁-CAP-1** | **E-2 的「运行期表示选择律」取哪一条？** (a) **存储边界规范化**——可选**聚合槽**（结构体字段 / 数组元素 / 全局槽 / match 结果）的**写点**把「裸 T 值」装箱为 `Some(v)`（`None` 保持对象），读取侧**零改动**（恒走既有装箱路径）；零新运行期状态、零布局变更、纯 IR（两后端同源）。 (b) **侧表 / 位图**——给聚合槽配运行期表示位（结构体 packed 位图 + 数组 element stride + 全局伴随位 + match 结果位），保持「同一槽可有两种表示」语义；代价 = **目标程序数据布局变更** + ELF/解释器两后端同改 + `type_size`/`@sizeOf` 面。 (c) 静态面收窄（不可静态证者拒绝）——**与 P4 裁决 5「解包侧判表示」相悖**，仅登记不推荐。 | E-2（T2）；(b) 会连带**发射面**（canary 豁免须显式声明） | **(a)**。理由：① 与既有机制同源（纯 IR、零 ABI/帧布局改动 ⇒ 两后端天然同源，`globals.cr:57-60` 的既有性质得以保持）；② 三态纪律友好（不新增「未配位 ⇒ 静默」面，规范化律 = 全配或硬拒）；③ canary 可保持**无豁免硬闸**（非可选程序零足迹、可选程序语料不在 canary 档）；④ 语义无损（`T ⊆ T?` 的裸值注入本来就等价于 `Some(v)`——P3 语义口径）。风险留白：写点**漏一处即静默错值** ⇒ 必须配写点完备性审计（T2 Step 3） | T2 不实施；维持「响亮失败」现状并登记（**不得**先按 (a) 落码） |
| **裁-CAP-2** | **E-3 解除后的「上限策略」取哪一条？** (a) **记录布局迁侧表**（无硬上限；P4/P5 登记方向）；(b) **对齐镜像**（16 → 64，与 `ast.cr` 镜像逐字一致；变体数受覆盖位限制须 ≤63，且仍是有界，③ 位表示改造仍不可免）；(c) **维持现状**（在野需求 0：语料最大结构体 9 字段、无超限枚举）。 | E-3（T3） | **(a)**。理由：① 它同时闭合 ② 的镜像不一致（侧表无「槽数」概念，镜像可删/改写为形态说明）；② (b) 只是把问题推迟且**仍需** ③ 的位表示改造（63 硬边界）；③ 受护访问器单源（54 调用点零改动）使迁移成本可控。**附问 1**：`MAX_GENERICS=4` 是否同批解除？推荐 **否**（写入侧有界 + parse 期硬报，P5 附录 E-3 已明写「属另一类」）。**附问 2**：解除后 `.ccr` 记录布局**是否再 bump 版本**？推荐 **否**——盘面已是变长（`ccr_io.cr:324/:332` + `ccr_sym_seg_size` 动态算式），本批只迁**进程内**记录 ⇒ 盘面零变化（须实测锁证）；若实施中发现盘面必须变 ⇒ **停下另裁**。 | T3 不实施；维持登记（在野需求 0）并保留 P022/P023 护栏为唯一拒绝面 |
| **裁-CAP-3** | **E-4 的收口面取哪一条？** (a) 快照**加表示位面**（格式变化 ⇒ `CIR_CACHE_VER` **17→18**〔注：提示词写「18→19」，**本树实值 = 17**（`cir_cache.cr:51`），该表述不成立〕）；(b) **装载侧重派生**（与 NOD 派生同构，`cir_cache.cr:43-46`/`:514-523` 先例；零格式变化——**须先证可派生**）；(c) **维持关缓存**（现状；代价 = 可选程序每次全量重建）。 | E-4（T4）；依赖 E-2 | **(b) 优先，但以机制实证为门**：**第一步必须先做 RED 实证**（T4 Step 1）——本计划的静态枚举**未能复现** P4 报告所述分歧机制（全部 `irv_rep` 读取点在同函数生成内 ⇒ 命中恢复函数的 IR 自含表示语义）；若实证**确实出现**冷/热分歧 ⇒ 按分歧面选 (a)/(b)；若**证伪**（无分歧）⇒ 按 (c) 收口并登记「门已无必要」的结论（保留门 = 零成本保险亦可）。**不建议**在机制未明时直接取 (a)（格式变更 = 最贵且须 bump 版本族） | T4 止于**机制 RED 报告**；`main.cr:488-490` 门**保持现状**（不动 = 无害） |

**三条主问 + 三条附问 = 6 问句**。另有两处**本计划无法确定、须实施时实证**（不猜）：① E-2 的「可选的惰性 thunk」面是否属本批（P4 未覆盖面清单含它，P5 附录 E-2 只列 4 面 ⇒ 若裁-CAP-1 未点名，**本批不做**、登记）；② E-3 迁侧表后 `FuncInfo`/`StructInfo`/`EnumInfo` 三个 `ast.cr` 声明是否为死声明（可删）——须实施时以「删除后重建 + 全量回归逐字节同」实证。

---

## 任务总表（**本批实施面**；状态一律「待实施」，收官时按实测回填）

| 任务 | 内容 | 对应 | 依赖 |
|---|---|---|---|
| **T1** | 前置侦查 + **冻结基线重建（换代到本批起点）** + 起点判据 + 三面 RED 现状 + E-3 四前置实核 + 锚点实核（**源码零改动**） | E-2/E-3/E-4 前置 | — |
| **T2** | E-2 表示面聚合扩展（**Step 1 = 裁-CAP-1 先裁 + 设计落纸**；写点完备性审计；未覆盖面 4 例重钉 + 新增用例） | E-2 | T1、**裁-CAP-1** |
| **T3** | E-3 记录布局迁侧表（**Step 1 = 裁-CAP-2 先裁 + 逐字段布局表 + 镜像归位处置**；覆盖位表示改造；`test_enum_limit` 重钉） | E-3 | T1、**裁-CAP-2** |
| **T4** | E-4 可选程序收回 `.cir` 缓存（**Step 1 = 机制 RED 实证（必做）+ 裁-CAP-3 先裁**） | E-4 | T1、T2、**裁-CAP-3** |
| **T5** | 收官：全量回归 + 判据复验 + 统一台账 + 文档（spec §9 容量批行 / format spec / TODO / CLAUDE.md 如有结构面变化）+ 容量批终态与剩余登记 | 全部 | 全部 |

**顺序理由**：T1 必须先于一切（本树含 P6 记录之后的修复 ⇒ 所有起点值须重测，且三面现状须以本树 RED 复现为准）；T2 先于 T4（E-4 的「须重建状态清单」由 E-2 的表示律决定）；T3 与 T2 **技术上可并行**（不同触面，见「锚点漂移 #6」）但**同批串行执行**（多 agent 纪律：一构建一编译串行；且两者都要重跑同一套判据，串行避免判据互相污染）；T4 排在 T2 之后且自带机制门。

---

## 本批不做（**硬边界**；逐条 = 理由）

1. **不做** E-2 的「可选的惰性 thunk」面（P4 清单含、P5 附录 E-2 未列；未取裁 ⇒ 不实施）。
2. **不做** `MAX_GENERICS` 解除（写入侧有界 + parse 期硬报；P5 附录 E-3 明写「属另一类」）。
3. **不做** `MAX_IFACE_METHODS` / `MAX_IFACE_METHOD_PARAMS`（P3b 显式登记**保留**；`dyn_arr.cr:190-195` 注：解除需迁侧表，护栏已是硬错）。
4. **不做** `.ccr` 盘面格式改版（E-3 只迁进程内记录；盘面已有变长形态；若要变 ⇒ 停下另裁版本面）。
5. **不做**：P6 未取裁的其余项（E-6 iterable/命名消费语法 / E-12 调用位点 / E-14 政策五问 / E-15 混比 / `.csr`·TagNode / `lit_empty_pair` 正×正面候选）——一律维持登记。
6. **不做**：新用户面语法、ABI/帧布局改动、后端旁路状态（`g_x86_mw_tag_off` 式两后端各写一份 = 反例）。
7. **不做**：可达性/代码消除类语义变更（P6 T4a 的边界先例）。
8. **不做**：`.cir` 缓存**装载读缓冲**与命中路径的既存面（`test_cir_warm_path.py` 已钉的 19+2 例）；E-4 只动「可选程序是否启用缓存」这一处门与其快照面（若取裁需要）。

---

## Global Constraints（照 P6 简版；容量批版本）

- **jj only（禁 git 含只读/复合）**；**提交路径限定**（多 agent 在场：一律 `jj commit -m … <paths>`，配方见记忆条 `jj_commit_recipe`）；所有命令 `nice -n 19`；一构建一编译**串行**（每段前 `ps` 实查）；cwd = 仓库根（`/tmp` cwd 触发 import 解析假失败）。
- **基线纪律（本批换代）**：每任务开工先取**同任务起点**基线（二进制 sha + 语料计数 + 探针 rc 表），**不得沿用上一任务的计数**。本批 T1 起基线 = **入仓配方重建的二/三件**（`tools/baseline/rebuild.sh`；白名单**换代到本批起点修订**，旧值按 `REBUILD.md`「换代纪律 2」留痕）。
- **ELF canary（硬闸，全批）**：`clean-cache` → `build tests/suite/ptr_arith.cr --static -o /tmp/…` → sha256 须 = `95084e7bc68d6550d21d3d96fa3afd89c67a5d89edce5656a3d2e74fc923d475`（**值须 T1 实测复核**——本树含 P6 之后修复）。**变即停**。**豁免条款**：仅当某任务的**先裁案**被维护者取为「显式声明发射面变更」的形态（唯一现实候选 = 裁-CAP-1 的 (b) 案）时，该任务卡**必须**在正文写明豁免理由与新增判据（逐段 delta 归因 + 两路径同值 + 新语料 canary 重锁），否则**无豁免**。**该语料对类型层/表示层不敏感**——它是「发射面零泄漏」证据，**不得**当新语义正确性证据（P3 附录 C.5-6 口径继承）。
- **三态纪律（不得稀释）**：引擎 -1 不得当 0/1 用；载入失败/段缺失/行不可译/项不可建 ⇒ **拒绝**；save 侧遇不可译行 ⇒ 拒绝。**未配位/未覆盖面**不得以「假定」静默收口（E-2 的全部判据围绕此条）。**新硬错先 report-only**：本批新增的任何 rc=1 门（含 E-3 若保留上限的 P022/P023 新阈值）必须先在**全语料**（`src/compiler/_import.cr` 自指 + `tests/suite` + `tests/selfhost` 内联源 + `src/stdlib` + `examples` + 全枚举套件）report-only 跑一遍，清单审查后才开门。
- **判定面回归网（P6 T1 起，本批继承）** = ① 冻结基线同源对拍（`tools/baseline/parity_run.sh`，72 档 rc + 日志逐档 diff）② 行为探针（`tests/selfhost/*.py` + 入仓探针语料 `tests/probes/` 29 档 + `tools/baseline/probes_run.sh`）③ 突变控制 ④ 三态纪律（ICE04）。**影子通道已死**——任何引用其计数/摘要/站点直方图即违规。
- **`.ccr` 记录值引用口径（D28 继承）**：一律带**命令口径**（`corec ccr F -o O` vs `corec build F -o O --static`，差恒 143B）+ **缓存态**（冷/热；比较任何 `.ccr`/缓存态产物前 `clean-cache`）。
- **判据口径按 TODO #2026-09-11-9**：不采用「与旧版逐字节同」当语义证据；结构性断言 + 语义零变化 + 自举稳定为主判据；「≥N 例」= 下限（可增不可减）；**自测用例只增不减**（重钉/改名允许，**删除须给死亡证据**——本批的重钉点 = `test_optional.py` 未覆盖面 4 例（-11 → 正确值）与 `test_enum_limit.py` 的 17 越界族，两者的死亡证据 = 本批的能力变更本身）。
- **清单三面同核（B.6，硬性）**：任何清单/段表改动必须**三面同核**——`build_selfhost_native.py`（`corec_files`/`corearch_files`/`corelsp_files`）↔ `src/targets/x86_64-linux/_import.cr` ↔ `src/compiler/_import.cr`；每任务回归面**必含** `tests/selfhost/test_backend_bootstrap.py`（rc=0 + 构建日志 `error[` = 0）。本批预计**零清单增删**（全部改动落在既有文件）；若需新增 `.cr` 文件 ⇒ 三面同核 + `test_backend_bootstrap`。
- **不得回退既有收纳**：P4 全批（段机制/确定性装填/允许清单制/可选表示）· P5 全批（单槽化/命名面判定化/引擎唯一权威/ICE04/TA02/影子下线）· P6 全批（`.ccr` v9 项索引/loader 一致性硬校验/`-> never` 调用返回型/回归网入仓/`MIN_CASES` 收紧）· P022/P023 护栏与 `MAX_FN_PARAMS=64`/P020 · F5 槽位契约（#25/#28）与聚合字面量三校验（#29）· 自测用例下限——**一律不动**；冲突 ⇒ 停下上报。
- **本语言约束（实施时必守）**：无三元运算符；取模须非负；键比较不得依赖 i64 回绕；noclobber（`>|`）；**不写 `buf == 0` 形态的空判**（string-int 混比在 bootstrap 侧编成 `str_eq` ⇒ 自崩）。
- **多 agent 纪律**：不并发构建；提交路径限定；实施时逐任务串行；突变控制一律在**仓库外实拷贝**（`tests/` 必须**实拷贝**而非软链——假绿教训）；**含「先裁」的任务：先裁未取前不得落码**（P6 事故护栏）。

---

## 起点基线值（**提示词给定值 + 出处 + 本树状态**；不得预填实测数字）

> **总原则**：本工作树含 P6 记录之后的修复（见「前置状态」的勘误条）⇒ **下列值一律不得直接当本批起点**；T1 必须全量重测并重锁，重测结果写入 T1 报告与收官台账。下表「本树可静态核」列已给出本实例**不跑即可核**的部分。

| 项 | 提示词给定值（出处） | 本树静态实核（本实例） | T1 须实测 |
|---|---|---|---|
| `.ccr` 两口径四条 | 96015 `680a6f98…` / 96158 `76f36e6a…` / 142793 `41e9d845…` / 142936 `704316c8…`，差恒 143B（出处：P6 T3 报告 §4 + T6 报告 §Step 2；本实例实读该两报告） | **须重测**（`ptr_arith`/`generics_test` 两档 × `ccr`/`build --static` 两口径 × 冷/热） | ✔ |
| `selftest-types` | 415/415（出处：`src/ci/run.sh:85` 计数注 + `tests/selfhost/test_type_engine.py:23` `MIN_CASES = 415`——**本实例实读两处在位**） | 计数**下限**在位；实际值须跑 | ✔ |
| 全枚举 | 58/58（出处：P6 T6 报告） | 套件档数 **58**（本实例实核：`ls tests/selfhost/test_*.py` = 51 + `ls tests/bootstrap/*.py` = 7） | ✔（rc 全绿须跑） |
| 五 CI job | 全 rc=0（出处：P6 T6 报告） | — | ✔ |
| 自举链 | `corec2==corec3` `0b2e06d0…` + N06=0 + 冒烟 42 + `--help` rc=1（出处：P6 T6 报告） | — | ✔（sha 必变——本树含 P6 后修复 ⇒ 旧 sha 不得沿用） |
| `test_optional` | 34/34（出处：P6 T0 §5.1） | 用例数 **34**（本实例实核：`ok` 列表条目；含未覆盖面 4 例 `:347-352`） | ✔（rc） |
| `test_enum_limit` | 13/13（出处：P6 T0 §5.2） | 用例数 **13**（本实例实核） | ✔（rc） |
| 冻结基线白名单 | `corec 5d2b15ad…` / `corearch 493dc490…` / `corelsp 18b94bd9…`（= pinned `9bcb7083`，出处：`tools/baseline/REBUILD.md` 白名单表） | 白名单在位（本实例实读 `REBUILD.md`） | ✔（重建核 + **换代**：新白名单 = 本批起点修订） |
| 探针语料 | 29 档（出处：`tests/probes/README.md`「清点」节） | 29 档在位（本实例实核 `ls tests/probes/*.cr`） | ✔（两态零差异） |
| 语料 | 72 档，rc 分布 34×0/38×1（出处：P6 T0 表 C） | — | ✔（**须重清点**：本树套件面已变） |

---

## 侦查底座（2026-09-14 实读实核；file:line = 当前工作树；未标注者为本实例实读）

**E-2 面（表示位现状与未覆盖面）**
- 表示位侧表：`globals.cr:63` `g_ir_var_rep : string, mut;     g_ir_var_rep_cap : int, mut;`（注 `:57-66` 明撰「每个可选槽（局部/形参/调用结果）配一个……**无表示位的槽走既有装箱假定（未覆盖面：结构体字段/数组元素/全局槽/match 结果/惰性 thunk）**」）。
- 生长/读写：`ir_gen.cr:214-224`（`grow_ir_var_rep`，新槽填 -1 哨兵）· `:230-235`（`irv_rep`）· `:237-241`（`irv_set_rep`）· `:244-249`（`emit_rep_set`）· `:251-259`（`opt_rep_slot`）。
- 判定辅助：`global_decl_optional` `:261-277` · `callee_ret_optional` `:279-295` · `rep_enc_of_expr` `:297-330`（含 `:318` `EXPR_STRUCT/EXPR_ARRAY/EXPR_TUPLE → oe_bare()` 与 `:329` 兜底 `return oe_boxed();`）。
- 写点（已配位）：`:2138-2141`+`:2172`（LET）· `:1147-1149`（赋值局部）· `:1745-1762`（调用点写实参信道 + 读返回信道）· `:2209-2210`（返回点写信道）· `:2536-2544`（序言读形参信道）· `:2578-2598`（函数尾）· `:2829`（`ir_gen_globals` 尾部 `reg_optrep_globals`）。
- **未配位写点（E-2 的实施靶）**：字段 `:1160-1164`（EXPR_ASSIGN 的 EXPR_FIELD 分支）· `:2346`（EXPR_STRUCT）· 枚举载荷 `:2312`；数组 `:1177`/`:1184`（EXPR_ASSIGN 的 EXPR_INDEX）· `:2369`（EXPR_ARRAY）· `:1314`（`go` 指数形式建数组）· 全局 `:1152-1155`（EXPR_ASSIGN 全局分支）· `:2735`（全局初值回填）；match 结果 `:1960`/`:2076`。
- 未配位读点：字段 `:2230`；数组 `:2287`/`:2295`；`?` `:2458`；match 载荷 `:2046`/`:2063`（仅当 `rep_v < 0` 的装箱分支）。
- 判据载体：`tests/selfhost/test_optional.py:347`（`uncovered_field_bare_is_loud` → `-11`）· `:348`（装箱正确）· `:351-352`（全局装箱/None 正确）· 头注 `:22-24`（未覆盖面登记原文）。**数组元素面与 match 结果面零用例**（本实例 grep：`tests/` 内无 `[int?]`/可选数组字面量；`test_optional.py` 未覆盖面只钉字段与全局）⇒ T1 须**新造最小复现**。

**E-3 面（上限载体）**
- 常量：`ast.cr:115-118`。镜像 struct：`:130-138`（FuncInfo）· `:141-149`（StructInfo）· `:152-156`（EnumVariant）· `:159-165`（EnumInfo）。
- 布局：`dyn_arr.cr:56-58`（`ESZ_FUNCINFO=688` / `ESZ_STRUCTINFO=440` / `ESZ_ENUMINFO=4408`）· `:90-94`（FuncInfo 偏移，含 `MAX_FN_PARAMS=64` 的槽区 `:89`）· `:97-100`（StructInfo：`OFF_SI_FIELD_NAMES=8` / `OFF_SI_FIELD_TYPES=136` / `OFF_SI_FIELD_TYPE_NODES=264` / `OFF_SI_FIELD_COUNT=392` / `OFF_SI_GENERIC_NAMES=400` / `OFF_SI_GENERIC_COUNT=432`）· `:231-243`（EnumInfo：`OFF_EI_VARIANTS=8` / `OFF_EI_VARIANT_COUNT=4360` / `OFF_EV_SIZE=272` / `OFF_EV_TYPES=8` / `OFF_EV_TYPE_COUNT=136` / `OFF_EV_TYPE_NODES=144`）。
- 受护访问器（**迁移单源**）：`dyn_arr.cr:462-464`（`fi_param_type` 护栏）· `:485-494`（`si_field_*` 读护栏 -1 哨兵）· `:503-516`（`ei_variant_*` 读护栏）· `:529-549`（`ei_set_*`/`si_set_*` 写护栏）· 契约注 `:518-528`（「唯一受护写点 + 读护栏 + P022/P023 承担用户面拒绝」）。
- 写入侧：`parser.cr:1448-1461`（P020 形参上限，`MAX_FN_PARAMS`）· `:1634-1640`（`si_set_field_*`）· `:1648-1652`（P023 定位硬错）· `:1695-1716`（`ei_set_variant_*`）· `:1724-1733`（P022 两串：变体数 / 载荷数）。
- 读回侧：`ccr_io.cr:1203`（struct 字段数闸）· `:1245`（变体数闸）· `:1272`（载荷数闸）· 清零循环 `:1208-1215`/`:1219`/`:1250-1262`/`:1265`。
- 覆盖位：`ty_shadow.cr:1204-1213`（`sh_match_bit`）· `:1217-1226`（`sh_match_first_missing`）· `checker.cr:3033-3040`（`m_cover` 累积与冗余臂判据）。
- 判据载体：`tests/selfhost/test_enum_limit.py`（16 边界三面正控 + 17 越界 P022/P023 + 无产物 + 「拒绝发生在**前端**」的 `forbid` 阶段串 `:124-127`）· `src/ci/run.sh:108` 挂点。

**E-4 面（缓存）**
- 门：`main.cr:484-487`（注：快照不载侧表 ⇒ 命中恢复路径与冷路径产物分歧）· `:488-490`（`if g_optrep_on != 0 { cache_enabled = 0; }`）· 缓存开关族 `:477-483`（泛型函数在场亦关）· 调用点 `:532`（load）· `:553`（save）· 目录 `:473`（`make_cir_cache_dir`）。
- 快照面：`cir_cache.cr:51`（`CIR_CACHE_VER=17`）· `:7-49`（版本族注：v12..v18，含「`.cir` 写点早于 TYPE 段重建 ⇒ 项索引不能落盘」）· `:57-90`（编译器身份 = 运行中二进制自哈希）· `:187-249`（save 头部/尺寸）· `:436-492`（load 五闸 + 变量恢复「按**绝对** var id 增长数组」）。
- 判据载体：`tests/selfhost/test_cir_warm_path.py`（21 例；A 组冷/暖 7 例含 `.ccr` **段级契约** `:191-198`；C 组可选面 2 例 `:277-287`）· `tests/selfhost/test_cache_identity.py`。

**回归网 / 工具面**
- `tools/baseline/REBUILD.md`（配方 + 白名单 + 换代纪律）· `rebuild.sh`（`jj workspace add --revision <PINNED_REV>` → `build_selfhost_native.py` → sha 硬闸 → 冒烟）· `parity_run.sh`（72 档 `check`；影子分支已剥；计数断言）· `probes_run.sh`（29 档显式清单 + 计数断言）。
- `tests/probes/`（29 `.cr` + README 来源表）· `src/ci/run.sh:68-113`（selfhost-tests；挂点 **28** 档 py，本实例 `grep -c` 实核）· `:85`（selftest 计数注 415）· `:103`（test_optional 34）· `:108`（test_enum_limit 13）。

**锚点漂移（本实例实读发现；实施时以实施树为准）**
1. **报告位置**：`.superpowers/sdd/` 不在 `/tmp/p7wt`（jj-ignored，不入检出）；实体在 `/home/DslsDZC/core/.superpowers/sdd/`（见「前置状态」）。
2. **修订面**：本树含 P6 记录之后的修复（逐条证据见「前置状态」勘误条）⇒ P6 记录值不得直接沿用；`97f4394f` 的一致性留给 T1。
3. **`CIR_CACHE_VER` 实值 = 17**（`cir_cache.cr:51`）——提示词/裁-CAP-3 问句所写「18→19」不成立（`REBUILD.md` 与 format spec 亦记 17）。
4. **`ast.cr` 镜像不一致面宽于登记值**：除 `StructInfo.field_*`（64 vs 16）外，另有 3 处（`StructInfo.generic_names` 16 vs 4 · `EnumVariant.types` 64 vs 16 · `EnumInfo.generic_names` 16 vs 4）；且这些 struct 在 `ast.cr` 之外**仅注释命中**（无类型使用点 ⇒ 疑为死声明，须实证）。
5. **受护访问器区间**：P6 计划写 `dyn_arr.cr:481-548`，实读 **`:462-549`**（契约注 `:518-528`）。
6. **「同触 `dyn_arr.cr` 布局」的合批理由需更正**（P4 附录 D-3-8 原话）：E-3 触的是**编译器自身符号表记录**（`ESZ_STRUCTINFO`/`ESZ_ENUMINFO`）；E-2 的推荐案（裁-CAP-1 (a)）**不触**任何布局，若取 (b) 案则触的是**目标程序**数据布局（`type_size`/ELF 发射）——两者**不同物**。合批理由应表述为「同类裁决面（容量/表示）同批」，非「同触布局」。
7. **E-4 机制存疑**：P4 报告「命中恢复的函数解包点回落装箱路径」在本树静态枚举下**未能复现**（全部 `irv_rep` 读取点 `ir_gen.cr:304`/`:1148`/`:1987`/`:2443` 都在**同函数生成内**；命中函数不生成 ⇒ 其 IR 自含表示语义）⇒ 机制须 RED 实证（裁-CAP-3）。
8. **`sh_match_bit` 的 63 边界**（`ty_shadow.cr:1204-1213` 头注）是 **i64 位宽**边界而非 16 派生 ⇒ 上限策略（裁-CAP-2）与覆盖位改造**耦合**，登记文字「>63 变体溢出」的边界值须按此理解。

---

## Task 1：前置侦查 + 冻结基线重建（换代）+ 起点判据 + 三面 RED 现状（**源码零改动**）

**Files:** 无源码改动。产物 = 报告 `.superpowers/sdd/cap-task1-report.md`（工作区文件，不入提交）+ 重建基线 `/tmp/cap-t1/base/{corec,corearch,corelsp}` + 原始输出 `/tmp/cap-t1/*`。

**Interfaces（六表，全部落报告；数字一律本实例实测，未实测标「待实测」）：**
- **表 A：冻结基线重建 + 白名单换代**——① 用现行配方核旧白名单仍可复现（`bash tools/baseline/rebuild.sh /tmp/cap-t1/base-p4` ⇒ 三 sha = `REBUILD.md` 白名单）；② **换代**：以本工作树的 `@-` 修订（T1 以 `jj log -r @- --no-graph -T …` 实读；**须与提示词给定的 `97f4394f` 核对，不符则登记并以上报裁决**）为源修订，重建本批起点基线并记新白名单（三 sha + 源修订），按 `REBUILD.md`「换代纪律」更新该文件（旧值留痕）。
- **表 B：本批起点判据**——五 CI job；全枚举（档数以本实例 `ls` 重清点为准）；`check src/compiler` rc；`selftest-types` 实测值；构建确定性 ×2；自举链（`corec2==corec3` + N06 + 冒烟 42 + `--help` rc=1）；**ELF canary 实测**（值须与 Global Constraints 的白名单值比对，变即停）；`.ccr` 两口径四条（冷 + 热）；`--dump-objects` 行数；`.cir` DOT；`test_optional` rc；`test_enum_limit` rc。
- **表 C：语料与探针清点**——72 档清单逐档实核（含 0 字节 fixture 与既有 rc=1 档的划界）+ `parity_run.sh` 一次得 rc 分布（与 P6 表 C 对照）；`probes_run.sh` 29 档 rc 表。
- **表 D：锚点实核**——本计划「侦查底座」每个 file:line 逐条复核；漂移登记（含本计划已列 8 条）。
- **表 E：三面 RED 现状**——E-2：`test_optional` 未覆盖面 4 例 rc 复现 + **数组元素面 / match 结果面的新造最小复现**（各写载裸值 + Some 臂、装箱 + Some 臂两形态，四路径记录 rc）；E-3：16 边界正控 + 17 越界 P022/P023（`check`/`build` 两命令 + 无产物）；E-4：可选程序编译**零条目落盘**复现 + 非可选程序冷/暖条目命中对照。
- **表 F：E-3 四前置实核**——逐条 {结论 / 缺什么}（本计划已给初判，T1 须以本树重核并补实测证据）。

- [ ] **Step 1: 基线重建 + 换代**（表 A；重建在仓库工作树之外，收工 `jj workspace forget` + 删目录；**仓库工作树零改动**）。判据：旧白名单重建逐条同 + 新基线三 sha 记录 + 冒烟 42。**不符 ⇒ 停下上报**。
- [ ] **Step 2: 起点判据**（表 B；cwd = 仓库根、逐段 `ps` 实查无并发、比较前 `clean-cache`）。
- [ ] **Step 3: 语料/探针清点**（表 C）。
- [ ] **Step 4: 锚点实核**（表 D；含本计划 8 条漂移的逐条复核）。
- [ ] **Step 5: 三面 RED 现状 + 四前置实核**（表 E/表 F；**新造数组元素/match 结果两面复现**——这是 E-2 实施的前置证据）。
- [ ] **Step 6: 报告**（不入提交；收工 `jj status` 应为空）。

**判据**：六表齐 + 基线 sha 与换代白名单逐条同 + 源码零改动（`jj status` 干净）+ 报告内数字全部可追溯（命令 + 原始输出片段）。

**停条件**：① 重建 sha 不符白名单 ⇒ 停下（**不得**改白名单迁就）；② 换代源修订与提示词给定值不符且无法归因 ⇒ 停下上报；③ E-2 的数组元素/match 结果两面**复现不出任何形态**（既非失败也非正确）⇒ 停下（本计划的现状判断有误，须重查）；④ canary 实测值 ≠ 白名单值（起点即变）⇒ 停下上报（起点不变量不成立）。

---

## Task 2：E-2 表示面聚合扩展（**Step 1 = 先裁**；挂裁-CAP-1）

**Files:** Modify `src/compiler/ir_gen.cr`（写点规范化/表示位 + 解包侧 + `rep_enc_of_expr` 面）· Modify `src/compiler/globals.cr`（若需新全局/常量）· Modify `src/arch/x86_64/instr.cr`（**仅当**新 IR 形态需要后端分派——须实证）· Modify `src/compiler/interp.cr`（同）· Modify `tests/selfhost/test_optional.py`（未覆盖面 4 例重钉 + 新增）· Modify `src/ci/run.sh`（计数注）。

**Interfaces（**Step 1 未取裁前不得落码**）：**
```
// 表示律（裁-CAP-1 取裁后落纸；以下为推荐案 (a) 的形态说明，非既成事实）：
//   可选聚合槽（字段/数组元素/全局槽/match 结果）的**写点**把「静态裸值」装箱为 Some(v)；
//   None 保持对象；读取侧零改动（恒装箱路径）。纯 IR（两后端同源），零布局变更。
// 写点完备性清单（须逐条判定「是否可达可选槽」；漏一处即静默错值 ⇒ 停条件①）：
//   struct field : ir_gen.cr:1160(EXPR_ASSIGN) · :2346(EXPR_STRUCT) · :2312(枚举载荷，邻域面须实证)
//   array elem   : ir_gen.cr:1177/:1184(EXPR_ASSIGN) · :2369(EXPR_ARRAY) · :1314(go 指数形式)
//   global slot  : ir_gen.cr:1152-1155(EXPR_ASSIGN) · :2735(全局初值)
//   match result : ir_gen.cr:1960(声明) · :2076(臂写) · 通配臂 / 无体臂路径
// 读点（保持既有装箱假定或按律解包）： :2230 · :2287/:2295 · :2458 · :322-327
// 若取裁为 (b) 案（位图/stride）：布局表 + 两后端同改清单 + canary 豁免声明（须写进任务卡正文）
```
- [ ] **Step 1: 先裁落纸**（裁-CAP-1 取裁结果 + 与 P4 裁决 5 / D20 / 三态纪律的一致性论证 + 面清单 + 「漏写点 ⇒ 静默」风险表 + 若为 (b) 案则含 `type_size`/ELF/解释器/`@sizeOf` 影响面与 canary 豁免声明）。判据：落纸齐 + 无「大概/应该」措辞。**未取裁 ⇒ 本任务止步于此**（不发任何改动，登记）。
- [ ] **Step 2: RED 证据**：逐面最小复现（裸值 + Some 臂 / 装箱；含数组元素与 match 结果两面——T1 表 E 的复现件在此转成用例）。判据：四面的 rc/产物矩阵与 T1 记录逐条同。
- [ ] **Step 3: 实现 + 写点完备性审计**（`grep -n "IR_STORE_FIELD\|IR_STORE_INDEX\|IR_STORE_INDEX_VAR\|IR_STORE_PTR\|IR_STORE," src/compiler/ir_gen.cr` 逐条判定 + 指针/别名写可选槽的可达性实证）。判据：审计表逐条有结论；**存在不可覆盖写点 ⇒ 停下**（规范化律不成立）。
- [ ] **Step 4: 用例**：未覆盖面 4 例**重钉**（死亡证据 = 本任务的能力变更）+ 新增（4 面 × {裸, 装箱, None, 条件写, 跨函数/跨解包点} 的 `case_dual` 三路同证 + 负控）。
- [ ] **Step 5: 判据（硬闸）**：`test_optional` ≥ 34（只增）· **ELF canary IDENTICAL**（推荐案下**无豁免**；非可选程序零足迹 = `g_optrep_on` 门）· `.ccr` 两口径四条（冷/热）**预期不变** ⇒ 逐条实测，变则逐段 delta 归因 · 72 档对拍零差异 + 探针 29 档零差异 · 五 CI job + 全枚举 + `selftest-types`（≥ T1 实测值）+ `check src/compiler` rc=0 + 自举链 + `test_backend_bootstrap` rc=0 + `error[`=0 · **突变控制 ≥3**（漏一个写点 / 装箱分支反转 / 解包侧回落装箱路径 ⇒ 各自转红）+ 精确回滚。
- [ ] **Step 6: 提交**（路径限定）：`feat: 容量批 Task 2——E-2 表示面聚合扩展（结构体字段/数组元素/全局槽/match 结果；发射面零泄漏）`。

**停条件**：① 任一写点不可覆盖（经指针/别名写可选聚合槽、或路径不可静态判定）⇒ 停下（改取裁）；② canary 变 ⇒ 停下（除非任务卡正文已按裁-CAP-1 (b) 声明豁免）；③ 需引入后端旁路状态（`g_x86_mw_tag_off` 式）⇒ 停下（两路径同源被破）；④ 全语料出现任何**静默错值**（rc=0 而值与语义不符）⇒ 停下（三态纪律）。

---

## Task 3：E-3 记录布局迁侧表（**Step 1 = 先裁**；挂裁-CAP-2）

**Files:** Modify `src/compiler/dyn_arr.cr`（记录布局 + 访问器 + 侧表 grow/append + 新偏移常量）· Modify `src/compiler/parser.cr`（写入循环改 append + 上限策略落点）· Modify `src/compiler/ccr_io.cr`（读回闸 → 动态填充；清零循环）· Modify `src/compiler/ty_shadow.cr` + `src/compiler/checker.cr`（覆盖位表示与消费点）· Modify `src/compiler/ast.cr`（常量 + 镜像归位/删除）· Modify `src/compiler/globals.cr`（侧表全局）· Modify `tests/selfhost/test_enum_limit.py`（重钉）+ `tests/selfhost/test_ccr_types.py`（若段尺寸断言涉）· Modify `src/ci/run.sh`（计数注）。

**Interfaces（**Step 1 未取裁前不得落码**）：**
```
// 侧表形态（示例；最终以先裁的逐字段布局表为准）：
//   结构体字段：(row, fi) → {name_ni, type_code, type_node}；行基址侧表 g_si_field_base[row]
//   枚举变体  ：(row, vi) → {name_ni, type_base, type_count}；行基址侧表 g_ei_variant_base[row]
//   变体载荷  ：(variant_row) → type_code / type_node（二级侧表）
// 访问器签名不变（si_field_name(n,fi) / ei_variant_type(n,vi,ti) / si_set_* / ei_set_*）
//   ⇒ 54 个调用点零改动（受护访问器单源的价值所在；若必须先裁为改签名 ⇒ 停下另议）
// 覆盖位：sh_match_bit(sh_match_first_missing) + checker.cr:3033-3040 的 m_cover 同批换表示
// 上限策略（裁-CAP-2）：(a) 无硬上限 | (b) 对齐镜像 16→64（变体 ≤63） | (c) 维持现状
```
- [ ] **Step 1: 先裁落纸**（裁-CAP-2 取裁结果 + **逐字段布局表**（内存 & 盘面两侧）+ 失效面清单（在野 `.ccr` 版本面 = 见附问 2）+ 镜像归位/删除处置（含死声明实证方案）+ 覆盖位新表示的三态语义）。判据：布局表逐字段有依据 + 与「盘面已是变长」事实一致。**未取裁 ⇒ 本任务止步于此**。
- [ ] **Step 2: 用例（红）**：新上限族（构造/`match` 穷尽性/`.ccr` 读回/泛型实例化各面）+ 旧 16/17 边界用例的**重钉或死亡证据**（`test_enum_limit.py` 13 例：16 边界正控保留；17 越界拒绝面按新上限重钉；**删除须给死亡证据**）。判据：新用例在当前树上**红**（或按 (c) 案 ⇒ 本任务不实施）。
- [ ] **Step 3: 实现**（记录 + 访问器 + parser 写入 + ccr_io 动态填充 + 覆盖位）。**单源纪律**：所有读写经受护访问器（`dyn_arr.cr:518-528` 的三条契约不破）；`count` 槽保持真值（既有口径）。
- [ ] **Step 4: 判据（硬闸）**：**ELF canary IDENTICAL**（记录布局不进发射面 ⇒ 无豁免）· `.ccr` 两口径四条（冷/热）**预期不变** ⇒ 逐条实测 + 有变则逐段 delta 归因 + 走附问 2 · 72 档对拍零差异（>16 声明在语料零命中 ⇒ 零差异**是**判据）· 探针 29 档零差异 · 五 CI job + 全枚举 + `selftest-types`（≥ T1 值）+ `check src/compiler` rc=0 + 自举链 + `test_backend_bootstrap` · `test_enum_limit`（重钉后）+ `test_ccr_types` + `test_ccr_v7` + `test_region_cfg` 全绿 · **突变控制 ≥3**（漏改一个访问器 / 读回闸未改（仍按 16 拒） / 覆盖位表示退化到单 int ⇒ 各自转红）+ 精确回滚。
- [ ] **Step 5: 文档**：format spec §3.2 SYM（`docs/superpowers/specs/2026-09-09-lattice-ir-v7-format.md:57`，「不变」句若涉盘面则改，否则补「进程内记录迁侧表」注）· `CLAUDE.md` 若有 `MAX_*` 叙述 · TODO · 本计划回填。
- [ ] **Step 6: 提交**（路径限定）：`feat: 容量批 Task 3——E-3 记录布局迁侧表（MAX_STRUCT_FIELDS/MAX_ENUM_VARIANTS/MAX_VARIANT_TYPES 解除；盘面零变化）`。

**停条件**：① 迁移需改 `.ccr` **盘面格式** ⇒ 停下（超授权，另裁版本面）；② canary 变 ⇒ 停下；③ 出现任何「越界写静默」形态（含读回侧静默接受/拒收错位）⇒ 停下（三态纪律）；④ 覆盖位改造若被迫动 `ty_exhaustive` 的**生产语义**（非表示层）⇒ 停下（超章程；该面经 `ty_inhabited → ty_exhaustive` 达 TM03 生产面——P6 已有先例裁定）。

---

## Task 4：E-4 可选程序收回 `.cir` 缓存（**Step 1 = 机制 RED 实证（必做）+ 先裁**；挂裁-CAP-3；依赖 T2）

**Files:** Modify `src/compiler/cir_cache.cr`（快照面或装载侧派生）· Modify `src/compiler/main.cr`（`:488-490` 门）· Modify `tests/selfhost/test_cir_warm_path.py`（C 组 2 例重钉 + 新增）· Modify `src/ci/run.sh`（计数注）。

**Interfaces（**机制未实证前不得落码**）：**
```
// Step 1 实证件（临时沙箱，仓库外实拷贝；不得进提交）：
//   ① 放开 main.cr:488-490 门（沙箱内）⇒ 可选程序连续两跑：比 ELF 逐字节 / .ccr 段级契约 / IR dump
//   ② 全命中面：暖跑条目 size+mtime 不变（真命中）；混态：部分函数命中 + 部分 miss
//   ③ 结论二选一：出现静默分歧（归因到具体状态：侧表 / 信道全局 / var 序）⇒ 落 (a)/(b) 案
//                 无分歧（证伪 P4 机制陈述）⇒ 落 (c) 案 + 「门已无必要」的登记
// 收口案（裁-CAP-3）：
//   (a) 快照加表示位面 ⇒ CIR_CACHE_VER 17→18（本树实值 17）+ 失效面登记
//   (b) 装载侧重派生（同 NOD 派生先例 cir_cache.cr:43-46/:514-523）⇒ 零格式变化
//   (c) 维持关缓存（现状）
```
- [ ] **Step 1: 机制 RED 实证 + 先裁**（沙箱放开门的冷/暖对比；结论 + 归因）。判据：有冷/暖原始产物对比件 + 分歧/无分歧的判据明确。**未取裁 ⇒ 本任务止步于此**（门保持现状）。
- [ ] **Step 2: 用例**：冷/暖 ELF 逐字节同 + `.ccr` **段级契约**（冷=暖：SYM..IFACE 七段；STR 段冷≠暖为**预存**口径，`test_cir_warm_path.py:191-198`）· 条目真命中（size/mtime 不变）· 表示面各形态（裸/装箱/条件写/跨函数/聚合面——与 T2 的交互面）双跑同产物 · 负控（编译器身份变 ⇒ miss；格式位不符 ⇒ miss）。
- [ ] **Step 3: 实现**（按取裁；**不新增后端旁路状态**）。
- [ ] **Step 4: 判据（硬闸）**：**ELF canary IDENTICAL** · `.ccr` 两口径四条（冷/热）逐条实测 · `test_cir_warm_path` ≥ 21 例（C 组重钉须给死亡证据）+ `test_cache_identity` 绿 · 72 档对拍零差异 + 探针零差异 · 五 CI job + 全枚举 + 自举链 + `test_backend_bootstrap` · **突变控制 ≥2**（关掉命中比对 / 快照面丢一个状态 ⇒ 各自转红）+ 精确回滚。
- [ ] **Step 5: 文档**（`cir_cache.cr` 版本族注 · format spec 的 `.cir` 版本行 · TODO）。
- [ ] **Step 6: 提交**（路径限定）：`perf: 容量批 Task 4——E-4 可选程序收回 .cir 缓存（表示面可重建；冷/暖产物同）`。

**停条件**：① 冷/暖对比出现**任何静默分歧**（rc=0 而产物不同）且无法归因 ⇒ 停下；② 机制证伪且无分歧 ⇒ 按 (c) 收口（登记，不落码）；③ 快照格式变更被迫波及 `.ccr` 或后端 ⇒ 停下。

---

## Task 5：收官（回归 + 判据 + 台账 + 文档 + 终态）

- [ ] **Step 1: 全量回归**（cwd = 仓库根、`nice -n 19`、串行、比较前 `clean-cache`）：五 CI job 全 rc=0；全枚举（实测档数）；`check src/compiler` rc=0；`selftest-types` 终值；构建确定性 ×2；`test_backend_bootstrap` rc=0 + `error[`=0。
- [ ] **Step 2: 判据复验**（用**入仓配方重建的本批起点基线**）：同源对拍 72 档（rc + 日志逐档 diff；预期差异须逐条归因）· 探针 29 档 · 突变控制快照 · ELF canary · 自举链（`cmp` + N06 + 冒烟 42 + `--help` rc=1）· `.ccr` 两口径（冷/热）· `--dump-objects` · `.cir` DOT。
- [ ] **Step 3: 统一台账**：逐任务列 {实施 / 删除 / 收紧 / 放宽 / 修复 / 登记 / 偏差}，与各任务实际 diff 逐条对应；三裁定（裁-CAP-1..3）的**取裁结果与落地情况**单列；未取裁项列「已登记、未实施」。
- [ ] **Step 4: 文档**：spec §9 加**容量批行**（实施项 / 判据 / 剩余登记）· format spec（若涉）· TODO 新条目（容量批收官 + 三裁定结果）· `CLAUDE.md`（若引入新文件/目录或 `MAX_*` 叙述变化）。
- [ ] **Step 5: 容量批终态陈述（一页）**：已实施面 / 已裁定面 / 剩余登记面三栏；下一批指向。
- [ ] **Step 6: 提交**（路径限定）。

**判据**：五 CI job + 全枚举 + canary + 自举链 + 同源对拍 + 探针 + 台账逐条可溯源 + 文档指针链闭合（spec §9 → 本计划 → 统一台账）。

---

## 停条件汇总 · 未决项（**不猜**）· 自检记录

**全批级停条件**：① 任何任务使 ELF canary 变（除任务卡正文已声明豁免的形态）⇒ 停下；② 任何「静默错值/静默分歧/静默接受」形态出现 ⇒ 停下（三态纪律）；③ 需改 `.ccr` 盘面格式 / `.cir` 格式面而超出该任务的批准面 ⇒ 停下另裁；④ 需动清单三面（`build_selfhost_native.py` ↔ 两 `_import.cr`）而计划未列 ⇒ 停下（并补三面同核 + `test_backend_bootstrap`）；⑤ 与既有收纳（P4/P5/P6 全批 + 本计划的「不得回退」清单）冲突 ⇒ 停下上报。

**未决项（本计划**无法确定**、实施时实证或请维护者补一句）**：
1. **修订一致性**：工作树 `@-` 与提示词给定 `97f4394f` 是否同一提交——本实例不能跑 `jj` ⇒ T1 实核并登记。
2. **E-4 机制**：P4 的「命中恢复 ⇒ 解包回落装箱」陈述在本树静态枚举下**未能复现**（见「锚点漂移 #7」）⇒ T4 Step 1 实证（**不得**照抄「装载侧重派生」方案当既成事实）。
3. **E-2 的惰性 thunk 面**：P4 未覆盖面清单含它、P5 附录 E-2 未列 ⇒ 归属未裁（默认本批不做；若维护者点名则入 T2 的面清单）。
4. **`ast.cr` 三个 struct 声明是否为死声明**：本实例 grep 无类型使用点，但 bootstrap 侧处理未实证 ⇒ T3 实核。
5. **E-2 写点的指针/别名可达性**（经 `IR_STORE_PTR` 写可选聚合槽是否可能）⇒ T2 Step 3 审计实证；若可达且不可静态覆盖 ⇒ 停条件①。

**自检记录**：
- **分诊诚实性**：三项**未凑数**——E-2/E-3/E-4 各自给了「现状（file:line）→ 影响面 → 依赖 → 前置 → 处置」，**没有任何一项被推荐案冒充裁定**（三条裁决门逐条点名问句 + 未取裁时的行为写死为「不实施 / 维持现状」）；E-3 的四条前置**逐条给了结论**，其中 ② 的缺口**实读发现宽于登记**（多 3 处镜像不一致）、③ 的边界**按位宽理解修正**（63 而非 16 派生）——两项均落进「锚点漂移」。
- **根因 vs 止血**：① E-2 的落点 = **写点完备性**（不是给每面加特例分支）；② E-3 的落点是**记录布局单源**（受护访问器 54 调用点零改动 = 迁移可行性的根据），不是逐点加护栏（护栏已在位，是安全网）；③ E-4 先**证伪/证实机制**再定收口面（不先动格式）。
- **与既有裁决的一致性复核**：P4 裁决 5「解包侧判表示」（裁-CAP-1 的 (c) 案与其相悖 ⇒ 已标不推荐）· D20（进程内引用不得跨进程 ⇒ E-4 若取 (a) 案的表示位面须是**可重派生/文件内**形态）· D22/D23（互斥与派生码纪律）· D28（`.ccr` 两口径 + 冷/热豁免）· TODO #2026-09-11-9（判据口径）· P6 事故护栏（「先裁前不得落码」写进 T2/T3/T4 任务卡正文）。
- **占位符扫描**：无 TBD；所有需实测处均标「须 T1/T* 实测」并给出测量命令或出处；五处未决项明写，**未预填数字**。
- **本计划的自我限制（诚实登记）**：本计划**未跑任何构建 / 编译 / 测试 / `jj`**（纯只读侦查：`ls`/`grep`/`sed`/`wc`/`diff`/`ps` 级实核）；所有「现状值」为实读源码/文档或既有报告记录值；**起点基线值一律标「须 T1 重测」**（理由 = 本树含 P6 记录之后的修复）；三处需实测的数字（canary、`.ccr` 四条、selftest 计数）**不得**从 P6 记录沿用。
- **风险面**：① **E-2 写点漏一处 = 静默错值**（本批最大风险）——缓解 = 写点完备性审计 + 指针/别名可达性实证 + 突变控制 ≥3；② **E-3 的记录迁移面广**（54 调用点虽零改动，但记录布局 + 读写闸 + 覆盖位三处同批）——缓解 = 受护访问器单源 + 旧边界用例重钉 + 突变 ≥3 + 盘面零变化（判据 = `.ccr` 四条不变）；③ **E-4 的机制未明**——缓解 = 先实证后落码 + 证伪即按现状收口；④ 修订/基线与提示词给定值不一致（T1 停条件②）——缓解 = T1 首步实核 + 白名单换代留痕；⑤ 多 agent 并发与「先裁抢跑」（P6 事故先例）——缓解 = 任务卡正文护栏 + 一构建一编译串行 + 路径限定提交。

---

## 任务实施记录（T1–T5；**T5 收官回填**）

| 任务 | 提交 | 交付 | 判据（实测） |
|---|---|---|---|
| **T1** 前置侦查 + 基线换代 | `4607624c`（白名单换代）+ 报告 `.superpowers/sdd/cap-task1-report.md` | `tools/baseline/{REBUILD.md,rebuild.sh}` 白名单 **`9bcb7083 → 97f4394f`**（旧值留痕）；六表侦查（含三面 RED、E-3 四前置、8 条锚点漂移） | 起点判据 **14/14 与 P6 终态记录全同**（canary `95084e7b…d475` · `.ccr` 四条 96015/96158/142793/142936 · selftest 415 · 枚举 58→59 · 72 档 34×0/38×1 · 探针 17×0/12×1）；换代自证 = 重跑配方 ×2 三 sha 逐条同 + 冒烟 42 + `cmp` IDENTICAL ×3 |
| **T2** E-2 表示面聚合扩展 | `432c2e40` | `globals.cr`(+5)/`ir_gen.cr`(+230)：写点装箱单源件 + A 类 8 点 + B 类 2 点（#10/#11 实证可达升 A）+ match 面裁决 (i)；`test_optional` 34 → **48** | canary IDENTICAL（无豁免）· `.ccr` 四条冷+暖逐字节同起点 · 72 档仅行号位移 · 探针零差异 · 五 CI job · 枚举 59/59 · selftest 415/415 · 链 `819af35c…`；突变 M1/M3/M4/M8 全红（**M4 = rc=0 静默错值仅精确值断言可抓** · M8 = canary 变） |
| **T2 登记** | `7c4d6603` | TODO **#56**（`c3` 双路径分歧静默错值；**今天就存在 · 本批不修 · 非本批引入**） | 三子形态 c1 正确但脆弱 / c2 139 / c3 ELF 8 vs interp 216 |
| **T3** E-3 记录布局迁侧表 | `0b0d9aef` | 10 文件（+381/−271）：结构体字段/枚举变体载荷**侧表** + **覆盖位无界位图**（`mc_*`）+ 三 `MAX_*` 退役 + P022/P023 停发 + `ast.cr` 四死镜像删除 + `test_enum_limit` 13 → **18** | 17/40 字段 · 17/40/64/70 变体 · 17 载荷 ⇒ 编译+运行正确（旧拒绝面转正）· 70 变体旧态 **SIGFPE rc=136** → 现穷尽正确且反例名正确 · TM03/TM04 真错仍拒 · canary IDENTICAL · **`.ccr` 四条冷+暖逐字节同 ⇒ 「不 bump」成立** · 72 档仅行号 +116 位移 · 探针零差异 · 五 CI job · 枚举 59/59 · selftest 415/415；突变 MA/MB/MC 全红 |
| **T4** E-4 机制实证 | `15357b04` | TODO **#57** + 报告（**零源码改动**）：沙箱放开门 ⇒ 冷/暖 × 16 档 + 混态 3 态 × 2 程序 **ELF 逐字节全同** ⇒ **机制证伪 ⇒ 按裁 (c) 收口**；「移除门」登记为待裁候选 | 门原样保留；证据 `/tmp/capt4/**` |
| **T5** 收官 | 本提交 | 全量回归 + 本台账 + spec §9 容量批行 + TODO #2026-09-15-9 + 终态陈述 | 见下 |

---

## 统一台账（容量批 T1–T5；**T5 收口**）

> 口径（继承 P6）：每件给证据与替代/覆盖；全语料命中一律与**同任务起点**基线逐条比。**KPI**：实施 **4**（T1 回归网换代 · T2 · T3 · T4 实证收口）· 删除 **3 类**（`ast.cr` 四个死镜像结构 · P022/P023 两闸 · 定长槽清零循环两处）· 迁移 **2 类**（结构体字段→侧表 · 枚举变体/载荷→侧表；另 selftest 直接写点 44 处改访问器）· 收紧 **0** · 放宽 **2 类**（T2 聚合面裸值 139→正确值 · T3 三面上限解除）· 修复 **3 件**（T2 global 双根因：读点常量折叠 + 写点静态度量初值；T3 覆盖位 ≥63 溢出崩溃）· 登记 **12** · 偏差 **6** · **未取裁 2**（T4 移除门候选 · (D) 语言面拒绝候选）。

### 1. 实施（4 件）
逐件见上「任务实施记录」表（T1/T2/T3/T4 的提交、交付、判据）。

### 2. 删除 / 迁移
| # | 件 | 任务 | 证据 |
|---|---|---|---|
| 1 | **删除**：`ast.cr` 四个死镜像结构（`FuncInfo`/`StructInfo`/`EnumInfo`/`EnumVariant`） | T3 | 全仓零类型使用点（仅注释命中）；`[.;64]` 与实际布局本已不一致；布局真源 = `dyn_arr.cr` |
| 2 | **删除**：`P022`/`P023` 两闸 + `MAX_STRUCT_FIELDS`/`MAX_ENUM_VARIANTS`/`MAX_VARIANT_TYPES` 三常量退役 | T3 | 上限解除 ⇒ 无越界写对象；错误码保留编号并标「已退役、零 raise」 |
| 3 | **迁移**：结构体字段三条 + 枚举变体/载荷五条**侧表**（记录尺寸与 `OFF_*` 不变 ⇒ **110 个访问器调用点零改动**） | T3 | 记录面判据全绿 + 突变 MA/MB 转红 |

### 3. 放宽（2 类；全语料命中 = 0，均为**新能力面**）
| # | 形态（前 → 后） | 任务 | 覆盖 |
|---|---|---|---|
| 1 | 可选**聚合槽**裸值：双路径 139（响亮失败）→ **正确值**（写点装箱） | T2 | 4 面（字段/元素/全局/match 结果）+ 2 邻域（枚举载荷/元组元素）；`test_optional` 48/48；72 档/探针零差异（语料无可选聚合） |
| 2 | 三上限（16）解除：>16 ⇒ 定位硬错拒绝 → **接受且语义正确** | T3 | 17/40/64/70 变体 · 17/40 字段 · 17 载荷；`test_enum_limit` 18/18；72 档零差异（语料最大 9 字段 / 无超限枚举） |

### 4. 修复（3 件）
| # | 形态 | 任务 | 根因 |
|---|---|---|---|
| 1 | `g : int? = 5` 读点被**常量折叠**成裸常量 ⇒ match 的 tag 读解引用裸值（139） | T2 | `find_global_const_node` 对可选全局的折叠不合法（`T?` 值不是裸常量） |
| 2 | 全局**初值**走静态度量（`.quad` 裸值）⇒ 读侧按装箱解引用 | T2 | `global_needs_runtime_init` 对字面量返回 0 |
| 3 | ≥63 变体：覆盖位 `2^vi` 溢出 ⇒ 70 变体 **SIGFPE rc=136** / 判定失真 | T3 | 单 int 位图 ⇒ 换**无界位图**（`mc_*` + 栈纪律） |

### 5. 登记（12 件；逐条 = 归属 / 状态）
| # | 件 | 归属 | 状态 |
|---|---|---|---|
| 1 | **`c3` 指针写可选槽双路径分歧静默错值**（高优先；含 c1/c2 三子形态） | TODO **#56**（T5 前已立） | 已登记、本批不修、非本批引入 |
| 2 | 指针面五形态（局部 139 · 数组元素 139 · 全局分歧 · `&s.a` **写入丢失** · 对照正确） | T2 报告 §8-1/8-2 | 已登记（裁 (A) 不修） |
| 3 | **指针面根因链**（同站点双语义 + 绕过 `emit_rep_set` + 别名不可静态闭合） | T2 报告 §8-2 | 已登记 |
| 4 | **(D) 语言面拒绝 `*p = v`（pointee 为 `T?`）候选** | T2 报告 §8-4 → 已上报维护者 | 待裁（影响面已实读：拒绝的是**已错**形态） |
| 5 | **T4「移除门」候选**（E-4 缓存门；性能面） | TODO #2026-09-15-1 | 待裁（附补证清单） |
| 6 | 元组/数组字面量的**非 ident/非调用**元素（`elem_node_optional` 保守判 0） | T2 报告 §8-5 | 已登记 |
| 7 | `struct S[T] { a: T }` 实例化为 `T = int?` 的字段（字段类型节点是泛型形参 ident） | T2 报告 §8-5 | 已登记 |
| 8 | `&s.a` 写入丢失（`UOP_REF` 无字段分支 ⇒ 取临时量地址） | T2 报告 §8-1（预存缺陷） | 已登记 |
| 9 | `MAX_GENERICS = 4` **不同批**（写入侧有界 + parse 期硬报） | 裁-CAP-2 附问 | 维持（本批不动） |
| 10 | `sh_match_bit`/`sh_match_first_missing` **遗留**（仅 selftest ≤62 域） | T3 报告 §6-3 | 已登记（生产已换 `mc_*`） |
| 11 | 侧表内存**只增不减**（编译进程内高水位；与既有侧表同族） | T3 报告 §6-4 | 已登记不修 |
| 12 | `.cir` 命中路径按**绝对 var id** 恢复（依赖「同指纹 ⇒ 同生成序」不变量） | T4 报告 §2 关联事实 | 已登记（混态实证成立） |

### 6. 偏差（6 条）
| # | 现象 | 处置 / 出处 |
|---|---|---|
| 1 | **T2 零足迹事故**：首版实现未门控 ⇒ 非可选 `generics_test.cr` 的 `.ccr` **+168B（全在 TYPE 段）** | 根因 = 辅助函数无条件调 `res_type_node`（对数组/命名类型节点**每次 alloc 新类型行**）⇒ 12 处补 `g_optrep_on` 门后逐字节复原；**教训 = 「新增类型面查询前必须核是否 alloc」**（T2 报告 §3） |
| 2 | **T2 STR 推断被实测推翻**：原推断「新增 `Some` 串」不成立 | 实测新增的是 `box`/`match_res_rep` 两个 IR 变量名（`Some` 早已驻留）；m7 另 −`const`；非可选程序 STR 差集为空（T2 报告 §5） |
| 3 | **T3 MB 突变首版假阴性**：跨记录用例只调构造函数（走符号表、不经变体记录）⇒ 基址碰撞未暴露 | 补强为「大枚举缺臂须在声明小枚举之后仍报 TM03 + 正确反例名」后转红；**教训 = 「用例必须落在经记录的判定路径上」**（T3 报告 §7-1） |
| 4 | **T2/T3/T4 三次断电（API 402/畸形响应）** | 固盘纪律 = `t2-state.md`/`t3-state.md`/`t4-state.md`/`t5-state.md` 分段更新（本批新增纪律，已生效三次接力） |
| 5 | **T1 计划勘误**：「本树含 P6 记录之后的修复」推断作废 | 图证据 `jj diff --from 97f4394f --to 661ae569` = 仅 2 个计划 md 新增 ⇒ 起点树 = P6 终态树（T1 报告 §0） |
| 6 | **T2 数组字面量 wrapper 解引用**：首版对 wrapper 判定（非值节点）⇒ 一处漏装箱 | `ast_a(en)` 解引用后修复（T2 实施过程；T3 无同类） |

### 7. 基线换代与二进制代（逐代留痕）
| 代 | 源修订 | corec / corearch / corelsp | 自举链 |
|---|---|---|---|
| P6 终态 | `97f4394f` | `ae01de75…` / `228f82e9…` / `90eb19c6…`（= 现行白名单） | `0b2e06d0…`（P6 记录） |
| TC02 后 | `7f53305d` | `269d0a47…` / `228f82e9…` / `956cb82c…` | `c8c63985…` |
| T2 后 | `432c2e40` | `290639a9…` / `8f69d87d…` / `1c61a571…` | `819af35c…` |
| **T5 起点（T3/T4 后）** | `15357b04` | **`886a72bb…` / `55ea2a7c…` / `3889d9ed…`** | **`f69240ae…`** |

---

## 容量批终态陈述（一页；维护者先读）

**一、已实施面（本批交付，全部实测判据）**

| 面 | 终态 | 出处 |
|---|---|---|
| **E-2 表示面聚合** | 可选**聚合槽**（结构体字段 / 数组·切片元素 / 元组元素 / 枚举载荷 / 全局槽）恒持装箱值或 `None`；裸值在**写点**装箱（纯 IR，零布局变更）⇒ 原「响亮失败（139）」转为**正确值** | T2（`432c2e40`） |
| **E-3 记录布局迁侧表** | 字段/变体/载荷**无硬上限**（侧表；记录尺寸与 `OFF_*` 不变 ⇒ 110 访问器零改动）；覆盖位无界位图 ⇒ ≥63 变体判定正确；P022/P023 退役 | T3（`0b0d9aef`） |
| **E-4 缓存门** | **维持关缓存**（机制实证证伪 ⇒ 裁 (c)） | T4（`15357b04`） |
| **回归网** | 白名单**换代到 `97f4394f`**（配方重建 ×2 三 sha 逐条同）；腿①/探针/突变/三态四腿齐备 | T1（`4607624c`） |
| 判据终值 | canary `95084e7b…d475`(28822B) IDENTICAL · `.ccr` 四条 96015/96158/142793/142936（**冷+暖**，差恒 143B）· 72 档 34×0/38×1 **零差异（用换代后重建基线 × 当前源）** · 探针 29 档零差异 · 五 CI job rc=0 · 枚举 59/59 · `selftest-types` 415/415 · `check src/compiler` rc=0 · 链 `f69240ae…` + N06=0 + 冒烟 42 + `--help` rc=1 · `test_optional` 48/48 · `test_enum_limit` 18/18 | T5 |

**二、已裁定面**：裁-CAP-1 (a)（存储边界规范化）已兑现 · 裁-CAP-2 (a)（迁侧表；`MAX_GENERICS` 不同批 · `.ccr` 不 bump）已兑现 · 裁-CAP-3（先机制实证）已兑现且**证伪**⇒ 按 (c) 收口 · match 面裁决 (i) 已兑现 · #9 指针面裁 (A)（登记不修）已遵。

**三、剩余登记面**（12 件，逐条 = 台账 §5；其中影响下一批指向的四条）：
1. **(D) 语言面拒绝 `*p = v`（pointee `T?`）** —— 已上报维护者，**待裁**（影响面实读：拒绝的是**已错**形态）。
2. **T4「移除门」候选**（可选程序恢复 `.cir` 缓存）—— 待裁，附补证清单（自源冷/暖 · `test_optional` 同路径冷/暖）。
3. **`c3` 指针写双路径分歧静默错值**（TODO #2026-09-16-4）—— 高优先、预存、本批不修。
4. **能力面遗留**：元组/数组字面量非 ident 元素 · 泛型形参字段实例化为 `T?` · `&s.a` 写入丢失 · `sh_match_bit` 遗留 · 侧表只增不减 · 绝对 var id 恢复不变量。

---

## 自检记录（T5 补）

- **分诊诚实性**：本批三项（E-2/E-3/E-4）**全部落地或按裁定收口**；三项裁决门**逐条取裁并兑现**；**无一项被推荐案冒充裁定**。E-3 的四条前置**逐条在 T1 实核**（结论：全部未满足 ⇒ 本批逐条收口：① 读回闸 → T3 删除 ② 镜像 → T3 删除死镜像 ③ 覆盖位 → T3 无界位图 ④ 冷热/canary 判据 → T3+T5 重锁并实测「不 bump」）。
- **根因 vs 止血**：① T2 直达「表示律」（写点规范化单源，非逐点特判）；② T2 的 global 面挖到**两层预存根因**（常量折叠 + 静态度量初值）而非只补一层；③ T3 走**记录布局**（侧表）而非逐点护栏；④ T4 先**证伪**再收口（不臆造修复面）。
- **与既有裁决的一致性复核**：P4 裁决 5（解包侧判表示，局部面不变）· D20/D22/D23 · D28（两口径 + 冷/热）· TODO #2026-09-11-9（判据口径）· P6 全批收纳（`.ccr` v9 / loader 硬校验 / 回归网 / `MIN_CASES`）· P022/P023 退役属「上限解除」的**必然结果**（非回退）。
- **占位符扫描**：无 TBD；未实测项（自源冷/暖 · LSP 会话级）逐处标注。
- **本计划的自我限制**：本计划 T5 前**未跑构建/测试**（纯只读侦查）；三任务实施期的全部数字为实跑，出处逐条标注；**T5 的回归与判据为实跑**（`/tmp/capt5/**`）。
