# 全局行操作数 seam 收编（同族六点）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 消灭「**全局行**（IR var 索引 < 当前函数 `var_start`）的操作数地址未经全局分派就吃 `g2_slot`」这一同族静默缺陷：`g2_slot` 对全局行返回**帧外伪 rbp 偏移**（指向调用者帧的合法位移）⇒ 读侧静默错值、写侧静默丢写、个别形态响亮 SIGSEGV，而 `corec build` 恒 rc=0。本批 = **读侧 seam 收编**（`e2_load_var` 已存在的形态无关单入口 + 新增 dex 面两入口）+ 逐点迁移；写侧与相邻三项按裁决门定范围。

**边界纪律（本批）：**
- **零足迹优先**：新 seam 只允许改变**全局行**的发射字节；一切**非可选、非全局**形态（含局部行 / `E2_REG_SLOT_BASE` 寄存器槽 / 既有已修点）的发射字节必须**逐字节不变**（判据 = ELF canary + 发射面 diff，见 Global Constraints）。
- **不改语言语法**；不动 `.ccr` 格式（本批 = 后端发射面改动，`.ccr` 逐字节不变是构造性结论，但仍须实测四条锁定值）。
- **不改 checker / 类型引擎 / 前端**（唯一例外 = 裁-SEAM-3 若取「同批修转换环覆盖面」，届时显式列出并单独判据）。
- 不重开 E-2 写点收纳、不重开 (A) 批的取址面收纳。

**Architecture:** ① **T1 前置侦查**——冻结基线 + 起点判据 + 六点 RED 复现（含可达性实证：审计给的可达形**逐条编译实跑**）；② **T2 先裁落纸 + 完备性枚举**——全仓 `g2_slot(` / `e2_sd_*` / `e2_ld` / `e2_st` / `e2_load_var` 调用点**逐条分类**（零未分类），裁决门结果回填；③ **T3 实现**（新 seam 原语 + 调用点迁移，按裁）；④ **T4 探针 + 突变控制**；⑤ **T5 判据复验**；⑥ **T6 收官**（台账 + 文档 + 路径限定提交）。

**Tech Stack（本实例实读锚点；行号 = 工作副本 `084a0fa363fb`，逐条自读核对）：**
- **根因单点**：`g2_slot` = `src/arch/x86_64/instr.cr:55-72`；全局行落**第 71 行** `if v >= 0 { return -(v + 1 - g_current_func_var_start) * 8; }`（`v < var_start` ⇒ 括号内为负 ⇒ **返回正值 = 调用者帧内合法位移**）。
- **已收编的读侧先例**：`e2_load_var` = `instr.cr:427-444`（全局 → `lea r11,[rip+0]` + `g_x86_rip_patch_*` 登记 + `mov reg,[r11]`；局部 ⇒ `e2_ld(g2_slot(v))` 逐字节原样）；消费点 = `IR_STORE`(1045-1057 全局支)/`IR_LOAD_FIELD`(1068-1084)/`IR_STORE_FIELD`(1086-1094)/`IR_LOAD_ENUM_TAG`(1230-1244)/`IR_LOAD_INDEX`(1246-1261)/`IR_STORE_INDEX`(1263-...)/`IR_LOAD_INDEX_VAR`/`IR_STORE_INDEX_VAR`/`IR_DEREF`(1114-1130) 及 `cs_args_dispatch` 的 int 支（`callseq.cr:81`）。
- **dex/浮点面原语（本批 seam 的组成件，均**只吃偏移、无全局分派**）**：`e2_sd_load`(348) · `e2_sd_load1`(360) · `e2_sd_load_x`(372) · `e2_sd_cvt`(400) · `e2_sd_store`(413)。
- **写侧手写 rip 序三处（漂移源，现状正确）**：`IR_CONST` 全局支 493-513 · `IR_STORE` 全局支 1045-1057 · `IR_REF` 全局取址支 1096-1112（三处各自 `grow_rip_patch` + `e2_lrb`/`e2_lr` + `mov`，无共享原语）。
- **寄存器槽哨兵**：`E2_REG_SLOT_BASE = 1000000000`（`instr.cr:11`）；`e2_ld`/`e2_st`/`e2_li` 有该分支，`e2_sd_*` 五件**均无**（见 §未决项 U4）。
- **regalloc 类型门（dex 行不参与寄存器分配）**：`src/arch/x86_64/regalloc.cr:680-681`（`TI_DEX/TI_DEX_S/TI_DYN/TI_UNIT` → `continue`）+ 引用面 hazard 门 `:458-462`。
- **B6 调用点（OS 轴）**：`src/os/linux/callseq.cr:73`（寄存器参数 dex 支）· `:126`（栈参 dex 支）；int 支 `:81` 已走 `e2_load_var`。
- **B6 的 ir_gen 门**：dex 实参形式转换环 = `src/compiler/ir_gen.cr:1810`（`if func_ni >= 0 && ast_kind(func_node) == EXPR_IDENT`）；环内 extern `scaled→bits` `:1824-1829`、Core `bits→scaled` `:1831-1833`。
- **B7**：`callseq.cr:158-191`（`cs_ret_value`），次序问题 = dex 判定 `:160` 在全局判定 `:163` **之前**。
- **B8（HIT 表实例）**：`hit_ev_emit_one` = `instr.cr:1800-1895`；`g2_slot` 全点 = **1817 / 1852 / 1860 / 1880 / 1883 / 1891**（审计只给了 4 点，本计划**补全**，见 §审计坐标校正）；同函数内操作数装载 1843/1844 **已走 `e2_load_var`**。
- **相邻三项**：全局行 IR 型面 = `reg_one_global` `ir_gen.cr:3106-3156`（默认 `gtype : ., mut = TI_INT;` = **3116**；dex 判定 3119-3131；apx 位 = `ast_int_val(lnode) != 0` = **3125**；`apx` 标签入 EXPR_LET 的 iv 字段 = `parser.cr:819`/`:866`，消费 `ir_gen.cr:2370`/`:2390`）· 内容比较判定 `ir_gen.cr:1196` · `IR_LOAD_INDEX` 的 `TI_STR` 门 `instr.cr:1250`（字节支 1251-1254 / 8 字节支 1255-1258）· 死码 `sz_ofs`(447-450)/`sz_load_var`(452-456) · 死变量 `instr.cr:1231` `o1 := g2_slot(s1)`。

**前置文档**：TODO #8/#25 收口提交（全局 vs 局部槽位同族先例）· (A) 批 `docs/superpowers/plans/2026-09-16-opt-rep-follows-storage.md`（**裁决门 / Global Constraints / 探针设计表体例本计划直接沿用**）· `.superpowers/sdd/` 各批报告（判据口径）。

**前置状态（本计划 = 零构建 / 零测试 / 零源码改动；只读实读 + `jj` 读面）：**
- 工作树 = `/home/DslsDZC/core`，rev `084a0fa363fb`；**本计划落纸前无任何构建**（构建槽位由另一批占用）。
- **审计给的坐标逐条自读复核**：B1/B2/B4/B5/B6/B7 与死码/死变量坐标**全部命中**；B8 与相邻三项有校正/补全（见下）。

---

## 审计坐标校正（**以实读为准**）

| 项 | 审计坐标 | 实读坐标 | 差异 |
|---|---|---|---|
| `g2_slot` 全局回退 | `instr.cr:71` | `:55-72`（判定行 = **71**） | 命中 |
| B1 `IR_BINARY` TI_DEX s1/s2 | `instr.cr:520-521` | 520/521（分支 `:515` 起） | 命中 |
| B2 `IR_I2F` s1 | `instr.cr:468` | 468（`do2` 在 467；`IR_F2I` s1 在 476） | 命中 |
| B4 `IR_SLICE` s1/s2 | `instr.cr:1334-1335` | 1334（三槽）/1335（两条 `e2_ld`） | 命中 |
| B5 `IR_AWAIT` s1 | `instr.cr:1345` | 1345（读）/1346（写 d）/1347 | 命中 |
| B6(a)(b)(c) | `callseq.cr:73` · `:126` | 73 · 126 | 命中 |
| B7 | `callseq.cr:160-163` | 160-163（dex 判定 **160**、全局判定 **163**） | 命中 |
| B8 表路径 | `instr.cr:1817/1860/1880/1891` | **1817 · 1852 · 1860 · 1880 · 1883 · 1891**（**六点**） | **补全**：1852（ALU 结果回存 `e2_st(...,g2_slot(d))`）与 1883（MEM load 结果回存）同族漏列；1860/1880/1891 是 `hit_st_modrm_disp` 的 **disp 槽位参**（非 `e2_ld/e2_st` 的偏移参），修法不同（不能直接换成 `mov [r11]` 形） |
| 死码 | `instr.cr:447-457` | `sz_ofs` 447-450 · `sz_load_var` 452-456（全仓零调用者，实核 `grep` 仅定义处命中） | 命中 |
| 死变量 | `instr.cr:1231` | 1231（`o1` 在 `IR_LOAD_ENUM_TAG` 分支，已被 `e2_load_var` 取代） | 命中 |
| 全局行型面 | `ir_gen.cr:3116-3131` | `:3116` 默认型 + `:3119-3131` dex 判定（函数体 `:3106-3156`） | 命中（范围口径微调） |
| 内容比较 | `ir_gen.cr:1196` | 1196 | 命中 |
| `g[0]` 字节/8 字节分支 | `instr.cr:1250-1258` | 门 = **1250**；字节支 1251-1254、8 字节支 1255-1258 | 命中 |

---

## 现状根因（一句话 + file:line）

**`g2_slot`（`src/arch/x86_64/instr.cr:55-72`）对全局行返回帧外伪 rbp 偏移**（第 71 行；`v < g_current_func_var_start` ⇒ 偏移为正 ⇒ 落在**调用者帧**），而「全局 vs 局部」的分派被要求**在每个操作数消费点各自手写**（正确者走 `e2_load_var`；遗漏者直吃 `g2_slot`）⇒ 同族缺陷按 `IR_*` 分支**逐个漏**：已修 4 处（`IR_LOAD`/`IR_LOAD_FIELD`/`IR_REF` 取址/`IR_LOAD_ENUM_TAG` 读源）后，同族仍有 **6 条活点 + 1 条次序不变量缺口（B7）+ 1 条仅表实例可达（B8）**，全族后果统一为**读静默错值 / 写静默丢写 / 个别响亮 139**，且 `corec build` rc=0、解释器路径正确 ⇒ **双路径分歧**。

**结构判断（本批的立项理由）**：这不是「六个孤立的漏点」，而是「**判定散落**」——同一形态判定（`r64(g_x86_is_global, v*8) != 0`）在 int 面已收进 `e2_load_var` 单入口，dex 面（`e2_sd_*` 五件）与写侧（三处手写 rip 序）**从未收编**。只修点 = 治标（本仓已有「修 `IR_REF` 后仍漏 `IR_SLICE`/`IR_AWAIT`」的先例）。

---

## 活点清单（逐条 = 坐标 / 形态 / 可达性 / 预判现象 / 修复归属）

| # | 坐标（实读） | 形态 | 可达性（审计判定 + 本计划复核） | 预判现象 | 修复归属 |
|---|---|---|---|---|---|
| **B1** | `instr.cr:520-521` | `IR_BINARY` `ti==TI_DEX` 的 s1/s2 走 `e2_sd_load(g2_slot)` / `e2_sd_load1(g2_slot)` | 可达（**必需 apx 标注全局**：无注解全局行型 = `TI_DEX_S` ⇒ 走 int 精确路径，寻址正确） | 静默错值（读调用者帧当 binary64） | dex 读 seam |
| **B2** | `instr.cr:468` | `IR_I2F` s1 走 `e2_sd_cvt(g2_slot)` | 可达（`d : dex, apx = h`，h = 精确全局） | 静默错值 | dex 读 seam（**cvt 形**，非 movsd 形） |
| **B4** | `instr.cr:1334-1335` | `IR_SLICE` s1（数组指针）/s2（low） | 可达（全局数组 + 全局 low；两者**都**直吃 `g2_slot`） | 响亮 SIGSEGV 或静默错值（取决于伪偏移是否可读） | int 读 seam（`e2_load_var` 已有，**只差迁移**） |
| **B5** | `instr.cr:1345` | `IR_AWAIT` s1（`e2_ld`），d（1346，`e2_st`） | 可达（`x := await g`，g 全局 int；d 恒临时） | 静默错值 | int 读 seam（迁移）+ d 写侧清点 |
| **B6(a)** | `callseq.cr:73` | 寄存器参数 dex 支 `e2_sd_load_x(g2_slot(fa+ai))` | **已排除**（裸标识符调 Core 函数：`ir_gen.cr:1810` 转换环先做 `dex_bits_to_scaled` ⇒ 实参必为临时；`TI_DEX_S` 实参走 int 支 `:81` 已分派） | — | 不改（判据 = 回归钉） |
| **B6(b)** | 同上 | 同上（**extern** 调用点） | 可达（extern 环只转 `TI_DEX_S→bits`；**apx 全局**保持 `TI_DEX` 直达分派） | 静默错值；静态构建下**无 .so 不能真跑** ⇒ 判据降级到发射字节（裁-SEAM-2） | dex 读 seam |
| **B6(c)** | 同上 | 同上（**方法调用** `obj.m(x)`） | 可达（环门 = `ast_kind(func_node)==EXPR_IDENT`，方法调用 = `EXPR_FIELD` ⇒ **环整段跳过**） | **两级缺陷**：① 寻址（本批 seam 覆盖）② 形式转换缺失（bits 当 scaled 用；callee 形参型 `TI_DEX ⇒ TI_DEX_S`，`ir_gen.cr:2866`）⇒ **修①后仍静默错值**（裁-SEAM-3） | dex 读 seam + 第二根因单列/同批（取裁） |
| **B6(c-栈)** | `callseq.cr:126` | 栈参 dex 支 `e2_sd_load_x(g2_slot(fa+stack_ai),0)` | 可达（同 B6(b)/(c)，第 9 个 binary64 起落栈） | 同 B6(c) | dex 读 seam |
| **B7** | `callseq.cr:160-163` | `cs_ret_value`：`irv_type(s1)==TI_DEX` **先于**全局判定 | **未确证可达**（读码构不出：返回点 `ir_gen.cr:2495` 已把 apx 值转 scaled ⇒ s1 恒临时）；属**次序错的不变量缺口** | 若未来可达 ⇒ 全局 dex 返回值走错路径（见裁-SEAM-4 修法修正） | 加固（取裁） |
| **B8** | `instr.cr:1817·1852·1860·1880·1883·1891` | HIT 表事件发射 dest/addr 槽 | **仅表实例可达**（① 活动实例 `allow_table=1`，`corearch.cr:451/455`；② 指令落降低子集且 d/s1 为全局行；③ 未实证） | 写落伪偏移、读侧正确 ⇒ 静默错值 | 取裁（裁-SEAM-5）；若 seam 覆盖到该函数则自动消失（但 1860/1880/1891 是 **disp 参**，需单独形态） |

**已排除（审计依据 + 本计划采信，不重复侦查）**：B3 `IR_F2I` s1（唯一发射点恒内部临时）· `IR_YIELD`（`ir_gen.cr:2470-2476` 恒 `d=-1`，实读确认；且 `instr.cr:1418-1426` 有 `d>=0 && s1>=0` 门）· `IR_DYN_*`（全局行型恒不为 `TI_DYN`）· `IR_LAZY_THUNK`/`IR_LAZY_FORCE` s1（`instr.cr:1395-1406` 已走 `e2_load_var`；d 走 `g2_slot` — 见 U3 清点）· `IR_CONST` TI_STR + d=全局 · `IR_LOAD_ENUM_TAG` dest · `IR_LOAD` dest。

---

## 设计候选（**三条**；逐条给 改动面 / 风险 / 可证伪判据 / 与 canary 零足迹的关系）

### (i) 纯 seam 收编（**推荐主体**）
**法则**：全局/局部判定**收进单入口**，调用点只表达「取哪个操作数」，不再表达「怎么寻址」。
- **新增三个入口**（放 `instr.cr`，紧邻 `e2_load_var`；架构轴，OS 轴 `callseq.cr` 消费）：
  1. `e2_lea_glob(buf, pos, var_idx) -> int`——`lea r11,[rip+0]`（`e2_lrb`）+ `g_x86_rip_patch_*` 登记（**与 `e2_load_var` 内联段同规约：`pos+3` = disp32 位置**）；只在 `r64(g_x86_is_global, v*8) != 0` 时调用。
  2. `e2_sd_ld_var(buf, pos, xmmn, v) -> int`——全局：`e2_lea_glob` + `movsd xmm{n}, [r11]`（`F2 0F 10 /n`，ModRM `mod=00 reg=n%8 rm=3`）；局部：**现 `e2_sd_load`/`e2_sd_load1`/`e2_sd_load_x(g2_slot(v))` 逐字节原样**（三形按 `xmmn` 分派，字节 = 现状）。
  3. `e2_sd_cvt_var(buf, pos, v) -> int`——全局：`e2_lea_glob` + `cvtsi2sd xmm0,[r11]`（`F2 48 0F 2A 03`）；局部：现 `e2_sd_cvt(g2_slot(v))` 原样。
  4. `e2_sd_st_var(buf, pos, v) -> int`——全局：`e2_lea_glob` + `movsd [r11],xmm0`（`F2 0F 11 03`）；局部：现 `e2_sd_store(g2_slot(v))` 原样。
- **调用点迁移**：B1（520/521）· B2（468）· B5（1345，**改走 `e2_load_var`**）· B4（1334/1335，**改走 `e2_load_var`**）· B6（`callseq.cr:73`/`:126` → `e2_sd_ld_var`）· B7（按裁-SEAM-4）· B8（按裁-SEAM-5）。
- **scratch 寄存器安全性论证（必须写进实现注释 + 判据）**：全局形用 **r11**（与 `e2_load_var` 同寄存器）——每次 `lea` 后**立即**消费进 xmm/寄存器，**不跨指令存活**；B1 双全局操作数 = 两次「lea r11 + movsd xmm{n}」序列，xmm0/xmm1 各自独立 ⇒ **无需第二 scratch**；`e2_push_xmm0`（`instr.cr:393-398`）在 dex 栈参序列中在装载之后 ⇒ 无 r11 冲突。
- **改动面**：`instr.cr`（+4 助手、~8 调用点）、`callseq.cr`（2 调用点 + 取裁的 B7）。**零前端、零 `.ccr`、零解释器**。
- **风险**：① 助手内分支写错 ⇒ 非全局行字节漂移（**判据 = canary + 发射面 diff 直接拦截**）；② r11 存活期假设错 ⇒ 需在 T4 用「双全局 B1 探针 + 局部对照」实测。
- **可证伪判据**：① 六点探针**双路径同值**（见 §探针设计表）；② canary IDENTICAL；③ `.ccr` 四值不变；④ 全量判据集（Global Constraints）。
- **与 canary 零足迹的关系**：canary 档 `tests/suite/ptr_arith.cr` **实读无全局、无 dex**（`cat` 全文 15 行；无 `g_` 行、无 `dex`）⇒ 新全局分支**构造性不可达**，非全局分支 = 逐字节复制现状 ⇒ **预期 IDENTICAL**（仍须实测，不得只做构造性论证）。

### (ii) seam 收编 + 完备性清单/护栏（**推荐 = (i) + 本项**）
在 (i) 之上加**两层防线**：
- **完备性清单（T2 产出，入计划/报告）**：全仓枚举 ① `g2_slot(` 调用点（实读 ~50 处）② 一切直吃偏移的 `e2_ld`/`e2_st`/`e2_li`/`e2_lb`/`e2_lr`/`e2_sd_*` 调用点 ③ `r64(g_x86_is_global, ...)` 现判定点；逐条分类 = **局部专用 / 已分派 / 全局必须分派（本批迁移）/ 待实证**，**零未分类**为完成判据（照 E-2 写点审计法）。
- **突变控制**：人为把 seam 的全局分支改坏（如把 `lea r11,[rip+0]` 的登记去掉）⇒ 六点探针必红；人为把**非全局分支**改坏 ⇒ canary 必红。两向都做才证明「判据网真的覆盖本批」。
- **改动面**：+ 审计工时（零源码）；若取「护栏」则 + 一个 debug 门（**建议只做静态清单，不做运行期护栏**——运行期断言本身就在发射路径上，零足迹证明成本高于收益）。
- **可证伪判据**：清单零未分类 + 突变双向红。

### (iii) 逐点最小修（不建 seam；照 `IR_REF`/`IR_LOAD_ENUM_TAG` 先例逐点手写全局分支）
- **改动面**：每点独立手写「全局 → lea + 补丁 + 形态专属 mov」；B1 两操作数、B2 cvt 形、B4/B5 int 形、B6 两处、B8 六处 ⇒ **7-10 段近似重复代码**。
- **风险**：**漂移源原样保留**——下一个 `IR_*` 分支仍会漏；写侧三处手写 rip 序继续与读侧分家。
- **可证伪判据**：同 (i) ①②③，但**无「漏点不再生」的机械判据**（这是它被否的决定性理由）。
- **与 canary 零足迹的关系**：同为构造性不可达 + 逐字节复制 ⇒ 预期 IDENTICAL。

### 推荐 = **(i) + (ii) 的清单层**，理由
① 根因是**判定散落**（一句话根因已示），只修点是治标——本仓已有「`IR_REF` 修完仍漏 `IR_SLICE`/`IR_AWAIT`」的实证；② dex 面五件原语**从未收编**，收编后 B1/B2/B6/B7 **共用同一入口**（形态无关），B4/B5 只是「改用已有的 `e2_load_var`」——净新增代码 ~3 个小函数；③ 零足迹可**逐字节**证明（非全局分支 = 复制现状），比逐点手写更可证（每点手写 = 每点一次漂移机会）；④ (ii) 的清单层把「还漏没漏」从人眼变成机械枚举，**成本零源码**。**(iii) 保留为退路**：若 T2 枚举发现 seam 化会触及「形态专属字节」（如 B8 的 disp 参形态）而无法保持非全局行逐字节，则对**那些点**退回 (iii)。

---

## 裁决门（**开工前置**；逐条 = 问句 / 影响项 / 推荐 / 未取裁时的行为）

| # | 问句（逐条照抄给维护者） | 影响项 | 本计划推荐 | 未取裁时 |
|---|---|---|---|---|
| **裁-SEAM-1** | **写侧是否一并收编**？现状：`IR_CONST`(493-513)/`IR_STORE`(1045-1057)/`IR_REF`(1096-1112) **三处各自手写 rip 序**（现状**正确**，纯漂移源），另有**一批写侧 d 域未经「d 可否为全局行」清点**（`IR_REF` dst · `IR_ALLOC_STRUCT` · `IR_FNADDR` · `IR_MAKE_ENUM` · `e2_store_ret`(385-391，dex 支 387/int 支 389) · `IR_SPAWN`(872-920) · `IR_HOTPATCH_ROUTE` · `IR_LOAD_ENUM_TAG` dst〔代码注释自认「全局目的行未接——当前零发射点」〕· `IR_LAZY_*` dst · `IR_DYN_PACK` dst 等，逐条见 T2）。三选：**(a)** 本批只做「读侧 + dex 写侧新点」，写侧手写序**不收编**、d 域**只清点登记**；**(b)** 顺带把三处手写序合并进 `e2_lea_glob`（**零行为收益的纯重构**，字节须逐字节同）；**(c)** 连 d 域可达者一并修。 | 改动面 + 字节风险 | **(a)**：零足迹优先——三处现状正确，重构只有漂移收益、却有字节风险；d 域清点（T2）**必须做**（回答「还有没有活点」），修则单列 | 取 (a)：不收编、只清点登记 |
| **裁-SEAM-2** | **B6(b) extern 是否本批修**？extern 在**静态构建下无 .so 无法真跑** ⇒ 判据只能到**发射字节/反汇编**级（正片段 `4c 8d 1d` + `f2 0f 10` ModRM `rm=3`；负片段 = 该实参由 rbp 相对 `movsd` 承载）。 | 判据强度（无运行期证据） | **修**（同一 seam，改动 = 1 调用点），判据降级并**显式标注「发射字节级，无运行期腿」** | 修 + 降级判据（不修则登记为活点） |
| **裁-SEAM-3** | **B6(c) 方法调用的「第二根因」是否同批修**？寻址修好后，**转换环覆盖面缺口**仍在：`ir_gen.cr:1810` 以 `EXPR_IDENT` 为门 ⇒ 方法调用的 dex 实参**不做 bits↔scaled 形式转换**，而 callee 形参按 `TI_DEX_S`（`ir_gen.cr:2866`）解释 ⇒ **apx 全局经方法调用仍静默错值**（把 bits 当 scaled）。三选：**(a)** 同批修环门（放开门禁到「可解析到 Core 函数的方法调用」；须先在 T1 实证 `func_ni` 与方法形参链在该点可用）；**(b)** 本批只修寻址，第二根因**单列条目 + 探针钉住实际值**（禁止算「已修」）；**(c)** 语言面/诊断面限制。 | 本批范围 + 一个前端改动（**破「零前端」边界纪律**） | **(b)**：本批聚焦寻址面（一句话根因）；第二根因是**另一个根因**（形式转换覆盖 ≠ 寻址分派），须独立探针与独立判据。**但探针必须双钉**：修后「不得 139/垃圾」+ 值是否正确的现状记录入条目 | (b)：修寻址 + 单列 + 钉值 |
| **裁-SEAM-4** | **B7 是否顺手加固？修法需修正审计建议**——审计建议「全局判定前移一行（零字节代价）」；**实读修正**：前移后全局 dex 返回值会落 `:163` 支走 `rax`（`mov rax,[r10]`），而 dex 返回约定在 **xmm0** ⇒ **只前移是错的**。正确修法 = **全局支自身按型分派**（`is_global && TI_DEX → e2_sd_ld_var`；`is_global && 非 dex → rax 路径`；非全局按现状）。两种取法：**(a)** 按修正修法加固（当前不可达 ⇒ 零字节变化，纯不变量修复）；**(b)** 维持现状 + 登记（不可达 ⇒ 不修也不漏）。 | 次序不变量 | **(a)**：修法已修正、零字节代价、把「未确证可达」的缺口钉死；判据 = canary IDENTICAL + 非 dex 全局返回/局部 dex 返回**非回归**探针 | (b)：登记，B7 移入 §未决项 |
| **裁-SEAM-5** | **B8（HIT 表实例）是否本批**？仅 `corearch --table`（`allow_table=1`，`corearch.cr:451/455`）可达，**默认 x86 管线不可达**，CI 五 job 不含表实例 ⇒ 判据只能到「代码路径已分派」级。且实读发现 **三种形态**：1817/1852/1883 = `e2_st(...,g2_slot(d))`（可直接换 `e2_st`+分派）；1860/1880/1891 = `hit_st_modrm_disp` 的 **disp 槽位参**（需「先 lea r11 + ModRM `rm=11` 基址形」或改为「装载到临时槽」——**非零设计**）。 | 表实例面（非 CI 面） | **(a)** 只迁移「可直接换」的 1817/1852/1883（经 `e2_lea_glob` + 形态专属 mov 构成），**disp 参三处（1860/1880/1891）单列登记**（属表管线设计面，须表实例判据才可证）；不加新 CI 腿 | 登记（B8 全条移入 §未决项 U2） |
| **裁-SEAM-6** | **相邻三项**（① 全局行 IR 型面不完整：`reg_one_global` 只给 `TI_INT/TI_DEX/TI_DEX_S` ⇒ 全局 `string` 行 = `TI_INT` ⇒ `g[0]` 走 8 字节元素支（`instr.cr:1250-1258`）、`g == "s"` 不做内容比较（`ir_gen.cr:1196`）· ② 死码 `sz_ofs`/`sz_load_var`（447-456，零调用者实核）· ③ 死变量 `instr.cr:1231`）：并入还是单列？ | 本批范围 | **① 单列**（语义面缺陷、涉及 IR 型面/两路径语义/checker，探针面独立，须独立立项）；**②③ 本批顺带删**（零发射面、零字节、`grep` 零命中可证） | ① 单列登记；②③ 不删（保持登记） |
| **裁-SEAM-7** | **探针入仓形态**：新 `tests/selfhost/test_global_seams.py`（照 `test_params_limit.py` 形态：临时源 + `build` + 跑产物 + 双路径）+ `src/ci/run.sh` 的 `selfhost-tests` 挂钩；是否同时入 `tests/suite/` 一档 runtime 语料（走 `run_suite` 的 build+run 腿）？ | 回归网覆盖面 | **两者都做**：py 套件（逐点 RED/正例/边界/局部对照）+ 一档 `tests/suite/global_seam_test.cr`（`run_suite` 每档独立 build+run ⇒ 提供**非探针语料的常规腿**）。注意：新增 suite 档会进入 `run_suite` 全档循环 ⇒ 若该档跑红会拖挂 suite job，故 suite 档只放**已绿**形态 | 只入 py 套件 + run.sh 挂钩 |

**未取裁时一律取「推荐」列**（并在 T6 报告中标「按推荐默认执行、待维护者追认」）；**未取裁且推荐 = 单列/登记**的项，本批**不得顺手修**。

---

## 任务总表（T1..T6；状态「待实施」，收官时回填）

| # | 任务 | 源码改动 | 关键判据 |
|---|---|---|---|
| T1 | 前置侦查 + 冻结基线 + 六点 RED 复现（**零源码改动**） | 无 | 每点 ELF 现象实测 + interp 对照 + 局部对照；基线 sha |
| T2 | 先裁落纸 + **完备性枚举**（零未分类） | 无 | 枚举表零未分类 + 裁决门回填 |
| T3 | 实现（seam 原语 + 调用点迁移，按裁） | `instr.cr` / `callseq.cr` | 非全局行逐字节不变 + 六点转正 |
| T4 | 探针 + 突变控制 | `tests/selfhost/test_global_seams.py`（+ `run.sh` 挂钩 + 取裁的 suite 档） | 六点双路径同值 + 突变双向红 |
| T5 | 判据复验（全套 + 自举链） | 无 | Global Constraints 全绿 |
| T6 | 收官（台账 + 文档 + 路径限定提交） | 计划/报告（+ 取裁的 TODO 登记） | `jj status` 干净 + 报告归档 |

---

## Task 1：前置侦查 + 冻结基线 + 六点 RED（**源码零改动**）

**目标**：把「六点可达 + 现象」从审计断言变成**本计划自己的实测证据**，并冻结可比基线。

**步骤**
1. 冻结基线：`python3 build_selfhost_native.py`（`nice -n 19`，一次一构建，前 `ps` 实查）；记录 `corec`/`corearch` sha + `git`/`jj` rev；建 `/tmp/seam1/base/`。
2. 逐点写最小探针源（§探针设计表源码列），**每点两份**：全局版 + 局部对照版；每份跑 ① `./build/corec run '<src>'`（解释器）② `./build/corec build <f> -o <out> --static` + `<out>; echo $?`。记录 6×2×2 = 24 组 `rc + stdout`。
3. 对**预判为静默错值**的点，额外取「错误值本身」（而非只看 rc），证明「读的是调用者帧」——可用「同一函数里先放一个同槽位局部量」的加扰探针（局部量 = 伪偏移所指位置时，输出随局部量变化 ⇒ 址错实证）。
4. 对 B6(b) extern：构建产物做**字节判据**（§探针设计表；静态构建无 .so ⇒ 不跑）。
5. 对 B7：构造性检查（返回点转换环 `ir_gen.cr:2495` + 2493 `g_cur_ret_ti` 门）⇒ 产出「可达性 = 未确证」的**实读结论**（不得写成「不可达」）。

**RED 判据**：六点中**至少 B1/B2/B4/B5** 在 ELF 路径上出现「解释器正确 vs 产物错值/139」的分歧（或 B4 显著 139）；B6(b) 至少字节判据成立；B6(c) 至少寻址面成立（第二根因现象一并记录）；B7 若确认不可达 ⇒ 记「不变量缺口」。
**完成判据**：`/tmp/seam1/task1-report.md`（逐点 = 源 / 两路径 rc / 现象 / 局部对照 / 截图级证据）+ 基线 sha 入账。

## Task 2：先裁落纸 + 完备性枚举（**未取裁前不得落码**）

**目标**：把 §裁决门 七条**逐条回填取裁结果**（维护者答复或「按推荐默认」），并把「还有没有活点」变成机械清单。

**步骤**
1. 枚举 `g2_slot(` 全部调用点（`ugrep -n` 全仓 `src/`），逐条填表：**坐标 / 所在 `IR_*` 分支 / 直接消费者（`e2_ld`/`e2_st`/`e2_li`/`hit_st_modrm_disp`/…）/ 分类**（局部专用（附依据：ir_gen 发射点恒临时）/ 已分派 / 本批迁移 / 取裁后处理 / 待实证）。
2. 枚举「直吃偏移」原语调用点：`e2_ld(` / `e2_st(` / `e2_li(` / `e2_lb(` / `e2_lr(` / `e2_lrb(` / `e2_sd_load(` / `e2_sd_load1(` / `e2_sd_load_x(` / `e2_sd_cvt(` / `e2_sd_store(` ——**逐条**判「第一参能否是全局行索引」。
3. 写侧 d 域清点（裁-SEAM-1(a) 要求）：对每个「写 d」分支（`e2_st(...,g2_slot(d))` / `e2_sd_store(g2_slot(d))` / `e2_store_ret`），回到 ir_gen 找 d 的**发射点**，给出「d 可否为全局行」的判定 + 依据行号。
4. **零未分类**校验：任何一条无法分类 ⇒ 记 §未决项，不得默认「安全」。

**RED 判据**：清单初版必有「待实证」条目（否则说明分类过粗）。
**完成判据**：清单零未分类（或未分类项全部逐条登记 + 停条件触发）+ 裁决门七条全部有结论 + 计划落纸更新。

## Task 3：实现（按取裁）

**目标**：落 seam 原语 + 迁移调用点，**非全局行零字节**。

**步骤**
1. `instr.cr` 增 `e2_lea_glob` / `e2_sd_ld_var` / `e2_sd_cvt_var` / `e2_sd_st_var`（放在 `e2_load_var` 邻近；头注写「全局 vs 局部判定单入口」+ scratch r11 存活期论证）。
2. 迁移：B1（520/521）· B2（468）· B4（1334/1335 → `e2_load_var`）· B5（1345 → `e2_load_var`）· B6（`callseq.cr:73`/`:126`）· B7（按裁-SEAM-4 修正修法）· B8（按裁-SEAM-5 的 (a) 范围）。
3. **迁移纪律**：每次只改一个点 + 立刻跑该点探针（T4 先落红例）；**不改** `e2_sd_*` 五件原语的既有字节（新增入口只是组合）。
4. 若裁-SEAM-6 取「顺带删」⇒ 删 `sz_ofs`/`sz_load_var`（447-456）与 `instr.cr:1231` 的 `o1`（附 `grep` 零命中判据）。

**RED 判据**：迁移前六点探针红（T1 证据）；迁移后逐点转绿；中间态任何一步**不得**引入新红档。
**完成判据**：六点（按裁范围）双路径同值；非全局行发射面**逐字节 diff 为空**（用 T1 基线产物对拍，选一个**含 dex 局部/局部数组切片/await 局部**的语料，证明非全局分支字节未动）。

## Task 4：探针 + 突变控制

**目标**：把六点钉进回归网，并证明判据网**真的覆盖**本批改动。

**步骤**
1. 新 `tests/selfhost/test_global_seams.py`：逐点 = 全局版（正例，期望值）+ 局部对照版（同值对拍）+ 边界（如 B4 的 `lo` 全局/字面量两形、B5 的 `await` 局部对照）；每例断言 `corec run` rc/stdout 与 `build --static` 产物 rc/stdout **同值**。
2. `src/ci/run.sh` 的 `selfhost-tests` 加挂钩（照 `test_params_limit.py` 行注体例）。
3. 取裁的 `tests/suite/global_seam_test.cr`（只放已绿形态；预期 rc=0）。
4. **突变控制（双向）**：① 摘掉 seam 的 rip 补丁登记 ⇒ 六点探针必红；② 改坏非全局分支（如把 `e2_sd_load` 的 ModRM 改一格）⇒ canary 必红。两向证据入报告。

**RED 判据**：新套件在**未修**的基线二进制上必红（至少 B1/B2/B4/B5 例）。
**完成判据**：套件全绿 + 突变双向红 + `run.sh` 挂钩生效（`CI_JOB_NAME=selfhost-tests bash src/ci/run.sh` 本地可复跑）。

## Task 5：判据复验（全套）

**目标**：Global Constraints 逐条实测，**不得只做构造性论证**。

**步骤**：按 §Global Constraints 的判据清单逐条跑（canary / `.ccr` 四值 / 腿① 72 档 / 探针 29 / 枚举 61/61 / selftest-types / 五 CI job / 自举链）。

**RED 判据**：任一条不达 ⇒ 停（§停条件）。
**完成判据**：全绿 + 逐条记录命令与输出入报告。

## Task 6：收官

**步骤**：① 统一台账（本计划回填 T1-T5 实施记录 + 取裁结果）；② 报告 `.superpowers/sdd/`（或本批约定目录）；③ **路径限定** `jj commit -m '…' <本计划路径> [取裁涉及的源码/测试路径]`；④ 若取裁含「单列」，在报告里给出可直接转录的条目文本（TODO.md 由收官批/维护者统一落，避免多 agent 同时改同一文件冲突）。

**完成判据**：`jj status` 干净（或仅余他人文件）+ 报告含六点前后对照表。

---

## Global Constraints（照本仓体例）

- **jj only（禁 git）**；提交**路径限定**；所有长编译 `nice -n 19`（≤10% CPU）；一构建一编译**串行**（每段前 `ps` 实查）；cwd = 仓库根；比较前 `clean-cache`（相对 cwd）。
- **ELF canary 硬闸（本批无豁免面）**：`95084e7bc68d6550d21d3d96fa3afd89c67a5d89edce5656a3d2e74fc923d475`（28822B）必须 **IDENTICAL**。分析：canary 档 `tests/suite/ptr_arith.cr` 实读**无全局、无 dex**（15 行全文）⇒ 新全局分支不可达；非全局分支 = 逐字节复制现状 ⇒ **预期 IDENTICAL**，但**必须实测**。**若变即停**（变即说明非全局分支被改动 ⇒ 违反本批边界纪律）。
- **`.ccr` 两口径四条锁定值不变**（格式面不动；冷 + 暖）：96015 `680a6f98…` / 96158 `76f36e6a…` / 142793 `41e9d845…` / 142936 `704316c8…`，差恒 143B。
- **判据全套**：腿① **72 档**冻结基线同源对拍（`check` rc + 日志逐档 diff）· 行为探针 **29 档**（`tests/probes/`，`tools/baseline/probes_run.sh`，逐档 clean-cache）· **枚举 61/61** · **selftest-types 415/415** · **五 CI job**（`check`/`bootstrap-tests`/`selfhost-tests`/`suite`/`full-bootstrap`）· **自举链** `corec2 == corec3` 逐字节 + **N06=0** + **冒烟 42**。
- **三态纪律**：不得把「未判」当 0/1；任何「不可静态判定」形态必须**响亮或登记**，**不得静默**（本批核心判据 = 「全局行操作数静默错值」清零）。
- **先裁落纸再改码**：T2 未完成（裁决门未回填）不得进 T3。
- **单列不顺手**：裁决门判「单列/登记」的项，本批**不得**顺手修（避免范围蔓延与判据缺腿）。
- **测量口径**：所有「同值」断言必须**双路径**（`corec run` + `build --static` 产物）；所有「字节不变」断言必须**逐字节**（sha/diff），不得用「语义等价」代替。

---

## 探针设计表

> 通用约定：`run` 腿 = `./build/corec run '<src>'`；ELF 腿 = `./build/corec build <f> -o /tmp/p --static && /tmp/p; echo $?`；退出码 = `main` 返回值低 8 位。每例**必配局部对照**（同形局部版），判据 = **两版同值**（不一致本身就是红）。

| # | 源码（全局版） | 期望 | 局部对照（源码差异） | 预判现象（修复前） | 双路径判据 |
|---|---|---|---|---|---|
| **B1** | `g : dex, apx = 1.5;`<br>`fn main() -> int { if g + 1.0 != 2.5 { return 1; } return 7; }` | rc=7 | `fn main` 内 `g : dex, apx = 1.5;`（同语句下沉） | **静默错值**（rc=1 或其它，无 trap）；`run` 恒 7 | 两路径同 rc=7 + 两版同值；**加扰版**：`fn main` 先放 `pad : int = 12345;` 观察是否影响结果（址错实证） |
| **B2** | `h : dex = 1.5;`<br>`fn main() -> int { d : dex, apx = h; if d != 1.5 { return 1; } return 7; }` | rc=7 | `h` 下沉为局部（`h : dex = 1.5;` 在 main 内） | **静默错值**（cvt 读帧外） | 两路径同 rc=7 + 两版同值 |
| **B4** | `g : [int;4] = [10,20,30,40]; lo : int = 1;`<br>`fn main() -> int { s := g[lo..3]; return s[0]; }` | rc=20 | `g`/`lo` 下沉为局部 | **响亮 SIGSEGV（139）或静默错值**（取决于伪偏移可读性） | 两路径同 rc=20；若全局版 139 ⇒ RED 成立；**附加子例**：`lo` 用字面量（隔离 s1 与 s2 两个 seam） |
| **B5** | `g : int = 5;`<br>`fn main() -> int { x := await g; return x; }` | rc=5 | `g` 下沉为局部 | **静默错值** | 两路径同 rc=5 |
| **B6(b)** | `g : dex, apx = 1.5;`<br>`extern fn seam_ext(x: dex) -> dex;`<br>`fn main() -> int { y := seam_ext(g); if y != 3.0 { return 1; } return 7; }` | **不可真跑**（静态无 .so） | — | 发射字节含 **rbp 相对 `movsd`** 承载 apx 全局实参 | **字节判据**：产物中 extern 调用点相邻窗口须出现 `4c 8d 1d`（`lea r11,[rip+disp32]`）后接 `f2 0f 10` ModRM `mod=00 rm=3`（`0x03 | n<<3`）；**负片段** = 该实参由 ModRM `01/10 000 101`（`0x45/0x85`）承载。判据级 = **发射字节**（报告中显式标注无运行期腿） |
| **B6(c)** | `struct S {}`<br>`impl S { fn m(self, x: dex) -> dex { return x * 2.0; } }`<br>`g : dex, apx = 1.5;`<br>`fn main() -> int { s := S{}; y := s.m(g); if y != 3.0 { return 1; } return 7; }` | 修①后**仍可能非 7**（第二根因） | 实参改局部 apx 量 | 修复前：寻址错 ⇒ 垃圾/139；修①后：②若未修 ⇒ **静默错值**（bits 当 scaled） | **双钉**：① 「不得 139、不得垃圾」（寻址面判据）② 值是否 = 7 **按裁-SEAM-3 记录现实值并钉住**（若取 (b) 单列 ⇒ 探针**允许**记录 ≠7 但必须**显式标注为已知第二根因**，禁止把 ≠7 当通过） |
| **B6(c-栈)** | 同上但方法加 dex 形参使实参 ≥9 个 binary64/或直接测 `cs_stack_args` 形的 extern | 同 B6(b)/(c) | — | 同上 + `e2_push_xmm0` 序列 | 同 B6(b) 字节判据（栈参装载窗口） |
| **B7** | `g : int = 5;`<br>`fn ret_g() -> int { return g; }`<br>`fn main() -> int { return ret_g(); }` | rc=5（**非回归**） | `f() -> dex` 局部 apx 返回对照（`v : dex, apx = 3.0; return v;`，期望 = 3.0 语义） | 现状**正确**（全局支在 :163 已处理非 dex 全局） | 两路径同值；若取裁 (a) ⇒ 再加「次序不变量」钉（代码次序 + 三种 s1（int 全局/dex 局部/int 局部）全绿） |
| **B8** | `corearch --table` 实例（**非默认管线**） | 取裁-SEAM-5 | — | 写落伪偏移 | 取 (a) 时判据 = 「该函数 g2_slot 调用点已分类零未分类」；无运行期腿（表实例不在 CI） |

**突变控制（T4）**：① seam 全局分支改坏 ⇒ B1/B2/B4/B5（+B6 按裁）探针必红；② 非全局分支改坏 ⇒ canary/局部对照必红。缺任一向 ⇒ 判据网未覆盖本批，T4 不算完成。

---

## 未决项 · 停条件（**不猜**）

**未决项（须实施时实证，不得默认）**
- **U1** B8 可达性（三条判据：活动实例 `allow_table=1` / 指令落降低子集且 d 或 s1 为全局行 / 静默错值）**未实证**；且 1860/1880/1891 是 **disp 参形态**（非 `e2_ld/e2_st` 偏移参）⇒ 修法须单独设计（seam 覆盖不到）。
- **U2** B7 可达性**未确证**（读码构不出；返回点 `ir_gen.cr:2493-2495` 已把 apx 值转 scaled）⇒ 取裁 (a) 的收益 = 不变量，而非实测修复。
- **U3** **写侧 d 域**（`IR_REF` dst · `IR_ALLOC_STRUCT` · `IR_FNADDR` · `IR_MAKE_ENUM` · `e2_store_ret` 385-391 · `IR_SPAWN` · `IR_HOTPATCH_ROUTE` · `IR_LOAD_ENUM_TAG` dst〔代码注释自认未接〕· `IR_LAZY_THUNK/FORCE` dst · `IR_DYN_PACK` dst · `IR_FNADDR` 等）逐条「d 可否为全局行」**未清点** —— T2 产出；清点前**不得**宣称「写侧无活点」。
- **U4** `e2_sd_load`/`load1`/`load_x`/`cvt`/`store` 五件**均无 `E2_REG_SLOT_BASE` 分支**（`e2_ld/e2_st/e2_li` 有）：本批实读 regalloc 类型门（`regalloc.cr:680-681` 排除 `TI_DEX/TI_DEX_S/TI_DYN/TI_UNIT` + `:458-462` hazard 门）⇒ **dex 行不参与寄存器分配 ⇒ 该缺口现不可达**；但**若未来 regalloc 放开 dex**，五件会把 `E2_REG_SLOT_BASE+rn`（≥1e9）当 disp32 写 ⇒ 静默错值。本批 = **登记**（不改，避免混淆 seam 边界）。
- **U5** 全局 `string` 型面（`reg_one_global:3116` 默认 `TI_INT`）的确切现象（`g[0]` 走 8 字节元素支、`g == "s"` 不做内容比较）**未实测**（现有语料用 `str_len(g)` 以外无覆盖）⇒ 独立条目 + T1 顺带取证据。
- **U6** B6(b) extern 在静态构建下的**确切链接行为**（未定义符号 ⇒ 链接期失败 or 留 undefined 由装载器解析）**未实测** ⇒ T1 实核；若链接失败则字节判据改为「用 `--elf` 单步产物（corearch 直出 ELF，不经链接器）」。

**停条件（触发即停，不自行放宽）**
1. canary 变化（`95084e7b…d475` ≠）⇒ 停（非全局行字节被改）。
2. `.ccr` 四条锁定值任一变化 ⇒ 停。
3. 任一非全局行发射字节变化（含局部对照档 diff）⇒ 停。
4. 腿① 72 档任一档 `check` rc 或日志出现差异且非本批登记面 ⇒ 停。
5. 探针出现「新 139 或新静默错值」且无对应条目 ⇒ 停。
6. 出现「必须改前端才能修寻址面」的情形 ⇒ 停（越界，须另裁）。
7. 裁决门任一条被维护者明确否 ⇒ 停该项，其余按取裁继续。
