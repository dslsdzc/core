# 状态版本族（S-B5）：check/use 版本分离 + handle 身份绑定

日期：2026-09-11
状态：**设计起草**（待用户/评审裁决；族内实施计划后立）
性质：语义安全线 S-B×5 之一（路线图 §2 S-B5 行）——`state-version` 属性轴的 TOCTOU 判据 + handle 身份绑定设计。不含代码、不含字节格式、**不新增载体机制**。

关联：
- `docs/superpowers/specs/2026-09-11-semantic-safety-roadmap.md`（**共享契约**——术语 §1.1 / 原则 §1.2 / 不承诺 §1.3 逐字遵守，本文不重定义）
- `docs/superpowers/specs/2026-09-11-semantic-safety-obligations-design.md`（S-A：属性轴 / 四档义务 / policy 接口）——**本族的依赖项**；起草时该文件不存在，本文按路线图 §1.1 术语引用，衔接点挂 §7 开放问题 6
- `docs/superpowers/specs/2026-09-11-policy-resource-confinement-design.md`（S-B4：`Path<Root>` 围栏）——**衔接面 = §3.2**（该 spec 已定稿：§3 为 S-B5 专设接力点，并把 handle 类型表示裁定权交给「S-B5 + 映射层」；本族 §3.2 回接其 §1.4 / §3 / Q7）
- 路线图 §1.4（2026-09-11 追加裁决：确定性轴族 + 「不确定性 = 显式请求」）——本族轴与 `determinism` 族相邻（`D_external` / `D_effect`），执法形态同构（§2.1）；汇合语义本体归 **S-E**，不入本族
- `docs/superpowers/specs/2026-09-09-lattice-ir-v7-carrier-design.md` + `2026-09-09-lattice-ir-v7-format.md`（**既有机制**：ENT 条目版本 / 存在区间 / EDG kind=1 state 边——本族全部复用，零重造）
- `docs/memory-model.md` §一（条款 4 边界公理 / 4b 图内不可重算 / 5 赋值 = 版本化）
- `docs/memory-model-capability-lattice.md` v4 §六（身份 → 图节点身份，无独立身份符号；授权归治理层）
- `docs/superpowers/specs/2026-09-10-type-interface-unification-design.md`（R2：判定走引擎 `ty_sub`/`ty_equiv`；§6.1 TYPE/IFACE 段 = 注解若需持久化的既有去向）
- 源码事实底座：`src/compiler/dataflow.cr:160-177`、`src/compiler/checker.cr:1103/1138/2020-2021`、`src/stdlib/io.cr:27/72`、`src/arch/x86_64/instr.cr:681`

---

## 0. 范围与裁决

**论题**：`check(x); …; use(x)` 中间外部状态可变化 → 图上应是**两个分离的状态版本**；更合理的 API = `open → verified handle → operation`（**handle 绑定身份**，而非按 pathname 重查）。

**本族的轴**：拥有 `state-version`（路线图 §1.1）；借用 `effect`（变更事件）与 `authority`（S-B2）——只消费，不重定义。

**与确定性轴族的关系（路线图 §1.4，2026-09-11 追加）**：本族 = `determinism` 族 `D_effect` / `D_external` 两轴在**外部不可见面**的具体化。§1.4「不确定性必须可归因到显式语义源」在本族读作：**外部世界的版本推进不得静默变成程序里的陈旧检查**——与「隐式竞争 → reject」同一条执法线（§2.1 对表）。

**族内裁决**：

- **决 1（版本载体）**：状态版本 = ENT `(var_id, version)`（v7 格式 §3.5）——本族**不引入第二套版本机制**；存在区间 = `live_start/live_end`，无配方 = `flags bit0`，序 = EDG `kind=1`。重造即偏离既有定案。
- **决 2（外部对象进图）**：外部对象 = **无配方条目**（memory-model 条款 4/4b 统一规则）——外部状态**不需要新通道**，它已经以边界条目身份在图里。
- **决 3（判据性质）**：图上只有**图版本**；**世界版本按定义不进图**。因此判据不是"版本号相等"本身，而是「图版本同版本支配 + 窗口内世界版本无变更」（§2），且后者**永不静默**（§4）。

**范围外**：并发线程/flow 间竞争（§7 开放问题 4）；OS 内核内部 TOCTOU（S-B4/平台面）；不新增关键字、不加语言 primitive（路线图 §1.2 原则 1）。

---

## 1. 状态版本模型

### 1.1 图版本（既有，逐字复用，零新增字段）

| 面 | 既有载体（v7） | 本族用法 |
|---|---|---|
| 版本身份 | ENT `(var_id, version)`（1-based，每次定值 +1） | 同一性判定的原子单位 |
| 定值点 | ENT `def_nod` | 版本推进 = `def_nod` 换新（memory-model 条款 5：x₁ 创建、x₀ 失效） |
| 存在区间 | ENT `[live_start, live_end)`（NOD id 坐标，半开） | 版本在图上的存活窗口 |
| 无配方 | ENT `flags bit0`（图内不可重算 / 边界） | **外部对象（观察结果）的标注面** |
| 序 | EDG `kind=1`（state 边 = 副作用序约束集） | 变更与观察的顺序约束 |

本族声明：**上表字段一个不改、一个不加**。§5 切片不加任何格式字段。

### 1.2 外部状态进入图的四条通道（三条既有 + 一条注解）

| 通道 | 载体 | 状态 |
|---|---|---|
| ① 观察 | 边界节点（extern / `unsafe` / syscall / MMIO）产出条目，`flags bit0`；语义 = 符号常量（条款 4） | **既有** |
| ② 身份 | handle 值 = 条目；身份 = **产生它的图节点身份**（capability-lattice v4 §六：无独立身份符号） | **既有**（本族只规定用法） |
| ③ 变更 | 图内变更 = STORE 族节点（已在 state 链）；**图外变更 = 无节点**——这正是 TOCTOU 的本体 | 变更**声明** = 注解（policy 层，路线图 §1.2 原则 1） |
| ④ 序 | EDG `kind=1` state 边 | **既有** |

- ③ 的注解面：外部调用节点可带 `mutates(κ)`（κ = 身份键）；**未标注的 extern / 未知调用保守视为「变更一切外部身份」**——沿用既有先例（`dataflow.cr:166-172`：未知/外部函数保守视为有副作用）。
- **事实底座（2026-09-11 核对，实施时实测确认）**：今日纯度标注是乐观的——`checker.cr:1103`「all functions are pure」+ `checker.cr:1138`「extern functions are pure」；且 `df_connect_state`（`dataflow.cr:160-177`）的分类表**不含 `IR_CALL_EXTERN`**。结论：**外部效应今日无保证的序通道**（对 TOCTOU 恰好是最坏情形）。本族**不要求改它**；切片自带外部变更识别的最小面，且负控（§5.4）显式建立在该面之上。
- 今日语言面事实：`syscall3/4` 是特判内置（`checker.cr:2020-2021`；`instr.cr:681` 内联发射），stdlib `read_file(path)` / `write_file(path, …)`（`io.cr:27/72`）是**按 pathname 重查**的形态（P3，§2.4）。

### 1.3 图版本 vs 世界版本（判据的诚实核心）

- **图版本** `v_g`：ENT 里那个版本号——图知道的状态。
- **世界版本** `v_w(κ, t)`：身份 κ 在运行时刻的真实状态——**按定义不进图**（无配方条目 = 符号常量，条款 4/4b）。
- 推进 `v_w` 的三类来源：
  - **(a) 图内变更节点**——图上可见（STORE 族；③）。
  - **(b) 外部代理**（另一进程 / 线程 / 硬件 / 系统调用内部副作用）——图上**不可见**。
  - **(c) 时间本身**——不推进版本；只有变更推进。（显式排除，免得判据退化成"时间过去了"这一无信息量条件。）
- **本族要立的不变式**：`v_w(κ)|use = v_w(κ)|check`（检查窗口内世界版本未推进）。
- 图上**能**证的是 (a) 的缺席 + ② 的身份不可重解析；**不能**证的是 (b)。这条边界就是 §4 四档的分界来源，也是 §6 引用路线图 §1.3「恶意/失陷外部实体」的落点。

---

## 2. TOCTOU 判据

### 2.1 判据形态（C1 ∧ C2 ∧ C3）

**记号**：
- check 节点 `C`：验证谓词 `P` 于身份 `κ` 的版本 `v(C)`。
- use 节点 `U`：作用于身份 `κ` 的版本 `v(U)`。
- state 可达 `→*`：EDG `kind=1` 的传递闭包。图语义 = 约束集 ⇒ 偏序可达（v7 载体设计 §1.3）；今日生产形态 = 链（`g_last_state_node`）⇒ 判定退化为区间包含（实施快路径）。
- mutator：图内变更（③ a）或带 `mutates(κ)` 的边界节点；未标注 extern = 变更一切 κ。

**TOCTOU 判据**：`U` 合法 ⟺ ∃C 使 C1 ∧ C2 ∧ C3：

- **C1（同版本支配）**：`v(C) ≡ v(U)` **且** C 支配 U——C 经数据边可达 U，或 U 直接消费 C 的版本见证（条目）。
- **C2（窗口无变更）**：状态路径 `C →* U` 上不存在 κ 的 mutator，**且 U 本身不是对 κ 的重观察**（2.2）。
- **C3（稳定性声明）**：κ 在图存在区间内被声明为"外部代理不推进它"（独占 / 不可变）——治理层注解，非语义本体（capability-lattice v4：授权归治理层）。

**与路线图 §1.4 执法表的对表**（两者是同一形状的两个入口）：§1.4「依赖/状态边足证汇合与调度无关 → prove」↔ 本族 C1∧C2∧C3；§1.4「语义明文允许（`race`/`select_any`）→ 显式不确定、必须进报告面」↔ 本族 C3 声明的登记面；§1.4 **「既未证明也未标注（隐式竞争）→ reject ← 真正的执法点」** ↔ 本族 ④（P3/C2 缺口）。**同一执法点，不得两处各判一次**——本族承担外部不可见面，汇合语义本体承担并发面（S-E）。

### 2.2 版本分离的检测（本族要检什么）

- **重观察 = 新版本**：`use` 若重新读同一 pathname，则产生新边界节点 → 新条目 → 新 `def_nod` ⇒ `v(U) ≠ v(C)`，C1 不成立。**这就是 `check(x); use(x)` 的图形态：两次独立观察 = 两个分离的状态版本**（论题的直接兑现）。
- 版本分离**本身不是错误**（合法：写后重读）；**没有新 check 的 use 才是**——即分离出的版本上不存在同版本的 check。
- **pathname 重查识别**：边界节点以 pathname 形值（string）为输入、产出**新**条目，而作用域内已有同 κ 的活验证版本 ⇒ 重查模式（不必然非法，但**不继承既有验证**）。
- 判据落法：C1 走引擎（§2.3）；C2/C3 走图分析 + 注解——**不另建判定体系**（路线图 §1.2 原则 4）。

### 2.3 版本等价判定走引擎

- 版本身份编码为形状类型项（S-A 横切轴形态），如 `Observed<T, κ, v>` / `Verified<P, T, κ, v>`。**形状项定义属 S-A**；本族只规定判定 = `ty_sub(实际项, 要求项)`（R2 §3.2），要求项来源 = 操作的契约（领域库给出：`op` 要求 `Verified<P, Handle[κ], v]`）。
- 判定失败输出反例（`ty_witness`）：如"版本 v2 的 use 落到要求 v1 的契约上"。
- **不锁形状项语法**——S-A 定稿后对表（§7 开放问题 6）。

### 2.4 模式清单（本族「版本分离」图鉴；路线图 S-B5 行交付项）

| # | 模式 | 版本形态 | 判据结果 |
|---|---|---|---|
| P1 | `h = open(p); v = verify(h, P); op(v)`（身份绑定） | 单一版本 + 同 handle | C1 ✓ 构造性；C3 身份部分**被构造消解**（fd 不可重解析）→ prove |
| P2 | `v = observe(x); verify(v, P); use(v)`（值绑定：读一次、验缓冲、用缓冲） | 单一版本 | C1 ✓；C3 内容部分需稳定性声明或有界观察窗口 |
| P3 | `check(p); …; use(p)`（按名字重查） | 两版本（v1 ≠ v2） | C1 ✗ → enforce（版本重校验）/ reject |
| P4 | `v = verify(h, P); …; mutate(κ); …; op(v)`（同 handle 但窗口内变更） | 版本被推进 | C2 ✗ → enforce / reject |
| P5 | `check(p); …; use(p)`，窗口内无变更且 κ 独占 | v1 ≠ v2 | C1 ✗（仍分离）；即使可证观察等价 + 稳定性，**本族不默认放行**——重查默认不继承验证 |
| P6 | `write(h); v = observe(h); verify(v, P); use(v)`（写后重读） | 版本推进**合法** | C1 ✓（新 check 落在新版本上） |

- **P5 是本族最尖锐的裁决**：宁可 enforce 也不静默 prove——依据路线图 §1.2 原则 3「义务不静默消失」，且"两次读等价"本身需证明（内容可写）。
- P1/P2/P6 = 正控来源；P3/P4 = 负控来源（§5.4）。

---

## 3. handle 身份绑定

### 3.1 接口形态（open → verified handle → operation）

```
open(p, …)   -> Handle[κ]                       // 边界节点：一次身份解析，产出条目（flags bit0）
verify(h, P) -> Verified[P, Handle[κ], v]       // check 节点：把断言 P 绑到 (κ, v)
op(vh, …)    -> …                               // 操作只对 Verified 定义
```

- **关键性质**：`op` 不接收 pathname，只接收 handle ⇒ **重解析这个动作不存在**（② 身份 = 节点身份，v4 §六）⇒ C3 的身份部分**不靠证明、靠构造**。
- `verify` 携带版本见证 `v`（= 该条目 ENT `version`）⇒ C1 的判定输入。
- **谓词分流**：身份谓词（存在 / 类型 / 属主 / 路径）与内容谓词（权限位 / 内容摘要 / 大小）分开——前者 C3 由构造消解，后者 C3 不可消解 ⇒ 走 P2 值绑定或 enforce。

### 3.2 与 S-B4 围栏的衔接（回接其 §1.4 / §3 / Q7）

- **接力点（S-B4 §3 原文立场）**：S-B4 保证**名字 → capability 的构造**单步且不可回退（无"先检查字符串、再用字符串"）；S-B5 保证 **capability → 资源身份的绑定**是原子的（check/use 同一版本）。分工 = 命名空间判定（S-B4）∥ 时间判定（本族）；**两族互不重定义**：S-B4 不定义版本，本族不定义命名空间。
- **S-B4 交给下游的三样东西**（① 根身份 ② 派生链 ③ 可选的 handle 槽）中，本族消费 ③；① ② 归 S-B2 的 `Can` 判定。
- **L-1 / L-2 分工（S-B4 D-5 / §2.5）**：S-B4 语义层只判 L-1（构造合法性）；**L-2（symlink / mount / hardlink = 解析身份）值级不可判，显式登记为映射层义务**。本族随之划界：**κ 的可靠性上限 = 映射层 handle 化的能力**——无 handle 化时 ④ reject（S-B4 §4 同款），有 handle 化时 L-2 由映射层强制（本族 C3 的身份部分据此才成立）。
- **handle 类型表示的裁定（S-B4 §1.4 把裁定权交给「S-B5 + 映射层」）——本族建议：并列类型**。理由：本族需要 handle 携带版本见证 `(κ, v)`（§3.1），而"内嵌槽"方案使 `Path<R>` 值类型随绑定状态变化（状态依赖类型，与路线图 §1.2 原则 1「属性不升格 primitive」张力更大），且要求值级也带版本（S-B4 已裁定**纯值路径无版本变化**）。故取 `Handle<R>` 独立 + `bind(p: Path<R>) -> Handle<R>`：**转换点 = 唯一的 check/use 边界 = 本族 C1 的判定对象**（与 S-B4 §3 表述一致）。映射层确认项 = `bind` 的原子性由谁提供（fd / dirfd / `openat` 类；S-B4 §3「已 handle 化」行）。
- **两侧的 TOCTOU 分工（S-B4 §3 表格）**：已 handle 化 ⇒ 构造即绑定身份、**无重查 ⇒ 无 TOCTOU**（S-B4 产出即本族**版本载体**）；未 handle 化（纯值路径）⇒ 值级无版本变化、无 TOCTOU，**"值 → 资源身份"的转换点才产生 TOCTOU 面 = 本族判定对象**（= 本族 P3 重查模式的另一投影）。
- **切片依赖（实测）**：S-B4 切片 v0 = 纯值级、**不含 handle 槽** ⇒ 本族切片（§5）的 handle 只能从边界 `open` 起（衔接退化为并列，不阻塞任何一方）。若本族最终形态与 S-B4 §3 冲突，按 S-B4 Q7「两族共同复核」处理。

### 3.3 与既有外部操作的关系（事实面）

- `syscall3/4` 是特判内置（`checker.cr:2020-2021`、`instr.cr:681`），`read_file` / `write_file`（`io.cr:27/72`）是 P3 形态——**切片不改 stdlib**（§5 只立判据面）；stdlib 的 handle 化 = 切片之后的独立批（§7）。
- 不引入新的外部操作原语（路线图 §1.2 原则 2：策略面归库）：`open` / `verify` 是**领域库**的类型化构造器形态，语言只给流纪律与义务阶梯。

---

## 4. 义务落档（四档，路线图 §1.1 逐字）

> ① **prove**（静态可证 → 零成本通过）② **enforce**（不可证但可运行时强制 → 插入 enforcement）③ **proof-required**（不可证也不可强制 → 要求用户提供 proof/contract）④ **reject**（高安全模式下仍无法处置 → 拒绝编译）

| 档 | 本族条件 | 处置 |
|---|---|---|
| ① prove | C1 ∧ C2 ∧ C3 | 零成本通过；绑定构造性的（P1/P6）不问 C3 |
| ② enforce | C1 成立或可重建，C2/C3 不可证 | 插入**版本重校验**：use 前紧邻一次同身份观察 + 见证比对；不等 = **失败封闭（fail-closed）**。若身份无可观察的版本见证 ⇒ 退化为「观察与使用**原子化**为同一步」（= P1/P2 的唯一化） |
| ③ proof-required | 需要「κ 独占 / 不可变」而引擎不可判 | 要求用户 / 领域库提供 contract（C3 声明面） |
| ④ reject | 高安全模式下：C1 ✗ 且无可执行的 enforce 通道（如 P3 且存在外部变更可能） | 拒绝编译 |

- **不可证 → 不静默通过**（路线图 §1.2 原则 3）：每个判定结果必须落到 ①②③④ 之一，无第五种"算了"。
- **enforce 的语义纪律**：版本重校验**必须紧邻 use 且 fail-closed**——"检查后再用一个可能已变的东西"不是 enforcement，是 TOCTOU 复现。
- 与 where 三档的关系：C1 常量/结构可判 → 编译期（prove / reject）；C3 符号 → contract（proof-required）；C2 动态 → 运行时（enforce）——**四档是 where 三档在本族的展开形态**（S-A 定稿后对表）。

---

## 5. 最小可实施切片 + 判据

### 5.1 切片 S-B5-0（注解 + 判定，零载体改动）

1. **身份键 κ 与 `mutates(κ)` 注解面**（最小：handle 派生面 + 保守 extern 面）。**不落盘**——若需持久化，去向 = R2 §6.1 的 TYPE/IFACE 段（实施批定）；v7 格式 §6 开放点 1 的 EDG `kind 2+` 保留位**不动**。
2. **版本等价判定** = `ty_equiv` 调用（R2 引擎）+ 离线小用例表。
3. **判定 pass（一个文件）**：对每个 use 求 C1（同版本支配）、C2（state 边可达性 + mutator 集）、C3（取注解）→ 输出四档分类 + 反例。
4. **判据（见 §5.4）** + 不变式：**零产物变化**（注解未启用 = 今日程序不受影响——策略面归库，路线图 §1.2 原则 2）；全套件 + 自举判据按 repo 惯例，落地细节入实施计划。

### 5.2 切片边界（明确不做）

不改 stdlib、不改 extern 序通道（§1.2 事实底座）、不加载体字段、不做跨过程（过程间 κ 传播 = 切片后续）。

### 5.3 待论证（非设计定论）

κ 的粒度（实例 / 类 / 全外部）、C3 的声明形态、C3 的 contract 面是否并入 S-A 的 policy 接口。切片只要求"最小可用"。

### 5.4 正控 / 负控 / 精度控

- **正控 A（同 handle 复用 ✓）**：`h = open(p); v = verify(h, P); op(v)` → **prove**，零 enforcement 插入（P1）。
- **正控 B（值绑定 ✓）**：`v = observe(x); verify(v, P); use(v)` → **prove**（P2 / P6）。
- **负控 A（pathname 重查 + 中间 external mutation ✗）**：`check("p"); extern_mutate("p"); use("p")` → **必须非 prove**（enforce 或 reject），且反例指明两个分离版本。
- **负控 B（同 handle 但窗口内 mutator ✗）**：P4 形态 → C2 失败 → 非 prove。
- **精度控（不误杀）**：`verify(h, P)` 与 `op(h)` 之间夹一个**不同 κ** 的 extern 变更 → 仍 **prove**——证明判据不是一刀切拒绝。

---

## 6. 不承诺

（逐条落路线图 §1.3 三类不可消残留）

1. **恶意 / 失陷外部实体**（本族主落点）：外部代理可在 C 与 U 之间推进世界版本，**语言语义不能证明其不发生**——`v_w` 按定义不进图（§1.3）。本族只做三件事：使分离**可见**、使义务**有主**、使好 API **可表达**。**本族不可能消掉"外部实体恶意"本身**——这是路线图 §1.3 第二条的正面承认，不是实现缺口。§1.4 读法：世界版本推进是**物理不确定性**，本族要求它**不得静默变成语义不确定性**（陈旧检查）；但"外部代理不推进"这一物理事实永远只能被**声明 / 缩小**，不能被证明——同一条界线。
2. **错误 specification**：`verify(h, P)` 的 P 本身写错（检了错的属性）——语言只保证"检查与使用同版本"，不保证 P 正确。
3. **需求本身允许危险行为**：CLI 按名字 open 之类的语义有其正当性——目标不是禁止能力，而是**把风险压到一个显式义务上**（证明隔离边界）。
4. **不承诺**检测所有 TOCTOU：只覆盖图上可表达、policy 覆盖的那些；未覆盖面要么按保守面处理（策略启用时），要么明确不在义务范围——**无第三种"看起来安全"**。

---

## 7. 不做项 + 开放问题

### 7.1 不做项（YAGNI / 边界）

- 不新增语言 primitive / 关键字；不把 TOCTOU 做成内建特判（路线图 §1.2 原则 1/2——那是 lint 换皮）。
- **不重造版本机制**：ENT + state 边是唯一版本 / 序载体（决 1）。
- 不做并发线程 / flow 间竞争判定（声明边界，见开放问题 4）。
- 不做 OS 内核内部 TOCTOU、不做平台 syscall 语义正确性（S-B4 / 平台面）。
- 不做 stdlib handle 化（切片后独立批）。
- 不锁形状项语法（S-A 定稿后对表）。

### 7.2 开放问题

1. **κ 粒度与可靠性的张力**：实例身份（fd / inode 类）是**映射层的见证**，语义层不承诺；其可靠性上限已由 S-B4 明确 = **映射层 handle 化能力**（L-2：symlink / mount / hardlink 值级不可判，D-5）。类级或"全外部"保守面的精度代价——精度控（§5.4）只给出下界，上界待实测。
2. **无版本见证的外部对象**：世界版本不可观察（无可信 counter）⇒ enforce 无法"比对"，只能原子化或拒绝——是否需要第三形态 enforcement（独占声明 / 锁）仍待裁决。
3. **C3 归谁**：治理层注解（S-A policy 接口）vs 领域库 contract——本族倾向后者（原则 2），待 S-A 对表。
4. **与并发模型的关系**：共享可变状态的线程 / flow 面 = 同一判据的另一实例（mutator 图上可见但序不同）。本族**声明只碰外部不可见面**；并发面归 **S-E**（汇合语义）+ 路线图 §1.4（隐式竞争 → reject），本族只提供同形判据不越过边界。
5. **与条款 4b 写回规则的耦合**：外部对象条目是"无配方"（4b）——"驱逐必写回"对**只读**外部对象（写回什么？）需与 M4 / 映射面澄清。
6. **依赖对表**：S-B4 **已定稿**，本族 §3.2 已按其 §1.4 / §3 / Q7 回接（含 handle 表示建议 = 并列类型，待映射层确认）；**剩 S-A**（术语 / 四档 / policy 接口）起草时不存在——本文按其接口面引用，需在其定稿后对表一次。衔接点：§2.1（§1.4 执法表）、§2.3（形状项）、§3.2（`bind`→Handle）、§4 末（四档 ↔ where 三档）。

---

## 合规自检（共享契约逐字遵守）

- 术语：只使用路线图 §1.1 定义（属性轴 / 义务 / 四档 / policy / declassify / sink-source 未被重定义）；新增的族内记号（图版本 / 世界版本 / κ / C1-C3）**不进入**共享术语表语义。
- 原则：① 属性不升格 primitive（§5.1 注解面 = policy，不改语言）② 策略面归库（§3.3）③ 义务不静默（§4）④ 判定统一走引擎（§2.3）⑤ 只碰声明式上限（§0 范围外：state 链 / region / 调度不动）。
- 不承诺：§6 逐条落 §1.3 三类残留。
- 路线图 §1.4（追加裁决）：轴族相邻性已声明（§0），执法点同形已对表（§2.1），**未稀释汇合语义本体**（§7.2 Q4 归 S-E）。
- 复用而非重造：§1.1 零新增字段；§5.1 明确不动 EDG 保留位；无第二套版本机制。
- 设计与待论证分离：§5.3 待论证、§7.2 开放问题 1-6。
