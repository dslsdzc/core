# 语言面收窄与接口化交叉执行（定长退役裁决 + 宽度清理 + 依赖序）

日期：2026-09-10
状态：**已实施（R1）**——波 R1 语言面收窄批（计划 `docs/superpowers/plans/2026-09-10-language-surface-narrowing-r1.md`）Task 1-5 于 2026-09-10 收官：定长裁决注记落位、宽度三项清理完成、全局定长/初始化路径修复；全回归 + 自举守护全绿。R2（接口化主设计轮）后启。
性质：前端/语言面战线（根基革命 A：图本体/类型→接口）的启动裁决 + 与既有战线的交叉执行序。**本 spec 只裁方向与顺序，实施计划后立。**

关联：
- `docs/superpowers/specs/2026-08-30-type-system-direction-design.md`（类型方向定案——条目 7 宽度移出语言 / 条目 8 类型标注自动（数组/切片 = 序列接口 + 元素接口 + 长度值约束）；本 spec 为其条目 7/8 的执行裁决）
- `docs/superpowers/specs/2026-09-10-x86-instance-design.md`（实例化——波 2/3 与本 spec 的映射侧交叉）
- `TODO.md` width-out-of-language 条目（语言侧清理三项）
- `TODO.md` 边界判定图 pass（F11 长度来源链——长度语义归处的现状基础）

---

## 1. 定长数组裁决（2026-09-10 用户拍板）

### 1.1 裁决

**`[T; N]` 退役类型构造器身份；保留为「内联容量存储」表示提示**（A+B 融合）：

- **类型系统层**：`[T; N]` 不再作为类型构造器——类型相等/子类型判定不以其为成员概念；语义面归处：
  - N 元聚合 → **product 类型**（条目 8：元组/记录 = product 自动组合）
  - N 长序列 → **序列接口 + 长度值约束**（条目 8：F11 长度走 where 三档）
  - 长度入型做验证 → **图/F11 长度来源链**（比字面量构造器强——可表达依赖长度）
- **表示层**：`[T; N]` 语法保留 = **内联容量存储的表示提示**（映射参数——与 hw-map 同层；随实例选择生效/退化；非经典范式实例可忽略或另定——"内联"在量子/光学映射中未定义是正常的）
- **自举代码**：`ast.cr` 9 处 + `kernel/term_io.cr` 1 处用法零改造（它们本就是表示提示的正当消费——编译器需内联存储）

### 1.2 裁决依据（用途拆解）

| 用途 | 语义本质 | 归处 | 需要构造器身份？ |
|---|---|---|---|
| N 元聚合（矩阵/坐标/记录） | product | 条目 8 product 自动组合 | 否 |
| N 长序列 + 索引 | 序列 + 长度 | 条目 8 序列接口 + where | 否 |
| 长度入型做验证 | 长度事实 | F11/图（更强——字面量 N 表达力不够） | 否 |
| 内联存储/免分配/布局 | **经典映射性能关切** | 映射实例（表示层） | **这是真需求——但不属语义层** |
| 语法便利 | 糖 | 表示提示 | 否 |

关键判据（与宽度原则同构）：内联存储对经典机越有用、对非经典范式越无意义——正因此它不得升格为语言语义（机器形状出语言的既有原则）。

### 1.3 连带处置

- **全局定长数组 SIGSEGV**（实测：check 过/build rc=0/run rc=139——含解释器路径）：过渡期处置 = 按表示层语义修（全局内联容量 = 静态保留 N 格）或先编译期拒绝；**修复归属 = 实施批**（见 §3 波 R1）
  - **✅ R1 已处置（选「按表示语义修」）**：全局（聚合 / 非常量初值）降级为运行期初始化序列——main 序言 IR 注入 + 解释器初始化阶段；落点 71cb6278；判据 `tests/selfhost/test_global_init.py` 15 例双路径（解释器 / ELF）逐例精确 rc。非编译期拒绝（保留语义，符合「表示提示」裁决）。
- **checker 落实时点**：`[T; N]` 从类型判定中降格 = **接口化轮（R2）重做 checker 类型判定时自然完成**（届时不经类型相等/子类型）；R1 只做：裁决注记（ast.cr/checker 加「表示层概念」标注）+ 全局路径处置 + 语法面文档
  - **✅ R1 三项均已做**：注记 aa76f7f5（ast/checker/parser 七处，零行为）/ 全局路径 71cb6278 / 语法面文档（本 spec + grammar 同步 51d74be3）
- **表示提示机制**（提示如何被映射层消费） = 映射侧（R3/hw-map 通道）设计时定——R1 不建机制（现状语法行为不动）

---

## 2. 宽度语言侧清理（条目 7 执行项）

| 项 | 内容 | 裁决 | 结果 |
|---|---|---|---|
| 死条目移除 | `ast.cr` `T_INT_I8..T_INT_U64`（77-84）+ `T_FLOAT_F32/F64`（85-86）——**勿重编号**（编号空间稳定） | R1 直接做（机械） | ✅ ce3e541c——ast.cr 墓碑注释（勿重编号）+ `W_*` 值域 + parser 两处死分支（lexer 从不发射，引用面归零） |
| `_f32/_f64` 后缀 | 实况：`parser.cr:414` 注「data = 宽度标注（_f32/_f64，**保留**）」——后缀现作为宽度标注随字面量存 | **R1 侦查后裁**：若承载 apx/binary64 精度语义（语言语义）→ 保留并重命名注解（精度非宽度）；若纯机器宽度 → 随死条目清（或转 R3 映射通道标注） | ✅ 裁为**纯机器宽度 → 退役**（51d74be3）：lexer 不再消费后缀、一律响亮报错；宽度 = 机器形状，归映射层（parser 宽度分支随死条目一并清）。侦查结论：后缀位宽信息在词法层即被丢弃（`num_str` 已剔除），data 槽恒 0——无语言语义承载 |
| `1_000` 分歧 | bootstrap vs self-hosted 两编译器解析分歧复核 | R1 做（词法对照测试） | ✅ 51d74be3——`_` 分隔符**不支持**，两编译器统一响亮报错（原双向皆错：self-hosted 静默成值 0 / bootstrap 静默成 1000）；新增 `tests/selfhost/test_lexer_parity.py` |

---

## 3. 交叉执行序（前置依赖）

```
波 R1：语言面收窄批（可立即做——纯清理 + 小侦查 + 一处处置）——**✅ 已完成（2026-09-10，Task 1-5 收官）**
  ① 定长裁决注记（ast/checker「表示层概念」标注——零行为改动）→ ✅ aa76f7f5（ast/checker/parser 七处，零行为）
  ② 全局定长数组 SIGSEGV 处置（按表示语义修 或 过渡期编译期拒绝）→ ✅ 按表示语义修（71cb6278）
  ③ 宽度死条目移除 + `1_000` 分歧复核 + `_f32/_f64` 后缀侦查裁决 → ✅ ce3e541c（死条目）+ 51d74be3（词法面收窄）
  判据：全回归 + 自举 + 词法/语法面测试（行为零变化除外——全局定长修复是行为变化,单独标注）
    → ✅ Task 5 收官（2026-09-10）：11 套回归全绿 + 自举重建 BUILD SUCCESS / 三处 [GUARD] OK /
      full-bootstrap 链 cmp IDENTICAL + N06=0 + 冒烟 rc=42
  行为变化（三处——两处响亮报错 + 一处修复，均为本批「收窄」意图的直接后果）：
    1. `1_000`（`_` 分隔符）：不再接受，bootstrap / self-hosted 两编译器**统一响亮报错**
       （原双向皆错：self-hosted 静默消费 → `x := 1_000` 得 0 的实质误编译面；
       bootstrap 静默接受 → 1000）
    2. 宽度后缀（`_i32`/`_u64`/`_f32`/`_f64` 等）：**退役 + 一律响亮报错**
       （原被 lexer 静默消费、data 槽恒 0，parser 分支为死代码）
    3. 全局初始化：文件级全局（聚合 `[T; N]` / 非常量初值）两路径语义一致——
       原 `[T; N]` 全局 run rc=139（SIGSEGV）或静默 0；现 `main` 序言注入 + 解释器
       初始化阶段双路径逐例判据（15 例）
波 R2：接口化主设计轮（前置：R1 完成）
  类型 = 图标注 / 接口注册表（int/dex/string 原生条目）/ 检查器删特判统一查表
  / 数组 = 序列接口 + 长度 where / 泛型 = 编译期接口具体化
  —— 定长「类型身份退役」在此落实（checker 重做时自然剔除）
波 R3：映射侧（与 x86 实例化波 2/3 合批）
  hw-map 宽度显式通道 + 表示提示机制（内联容量存储的映射消费）+ 实例参数化
```

交叉点（已核）：R1③ 的词法面与 R2 的 checker 重做同文件族（parser/ast）；R2 的接口条目在 R3 的映射实例上实现；R3 与实例化波 2 是同一件事（编码层参数化）两个名字。

## 4. 挂账与不承诺

- R1 不做：表示提示机制（R3）、checker 类型判定重构（R2）、F11/where 落地（R2）
- 全局定长数组修复若改为「编译期拒绝」先行 = 过渡措施，R2/R3 后恢复完整语义
  - **R1 实际选择 = 按表示语义修**（非编译期拒绝）——不产生过渡态；R2/R3 只需在 checker/映射侧接续（已记 TODO「全局初始化机制」条目：未来非 main 入口 / 库形态需迁移到独立 init 区）
- 本 spec 不裁 `_f32/_f64` 最终归属（R1 侦查后裁）
  - **✅ R1 已裁**：退役 + 响亮报错（51d74be3）——后缀无语言语义承载（词法层即丢弃）；未来若需精度/位宽标注，走 R3 映射通道
- TODO 登记：定长裁决（随 R1 提交记 TODO 划销/挂账）、宽度清理三项（随 R1 完成划销）
  - **✅ 已办（Task 5）**：宽度三项划销 + 定长注记挂账（类型身份退役 = R2）+ 新登记（全局初始化机制 / lexer 诊断前缀不一致 / 枚举族 callee 内联缺口 / 嵌套类型别名一层解析 / test_compile 清单陈旧）
