# 已失效约定的引用面全仓扫描（**只读产出 · 只报不改**；2026-09-18）

> **基线**：`develop@origin` = **`662ff87d`**（含 `#130`）。
> **取数纪律**：一律 `jj --ignore-working-copy file show -r develop@origin <path>` / `jj file search -r develop@origin`；**未 grep 工作副本**。
> **锚**：**唯一子串**（不按行号）。
> **范围**：`docs/**` · `TODO.md` · `tests/**`（含 `.py` 判据与 `.cr` 语料注释/期望值）· `tools/**` · `src/**` 注释。
> **性质**：本文件**只出清单**；改法（一律「原文逐字保留 + 加注」，只增不删）由 lead 裁后另派。

**统计**：**必须处理 3 · 建议处理 5 · 可不动 6**（另：**第 5 类 = live 判据面零命中**，详见 §5）。
★ = 我判为最危险的 5 条。

> **〔统计更正（2026-09-19 · `docstale` 逐行清点；原句逐字保留不删）〕上两句中的数字皆与表体不符，作废；以 §0 为准。**
> - **表体行数 = 15**，非 14。逐行标签（第 1 列）：`1-1 1-2 1-3 1-4 1-5 2-1 2-2 2-3 3-1 3-2 3-3 4-1 4-2 5-1 5-2`。
> - **分类实数**：**必须处理 3 · 建议处理 2 · 可不动 10**（原句「建议 5 · 可不动 6」两处皆错：**建议多 3、可不动少 4**，`3+5+6=14` vs `3+2+10=15`，合计恰差 1）。
> - **★ 实数 = 4**（1-1 / 1-2 / 2-1 / 3-1），非 5。
> - **清点方法（可复核）**：
>   ```
>   P=docs/superpowers/specs/2026-09-18-stale-criteria-scan.md
>   R=$(jj log -r @ --no-graph -T commit_id)      # 现查现用；本命令对「加注前/加注后」两态**同值**（见下方反向断言）
>   jj --ignore-working-copy file show -r "$R" "$P" | grep -cE '^\| [0-9]+-[0-9]+ '            # ⇒ 15（表体行数）
>   jj --ignore-working-copy file show -r "$R" "$P" | grep -E '^\| [0-9]+-[0-9]+ ' | grep -c 必须处理   # ⇒ 3
>   jj --ignore-working-copy file show -r "$R" "$P" | grep -E '^\| [0-9]+-[0-9]+ ' | grep -c 建议处理   # ⇒ 2
>   jj --ignore-working-copy file show -r "$R" "$P" | grep -E '^\| [0-9]+-[0-9]+ ' | grep -c 可不动     # ⇒ 10
>   jj --ignore-working-copy file show -r "$R" "$P" | grep -E '^\| [0-9]+-[0-9]+ ' | grep -c ★          # ⇒ 4
>   ```
>   （判据取向：以**表体逐行**为出处、头统计为派生量；分类词取**第 2 列**，故用行首 `| N-M |` 锚定表体行，避免命中表外的散文提法。）
>   **格式约束（反向断言）**：§0.1 的复核表**首列写作 `` `1-1` ``（带反引号）**，正是为了不被上式二次命中——若上式的总行数变成 **30**（而非 15），即说明 §0.1 的 id 列被改成了裸 `| 1-1 |` 形，**计数命令与文档格式已失配**，须先修格式再取数。
> - **§6bis 不计入本统计**：它是「非失效约定·文档缺口」的附带发现，无编号、无表格行。

---

## 0 基线重定与逐条复核（2026-09-19 · `docstale`；**只增不删**——上文原文一字未改）

> **重核基线**：`develop@origin` = **`09f253b8a90a28973a282257d56dd251d12d2fc4`**（≠ 本报告原基线 `662ff87d`）。
> **基线在重核期间移动过（实测，见 §0.4）**：开工时 `develop@origin` = `7ddf6ed2`，重核中途被推进到 `09f253b8`（`#136`，TODO 登记 3 条）。**本节的 `-r` 参数一律写「现查现用的 commit id」，不写移动中的 bookmark**——行号亦以 `09f253b8` 为准。
> **取数**：一律 `jj --ignore-working-copy file show -r <现查现用 id> <path>` / `jj file search -r <现查现用 id>`；**未 grep 工作副本**。
> **锚**：沿用本报告原纪律「**唯一子串（不按行号）**」——**行号只作辅助**（`#136` 一处插入即让 TODO.md 第 173 行之后的全部行号 +3）。
> **消解者**：区间 `662ff87d..09f253b8` 内的文档面提交 **`2b103d69`（#131）**——其 description 自陈两段：
> 「活文档改内容（syntax.md apx 示例改为可编过 + errors.md 补 P024/P025/P026）」+「**记录加注四处**（原文逐字保留 + 〔…〕追加；只增不删）」。
> 该「四处加注」恰为本报告的 **1-1 / 1-2 / 2-1 / 4-1**；「活文档改内容」恰为 **3-1 / §6bis**。
> ⇒ **判定本身没错，是它的对象在发布后被修复了**：报告**声明了基线**（这点做对了），故差异可一行命令核出。

### 0.1 逐条判定（在场 / 已消解）——15 条 + §6bis

| # | 报告原档 | 现基线（`09f253b8`）判定 | 消解者 · 取证（现查现用；`grep -c` 除非另注；行号以 `09f253b8` 为准） |
|---|---|---|---|
| `1-1` | 必须处理 | **已消解** | `2b103d69` 加「映射表注」`TODO.md:157-160`——内容 = 报告建议的「映射行**字面不改**，以本注指明旧机理」+ 现行机理。取证：`grep -c '映射表注（2026-09-18'` ⇒ **1**（原基线 `662ff87d` 上 ⇒ **0**） |
| `1-2` | 必须处理 | **已消解** | `2b103d69` 加「标题注」`docs/superpowers/plans/2026-09-17-silence-closure.md:54-57`（「标题字面不改，只增不删」）。取证：`grep -c '标题注（2026-09-18'` ⇒ **1**（原基线 ⇒ **0**） |
| `1-3` | 可不动 | **在场 · 判定不变** | `TODO.md:1210` ⚠更正注 + `:1211`「本条内**四处**仍写着旧约定」逐处标注均在位 ⇒「无需动作」仍成立 |
| `1-4` | 可不动 | **在场 · 判定不变** | `src/compiler/monomorph.cr:835` · `tests/selfhost/test_generic_param_erasure.py:5-6`（两者均为**现行正确**机理） |
| `1-5` | 可不动 | **在场 · 判定不变** | `docs/superpowers/plans/2026-09-18-generic-param-erasure.md:28` |
| `2-1` | 建议处理 | **已消解** | `2b103d69` 加「表注」`docs/superpowers/plans/2026-09-16-apx-conversion-fix.md:166-168`——正面写有「**本表作为『可执行配方』已失效**」+「B-2 行的『期望 7』叠另一条失效面」 |
| `2-2` | 可不动 | **在场 · 判定不变** | `TODO.md:1212-1215` **四处逐处**标注在位 |
| `2-3` | 可不动 | **在场 · 判定不变** | `TODO.md:961`（元组最小复现）· `:1056`（`u11`）· `:1920`（元组数字下标条目，另一批）⇒「不属本轮失效面」仍成立 |
| `3-1` | 必须处理 | **已消解** | `2b103d69` 改 `docs/developer/syntax.md:219` 为 `speed   : dex, apx = 3.14;` + `:224-227` 新增「`apx` 的适用面（2026-09-18 起收紧）」块（含 `error[P26]`）。取证：`grep -cF 'speed   : ., apx = 3.14;'` ⇒ **0**（唯一子串已不存在）；`grep -c 'apx 的适用面'` ⇒ **1** |
| `3-2` | 可不动 | **在场 · 判定不变** | `TODO.md:1212-1215` · `docs/superpowers/specs/2026-09-17-opt-dex-batch-report.md:249` 补注在位 |
| `3-3` | 可不动 | **在场 · 判定不变（勿误改）** | `tools/baseline/canary_values.tsv:40/52/53/67`——`142793`/`142936` 仍在原位（`.ccr` 尺寸历史锁定值，与 `ELF 142` **同名不同物**） |
| `4-1` | 建议处理 | **已消解** | `2b103d69` 加「更正注」`TODO.md:1235-1238`——「『ELF 锚定值』半支作废 · interp 半支保留 · `255` 只属 interp 腿」逐条写进文档 |
| `4-2` | 可不动 | **在场 · 判定不变**；**行号已位移（两处）** | `tests/selfhost/test_opt_dex.py:11` · `opt-dex-batch-report.md:25/253` 均在位；但 `src/ci/run.sh` 的对应段**现为 `:241-244`**（报告写的 `:207-210` 现为 criteria-carrier canary 注释段）。报告已声明「**锚 = 唯一子串（不按行号）**」⇒ **不影响判定** |
| `5-1` | 可不动 | **在场 · 判定不变** | `docs/superpowers/plans/2026-09-11-r2-p3-capabilities.md:198` 自带 2026-09-13 作废注在位 |
| `5-2` | 可不动 | **在场 · 判定不变** | `tests/selfhost/test_tf01_fallthrough.py:4`「**修复前实测**」在位 |
| §6bis | （缺口，不计入统计） | **已消解** | `2b103d69` 补 `docs/developer/errors.md:62-64` 三行（P024/P025/P026 编号/文案/检查点）+ `:66` 打印形注。取证：行锚定式 `grep -cE '^\| P02[456] '` ⇒ **3**（表行数）；**非锚定式** `grep -cE 'P02[456]'` ⇒ **4**（多出的那行 = `:66` 的「打印形」注，**不是条目**——两式差 1 是格式事实，不是计数分歧）。原基线 `662ff87d` 上两式皆 ⇒ **0** |

### 0.2 汇总（现基线重新分类）

| 档 | 原分类（表体实数） | 现基线分类 | 差 |
|---|---|---|---|
| 必须处理 | 3（1-1 / 1-2 / 3-1） | **0** | −3（全消解） |
| 建议处理 | 2（2-1 / 4-1） | **0** | −2（全消解） |
| 可不动 | 10（1-3 … 5-2） | **10**（判定逐条复核不变） | 0 |
| **合计** | **15 行** | **15 行** | 0 |

> **§6bis**：原判「文档缺口」⇒ **已消解**（不计入上表）。
> **净结论**：**已消解 6**（1-1 / 1-2 / 2-1 / 3-1 / 4-1 / §6bis）· **在场 10**（**全为「可不动」**，处置 = 无动作）。
> ⇒ 本报告在现基线上**没有任何仍须处置的条目**：原「必须处理 3 条」**全部归零**，原「建议处理 2 条」**全部归零**；
> 余下 10 条的「可不动」依据经逐条复核**全部成立**（已标注的仍标注 · 正确的仍正确 · 勿误改的仍在原位）。

### 0.3 报告内第三处数字/事实缺陷：§5 括注过判（**在其自身基线上即已不成立**）

- 原文 §5 括注：「`^error[` 字面**在仓内已不存在**」。
- 实测（`662ff87d` / `7ddf6ed2` / `09f253b8` **三态皆同**）：`jj file search -r <现查现用 id> --pattern '^error\['` ⇒ **3 行 / 2 档**——
  `docs/superpowers/plans/2026-09-17-spec-grammar.md`（`error[V01]: #check(...) is statically false`）
  · `docs/superpowers/specs/2026-08-08-error-codes-design.md`（`error[T00001] cannot assign string to int` ×2）。
- **但 §5 的真判定成立**：限定 **live 判据面** ⇒ `jj file search -r 09f253b8a90a28973a282257d56dd251d12d2fc4 --pattern '^error\[' src tests tools` ⇒ **0 行**（空）。
- ⇒ 这是「**结论对、括注里的加强句错**」：那 3 行是**文档里举例的错误输出样式**，不是判据子句。详见 §5 处〔更正注〕。

### 0.4 方法学：重核期间 `develop@origin` **移动过**（本仓多代理共享对象库的固有面）

- **实测时间线**：本轮开工时 `develop@origin` = **`7ddf6ed2`**（与本批 brief 的预期一致）；重核进行中它被推进到 **`09f253b8`**（`#136`「批8 TODO 登记 3 条」，由另一代理合入）——**未经本代理 `jj git fetch`**。
- **症状（值得记住的形态）**：工作区磁盘上的 `TODO.md` 与 `-r develop@origin` 的 `TODO.md` **不一致**，且 `jj diff` 只显示本批自己的改动（**不报错、不告警**）。
- **根因**：`develop@origin` 是**移动的 bookmark**；`-r <bookmark>` 每次现解，故「同一条命令」在两次调用间可指向不同提交。
- **本轮撞上的具体后果**：`#136` 在 `TODO.md` 第 174 行处插入 3 行，使**第 173 行之后的全部行号 +3** —— 首轮的若干行号引用因此失效（已全部按 `09f253b8` 重取）。
- **纪律（本轮起）**：判据的 `-r` 参数**写现查现用的 commit id**，不写移动中的 bookmark；行号**只作辅助**，主锚仍为**唯一子串**；跨命令复用同一读数前，先重解一次 rev。
  （同族先例见本批 brief 的警告与仓内既有陷阱条目：**「现查现用、转发给名不给 id」**。）

### 0.5 本批动作清单（**改了哪些文件、以及**刻意**没改哪些**）

**改动 = 仅本文件**（`docs/superpowers/specs/2026-09-18-stale-criteria-scan.md`）：统计更正（§头部）· §0 基线重定与逐条复核 · 五类表各一条〔基线重核〕注 · §5〔更正注〕· §6/§6bis〔基线重核〕注。**全部为「只增不删」**——原 15 条判定与全部逐字引文**一字未改、一行未删**。

**刻意未改（逐项给理由，便于二审）**：

| 位置 | 为何不动 |
|---|---|
| `TODO.md` 映射表 `#2026-09-17-5` 行（1-1） | **已消解**：`2b103d69` 已加〔映射表注〕（`TODO.md:157-160`）。报告 1-1 的「建议处置」原文是**二选一**（「加注；**或**按惯例在条目侧再点明」），#131 已实现前一支且落点更贴近检索第一落点 ⇒ **无需再补**。映射行字面**未改**（符合「不改字」） |
| `TODO.md` #2026-09-17-5 正文（1-3 / 2-2 / 3-2 / 4-1） | **1-3 / 2-2 / 3-2 在场但「可不动」**（同处更正注齐全）；**4-1 已消解**（`:1235-1238` 已有〔更正注〕） |
| `docs/developer/syntax.md`（3-1） | **已消解**（`:219` 显式形 + `:224-227` 适用面块） |
| `docs/developer/errors.md`（§6bis） | **已消解**（`:62-64` 三行 + `:66` 注） |
| `docs/superpowers/plans/2026-09-17-silence-closure.md`（1-2） | **已消解**（`:54-57`〔标题注〕；**标题字面未改**） |
| `docs/superpowers/plans/2026-09-16-apx-conversion-fix.md`（2-1） | **已消解**（`:166-168`〔表注〕；表内原文一字未改） |
| `tools/baseline/canary_values.tsv`（3-3） | **勿误改**——`142793`/`142936` 是 **`.ccr` 尺寸**历史锁定值，与 `ELF 142` **同名不同物**。**本批一字未碰**（已复核仍在 `:40/52/53/67` 原位） |
| `src/**` · `tests/**/*.py` · `tools/**` | **本批范围外**（只读扫描的「仅文档面」边界）；报告所列诸项**无一项**需要改代码 |

> **边界自证（正反两条断言，均可当场跑）**：
> 1. **「只改本文件」**：`jj diff -r <本批提交> --stat` ⇒ 只应列出**本文件一个路径**；若列出第二个，本节即错。
> 2. **「只增不删」**：同一条 `--stat` 的计数应为 **`N insertions(+), 0 deletions(-)`**——**`0 deletions` 即「原文一字未删」的机械证据**（不是自述，是 diff 计数）。
>    ⇒ 实测（本批标注提交）：`1 file changed, 129 insertions(+), 0 deletions(-)`。**两条同时成立**。
> 3. **反向对照**：若有人日后删改了本报告的原文，上式 `deletions` 立刻 > 0 ⇒ 该断言当场红。

---

## 1 类一：已被推翻的 `dex?` 旧机理

**失效源**：`TODO #2026-09-18-3` + `#2026-09-17-5` 的 2026-09-18 更正注 + `docs/superpowers/specs/2026-09-17-silence-closure-t0-report.md` §判定 A（T0 实测：`monomorph.cr:600` 只深克隆函数体 `d` ⇒ 实例与声明**共用形参节点** ⇒ 实例形参槽**恒 `TI_INT`(GP)**；「callee 按实例判 ⇒ 槽 = `TI_DEX_S`」**不成立**）。

| # | 档 | 位置 | 逐字引文 | 为什么算失效 | 建议处置 |
|---|---|---|---|---|---|
| 1-1 ★ | **必须处理** | `TODO.md` 映射表行（`#2026-09-17-5` 行） | `\| — \| `#2026-09-17-5` \| 2026-09-17 \| 批 5 §10 分诊登记 \| ⚠【静默·双路径分歧】泛型实例化后 `dex?`：调用侧环按泛型声明、callee 槽按实例 ⇒ 说法不一致 \|` | 该行是 id 注册表的**门面摘要**，仍在陈述**已被推翻**的机理（「callee 槽按实例」）。正文虽已有更正注，但映射行是检索/引用第一落点 | 追加一条标注行（映射行**不宜改字** ⇒ 加注；或按惯例在条目侧再点明「映射行标题为旧机理」） |
| 1-2 ★ | **必须处理** | `docs/superpowers/plans/2026-09-17-silence-closure.md` §1-1 **标题**（唯一子串：`### 1. ⚠ **【S】泛型实例化后 `dex?`：环按声明判、槽按实例判**`） | `### 1. ⚠ **【S】泛型实例化后 `dex?`：环按声明判、槽按实例判**（`TODO #2026-09-17-5`）` | 正文有更正注 ✓，但**标题本身仍是旧机理**；标题是跨文档引用面（本仓多处按标题引用该节） | 追加标注行（「只增不删」⇒ 不动标题字面，紧随标题加 `【标题为旧机理；现行约定见下】`） |
| 1-3 | 可不动 | `TODO.md #2026-09-17-5` 机理段 / `2026-09-17-silence-closure.md` §1-1 机理段 | （旧机理原文，逐字保留） | 已被同处更正注 + T0 回填块 ⑤ **逐段作废并标注** ✓ | 无需动作 |
| 1-4 | 可不动 | `src/compiler/monomorph.cr` 注释 · `tests/selfhost/test_generic_param_erasure.py` | `形参数）**原样复制** ⇒ 实例与声明**共用形参节点链**` | 这是**现行（正确）**机理，非旧机理 | 无需动作 |
| 1-5 | 可不动 | `docs/superpowers/plans/2026-09-18-generic-param-erasure.md` | `⇒ **实例与声明共用同一个形参节点链**。` | 同上（正确约定） | 无需动作 |

> **〔基线重核（2026-09-19）〕** 本表 5 行：**1-1 / 1-2 已消解**（`2b103d69` = #131，各加一条注：`TODO.md:157-160`〔映射表注〕· `silence-closure.md:54-57`〔标题注〕——**映射行/标题字面均未改**，与「只增不删」一致）；**1-3 / 1-4 / 1-5 在场，判定不变**。详见 §0.1。

---

## 2 类二：`g(1,d)` 的「期望 7 / 7/7」

**失效源**：lead 2026-09-18 裁定 —— 该探针的 `T` 由首实参绑为 `int` ⇒ 第二实参 `dex` 与 `int?` 不符 ⇒ **正确语义 = 硬错拒绝（`rc=1`）**；「得 7」是另一条语义路线（字面量多态 / 多实参联合绑定）的产物，需另裁单独立条。

| # | 档 | 位置 | 逐字引文 | 为什么算失效 | 建议处置 |
|---|---|---|---|---|---|
| 2-1 ★ | **建议处理** | `docs/superpowers/plans/2026-09-16-apx-conversion-fix.md` 探针表 B-2 行 | `\| **B-2** \| 返回点门（**本计划判为已绿**…） \| `fn f() -> dex { d : ., apx = 7.0; return d; }` … \| 7 \| **P0-confirm / 预期非 RED** \|` | 期望值 **7** 属「另一条语义路线」，且其**探针源本身**（`. + apx`）今已被 P26 拒收（见 §3-1）⇒ 该行作为可执行配方**双重失效** | 加一条总注：本表 `. + apx` 形自 2026-09-18 起为硬错；且「期望 7」类结论需按 §1-1 现行约定重述 |
| 2-2 | 可不动 | `TODO.md #2026-09-17-5` 现象段 + 复现注释 + `2026-09-17-silence-closure.md` §1-1 同处 | `（**期望 7/7**…` / `// 期望 7；实测 ELF 142 / interp 15` | 已由 2026-09-18 更正注（四处逐处）标注作废 ✓ | 无需动作 |
| 2-3 | 可不动 | `TODO.md` 其它「期望 7」（`u11` 探针 · 元组数字下标条目） | `⇒ **ELF 15 / interp 15**（双路径同错，期望 7）` | 属**另一批**的探针语义（u11 = 非泛型 `dex?`；其期望 7 仍成立） | 无需动作（不属本轮失效面） |

> **〔基线重核（2026-09-19）〕** 本表 3 行：**2-1 已消解**（`2b103d69` 加〔表注〕`apx-conversion-fix.md:166-168`——已正面写明「本表作为『可执行配方』已失效」+ B-2「期望 7」叠失效面，与本行「建议处置」逐条对应）；**2-2 / 2-3 在场，判定不变**。详见 §0.1。

---

## 3 类三：`ELF 142` 当作锚定值

**失效源**：T0 重测（97 次运行命中 `142` 仅 1 次；复跑 60 次 0 次；关 ASLR 稳定 `rc=208`）⇒ 该腿**非确定**（读未初始化 GP 寄存器里的栈地址）⇒ 任何把 `142` 写进判据/锚/突变期望的表述失效。

| # | 档 | 位置 | 逐字引文 | 为什么算失效 | 建议处置 |
|---|---|---|---|---|---|
| 3-1 ★ | **必须处理** | `docs/developer/syntax.md`（apx 示例与说明，唯一子串 `speed   : ., apx = 3.14;`） | `speed   : ., apx = 3.14;    // apx：授权后端把 dex 运算降级为 binary64 快路径` | **这是开发者面语法文档**，而 `. + apx` 自 2026-09-18 起为 **`error[P26]` 硬错**（裁定 (B) 白名单 = 仅显式 `dex` + 显式 `int`；`parser.cr:874-885`；`tests/selfhost/test_apx_tag_scope.py` 覆盖 `. + apx` ⇒ rc=1）⇒ **文档教的写法今天编不过** | 示例改显式形（`speed : dex, apx = 3.14;`）+ 加一句「`apx` 仅适用于**显式** `dex` / 显式 `int`；`. + apx` / `auto` / `dex?` / `string` / `bool` + `apx` 为硬错（`error[P26]`）」 |
| 3-2 | 可不动 | `TODO.md #2026-09-17-5` 四处 · `2026-09-17-silence-closure.md` §1-1 各处 · `2026-09-17-opt-dex-batch-report.md` §10-U1 | （`ELF 142` / `142 / interp` 等） | **均已逐处标注「样本非锚」** ✓（本批 #130） | 无需动作 |
| 3-3 | 可不动 | `tools/baseline/canary_values.tsv`（`142793` / `142936`） | `gt 两条：142793/142936 → **142765/142908**（−28B）` | 这是 **`.ccr` 尺寸**（历史锁定值），与 `ELF 142` **同名不同物** | 无需动作（**勿误改**） |

> **〔基线重核（2026-09-19）〕** 本表 3 行：**3-1 已消解**——`2b103d69` 改 `docs/developer/syntax.md:219` 为可编过的显式形 `speed   : dex, apx = 3.14;`，并在 `:224-227` 新增「`apx` 的适用面（2026-09-18 起收紧）」块（白名单 + `error[P26]`），**逐条落实本行的「建议处置」**；唯一子串 `speed   : ., apx = 3.14;` 现**命中 0**（原基线命中 1）。**3-2 / 3-3 在场，判定不变**——`3-3` 的 `tools/baseline/canary_values.tsv` **本批一字未碰**。详见 §0.1。

---

## 4 类四：「禁第三态」被用到非确定值域

**失效源**：批 8 §1-1 回填块 **Ⅳ / Ⅳ-bis** —— 该纪律只适用**确定值域**；非确定值域改判「**必须可复现**」；`255` **只属 interp 腿**（`interp.cr` 的 `IR_I2F/IR_F2I` 能力边界）。

| # | 档 | 位置 | 逐字引文 | 为什么算失效 | 建议处置 |
|---|---|---|---|---|---|
| 4-1 | **建议处理** | `TODO.md #2026-09-17-5` **判据要件 ②** | `② **禁第三态**（apx 源 interp ∈ {255（能力边界）, ELF 锚定值}，出现第三值即红）` | 括注的**第二个元素「ELF 锚定值」**在非确定 ELF 腿上无意义 ⇒ 该子句失效（本条的 ④ 与四处 `142` 已标注，**② 未逐条标注**） | 加注：② 的 interp 半支保留（确定值域），**「ELF 锚定值」半支作废**，按 Ⅳ/Ⅳ-bis 改判「必须可复现」 |
| 4-2 | 可不动 | `2026-09-17-opt-dex.md`（G10 三条纪律 · `:3`）· `2026-09-17-opt-dex-batch-report.md`（禁第三态行）· `src/ci/run.sh:207-210` · `tests/selfhost/test_opt_dex.py`（`本档核心`） | `bits 源 ⇒ interp ∈ {255, 锚定值}` | 这些**全部限定 interp 腿**（确定值域），纪律仍成立 ✓ | 无需动作 |

> **〔基线重核（2026-09-19）〕** 本表 2 行：**4-1 已消解**（`2b103d69` 加〔更正注〕`TODO.md:1235-1238`——「『ELF 锚定值』半支作废 · interp 半支保留 · `255` 只属 interp 腿」，与本行「建议处置」逐条对应）；**4-2 在场，判定不变**，唯 `src/ci/run.sh` 的对应注释段**行号已位移至 `:241-244`**（报告写的 `:207-210` 现为 criteria-carrier canary 注释段）——本报告「锚 = 唯一子串（不按行号）」的纪律使此行号位移**不影响判定**。详见 §0.1。

---

## 5 类五：`check` 面「`^error[` 应恰为 2」

**期望改为**：现态 = **0**（两条 TF01 误报已由 `TODO #2026-09-13-1` 修复）。

**扫描结论：live 判据面零命中**（`^error[` 字面在仓内**已不存在**）。命中的两处均为**历史记录**：

> **〔更正注（2026-09-19 · `docstale` 基线重核；原句逐字保留不删）〕上句括注「`^error[` 字面在仓内**已不存在**」不成立**——该字面**在仓内仍存在**，且**在本报告自己的基线 `662ff87d` 上就已存在**（不是基线前进造成的）：
> `jj file search -r 662ff87daacb --pattern '^error\['` ⇒ **3 行 / 2 档**（`docs/superpowers/plans/2026-09-17-spec-grammar.md` 的 `error[V01]: #check(...) is statically false` · `docs/superpowers/specs/2026-08-08-error-codes-design.md` 的 `error[T00001] cannot assign string to int` ×2）；`-r 7ddf6ed2` 与 `-r 09f253b8` **皆同**（3 行 / 2 档）。
> **但本节的真判定（「live 判据面零命中」）成立**：限定 live 判据面 ⇒ `jj file search -r 09f253b8a90a28973a282257d56dd251d12d2fc4 --pattern '^error\[' src tests tools` ⇒ **0 行**（空）。
> ⇒ 属「**结论对、括注里的加强句错**」：那 3 行是**文档里举例的错误输出样式**，不是判据子句。**本节两行的「可不动」判定不受影响**，仅括注作废。

| # | 档 | 位置 | 逐字引文 | 判定 | 建议处置 |
|---|---|---|---|---|---|
| 5-1 | 可不动 | `docs/superpowers/plans/2026-09-11-r2-p3-capabilities.md` Step 1 | `check` job rc=1 = **既有 red 划界**（恰 2 条 TF01 误报 `lits_copy`/`ty_memo_slot_no_grow`，非本批引入）。…【**2026-09-13 更新**：两条已修（TODO #2026-09-13-1）⇒ `check src/compiler` rc=0，该划界约定作废（行保留为当期实跑记录）。】 | **已自带作废注** ✓ | 无需动作 |
| 5-2 | 可不动 | `tests/selfhost/test_tf01_fallthrough.py:4` | `背景（RED，修复前实测；`check src/compiler` 长期恰 2 条 error[TF01]，被当作` | 明标「**修复前实测**」的背景叙述；**活跃断言正确**（`tf01 != 0` 接受面 / `tf01 != 1` 拒收面，`:71-84`） | 无需动作 |

> **〔基线重核（2026-09-19）〕** 本表 2 行：**5-1 / 5-2 均在场，判定不变**（均自带「当期实跑记录 / 修复前实测」的时效标注，非活跃判据）。**§5 的「live 判据面零命中」结论成立**（`src tests tools` 行锚定命中 0），唯其括注作废——见上方〔更正注〕。详见 §0.1。

---

## 6 建议裁定顺序（我的意见）

1. **§3-1 `docs/developer/syntax.md`**（必须处理，最危险）：它是**开发者面文档**，且给出的是**今天直接编译失败**的写法 —— 唯一一条会让读者照抄后立刻撞错的条目。建议与 `docs/developer/errors.md` 的 P26 段一并核（该处已有 P24/P25/P26 的记载吗？**本扫描未见 P26 在 `errors.md`**，若确无 ⇒ 建议同批补，属**文档缺口**而非失效约定）。
2. **§1-1 / §1-2 两条标题面**（必须处理）：标题是引用面，且本轮已证明「同一失效约定会在多处并存」。
3. **§2-1 / §4-1**（建议处理）：历史计划的可执行配方 + 未被逐条标注的判据子句。

> **〔基线重核（2026-09-19）〕本节的 3 条建议在现基线上已全部执行完毕，本节作为「裁定依据」保留（不再需要动作）。**
> 1 条（§3-1）⇒ `2b103d69` 改示例为显式形 + 加适用面块；2 条（§1-1 / §1-2）⇒ `2b103d69` 各加〔映射表注〕/〔标题注〕；3 条（§2-1 / §4-1）⇒ `2b103d69` 各加〔表注〕/〔更正注〕。
> **全部为「原文逐字保留 + 加注」，无一处删改原字面**——与本报告建议的处置形态一致。详见 §0.1 / §0.2。

## 6bis 扫描顺带发现（**非失效约定，是文档缺口**）

- **`docs/developer/errors.md` 未记载 `P24` / `P25` / `P26`**：实读该文件 **`P24|P25|P26` 命中 = 0**。这三个是批 8 PR-A₁ 新增的**硬错码**（`extern` 含可选 / 顶层兜底 / `apx` 标签适用范围），已有套件与 CI 覆盖，但**开发者错误码参考里没有条目** ⇒ 与 §3-1（`syntax.md` 教了被拒写法）合起来 = **同一族：新硬错没有进开发者文档**。建议同批补（若你认可，我可另提一条只增不删的补文档清单）。

> **〔基线重核（2026-09-19）〕本条发现的缺口已消解**：`2b103d69` 已在 `docs/developer/errors.md` **补上 `P024` / `P025` / `P026` 三行**（编号 · 文案 · 常量 · 检查点，全部实读源码）+ 一条「打印形」注（P024/P025/P026 ⇒ 实际输出 `error[P24]` 等，打印器取 `码 % 1000`）。
> 取证：行锚定式 `grep -cE '^\| P02[456] '` ⇒ **3**（表行数；非锚定式 ⇒ 4，多出的一行是 `:66` 的「打印形」注，非条目）。原基线 `662ff87d` 上两式皆 ⇒ **0**。⇒ 原判「命中 = 0」在**当时**属实，现基线**已不成立**。

## 7 未列入（我实读后判为不属本轮失效面，供你校验我的边界）

- `tests/selfhost/test_opt_dex.py` 的「期望 ∈ {255, …}」纪律（interp 腿，确定值域）✓
- `src/stdlib/dex.cr:39` `dex_str` / `fmt.cr` `chr` 等转换器签名（与插值规则面的关系属另一条线）
- `docs/archive/**` 历史归档（本仓惯例：归档不动）
