# Core 惰性求值

> 定位:受众 = 维护者/贡献者;状态 = **proposal(未实现)**——图天然惰性论点成立,eager 为当前策略;使用点 force 机制与设计语义一致(部分),控制流级惰性未做

## 设计原则

- **图本来就是惰性的。** 节点不在边上游等待时就不执行。当前的 IR 生成策略是 eager——节点一出现就执行。这不是图的限制，是策略的选择。

## 自动惰性推导

编译器看图决定一个节点是否需要惰性求值：

```core
result := if cond {
    expensive1()     // 只在 cond=true 时用到
} else {
    0                // cond=false 时到这里
}
print(result);
```

| 条件 | 策略 |
|------|------|
| 节点结果只用在一个分支里 | 延迟到确定需要时执行 |
| 节点有副作用（IO、文件、volatile） | 永远 eager，不惰性 |
| 节点在循环体内且每次都用 | eager |
| 节点在循环体内但条件性用 | 惰性 |

## 无关键字

惰性推导和之前的指针验证、编译期求值、概率推导一样——编译器看图自动做，不引入 `lazy` 关键字。

```core
x := expensive();       // 编译器 auto: eager 或 lazy
y := cond ? x : 0;      // 如果 x 只用在这里且无副作用 → 自动惰性
```

## 副作用标记

编译器需要知道哪些节点有副作用。纯函数的调用自动可惰性。副作用的标记来源：

| 来源 | 判定 |
|------|------|
| 纯函数（无 IO、无变异） | 可惰性 |
| IO 操作（read_file、print） | 不可惰性 |
| volatile 内存操作 | 不可惰性 |
| FFI 调用 | 默认不可惰性 |

## 被推导但是用了怎么办

```core
x := expensive();   // 编译器决定惰性
y := cond ? x : 0;
z := x + 1;         // x 这里也被用了 → 必须求值
```

如果编译器发现惰性标注的节点实际上有多条路径需要它——自动升级为 eager。用户不感知。

## 惰性不改变语义

无论编译器选择惰性还是 eager，程序输出一致。惰性只是策略选择，不影响正确性。

---

## 当前实现状态

**更新（2026-08-09）**：控制流级惰性方向定为**编译期指令下沉路线**——纯调用移到唯一消费点（分支内），无运行时 thunk/flag/9 字节结构，interp / ELF 后端零改动；见 TODO.md「控制流自动惰性」。同时核实现状两点：THUNK/FORCE 当前为纯值搬运（IR_CALL 在 THUNK 前已急切发射，ELF/interp 均按值传递降级，无实际延迟）；`use_count <= 1` 判定存在时序问题（`compute_usage_counts()` 在全部 ir_gen 之后运行，判定时读到的恒为 0/未初始化，"单次使用"条件名存实亡）。

**更新（2026-08-08）**：惰性求值已部分实现，范围为调用级（文档设计的子集）：

已实现：
- IR 指令 `IR_LAZY_THUNK(46)` / `IR_LAZY_FORCE(47)`（ast.cr）
- 自动触发（ir_gen.cr）：调用**纯函数**（`fi_ispure`）且结果**单次使用**
  （`use_count <= 1`）时包裹为 thunk；有副作用则永远 eager，符合文档判定表
- 自动 force（ir_gen.cr `force_if_thunk()`）：全部操作数位置（算术、字段、
  索引、指针、赋值、返回值）在取值前检查生产者并发射 FORCE
- ELF 后端两个 opcode 均编码；解释器支持 thunk/force

未实现（文档设计的其余部分）：
- 控制流级惰性："结果只用在一个分支里 → 延迟执行"（if 条件惰性）未做
- 循环体内条件性使用的惰性未做
- "自动升级 eager"在实现中体现为使用点 force，机制不同但语义一致
