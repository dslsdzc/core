# 基线既有红白名单（判据模式 `run_scope.sh --assert` 下：这些档只上报、不判红）
#
# 收录条件（三条缺一不可）：
#   ① 该档在**本修复的基线**上就是红的；② 其红**与本缺陷（串域跨进程 id）无关**；
#   ③ 有独立证据（干净基线二进制复现）+ 有**触发器**（何时移除本行）。
#
# 格式：`<探针名>|<说明>`
#
# ── interp（字符串插值 `"n=${n}"`）—— develop 面既有回归（2026-09-18 登记）─────────
# 现象：`fn main() -> int { n := 7; println("n=${n}"); return 0; }` 在 develop `f7f23b83`
#   上 build **rc=1**（`error: Unexpected token in expression` ×8）；变体
#   `s := "n=${n}"; println(s);` 报 `error[P21]: Nested function declaration 'read_file'/
#   'write_file' is not supported`（把 `src/stdlib/io.cr` 的**顶层**函数当成嵌套声明
#   ⇒ 解析器被插值 token 带偏）。
# 证据（三方对照）：
#   ① 干净 `f7f23b83` 基线二进制（独立工作区 `core-strbase-ws` 重建，
#      `grep -c cir_rec_begin src/compiler/cir_cache.cr` = 0 ⇒ 不含本批改动）**同样 rc=1**；
#   ② 本批所用的旧二进制（develop `f57815ec` 面构建）对同一源 **rc=0**；
#   ③ `jj diff --stat -r 'f57815ec..f7f23b83' -- src/compiler src/stdlib` = 4 文件变更
#      ⇒ 区间内确有别的批次改了 src（插值 desugar 被破坏，非本批）。
# 触发器：**插值解析修复后，本行必须删除**（该档随即回到判据面）；若该档在修复前
#   变绿（例如插值被整体下线），同样删行并按新形态重述。
interp|插值解析崩（develop 既有回归，非本缺陷面）—— 证据与触发器见本文件头注
