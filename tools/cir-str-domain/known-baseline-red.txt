# 基线既有红白名单（判据模式 `run_scope.sh --assert` 下：这些档只上报、不判红）
#
# 收录条件（三条缺一不可）：
#   ① 该档在**本修复的基线**上就是红的；② 其红**与本缺陷（串域跨进程 id）无关**；
#   ③ 有独立证据（干净基线二进制复现）+ 有**触发器**（何时移除本行）。
#
# 格式：`<探针名>|<说明>`
#
# ── interp（字符串插值 `"x=${n}"`）—— **基线既有红（早于 `f57815ec`，引入点待定）** ──
# 现象：源 `fn main() -> int { n := 7; s := "x=${n}"; println(s); return 0; }` build **rc=1**，
#   报 `error[P21]: Nested function declaration 'str_len'/'chr'/'get_char' … is not supported`
#   （解析器被插值 token 带偏、留下未闭合构造，于是把 `fmt.cr`/`io.cr` 的**顶层**函数当成嵌套声明）；
#   `println("n=${n}")` 直用变体则报 `error: Unexpected token in expression` ×8。
# 隔离（team-lead 实测）：`println("plain")` **正常** · `println(int_str(n))` **正常** ·
#   **只有 `"x=${n}"` 崩** ⇒ **插值专属**。
# 证据（两个独立二进制，team-lead 实核）：
#   ① 干净 `f7f23b83` 基线（本目录所属批的 `core-strbase-ws` 重建）⇒ rc=1；
#   ② **A₁ 时代构建**（`core-optdex-ws`，07:12）⇒ **同样 rc=1**。
#   ⇒ **不是 `f57815ec→f7f23b83` 窗口引入的**；本文件初版曾据「旧二进制 rc=0」写成窗口引入，
#   **该基点不可靠（那个二进制比标称的更旧）已撤回**——口径 = 「基线既有红，引入点待定」。
#   归因/二分已由 team-lead 派给最近一次 parser 变更的作者（`plan-optdex`，task #153）。
# 触发器：**插值解析修复后，本行必须删除**（该档随即回到判据面）；若该档在修复前
#   变绿（例如插值被整体下线），同样删行并按新形态重述。
interp|插值解析崩（**基线既有红，早于 f57815ec，引入点待定**，非本缺陷面）—— 证据与触发器见本文件头注
