// .so 扩展索引夹具（**正常档**）：模拟 `$HOME/.core/lib/io/index` 的真实形态。
// 格式（module.cr:220-225 注）：每行 `name: params [-> ret] [, tags]`；`//` 行被解析器按
// 「无冒号 ⇒ continue」（module.cr:255）跳过——本夹具用 `//` 注释而非 `#`（`#` 亦被跳过，但
// 注释里若含冒号会被误解析，故一律用 `//`）。
//
// 本档要点：**含两条 io.cr 未声明的名字**（print_int / println_int，见 src/stdlib/io.cr:19,23
// 声明的是 print_i / println_i）——它们正是前批 PR #80 红档 +28B 的来源（TODO #99）。
print: string, variadic, auto_str
println: string, variadic, auto_str
print_int: int
println_int: int
read_file: string -> string
write_file: string, string -> int
